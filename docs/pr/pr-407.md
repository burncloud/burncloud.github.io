---
title: "PR #407 — Polish Providers operator workflow"
slug: /pr/pr-407
---

# PR #407 — Polish Providers operator workflow

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-08-14T02:06:44Z`  
> **Base：** `main@5f0d1eae3812`  
> **Head：** `agent/phase-2-providers-polish@a5d2bc1d2902`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `2`
- **Additions / Deletions：** `+167 / -35`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `2`

**PR 说明摘要：** ## Goal Apply the first page-by-page Phase 2 polish pass to Providers on top of the visual system merged in #406. The page keeps the existing backend contracts and provider CRUD behavior. This pass focuses on truthful loading state, operator hierarchy, table readability, and safer destructive-action styling. ## What changed ### Truthful loading and refresh state - distinguish initial/loading state from a genuinely empty provider inventory - prevent the page from flashing the "Connect your first upstream provider" empty state while the resource is still loading - disable Refresh while a load is in progress and show `Refreshing…` ### Provider supply conclusion - add a compact semantic health strip above the inventory when providers exist - show a healthy conclusion when all providers are active - show an attention conclusion when any provider is inactive/down - surface routing-group count without inventing new backend state ### KPI hierarchy - keep neutral facts such as Configured and Models Served visually neutral - reserve green/amber emphasis for actual health/attention state - simplify Needs Attention copy to the condition it represents ### Inventory readability - remove duplicated routing-group text from the Routing column - show routing group as the primary value and Priority/Weight as secondary policy detail - replace infinity glyphs with explicit `Unlimited` capacity lang…

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/client/src/functional_pages/providers.rs` | modified | 98 | 34 | 0 |
| `crates/client/src/product_ui.css` | modified | 69 | 1 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/client/src/functional_pages/providers.rs`
- `crates/client/src/product_ui.css`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
