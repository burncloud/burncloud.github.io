---
title: "PR #412 — Polish Logs operational triage"
slug: /pr/pr-412
---

# PR #412 — Polish Logs operational triage

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-08-14T02:37:49Z`  
> **Base：** `main@a27be4863f35`  
> **Head：** `agent/phase-2-logs-polish@ecffaafcd54f`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `2`
- **Additions / Deletions：** `+275 / -54`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `2`

**PR 说明摘要：** ## Goal Apply the Phase 2 Observe polish pass to Logs after completing Traffic Setup through Playground (#407–#410). Logs should answer the operator's first questions in order: did requests fail, did routing fall back, how slow were they, what did they cost, and which upstream actually handled a specific request? ## What changed ### Truthful loading - stop rendering zero-valued operational KPIs before request data has loaded - show a dedicated loading conclusion while router activity is being read - disable search/filter/refresh while the resource is still loading ### Triage-first KPIs Replace the lower-value `Requests Loaded` KPI with the operational priority order: - Failures - Fallbacks - Avg Latency - Loaded Cost Request count remains visible in the activity section and cost context. ### Overall operational conclusion - failures present → explicit failure attention state - no failures but fallbacks present → traffic succeeds, but fallback routing is active - no failures/fallbacks → loaded request activity is healthy - include loaded success rate as supporting context ### Request activity readability - keep chronology intact rather than artificially reordering requests by severity - add safe truncation/title preservation for timestamps, request IDs, user/path, model, and upstream - format latency as milliseconds or seconds depending on magnitude - make rows explicitly intera…

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/client/src/functional_pages/logs_full.rs` | modified | 167 | 52 | 0 |
| `crates/client/src/product_ui.css` | modified | 108 | 2 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/client/src/functional_pages/logs_full.rs`
- `crates/client/src/product_ui.css`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
