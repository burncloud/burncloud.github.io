---
title: "PR #408 — Polish Models availability workflow"
slug: /pr/pr-408
---

# PR #408 — Polish Models availability workflow

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-08-14T02:13:02Z`  
> **Base：** `main@d4d8ee526d33`  
> **Head：** `agent/phase-2-models-polish@b9a6a0096791`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `2`
- **Additions / Deletions：** `+158 / -31`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `2`

**PR 说明摘要：** ## Goal Apply the Phase 2 page-by-page polish pass to Models after the Providers polish merged in #407. This keeps the model catalog derived from real provider configuration and does not introduce new backend capabilities. The focus is truthful loading, availability-versus-redundancy hierarchy, and more actionable operator decisions. ## What changed ### Truthful loading - distinguish initial loading from a genuinely empty model catalog - prevent the page from flashing the empty-state while provider data is still loading - disable Refresh while loading and show `Refreshing…` ### Availability conclusion - add a compact model-supply conclusion above the table - show unavailable-model risk when any model has no active upstream - distinguish a fully available but single-upstream catalog from a resilient catalog - report full resilience only when every available model has 2+ active upstreams ### KPI hierarchy - keep total Models and Redundant counts visually neutral - reserve green for actual availability state - reserve red for models with no active upstream ### Table readability - show active upstream count as the primary value and provider names as secondary context - show routing-group count and a truncated group preview - pluralize configured-provider, upstream, and routing-group counts correctly - truncate long model IDs, upstream previews, and group previews while preserving f…

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/client/src/functional_pages/catalog.rs` | modified | 112 | 29 | 0 |
| `crates/client/src/product_ui.css` | modified | 46 | 2 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/client/src/functional_pages/catalog.rs`
- `crates/client/src/product_ui.css`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
