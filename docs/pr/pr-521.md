---
title: "PR #521 — security(installer): validate npm mirror arguments"
slug: /pr/pr-521
---

# PR #521 — security(installer): validate npm mirror arguments

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-15T01:11:47Z`  
> **Base：** `main@6daa44a10f21`  
> **Head：** `security/npm-mirror-513@5c4ccedacf23`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME`
- **Changed Files：** `2`
- **Additions / Deletions：** `+231 / -65`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `2`

**PR 说明摘要：** ## Summary Closes #513 - validate `NPM_MIRROR` as an absolute HTTP(S) URL with a host - reject URL userinfo, raw shell metacharacters, invalid schemes, and malformed non-blank values without echoing the input - preserve the existing LANG/LC_ALL China mirror fallback when the variable is missing or blank - invoke npm with direct process arguments on Windows and Unix; remove shell command construction from both online and bundle npm installs - log only the registry scheme and host - document the configuration contract and add positive/negative tests ## Scope Changed only the Issue Allowed Paths: - `crates/installer/src/installer.rs` - `.env.example` No registry allowlist, version/integrity policy, region strategy, directory, Auto Update, PATH, or Bundle trust changes. ## Verification Local static checks: - `git diff --check` - changed-path boundary check - no npm shell command construction remains - tests cover missing/blank values, valid HTTP/HTTPS, invalid schemes/host, URL userinfo, shell metacharacters, redacted log labels, and Windows/Unix argument boundaries The workspace image does not include the Rust toolchain, so formatting, test, check, and clippy are delegated to GitHub Actions.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `.env.example` | modified | 3 | 1 | 0 |
| `crates/installer/src/installer.rs` | modified | 228 | 64 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `.env.example`
- `crates/installer/src/installer.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
