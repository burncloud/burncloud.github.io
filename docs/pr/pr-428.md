---
title: "PR #428 — feat(node): introduce Node hardware profile foundation"
slug: /pr/pr-428
---

# PR #428 — feat(node): introduce Node hardware profile foundation

> **状态：** OPEN  
> **作者：** `rustburn`  
> **创建：** `2026-08-31T16:52:36Z`  
> **Base：** `main@c314bff9646f`  
> **Head：** `feature/node-v0.1-hardware-profile@662b46e8ba9b`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME`
- **Changed Files：** `6`
- **Additions / Deletions：** `+298 / -0`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `6`

**PR 说明摘要：** ## Behavior change Establish the first executable BurnCloud Node v0.1 boundary: a single reusable `HardwareProfile` for local model/runtime decisions. This PR does **not** change Router, billing, authentication, provider selection, or existing inference process behavior. ## Implementation - adds the new `burncloud-node` workspace crate; - reuses `burncloud-service-monitor` as the source for CPU, RAM, and disk metrics; - adds extensible `HardwareProfile`, `GpuDevice`, and GPU probe status types; - adds NVIDIA GPU/VRAM/driver discovery through `nvidia-smi` when available; - treats CPU-only hosts as valid nodes instead of making GPU presence mandatory; - adds parser/unit coverage for NVIDIA discovery; - adds Node hardware ownership to `docs/agent/TASK_ROUTER.md`. ## Architecture rationale Hardware Detection owns facts only. It does not choose a model artifact or runtime. The next layer, Model Resolver, will consume this profile together with model manifests/runtime capabilities. ## Verification Connector-side diff inspection completed: branch is 6 commits ahead of `main`, touches only the new Node crate, one workspace member entry, and one Agent task-router row. Repository CI is expected to run formatting/check/test gates on this PR. No local cargo command is claimed as run from this environment. ## Dynamic / unverified boundaries - NVIDIA discovery depends on the host `nvidia-smi…

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `Cargo.toml` | modified | 1 | 0 | 0 |
| `crates/node/Cargo.toml` | added | 19 | 0 | 0 |
| `crates/node/src/error.rs` | added | 10 | 0 | 0 |
| `crates/node/src/hardware.rs` | added | 256 | 0 | 0 |
| `crates/node/src/lib.rs` | added | 11 | 0 | 0 |
| `docs/agent/TASK_ROUTER.md` | modified | 1 | 0 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `Cargo.toml`
- `crates/node/Cargo.toml`
- `crates/node/src/error.rs`
- `crates/node/src/hardware.rs`
- `crates/node/src/lib.rs`
- `docs/agent/TASK_ROUTER.md`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
