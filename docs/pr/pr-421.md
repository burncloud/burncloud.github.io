---
title: "PR #421 — Align Overview with the canonical product flow"
slug: /pr/pr-421
---

# PR #421 — Align Overview with the canonical product flow

> **状态：** OPEN  
> **作者：** `rustburn`  
> **创建：** `2026-08-14T13:31:40Z`  
> **Base：** `agent/ui-product-flow-contract@634e6de81b1e`  
> **Head：** `agent/overview-product-flow@8c80c51c7e5c`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `2`
- **Additions / Deletions：** `+430 / -274`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `2`

**PR 说明摘要：** ## Goal Make Overview the Console's conclusion-and-handoff surface instead of a second Providers, Logs, Billing, and Settings page. This is the next incremental slice after #420. It is intentionally stacked on `agent/ui-product-flow-contract` so this PR contains only the Overview-specific work. ## What changed - Replaced the old mixed dashboard logic with an evidence-backed Overview. - Split the page into the canonical cross-page flow: **Supply → Access → Verify → Observe**. - Removed the false `routing_configured = provider + model + key` inference. Overview now reports only evidence it actually reads. - Distinguishes configured models from models available through active providers. - Summarizes configured routing-group evidence derived from provider configuration without claiming more than the source proves. - Separates readiness evidence from supporting usage/billing evidence, so a billing/usage failure does not incorrectly make traffic readiness unknown. - Preserves UNKNOWN states instead of converting failed/loading resources into healthy-looking zeroes. - Makes the hero choose one current conclusion and one primary next action. - Adds a small `Needs attention` section for cross-page conclusions only; detailed diagnosis is handed back to Providers, Models, and Logs. - Keeps one compact latest-request orientation block, while removing the Overview-owned routing receipt draw…

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/client/scripts/check-product-ux.sh` | modified | 22 | 6 | 0 |
| `crates/client/src/critical_pages/dashboard.rs` | modified | 408 | 268 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/client/scripts/check-product-ux.sh`
- `crates/client/src/critical_pages/dashboard.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
