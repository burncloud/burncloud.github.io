---
title: "PR #533 — test(node): prove phase-0 fake golden path"
slug: /pr/pr-533
---

# PR #533 — test(node): prove phase-0 fake golden path

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-16T18:57:52Z`  
> **Base：** `main@28d052dfae06`  
> **Head：** `feat/node-fake-golden-path@a120fb582350`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `TEST ONLY`
- **Changed Files：** `1`
- **Additions / Deletions：** `+86 / -0`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `1`

**PR 说明摘要：** ## Summary S0-07: add the Phase-0 acceptance test proving the Node skeleton can converge end-to-end from `Absent` to `Routable` using deterministic fake capabilities and evidence. ### Golden path `Absent -> Resolving -> PreparingArtifact -> ArtifactReady -> PreparingRuntime -> Starting -> WaitingReady -> Ready -> Routable` ### What the test proves - Node runtime is attached - fake machine hardware evidence can be inspected - reconciliation advances one stage at a time - fake process lifecycle can be invoked - `ProcessStarted` does not imply `Ready` - explicit readiness evidence is required - `Ready` is still not serving - only router-attachment evidence reaches `Routable` - `Routable` settles to `Noop` ### Boundary This PR intentionally does not implement real model resolution, model downloads, runtime preparation, readiness probes, or ModelRouter mutation. In particular, `RouterAttached` is still fake evidence; S0-08 must attach to the existing BurnCloud routing path rather than create a second router/server/listener. ### Why This is the Phase-0 skeleton acceptance test: all current rooms, sockets, rails, and fake adapters can be composed into one complete flow before real implementations replace them.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/platform/node/tests/golden_path.rs` | added | 86 | 0 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/platform/node/tests/golden_path.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
