---
title: "PR #532 — feat(node): add machine-level composition root"
slug: /pr/pr-532
---

# PR #532 — feat(node): add machine-level composition root

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-16T18:52:44Z`  
> **Base：** `main@15999e699618`  
> **Head：** `feat/node-composition-root@4d6b0d458c8b`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME`
- **Changed Files：** `3`
- **Additions / Deletions：** `+91 / -0`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `3`

**PR 说明摘要：** ## Summary S0-06: add the first Node composition root so lifecycle, reconciliation, hardware inspection, and process lifecycle can be wired through replaceable adapters. ### Adds - generic `NodeComposition` - injects `HardwareProbe` and `ProcessManager` instead of constructing implementations internally - owns the attached `NodeContext` and `DemandReconciler` - tests composition with `FakeHardwareProbe` and `FakeProcessManager` - proves composition startup does not make a workload routable ### Boundary This is deliberately only the machine-level composition root. It does not invent or absorb Model/Provider/Route business contracts. It also does not bind a port or create a second HTTP server/ModelRouter. ### Why The skeleton now has a fixed place where replaceable implementations are plugged in. The next S0-07 step can extend this wiring into a complete fake golden path, while domain-owned model/artifact/router contracts remain with their correct owners.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/platform/node/src/composition.rs` | added | 50 | 0 | 0 |
| `crates/platform/node/src/lib.rs` | modified | 2 | 0 | 0 |
| `crates/platform/node/tests/composition.rs` | added | 39 | 0 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/platform/node/src/composition.rs`
- `crates/platform/node/src/lib.rs`
- `crates/platform/node/tests/composition.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
