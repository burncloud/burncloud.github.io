---
title: "PR #536 — feat(router): identify Node route attachments by channel id"
slug: /pr/pr-536
---

# PR #536 — feat(router): identify Node route attachments by channel id

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-16T19:22:43Z`  
> **Base：** `main@ce821c9e2649`  
> **Head：** `feat/node-stable-attachment-id@982b5151bd82`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME`
- **Changed Files：** `3`
- **Additions / Deletions：** `+44 / -23`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `3`

**PR 说明摘要：** ## Summary Continue S0-08 by making local route attachment identity explicit and stable. ### Contract change - `attach(LocalRouteAttachment)` now returns `LocalRouteAttachmentId` - `LocalRouteAttachmentId` wraps the existing BurnCloud `channel_providers.id` - `detach(...)` now requires that stable id instead of a model name ### Production adapter - `ExistingRouterLocalAttacher::attach` returns the id produced by `ChannelProviderModel::create` - `detach` delegates to `ChannelProviderModel::delete`, which removes the channel's abilities and then the channel ### Invariant A Node may detach only the exact existing Channel created by its attachment operation. It must never delete routes by model name because many channels/nodes may serve the same model. ### Evidence The contract test now proves attach returns a stable id and detach removes only by that id. ### Next Wire the Node reconciliation workflow so `RouterAttached` is observed only after a successful `attach` returns an attachment id; then add the final integration acceptance around existing routing truth.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/traffic/router/src/local_attachment.rs` | modified | 9 | 2 | 0 |
| `crates/traffic/router/src/local_attachment_adapter.rs` | modified | 11 | 10 | 0 |
| `crates/traffic/router/tests/local_attachment_contract.rs` | modified | 24 | 11 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/traffic/router/src/local_attachment.rs`
- `crates/traffic/router/src/local_attachment_adapter.rs`
- `crates/traffic/router/tests/local_attachment_contract.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
