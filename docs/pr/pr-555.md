---
title: "PR #555 — feat(node): settle unsupported local demand"
slug: /pr/pr-555
---

# PR #555 — feat(node): settle unsupported local demand

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-17T16:11:43Z`  
> **Base：** `main@2d32798f5c15`  
> **Head：** `feat/node-local-unsupported-convergence@20de80b8ff77`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME`
- **Changed Files：** `3`
- **Additions / Deletions：** `+12 / -1`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `3`

**PR 说明摘要：** ## Node local unsupported convergence Makes `Unsupported` a settled local outcome instead of leaving the reconciler stuck in `Resolving`. ### Behavior `Resolving -> LocalUnsupported` When Supply proves that this machine has no acceptable local model variant: - record explicit `LocalUnsupported` evidence; - enter `NodeState::LocalUnsupported`; - return `Noop` on later reconciliation; - do not prepare artifacts; - do not prepare runtime; - do not spawn a process; - do not attach a local route; - do not classify the result as a runtime failure. The state may later be cleared back to `Absent` when demand/machine reality changes. ### Boundary This remains a framework-only change. It does not add real hardware thresholds, model catalog logic, downloads, runtimes, or routing behavior.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/interfaces/server/src/node_orchestrator.rs` | modified | 4 | 1 | 0 |
| `crates/platform/node/src/reconciler.rs` | modified | 3 | 0 | 0 |
| `crates/platform/node/src/state.rs` | modified | 5 | 0 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/interfaces/server/src/node_orchestrator.rs`
- `crates/platform/node/src/reconciler.rs`
- `crates/platform/node/src/state.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
