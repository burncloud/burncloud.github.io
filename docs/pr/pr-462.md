---
title: "PR #462 — feat(client): migrate Buyer Overview to modular Dioxus"
slug: /pr/pr-462
---

# PR #462 — feat(client): migrate Buyer Overview to modular Dioxus

> **状态：** OPEN  
> **作者：** `songyi77101`  
> **创建：** `2026-09-09T08:12:12Z`  
> **Base：** `main@c314bff9646f`  
> **Head：** `agent/buyer-overview-rust-migration@7c730acae2e2`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `336`
- **Additions / Deletions：** `+2972 / -13032`
- **受影响的完整 E2E Flow：** `58`
- **未映射 changed files：** `333`

**PR 说明摘要：** ## Summary - migrate the current crates/client implementation to the modular Dioxus directory contract - add the Buyer Overview page, routing aliases, shared Buyer console shell, localization, and design tokens - preserve placeholder routes for non-migrated Buyer/Supplier/Admin pages ## Verification - cargo fmt --package burncloud-client -- --check - cargo check -p burncloud-client --features liveview - cargo check -p burncloud-client --features desktop - cargo check -p burncloud-server The change is intentionally scoped to crates/client.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/client/Cargo.toml` | modified | 16 | 26 | 0 |
| `crates/client/assets/burncloud.rc` | removed | 0 | 1 | 0 |
| `crates/client/assets/favicon.ico` | removed | 0 | 0 | 0 |
| `crates/client/build.rs` | removed | 0 | 19 | 0 |
| `crates/client/scripts/check-functional-wiring.sh` | removed | 0 | 96 | 0 |
| `crates/client/scripts/check-product-ux.sh` | removed | 0 | 142 | 0 |
| `crates/client/scripts/check-ui-conventions.sh` | removed | 0 | 57 | 0 |
| `crates/client/scripts/check-visual-system.sh` | removed | 0 | 71 | 0 |
| `crates/client/src/api/admin/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/api/buyer/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/api/client.rs` | added | 1 | 0 | 0 |
| `crates/client/src/api/error.rs` | added | 1 | 0 | 0 |
| `crates/client/src/api/mod.rs` | added | 8 | 0 | 0 |
| `crates/client/src/api/request.rs` | added | 1 | 0 | 0 |
| `crates/client/src/api/response.rs` | added | 1 | 0 | 0 |
| `crates/client/src/api/shared/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/api/supplier/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/app.rs` | removed | 0 | 159 | 55 |
| `crates/client/src/app/app.rs` | added | 23 | 0 | 0 |
| `crates/client/src/app/bootstrap/auth.rs` | added | 1 | 0 | 0 |
| `crates/client/src/app/bootstrap/i18n.rs` | added | 1 | 0 | 0 |
| `crates/client/src/app/bootstrap/mod.rs` | added | 3 | 0 | 0 |
| `crates/client/src/app/bootstrap/observability.rs` | added | 1 | 0 | 0 |
| `crates/client/src/app/mod.rs` | added | 5 | 0 | 0 |
| `crates/client/src/app/router/console.rs` | added | 21 | 0 | 0 |
| `crates/client/src/app/router/mod.rs` | added | 4 | 0 | 0 |
| `crates/client/src/app/router/not_found.rs` | added | 1 | 0 | 0 |
| `crates/client/src/app/router/public.rs` | added | 1 | 0 | 0 |
| `crates/client/src/app/router/routes.rs` | added | 273 | 0 | 0 |
| `crates/client/src/auth/auth_gate.rs` | added | 1 | 0 | 0 |
| `crates/client/src/auth/authorization.rs` | added | 1 | 0 | 0 |
| `crates/client/src/auth/capabilities.rs` | added | 1 | 0 | 0 |
| `crates/client/src/auth/mod.rs` | added | 6 | 0 | 0 |
| `crates/client/src/auth/return_to.rs` | added | 1 | 0 | 0 |
| `crates/client/src/auth/session.rs` | added | 1 | 0 | 0 |
| `crates/client/src/auth/workspace_gate.rs` | added | 1 | 0 | 0 |
| `crates/client/src/auth_gate.rs` | removed | 0 | 35 | 0 |
| `crates/client/src/backend.rs` | removed | 0 | 737 | 0 |
| `crates/client/src/components.rs` | removed | 0 | 195 | 0 |
| `crates/client/src/critical_pages.css` | removed | 0 | 83 | 0 |
| `crates/client/src/critical_pages.rs` | removed | 0 | 7 | 0 |
| `crates/client/src/critical_pages/auth.rs` | removed | 0 | 387 | 0 |
| `crates/client/src/critical_pages/customers_portable.rs` | removed | 0 | 626 | 0 |
| `crates/client/src/critical_pages/dashboard.rs` | removed | 0 | 469 | 0 |
| `crates/client/src/critical_pages/logs.rs` | removed | 0 | 261 | 0 |
| `crates/client/src/data.rs` | removed | 0 | 183 | 0 |
| `crates/client/src/design/breakpoints.rs` | added | 3 | 0 | 0 |
| `crates/client/src/design/mod.rs` | added | 5 | 0 | 0 |
| `crates/client/src/design/radius.rs` | added | 3 | 0 | 0 |
| `crates/client/src/design/spacing.rs` | added | 4 | 0 | 0 |
| `crates/client/src/design/tokens.rs` | added | 8 | 0 | 0 |
| `crates/client/src/design/typography.rs` | added | 3 | 0 | 0 |
| `crates/client/src/desktop_chrome.css` | removed | 0 | 63 | 0 |
| `crates/client/src/desktop_chrome.rs` | removed | 0 | 98 | 0 |
| `crates/client/src/domains/admin/advanced/evaluation/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/evaluation/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/evaluation/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/evaluation/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/evaluation/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/evaluation/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/evaluation/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/guardrails/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/guardrails/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/guardrails/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/guardrails/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/guardrails/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/guardrails/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/guardrails/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/logs/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/logs/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/logs/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/logs/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/logs/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/logs/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/logs/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/providers/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/providers/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/providers/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/providers/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/providers/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/providers/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/providers/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/routes/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/routes/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/routes/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/routes/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/routes/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/routes/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/routes/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/team/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/team/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/team/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/team/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/team/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/team/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/advanced/team/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/billing/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/billing/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/billing/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/billing/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/billing/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/billing/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/billing/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/capacity/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/capacity/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/capacity/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/capacity/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/capacity/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/capacity/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/capacity/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/customers/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/customers/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/customers/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/customers/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/customers/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/customers/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/customers/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/demand/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/demand/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/demand/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/demand/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/demand/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/demand/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/demand/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/mod.rs` | added | 15 | 0 | 0 |
| `crates/client/src/domains/admin/models/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/models/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/models/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/models/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/models/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/models/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/models/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/navigation.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/operations/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/operations/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/operations/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/operations/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/operations/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/operations/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/operations/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/overview/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/overview/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/overview/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/overview/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/overview/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/overview/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/overview/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/revenue/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/revenue/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/revenue/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/revenue/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/revenue/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/revenue/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/revenue/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/routes.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/settings/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/settings/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/settings/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/settings/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/settings/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/settings/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/settings/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/settlements/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/settlements/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/settlements/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/settlements/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/settlements/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/settlements/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/settlements/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/suppliers/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/suppliers/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/suppliers/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/suppliers/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/suppliers/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/suppliers/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/suppliers/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/supply/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/supply/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/supply/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/admin/supply/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/supply/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/supply/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/admin/supply/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/api_keys/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/api_keys/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/api_keys/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/buyer/api_keys/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/api_keys/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/api_keys/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/api_keys/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/billing/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/billing/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/billing/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/buyer/billing/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/billing/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/billing/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/billing/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/logs/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/logs/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/logs/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/buyer/logs/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/logs/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/logs/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/logs/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/marketplace/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/marketplace/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/marketplace/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/buyer/marketplace/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/marketplace/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/marketplace/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/marketplace/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/mod.rs` | added | 9 | 0 | 0 |
| `crates/client/src/domains/buyer/navigation.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/overview/actions.rs` | added | 8 | 0 | 0 |
| `crates/client/src/domains/buyer/overview/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/overview/mod.rs` | added | 8 | 0 | 0 |
| `crates/client/src/domains/buyer/overview/model.rs` | added | 107 | 0 | 0 |
| `crates/client/src/domains/buyer/overview/page.rs` | added | 157 | 0 | 0 |
| `crates/client/src/domains/buyer/overview/state.rs` | added | 45 | 0 | 0 |
| `crates/client/src/domains/buyer/overview/tests.rs` | added | 70 | 0 | 0 |
| `crates/client/src/domains/buyer/playground/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/playground/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/playground/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/buyer/playground/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/playground/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/playground/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/playground/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/routes.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/usage/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/usage/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/usage/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/buyer/usage/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/usage/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/usage/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/buyer/usage/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/mod.rs` | added | 3 | 0 | 0 |
| `crates/client/src/domains/supplier/deployments/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/deployments/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/deployments/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/supplier/deployments/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/deployments/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/deployments/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/deployments/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/earnings/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/earnings/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/earnings/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/supplier/earnings/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/earnings/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/earnings/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/earnings/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/mod.rs` | added | 9 | 0 | 0 |
| `crates/client/src/domains/supplier/navigation.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/overview/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/overview/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/overview/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/supplier/overview/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/overview/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/overview/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/overview/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/reliability/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/reliability/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/reliability/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/supplier/reliability/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/reliability/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/reliability/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/reliability/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/resources/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/resources/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/resources/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/supplier/resources/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/resources/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/resources/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/resources/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/routes.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/settings/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/settings/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/settings/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/supplier/settings/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/settings/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/settings/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/settings/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/settlements/actions.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/settlements/components/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/settlements/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/domains/supplier/settlements/model.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/settlements/page.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/settlements/state.rs` | added | 1 | 0 | 0 |
| `crates/client/src/domains/supplier/settlements/tests.rs` | added | 1 | 0 | 0 |
| `crates/client/src/functional_api.rs` | removed | 0 | 180 | 0 |
| `crates/client/src/functional_layout.rs` | removed | 0 | 195 | 0 |
| `crates/client/src/functional_pages/access.rs` | removed | 0 | 299 | 0 |
| `crates/client/src/functional_pages/access_live.rs` | removed | 0 | 109 | 0 |
| `crates/client/src/functional_pages/analytics.rs` | removed | 0 | 253 | 0 |
| `crates/client/src/functional_pages/analytics_full.rs` | removed | 0 | 334 | 0 |
| `crates/client/src/functional_pages/api_keys_live.rs` | removed | 0 | 843 | 0 |
| `crates/client/src/functional_pages/catalog.rs` | removed | 0 | 520 | 0 |
| `crates/client/src/functional_pages/guardrails.rs` | removed | 0 | 213 | 0 |
| `crates/client/src/functional_pages/guardrails_live.rs` | removed | 0 | 531 | 0 |
| `crates/client/src/functional_pages/logs_full.rs` | removed | 0 | 416 | 0 |
| `crates/client/src/functional_pages/mod.rs` | removed | 0 | 21 | 0 |
| `crates/client/src/functional_pages/platform.rs` | removed | 0 | 361 | 0 |
| `crates/client/src/functional_pages/platform_live.rs` | removed | 0 | 390 | 0 |
| `crates/client/src/functional_pages/playground.rs` | removed | 0 | 154 | 0 |
| `crates/client/src/functional_pages/playground_live.rs` | removed | 0 | 503 | 0 |
| `crates/client/src/functional_pages/providers.rs` | removed | 0 | 571 | 0 |
| `crates/client/src/functional_pages/settings.rs` | removed | 0 | 161 | 0 |
| `crates/client/src/i18n/formatter.rs` | added | 31 | 0 | 0 |
| `crates/client/src/i18n/locale.rs` | added | 96 | 0 | 0 |
| `crates/client/src/i18n/locales/en.rs` | added | 93 | 0 | 0 |
| `crates/client/src/i18n/locales/ja.rs` | added | 93 | 0 | 0 |
| `crates/client/src/i18n/locales/mod.rs` | added | 98 | 0 | 0 |
| `crates/client/src/i18n/locales/zh.rs` | added | 102 | 0 | 0 |
| `crates/client/src/i18n/locales/zh_tw.rs` | added | 102 | 0 | 0 |
| `crates/client/src/i18n/machine_values.rs` | added | 1 | 0 | 0 |
| `crates/client/src/i18n/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/lib.rs` | modified | 74 | 70 | 19 |
| `crates/client/src/main.rs` | modified | 2 | 9 | 1 |
| `crates/client/src/observability.rs` | removed | 0 | 104 | 0 |
| `crates/client/src/pages.rs` | removed | 0 | 1674 | 0 |
| `crates/client/src/platform/desktop.rs` | added | 1 | 0 | 0 |
| `crates/client/src/platform/liveview.rs` | added | 1 | 0 | 0 |
| `crates/client/src/platform/mod.rs` | added | 3 | 0 | 0 |
| `crates/client/src/platform/web.rs` | added | 1 | 0 | 0 |
| `crates/client/src/product_ui.css` | removed | 0 | 722 | 0 |
| `crates/client/src/route_aliases.rs` | removed | 0 | 21 | 0 |
| `crates/client/src/shared/hooks/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/shared/layout/mod.rs` | added | 332 | 0 | 0 |
| `crates/client/src/shared/mod.rs` | added | 7 | 0 | 0 |
| `crates/client/src/shared/states/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/shared/types/mod.rs` | added | 257 | 0 | 0 |
| `crates/client/src/shared/ui/mod.rs` | added | 415 | 0 | 0 |
| `crates/client/src/shared/utils/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/shared/validation/mod.rs` | added | 1 | 0 | 0 |
| `crates/client/src/styles.css` | removed | 0 | 43 | 0 |
| `crates/client/src/visual_system.css` | removed | 0 | 620 | 0 |

## Affected E2E Impact Matrix

| # | 完整 E2E / Entry Point | 类型 | PR 命中的源码文件 |
|---:|---|---|---|
| 1 | `GET /` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 2 | `GET /home` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 3 | `GET /login` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 4 | `GET /register` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 5 | `GET /forgot-password` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 6 | `GET /reset-password` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 7 | `GET /console` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 8 | `GET /console/` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 9 | `GET /console/{*path}` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 10 | `GET /favicon.ico` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 11 | `GET /preview/home` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 12 | `GET /preview/login` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 13 | `GET /preview/console` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 14 | `GET /preview/console/` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 15 | `GET /preview/console/{*path}` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 16 | `GET /ws` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 17 | `burncloud` | CLI / Executables / burncloud | `crates/client/src/app.rs` |
| 18 | `server` | CLI / Executables / burncloud | `crates/client/src/lib.rs` |
| 19 | `router` | CLI / Executables / burncloud | `crates/client/src/lib.rs` |
| 20 | `client` | CLI / Executables / burncloud | `crates/client/src/app.rs` |
| 21 | `burncloud-client` | CLI / Executables / Workspace Binaries | `crates/client/src/main.rs`&lt;br/&gt;`crates/client/src/app.rs` |
| 22 | `Windows tray thread` | Background Jobs / Async Side Effects / Desktop Background Work | `crates/client/src/app.rs` |
| 23 | `Show-window poll loop` | Background Jobs / Async Side Effects / Desktop Background Work | `crates/client/src/app.rs` |
| 24 | `create_app` | Startup / Startup Chain | `crates/client/src/lib.rs` |
| 25 | `/` | UI-only Actions / Guest / Public | `crates/client/src/app.rs` |
| 26 | `/home` | UI-only Actions / Guest / Public | `crates/client/src/app.rs` |
| 27 | `/login` | UI-only Actions / Guest / Public | `crates/client/src/app.rs` |
| 28 | `/register` | UI-only Actions / Guest / Public | `crates/client/src/app.rs` |
| 29 | `/forgot-password` | UI-only Actions / Guest / Public | `crates/client/src/app.rs` |
| 30 | `/reset-password?:token` | UI-only Actions / Guest / Public | `crates/client/src/app.rs` |
| 31 | `/console/dashboard` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 32 | `/console/deploy` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 33 | `/console/monitor` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 34 | `/console/access` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 35 | `/console/models` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 36 | `/console/users` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 37 | `/console/settings` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 38 | `/console/finance` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 39 | `/console/logs` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 40 | `/console/connect` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 41 | `/console/playground` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 42 | `/console/:..segments → NotFound` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 43 | `/preview/home` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 44 | `/preview/login` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 45 | `/preview/console/dashboard` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 46 | `/preview/console/models` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 47 | `/preview/console/access` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 48 | `/preview/console/settings` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 49 | `/preview/console/finance` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 50 | `/preview/console/monitor` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 51 | `/preview/console/playground` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 52 | `i18n context` | UI-only Actions / Local UI State | `crates/client/src/app.rs` |
| 53 | `Toast state / ToastContainer` | UI-only Actions / Local UI State | `crates/client/src/app.rs` |
| 54 | `Auth context` | UI-only Actions / Local UI State | `crates/client/src/app.rs` |
| 55 | `Theme state` | UI-only Actions / Local UI State | `crates/client/src/app.rs` |
| 56 | `window maximize` | UI-only Actions / Desktop UI | `crates/client/src/app.rs` |
| 57 | `Windows tray startup` | UI-only Actions / Desktop UI | `crates/client/src/app.rs` |
| 58 | `show / hide / focus` | UI-only Actions / Desktop UI | `crates/client/src/app.rs` |

## 完整受影响 E2E Request Flow

### E2E FLOW #1 — GET /

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #2 — GET /home

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /home
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #3 — GET /login

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /login
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #4 — GET /register

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /register`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /register
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #5 — GET /forgot-password

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /forgot-password`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /forgot-password
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #6 — GET /reset-password

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /reset-password`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /reset-password
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #7 — GET /console

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /console`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /console
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #8 — GET /console/

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /console/`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /console/
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #9 — GET /console/&#123;*path&#125;

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /console/{*path}`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /console/{*path}
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #10 — GET /favicon.ico

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /favicon.ico`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /favicon.ico
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: image/x-icon
Content-Length: 5430

<binary favicon bytes>
```

### E2E FLOW #11 — GET /preview/home

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/home
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #12 — GET /preview/login

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/login
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #13 — GET /preview/console

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/console`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/console
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #14 — GET /preview/console/

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/console/`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/console/
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #15 — GET /preview/console/&#123;*path&#125;

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/console/{*path}`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/console/{*path}
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #16 — GET /ws

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /ws`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /ws
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView router
│         └─ NO → fallback routing
│
├─ Feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → /ws not served by LiveView path
│         └─ YES → websocket route available
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ WebSocket upgrade
│    ├─ validate Upgrade/Connection/Sec-WebSocket-* headers
│    └─ DECISION: WebSocket handshake valid?
│         ├─ NO → HTTP handshake error → END
│         └─ YES → 101 Switching Protocols
│
├─ LiveView session
│    ├─ create/attach Dioxus LiveView session
│    ├─ receive browser UI events
│    ├─ update Virtual DOM/server-side UI state
│    └─ send render patches/messages to browser
│
├─ Connection loop
│    └─ DECISION: socket still connected?
│         ├─ YES → await next UI message → repeat
│         └─ NO → cleanup session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Accept: <computed-value>

# 随后进入双向 WebSocket / LiveView 消息通道
```

### E2E FLOW #17 — burncloud

**树路径：** `CLI / Executables → burncloud → burncloud`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Shell / OS: burncloud（无参数）
│
▼
FILE: src/main.rs
│
├─ main()
│    ├─ dotenvy::dotenv()
│    ├─ ensure_master_key()
│    ├─ init_logging()
│    └─ match args == [binary]
│
├─ DECISION: target_os == windows?
│    ├─ YES
│    │    ├─ std::thread::spawn(background server thread)
│    │    │    ├─ tokio::runtime::Runtime::new()
│    │    │    ├─ HOST / PORT
│    │    │    └─ burncloud_server::start_server(host, port, false)
│    │    │         └─ enable_liveview=false：后台 Server 不挂 LiveView Router
│    │    └─ main thread → burncloud_client::launch_gui_with_tray()
│    │
│    └─ NO（Linux/macOS 等）
│         ├─ print Headless Mode startup line
│         └─ run_async_server()
│              └─ burncloud_server::start_server(host, port, true)
│
├─ Windows server thread 后续主链与 `burncloud server` 的 start_server/create_app/create_router_app 相同
├─ Non-Windows 后续主链与 `burncloud server` 完全相同
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Windows GUI branch: launch_gui_with_tray()
│    ├─ WindowBuilder
│    ├─ Config::new().with_window(...).with_data_directory(...)
│    └─ LaunchBuilder::desktop().launch(AppWithTray)
│
├─ AppWithTray()
│    ├─ DesktopMode context
│    ├─ maximize window
│    ├─ std::thread::spawn(start_tray)
│    ├─ spawn async show-window poll loop
│    └─ render App()
│
└─ App()
     ├─ use_init_i18n()
     ├─ use_init_toast()
     ├─ use_init_auth()
     ├─ use_init_theme()
     └─ Router<Route>
│
▼
END / LONG-RUNNING SERVER + UI LOOPS
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
# 非 Windows
Starting BurnCloud Server with LiveView (Headless Mode)...
Unified Gateway listening on 127.0.0.1:3000
- Dashboard: http://127.0.0.1:3000/
- LLM API:   http://127.0.0.1:3000/v1/...

# Windows
background_server_thread=running (enable_liveview=false)
desktop_gui_event_loop=running
system_tray_thread=running
```

### E2E FLOW #18 — server

**树路径：** `CLI / Executables → burncloud → server`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ Shell / OS 入口
│    └─ burncloud server
│    └─ 显式 Server 模式；当前实现同时 enable_liveview=true
│
▼
FILE: src/main.rs
│
├─ main()
│    ├─ dotenvy::dotenv().ok()
│    ├─ ensure_master_key()
│    │    ├─ is_valid_master_key()
│    │    │    ├─ env::var("MASTER_KEY")
│    │    │    ├─ hex::decode(...)
│    │    │    └─ DECISION: decoded key length == 32 bytes?
│    │    │         ├─ YES → keep existing key
│    │    │         └─ NO  → generate replacement
│    │    ├─ rand::random::<[u8;32]>()
│    │    ├─ hex::encode()
│    │    ├─ resolve .env path from CWD / current_exe()
│    │    ├─ read_to_string() when file exists
│    │    ├─ fs::write(.env)
│    │    └─ env::set_var("MASTER_KEY", ...)
│    ├─ env::args().collect()
│    ├─ is_server = args[1] in [server, router, client]
│    └─ burncloud_server::logging::init_logging()
│
▼
FILE: crates/server/src/logging.rs
│
├─ init_logging()
│    ├─ LOG_DIR / LOG_MAX_FILES / RUST_LOG
│    ├─ fs::create_dir_all(log_dir)
│    ├─ tracing_log::LogTracer::init()
│    ├─ file_appender(server/service/database/router)
│    │    └─ RollingFileAppender::builder() → non_blocking(...)
│    ├─ module_filter(...)
│    └─ tracing::subscriber::set_global_default(...)
│
▼
FILE: src/main.rs
│
├─ match args.as_slice()
│    └─ DECISION: subcommand == "server" OR "router"?
│         ├─ NO  → other direct/CLI branch
│         └─ YES → run_async_server()
│
├─ run_async_server() #[tokio::main]
│    ├─ HOST env or "127.0.0.1"
│    ├─ PORT env or DEFAULT_PORT
│    └─ burncloud_server::start_server(host, port, true)
│
├─ IMPORTANT
│    ├─ 当前源码中 `server` 与 `router` 都走同一个 run_async_server()
│    └─ 此 direct branch 不进入 Clap CLI dispatch
│
▼
FILE: crates/server/src/lib.rs
│
├─ start_server(host, port, enable_liveview=true)
│    ├─ create_default_database().await
│    ├─ RouterDatabase::init(&db).await
│    ├─ UserDatabase::init(&db).await
│    ├─ create_app(db, true).await
│    ├─ SocketAddr::parse()
│    ├─ TcpListener::bind(addr).await
│    └─ axum::serve(listener, app).await
│
▼
FILE: crates/database/src/database.rs
│
├─ create_default_database() → Database::new()
├─ Database::new()
│    ├─ DECISION: BURNCLOUD_DATABASE_URL exists?
│    │    ├─ YES → use configured URL
│    │    └─ NO
│    │         ├─ get_default_database_path()
│    │         ├─ create_directory_if_not_exists()
│    │         ├─ DECISION: BURNCLOUD_FRESH_DB == "1" and DB exists?
│    │         │    ├─ YES → fs::remove_file(default DB)
│    │         │    └─ NO  → preserve DB
│    │         └─ build sqlite://...?...mode=rwc
│    └─ Database::initialize().await
│
├─ Database::initialize()
│    ├─ sqlx::any::install_default_drivers()
│    ├─ DatabaseConnection::new(database_url)
│    │    ├─ AnyConnectOptions::from_str()
│    │    └─ AnyPoolOptions::new().max_connections(10).connect_with(...)
│    ├─ DECISION: db.kind() == sqlite?
│    │    └─ YES → PRAGMA journal_mode=WAL
│    ├─ MigrationRunner::run(self)
│    └─ Schema::init(self)
│
▼
FILE: crates/database/src/migration/mod.rs
│
├─ MigrationRunner::run()
│    ├─ ensure _schema_migrations table
│    ├─ choose SQLite / PostgreSQL migration catalogue
│    ├─ inspect already-applied versions
│    └─ execute pending versioned DDL migrations
│
▼
FILE: crates/database/src/schema/mod.rs
│
├─ Schema::init()
│    ├─ rename::migrate_table_renames()
│    ├─ router::migrate_router_logs()
│    ├─ price::migrate_prices()
│    └─ user::migrate_users_and_seed()
│
▼
FILE: crates/server/src/lib.rs
│
└─ start_server() continues → RouterDatabase::init(&db)
│
▼
FILE: crates/database/crates/router/src/lib.rs
│
├─ RouterDatabase::init()
│    ├─ db.get_connection()
│    ├─ db.kind()
│    ├─ CREATE TABLE IF NOT EXISTS router_tokens
│    └─ SQLite-only compatibility ALTER TABLE statements
│
▼
FILE: crates/server/src/lib.rs
│
└─ start_server() continues → UserDatabase::init(&db)
│
▼
FILE: crates/database/crates/user/src/lib.rs
│
├─ UserDatabase::init()
│    ├─ CREATE / verify user_roles
│    ├─ CREATE / verify user_role_bindings
│    ├─ CREATE / verify user_recharges
│    ├─ SQLite/PostgreSQL compatibility migrations
│    ├─ SELECT COUNT(*) FROM user_roles
│    ├─ DECISION: no roles exist?
│    │    └─ YES → seed admin/user roles
│    ├─ query users without role bindings
│    └─ assign_role(...) for orphan users when required
│
▼
FILE: crates/server/src/lib.rs
│
├─ create_app(db, enable_liveview=true)
│    ├─ SystemMonitorService::new()
│    ├─ monitor.start_auto_update().await
│    ├─ CacheService::new().await
│    ├─ cache.is_available().await
│    ├─ create_router_app(db.clone()).await
│    ├─ UserService::new()
│    ├─ api::routes(state.clone())
│    ├─ build /health + merge Management/Internal routers
│    ├─ DECISION: enable_liveview?
│    │    └─ YES → burncloud_client::liveview_router(db.clone()) → merge
│    ├─ fallback_service(router_app)
│    └─ SetRequestId / PropagateRequestId / Trace / CORS layers
│
▼
FILE: crates/service/crates/monitor/src/service.rs
│
├─ SystemMonitorService::new()
├─ start_auto_update()
│    └─ tokio::spawn
│         └─ LOOP interval(1s)
│              └─ collect_metrics_internal()
│                   ├─ CpuCollector::collect()
│                   ├─ MemoryCollector::collect()
│                   └─ DiskCollector::collect_all()
│
▼
FILE: crates/service/crates/cache/src/service.rs
│
├─ CacheService::new() → with_config(CacheConfig::default())
├─ DECISION: CACHE_ENABLED?
│    ├─ NO  → return disabled cache
│    └─ YES
│         ├─ require REDIS_URL
│         ├─ redis::Client::open()
│         ├─ get_connection_manager().await
│         └─ PING
│
▼
FILE: crates/router/src/lib.rs
│
├─ create_router_app(db)
│    ├─ reqwest::Client::builder() + timeouts/pool settings
│    ├─ RoundRobinBalancer::new()
│    ├─ RateLimiter::new(100, 10)
│    ├─ CircuitBreaker::new(...)
│    ├─ ModelRouter::new(db.clone())
│    ├─ ChannelStateTracker::new()
│    ├─ DynamicAdaptorFactory::new(db.clone())
│    ├─ ApiVersionDetector::new(db.clone())
│    ├─ PriceCache::load(&db).await
│    │    └─ DECISION: load fails? → PriceCache::empty()
│    ├─ CostCalculator::new(price_cache.clone())
│    ├─ ExchangeRateService::new(db.clone())
│    ├─ load_rates_from_db().await
│    ├─ tokio::spawn → ExchangeRateService::start_sync_task()
│    ├─ scheduler::load_scheduler_config()
│    ├─ AffinityCache::default()
│    ├─ InMemoryBudget::new()
│    ├─ configure_rate_budget_from_db(...).await
│    ├─ read BILLING_STRICT_MODE
│    ├─ read REQUEST_LOG_STORAGE_POLICY
│    ├─ tokio::spawn → AIMD budget-update consumer
│    ├─ price_sync::start_price_sync_task(...)
│    ├─ tokio::spawn → RouterLog writer → RouterDatabase::insert_log()
│    ├─ tokio::spawn → RequestLog writer → RouterDatabase::insert_request_log()
│    ├─ ChannelHealthManager::new()
│    ├─ build internal_app routes
│    │    ├─ /console/internal/health
│    │    ├─ /console/internal/prices/sync
│    │    ├─ /console/internal/circuit-breaker/trip-all
│    │    └─ /console/internal/metrics
│    └─ build data-plane app
│         ├─ GET /v1/models → models_handler [REGISTER ONLY]
│         ├─ GET /api/v1/usage → usage_handler [REGISTER ONLY]
│         ├─ GET /api/v1/usage/models → usage_models_handler [REGISTER ONLY]
│         └─ fallback(proxy_handler) [REGISTER ONLY]
│
├─ IMPORTANT: 上述 Handler 在启动时只被注册；收到对应 HTTP 请求后才执行
│
▼
FILE: crates/service/crates/billing/src/cache.rs
│
├─ PriceCache::load(db)
│    ├─ PriceCache::empty()
│    └─ refresh(db)
│         └─ BillingPriceModel::list(...)
│
▼
FILE: crates/router/src/exchange_rate.rs
│
├─ ExchangeRateService::new()
├─ load_rates_from_db()
└─ start_sync_task()
     └─ tokio::spawn hourly loop
│
▼
FILE: crates/router/src/price_sync.rs
│
├─ start_price_sync_task(...)
│    └─ tokio::spawn
│         ├─ PriceSyncService::new()/with_config()
│         ├─ startup sync_all(false) unless SKIP_INITIAL_PRICE_SYNC
│         └─ LOOP select(periodic tick, force-sync receiver)
│
▼
FILE: crates/server/src/api/mod.rs
│
├─ api::routes(state)
│    ├─ auth::public_routes() [REGISTER]
│    ├─ auth::protected_routes() [REGISTER]
│    ├─ billing::routes() [REGISTER]
│    ├─ channel::routes() [REGISTER]
│    ├─ token::routes() [REGISTER]
│    ├─ log::routes() [REGISTER]
│    ├─ monitor::routes() [REGISTER]
│    ├─ user::routes() [REGISTER]
│    ├─ security::security_routes() [REGISTER]
│    ├─ openapi::routes() [REGISTER]
│    ├─ cache::routes() [REGISTER]
│    └─ auth_middleware layered over protected router [REGISTER]
│
├─ IMPORTANT: routes() 函数在启动期执行；具体 HTTP Handler 此刻不执行
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ liveview_router(db)
│    ├─ LiveViewPool::new()
│    ├─ register HTML shell routes /, /home, /login, /register, ...
│    ├─ register /console and /console/*
│    ├─ register /favicon.ico
│    └─ register WS_PATH
│         └─ app::App is passed as future WebSocket-session callback [REGISTER ONLY]
│
▼
FILE: crates/server/src/lib.rs
│
├─ create_app() returns Unified Router
├─ SocketAddr::parse(host:port)
├─ TcpListener::bind(addr).await
├─ axum::serve(listener, app).await
│
└─ LONG-RUNNING STATE
     ├─ HTTP listener waits for Management / Internal / Data Plane / LiveView traffic
     ├─ monitor updater keeps running
     ├─ exchange-rate task keeps running
     ├─ price-sync task keeps running
     ├─ AIMD feedback consumer keeps running
     ├─ RouterLog writer keeps running
     └─ RequestLog writer keeps running
│
▼
END = server shutdown or fatal serve error
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
Unified Gateway listening on 127.0.0.1:3000
- Dashboard: http://127.0.0.1:3000/
- LLM API:   http://127.0.0.1:3000/v1/...

# 随后 axum::serve 持续运行，命令不会立即返回到 Shell。
```

### E2E FLOW #19 — router

**树路径：** `CLI / Executables → burncloud → router`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ Shell / OS 入口
│    └─ burncloud router
│    └─ 源码命令名为 router，但当前实现与 server 一样直接进入 run_async_server()
│
▼
FILE: src/main.rs
│
├─ main()
│    ├─ dotenvy::dotenv().ok()
│    ├─ ensure_master_key()
│    │    ├─ is_valid_master_key()
│    │    │    ├─ env::var("MASTER_KEY")
│    │    │    ├─ hex::decode(...)
│    │    │    └─ DECISION: decoded key length == 32 bytes?
│    │    │         ├─ YES → keep existing key
│    │    │         └─ NO  → generate replacement
│    │    ├─ rand::random::<[u8;32]>()
│    │    ├─ hex::encode()
│    │    ├─ resolve .env path from CWD / current_exe()
│    │    ├─ read_to_string() when file exists
│    │    ├─ fs::write(.env)
│    │    └─ env::set_var("MASTER_KEY", ...)
│    ├─ env::args().collect()
│    ├─ is_server = args[1] in [server, router, client]
│    └─ burncloud_server::logging::init_logging()
│
▼
FILE: crates/server/src/logging.rs
│
├─ init_logging()
│    ├─ LOG_DIR / LOG_MAX_FILES / RUST_LOG
│    ├─ fs::create_dir_all(log_dir)
│    ├─ tracing_log::LogTracer::init()
│    ├─ file_appender(server/service/database/router)
│    │    └─ RollingFileAppender::builder() → non_blocking(...)
│    ├─ module_filter(...)
│    └─ tracing::subscriber::set_global_default(...)
│
▼
FILE: src/main.rs
│
├─ match args.as_slice()
│    └─ DECISION: subcommand == "server" OR "router"?
│         ├─ NO  → other direct/CLI branch
│         └─ YES → run_async_server()
│
├─ run_async_server() #[tokio::main]
│    ├─ HOST env or "127.0.0.1"
│    ├─ PORT env or DEFAULT_PORT
│    └─ burncloud_server::start_server(host, port, true)
│
├─ IMPORTANT
│    ├─ 当前源码中 `server` 与 `router` 都走同一个 run_async_server()
│    └─ 此 direct branch 不进入 Clap CLI dispatch
│
▼
FILE: crates/server/src/lib.rs
│
├─ start_server(host, port, enable_liveview=true)
│    ├─ create_default_database().await
│    ├─ RouterDatabase::init(&db).await
│    ├─ UserDatabase::init(&db).await
│    ├─ create_app(db, true).await
│    ├─ SocketAddr::parse()
│    ├─ TcpListener::bind(addr).await
│    └─ axum::serve(listener, app).await
│
▼
FILE: crates/database/src/database.rs
│
├─ create_default_database() → Database::new()
├─ Database::new()
│    ├─ DECISION: BURNCLOUD_DATABASE_URL exists?
│    │    ├─ YES → use configured URL
│    │    └─ NO
│    │         ├─ get_default_database_path()
│    │         ├─ create_directory_if_not_exists()
│    │         ├─ DECISION: BURNCLOUD_FRESH_DB == "1" and DB exists?
│    │         │    ├─ YES → fs::remove_file(default DB)
│    │         │    └─ NO  → preserve DB
│    │         └─ build sqlite://...?...mode=rwc
│    └─ Database::initialize().await
│
├─ Database::initialize()
│    ├─ sqlx::any::install_default_drivers()
│    ├─ DatabaseConnection::new(database_url)
│    │    ├─ AnyConnectOptions::from_str()
│    │    └─ AnyPoolOptions::new().max_connections(10).connect_with(...)
│    ├─ DECISION: db.kind() == sqlite?
│    │    └─ YES → PRAGMA journal_mode=WAL
│    ├─ MigrationRunner::run(self)
│    └─ Schema::init(self)
│
▼
FILE: crates/database/src/migration/mod.rs
│
├─ MigrationRunner::run()
│    ├─ ensure _schema_migrations table
│    ├─ choose SQLite / PostgreSQL migration catalogue
│    ├─ inspect already-applied versions
│    └─ execute pending versioned DDL migrations
│
▼
FILE: crates/database/src/schema/mod.rs
│
├─ Schema::init()
│    ├─ rename::migrate_table_renames()
│    ├─ router::migrate_router_logs()
│    ├─ price::migrate_prices()
│    └─ user::migrate_users_and_seed()
│
▼
FILE: crates/server/src/lib.rs
│
└─ start_server() continues → RouterDatabase::init(&db)
│
▼
FILE: crates/database/crates/router/src/lib.rs
│
├─ RouterDatabase::init()
│    ├─ db.get_connection()
│    ├─ db.kind()
│    ├─ CREATE TABLE IF NOT EXISTS router_tokens
│    └─ SQLite-only compatibility ALTER TABLE statements
│
▼
FILE: crates/server/src/lib.rs
│
└─ start_server() continues → UserDatabase::init(&db)
│
▼
FILE: crates/database/crates/user/src/lib.rs
│
├─ UserDatabase::init()
│    ├─ CREATE / verify user_roles
│    ├─ CREATE / verify user_role_bindings
│    ├─ CREATE / verify user_recharges
│    ├─ SQLite/PostgreSQL compatibility migrations
│    ├─ SELECT COUNT(*) FROM user_roles
│    ├─ DECISION: no roles exist?
│    │    └─ YES → seed admin/user roles
│    ├─ query users without role bindings
│    └─ assign_role(...) for orphan users when required
│
▼
FILE: crates/server/src/lib.rs
│
├─ create_app(db, enable_liveview=true)
│    ├─ SystemMonitorService::new()
│    ├─ monitor.start_auto_update().await
│    ├─ CacheService::new().await
│    ├─ cache.is_available().await
│    ├─ create_router_app(db.clone()).await
│    ├─ UserService::new()
│    ├─ api::routes(state.clone())
│    ├─ build /health + merge Management/Internal routers
│    ├─ DECISION: enable_liveview?
│    │    └─ YES → burncloud_client::liveview_router(db.clone()) → merge
│    ├─ fallback_service(router_app)
│    └─ SetRequestId / PropagateRequestId / Trace / CORS layers
│
▼
FILE: crates/service/crates/monitor/src/service.rs
│
├─ SystemMonitorService::new()
├─ start_auto_update()
│    └─ tokio::spawn
│         └─ LOOP interval(1s)
│              └─ collect_metrics_internal()
│                   ├─ CpuCollector::collect()
│                   ├─ MemoryCollector::collect()
│                   └─ DiskCollector::collect_all()
│
▼
FILE: crates/service/crates/cache/src/service.rs
│
├─ CacheService::new() → with_config(CacheConfig::default())
├─ DECISION: CACHE_ENABLED?
│    ├─ NO  → return disabled cache
│    └─ YES
│         ├─ require REDIS_URL
│         ├─ redis::Client::open()
│         ├─ get_connection_manager().await
│         └─ PING
│
▼
FILE: crates/router/src/lib.rs
│
├─ create_router_app(db)
│    ├─ reqwest::Client::builder() + timeouts/pool settings
│    ├─ RoundRobinBalancer::new()
│    ├─ RateLimiter::new(100, 10)
│    ├─ CircuitBreaker::new(...)
│    ├─ ModelRouter::new(db.clone())
│    ├─ ChannelStateTracker::new()
│    ├─ DynamicAdaptorFactory::new(db.clone())
│    ├─ ApiVersionDetector::new(db.clone())
│    ├─ PriceCache::load(&db).await
│    │    └─ DECISION: load fails? → PriceCache::empty()
│    ├─ CostCalculator::new(price_cache.clone())
│    ├─ ExchangeRateService::new(db.clone())
│    ├─ load_rates_from_db().await
│    ├─ tokio::spawn → ExchangeRateService::start_sync_task()
│    ├─ scheduler::load_scheduler_config()
│    ├─ AffinityCache::default()
│    ├─ InMemoryBudget::new()
│    ├─ configure_rate_budget_from_db(...).await
│    ├─ read BILLING_STRICT_MODE
│    ├─ read REQUEST_LOG_STORAGE_POLICY
│    ├─ tokio::spawn → AIMD budget-update consumer
│    ├─ price_sync::start_price_sync_task(...)
│    ├─ tokio::spawn → RouterLog writer → RouterDatabase::insert_log()
│    ├─ tokio::spawn → RequestLog writer → RouterDatabase::insert_request_log()
│    ├─ ChannelHealthManager::new()
│    ├─ build internal_app routes
│    │    ├─ /console/internal/health
│    │    ├─ /console/internal/prices/sync
│    │    ├─ /console/internal/circuit-breaker/trip-all
│    │    └─ /console/internal/metrics
│    └─ build data-plane app
│         ├─ GET /v1/models → models_handler [REGISTER ONLY]
│         ├─ GET /api/v1/usage → usage_handler [REGISTER ONLY]
│         ├─ GET /api/v1/usage/models → usage_models_handler [REGISTER ONLY]
│         └─ fallback(proxy_handler) [REGISTER ONLY]
│
├─ IMPORTANT: 上述 Handler 在启动时只被注册；收到对应 HTTP 请求后才执行
│
▼
FILE: crates/service/crates/billing/src/cache.rs
│
├─ PriceCache::load(db)
│    ├─ PriceCache::empty()
│    └─ refresh(db)
│         └─ BillingPriceModel::list(...)
│
▼
FILE: crates/router/src/exchange_rate.rs
│
├─ ExchangeRateService::new()
├─ load_rates_from_db()
└─ start_sync_task()
     └─ tokio::spawn hourly loop
│
▼
FILE: crates/router/src/price_sync.rs
│
├─ start_price_sync_task(...)
│    └─ tokio::spawn
│         ├─ PriceSyncService::new()/with_config()
│         ├─ startup sync_all(false) unless SKIP_INITIAL_PRICE_SYNC
│         └─ LOOP select(periodic tick, force-sync receiver)
│
▼
FILE: crates/server/src/api/mod.rs
│
├─ api::routes(state)
│    ├─ auth::public_routes() [REGISTER]
│    ├─ auth::protected_routes() [REGISTER]
│    ├─ billing::routes() [REGISTER]
│    ├─ channel::routes() [REGISTER]
│    ├─ token::routes() [REGISTER]
│    ├─ log::routes() [REGISTER]
│    ├─ monitor::routes() [REGISTER]
│    ├─ user::routes() [REGISTER]
│    ├─ security::security_routes() [REGISTER]
│    ├─ openapi::routes() [REGISTER]
│    ├─ cache::routes() [REGISTER]
│    └─ auth_middleware layered over protected router [REGISTER]
│
├─ IMPORTANT: routes() 函数在启动期执行；具体 HTTP Handler 此刻不执行
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ liveview_router(db)
│    ├─ LiveViewPool::new()
│    ├─ register HTML shell routes /, /home, /login, /register, ...
│    ├─ register /console and /console/*
│    ├─ register /favicon.ico
│    └─ register WS_PATH
│         └─ app::App is passed as future WebSocket-session callback [REGISTER ONLY]
│
▼
FILE: crates/server/src/lib.rs
│
├─ create_app() returns Unified Router
├─ SocketAddr::parse(host:port)
├─ TcpListener::bind(addr).await
├─ axum::serve(listener, app).await
│
└─ LONG-RUNNING STATE
     ├─ HTTP listener waits for Management / Internal / Data Plane / LiveView traffic
     ├─ monitor updater keeps running
     ├─ exchange-rate task keeps running
     ├─ price-sync task keeps running
     ├─ AIMD feedback consumer keeps running
     ├─ RouterLog writer keeps running
     └─ RequestLog writer keeps running
│
▼
END = server shutdown or fatal serve error
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
Unified Gateway listening on 127.0.0.1:3000
- Dashboard: http://127.0.0.1:3000/
- LLM API:   http://127.0.0.1:3000/v1/...

# 随后 axum::serve 持续运行，命令不会立即返回到 Shell。
```

### E2E FLOW #20 — client

**树路径：** `CLI / Executables → burncloud → client`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Shell: burncloud client
│
▼
FILE: src/main.rs
│
├─ main()
│    ├─ dotenvy::dotenv()
│    ├─ ensure_master_key()
│    ├─ init_logging()
│    └─ match subcommand == "client"
│
├─ DECISION: target_os == windows?
│    ├─ YES → burncloud_client::launch_gui_with_tray()
│    └─ NO
│         ├─ println("Desktop GUI is only available on Windows.")
│         ├─ println("On Linux, use 'burncloud server' ...")
│         └─ return Ok(()) → END
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ launch_gui_with_tray()
│    ├─ WindowBuilder::new()
│    ├─ configure title / size / resizable / decorations
│    ├─ load Windows icon when available
│    ├─ temp_dir()/burncloud_webview_data
│    ├─ Config::new().with_window(...).with_data_directory(...)
│    └─ LaunchBuilder::desktop().with_cfg(config).launch(AppWithTray)
│
├─ AppWithTray()
│    ├─ use_context_provider(DesktopMode)
│    ├─ use_window()
│    ├─ use_effect → set_maximized(true)
│    ├─ std::thread::spawn → start_tray()
│    ├─ use_effect → spawn async 100ms show-window poll
│    │    └─ should_show_window() → set_visible / set_focus
│    └─ rsx! { App {} }
│
├─ App()
│    ├─ use_init_i18n()
│    ├─ use_init_toast()
│    ├─ use_init_auth()
│    ├─ use_init_theme()
│    ├─ ToastContainer
│    └─ Router<Route>
│
▼
END / DESKTOP EVENT LOOP
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
# Windows
BurnCloud desktop window + tray enter the long-running GUI event loop.

# 非 Windows（源码真实 stdout）
Desktop GUI is only available on Windows.
On Linux, use 'burncloud server' to start the web dashboard.
```

### E2E FLOW #21 — burncloud-client

**树路径：** `CLI / Executables → Workspace Binaries → burncloud-client`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/main.rs`, `crates/client/src/app.rs`

```text
START
│
├─ OS process launch
│    ├─ executable: burncloud-client
│    ├─ argv / cwd / environment inherited from OS
│    └─ DECISION: executable can be loaded/launched?
│         ├─ NO → OS-level error → END
│         └─ YES → main()
│
▼
FILE: crates/client/src/main.rs  [PR CHANGED]
│
├─ main() initialization
│    ├─ initialize executable-specific runtime/services
│    ├─ parse any supported arguments
│    └─ DECISION: platform/arguments/initialization valid?
│         ├─ NO → print/return error → process exit
│         └─ YES → continue
│
├─ Runtime work
│    ├─ create client/download/loop/tray structures as applicable
│    ├─ start event loop or execute one-shot job
│    └─ DECISION: long-running executable?
│         ├─ YES → enter event/service loop
│         └─ NO → produce output and exit
│
├─ Error boundary
│    └─ runtime error → log/return non-success according to executable implementation
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / RUNNING LOOP
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

**FILE: `crates/client/src/main.rs`**

HUNKS:
```text
@@ -3,12 +3,5 @@ fn main() {
```

BEFORE / REMOVED:
```diff
-#[cfg(all(not(feature = "desktop"), feature = "web"))]
-fn main() {
-    burncloud_client::launch_web();
-}
-
-#[cfg(all(not(feature = "desktop"), not(feature = "web")))]
-fn main() {
-    panic!("Please enable either the 'desktop' or 'web' feature");
```

AFTER / ADDED:
```diff
+#[cfg(not(feature = "desktop"))]
+fn main() {}
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
BurnCloud Client
ui=initialized
route=/
status=running
```

### E2E FLOW #22 — Windows tray thread

**树路径：** `Background Jobs / Async Side Effects → Desktop Background Work → Windows tray thread`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START / TRIGGER
│
├─ Trigger source
│    ├─ startup spawn / request-time tokio::spawn / manager restoration / channel message
│    └─ task: Windows tray thread
│
├─ Spawn / registration
│    ├─ parent runtime creates async task/thread/loop
│    └─ DECISION: spawn/runtime handle available?
│         ├─ NO → task never starts; parent may log error
│         └─ YES → task owns/borrows required shared state
│
├─ Wait boundary
│    ├─ timer sleep / mpsc receive / polling interval / restored work item
│    └─ DECISION: trigger/event available?
│         ├─ NO → continue waiting
│         └─ YES → one iteration begins
│
├─ Input snapshot
│    ├─ read latest runtime/DB/request-derived state needed by job
│    └─ freeze iteration context
│
├─ Core job operation
│    ├─ execute Windows tray thread
│    └─ may call DB / HTTP / filesystem / runtime service depending on task
│
├─ Operation result
│    └─ DECISION: iteration succeeds?
│         ├─ NO
│         │    ├─ log/record failure
│         │    ├─ preserve parent request availability when task is fail-open
│         │    └─ decide retry on next event/interval
│         └─ YES
│              ├─ update in-memory state and/or persistent state
│              └─ emit success telemetry/log
│
├─ Cancellation / lifetime
│    └─ DECISION: parent runtime still alive AND task should continue?
│         ├─ YES → 返回等待边界
│         └─ NO → release task resources
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / NEXT ITERATION
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
tray=started icon=visible menu_items=3 status=running
```

### E2E FLOW #23 — Show-window poll loop

**树路径：** `Background Jobs / Async Side Effects → Desktop Background Work → Show-window poll loop`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START / TRIGGER
│
├─ Trigger source
│    ├─ startup spawn / request-time tokio::spawn / manager restoration / channel message
│    └─ task: Show-window poll loop
│
├─ Spawn / registration
│    ├─ parent runtime creates async task/thread/loop
│    └─ DECISION: spawn/runtime handle available?
│         ├─ NO → task never starts; parent may log error
│         └─ YES → task owns/borrows required shared state
│
├─ Wait boundary
│    ├─ timer sleep / mpsc receive / polling interval / restored work item
│    └─ DECISION: trigger/event available?
│         ├─ NO → continue waiting
│         └─ YES → one iteration begins
│
├─ Input snapshot
│    ├─ read latest runtime/DB/request-derived state needed by job
│    └─ freeze iteration context
│
├─ Core job operation
│    ├─ execute Show-window poll loop
│    └─ may call DB / HTTP / filesystem / runtime service depending on task
│
├─ Operation result
│    └─ DECISION: iteration succeeds?
│         ├─ NO
│         │    ├─ log/record failure
│         │    ├─ preserve parent request availability when task is fail-open
│         │    └─ decide retry on next event/interval
│         └─ YES
│              ├─ update in-memory state and/or persistent state
│              └─ emit success telemetry/log
│
├─ Cancellation / lifetime
│    └─ DECISION: parent runtime still alive AND task should continue?
│         ├─ YES → 返回等待边界
│         └─ NO → release task resources
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / NEXT ITERATION
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
show_window=true visible=true focused=true poll_interval_ms=100
```

### E2E FLOW #24 — create_app

**树路径：** `Startup → Startup Chain → create_app`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ Process/environment input
│    ├─ startup target: create_app
│    ├─ environment variables / dotenv
│    ├─ CLI/platform mode
│    └─ filesystem/database availability
│
├─ Enter startup function
│    └─ execute create_app
│
├─ Dependency initialization
│    ├─ construct required DB/services/runtime state
│    ├─ register routes/tasks as applicable
│    └─ DECISION: dependency initialization succeeds?
│         ├─ NO → propagate startup error → process/runtime not ready → END
│         └─ YES → next dependency
│
├─ Runtime composition
│    ├─ wire shared Arc/State/services
│    ├─ compose routers/middleware/background jobs
│    └─ make dependencies reachable from runtime entrypoints
│
├─ Readiness boundary
│    └─ DECISION: all required startup stages complete?
│         ├─ NO → startup fails/returns Err
│         └─ YES → expose listener/client/event loop/runtime
│
├─ Steady-state handoff
│    ├─ long-running loops take ownership of runtime
│    └─ requests/events can now enter documented entrypoints
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/server/src/lib.rs
│    │    ├─ start_server()
│    │    │    └─ CALL → create_default_database() @ crates/database/src/database.rs
│    │    │    └─ CALL → RouterDatabase::init() @ crates/database/crates/router/src/lib.rs
│    │    │    └─ CALL → UserDatabase::init() @ crates/database/crates/user/src/lib.rs
│    │    │    └─ CALL → create_app() @ crates/server/src/lib.rs
│    │    ├─ create_app()
│    │    │    └─ CALL → SystemMonitorService::new() @ crates/service/crates/monitor/src/service.rs
│    │    │    └─ CALL → CacheService::new() @ crates/service/crates/cache/src/service.rs
│    │    │    └─ CALL → create_router_app() @ crates/router/src/lib.rs
│    │    │    └─ CALL → UserService::new() @ crates/service/crates/user/src/lib.rs
│    │    │    └─ CALL → routes() @ crates/server/src/api/mod.rs
│    ├─ FILE: crates/server/src/api/mod.rs
│    │    ├─ routes()
│    │    │    └─ CALL → public_routes() @ crates/server/src/api/auth.rs
│    │    │    └─ CALL → protected_routes() @ crates/server/src/api/auth.rs
│    │    │    └─ CALL → security_routes() @ crates/server/src/api/security.rs
│    ├─ FILE: crates/client/src/lib.rs  [PR CHANGED]
│    │    ├─ liveview_router()
│    │    │    └─ CALL → liveview_style_tags() @ crates/client/crates/client-shared/src/app_styles.rs
│    ├─ FILE: crates/router/src/lib.rs
│    │    ├─ create_router_app()
│    │    │    └─ CALL → RoundRobinBalancer::new() @ crates/router/src/balancer/mod.rs
│    │    │    └─ CALL → RateLimiter::new() @ crates/router/src/limiter.rs
│    │    │    └─ CALL → CircuitBreaker::new() @ crates/router/src/circuit_breaker.rs
│    │    │    └─ CALL → ModelRouter::new() @ crates/router/src/model_router.rs
│    │    │    └─ CALL → ChannelStateTracker::new() @ crates/router/src/channel_state.rs
│    │    │    └─ CALL → DynamicAdaptorFactory::new() @ crates/router/src/adaptor/factory.rs
│    │    │    └─ CALL → ApiVersionDetector::new() @ crates/router/src/adaptor/detector.rs
│    │    │    └─ CALL → PriceCache::load() @ crates/service/crates/billing/src/cache.rs
│    │    │    └─ CALL → PriceCache::empty() @ crates/service/crates/billing/src/cache.rs
│    │    │    └─ CALL → CostCalculator::new() @ crates/service/crates/billing/src/calculator.rs
│    │    ├─ proxy_handler()
│    │    │    └─ CALL → normalize_doubled_path() @ crates/router/src/lib.rs
│    │    │    └─ CALL → build_response_with_header() @ crates/router/src/lib.rs
│    │    │    └─ CALL → RouterDatabase::validate_token_and_get_info() @ crates/database/crates/router/src/lib.rs
│    │    │    └─ CALL → RouterDatabase::update_token_accessed_time() @ crates/database/crates/router/src/lib.rs
│    │    │    └─ CALL → RouterDatabase::validate_token_detailed() @ crates/database/crates/router/src/lib.rs
│    │    │    └─ CALL → extract_model_from_gemini_path() @ crates/router/src/passthrough.rs
│    │    │    └─ CALL → RouterVideoTaskModel::get_by_task_id() @ crates/database/crates/router/src/router_video_task.rs
│    │    │    └─ CALL → ChannelProviderModel::get_by_id() @ crates/database/crates/channel/src/channel_provider.rs
│    │    │    └─ CALL → UnifiedTokenCounter::new() @ crates/service/crates/billing/src/counter.rs
│    │    │    └─ CALL → proxy_logic() @ crates/router/src/lib.rs
│    │    ├─ proxy_logic()
│    │    │    └─ CALL → sanitize_request_body() @ crates/router/src/lib.rs
│    │    │    └─ CALL → sanitize_request_headers() @ crates/router/src/lib.rs
│    │    │    └─ CALL → extract_model_from_gemini_path() @ crates/router/src/passthrough.rs
│    │    │    └─ CALL → UserService::resolve_traffic_class() @ crates/service/crates/user/src/lib.rs
│    │    │    └─ CALL → OrderType::from_db_row() @ crates/router/src/order_type.rs
│    │    │    └─ CALL → build_response_with_header() @ crates/router/src/lib.rs
│    │    │    └─ CALL → record_failover_attempt() @ crates/router/src/lib.rs
│    │    │    └─ CALL → BudgetGuard::new() @ crates/router/src/rate_budget.rs
│    │    │    └─ CALL → should_passthrough() @ crates/router/src/passthrough.rs
│    │    │    └─ CALL → build_gemini_passthrough_url() @ crates/router/src/passthrough.rs
│    ├─ FILE: crates/database/src/database.rs
│    │    ├─ create_default_database()
│    │    │    └─ CALL → Database::new() @ crates/database/src/database.rs
│    ├─ FILE: crates/database/crates/router/src/lib.rs
│    │    ├─ RouterDatabase::init()
│    ├─ FILE: crates/database/crates/user/src/lib.rs
│    │    ├─ UserDatabase::init()
│    │    │    └─ CALL → UserDatabase::assign_role() @ crates/database/crates/user/src/lib.rs
│    ├─ FILE: crates/service/crates/monitor/src/service.rs
│    │    ├─ SystemMonitorService::new()
│    ├─ FILE: crates/service/crates/cache/src/service.rs
│    │    ├─ CacheService::new()
│    │    │    └─ CALL → CacheService::with_config() @ crates/service/crates/cache/src/service.rs
│    ├─ FILE: crates/service/crates/user/src/lib.rs
│    │    ├─ UserService::new()
│    ├─ FILE: crates/server/src/api/auth.rs
│    │    ├─ public_routes()
│    │    ├─ protected_routes()
│    ├─ FILE: crates/server/src/api/security.rs
│    │    ├─ security_routes()
│    ├─ FILE: crates/client/crates/client-shared/src/app_styles.rs
│    │    ├─ liveview_style_tags()
│    ├─ FILE: crates/router/src/balancer/mod.rs
│    │    ├─ RoundRobinBalancer::new()
│    ├─ FILE: crates/router/src/limiter.rs
│    │    ├─ RateLimiter::new()
│    ├─ FILE: crates/router/src/circuit_breaker.rs
│    │    ├─ CircuitBreaker::new()
│    ├─ FILE: crates/router/src/model_router.rs
│    │    ├─ ModelRouter::new()
│    ├─ FILE: crates/router/src/channel_state.rs
│    │    ├─ ChannelStateTracker::new()
│    ├─ FILE: crates/router/src/adaptor/factory.rs
│    │    ├─ DynamicAdaptorFactory::new()
│    ├─ FILE: crates/router/src/adaptor/detector.rs
│    │    ├─ ApiVersionDetector::new()
│    ├─ FILE: crates/service/crates/billing/src/cache.rs
│    │    ├─ PriceCache::load()
│    │    │    └─ CALL → PriceCache::empty() @ crates/service/crates/billing/src/cache.rs
│    │    ├─ PriceCache::empty()
│    ├─ FILE: crates/service/crates/billing/src/calculator.rs
│    │    ├─ CostCalculator::new()
│    └─ FILE: crates/router/src/exchange_rate.rs
│    │    ├─ ExchangeRateService::new()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ component is READY / RUNNING
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -1,84 +1,88 @@
```

BEFORE / REMOVED:
```diff
-pub mod app;
-pub mod auth_gate;
-pub mod backend;
-pub mod components;
-pub mod critical_pages;
-pub mod data;
-pub mod functional_api;
-pub mod functional_layout;
```

AFTER / ADDED:
```diff
+//! BurnCloud's single-crate Dioxus client.
+//!
+//! The module layout mirrors the directory contract in the UI architecture
+//! documentation.  Feature-specific adapters stay at this boundary while
+//! product domains remain grouped below `domains/`.
+pub mod api;
+pub mod app;
+pub mod auth;
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
monitor=started
cache=ready
management_routes=mounted
internal_routes=mounted
liveview=enabled
data_plane_fallback=mounted
```

### E2E FLOW #25 — /

**树路径：** `UI-only Actions → Guest / Public → /`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/home.rs
│
└─ Root / Home page module
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/
component=home
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #26 — /home

**树路径：** `UI-only Actions → Guest / Public → /home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /home
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/home.rs
│
└─ HomePage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/home
component=home
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #27 — /login

**树路径：** `UI-only Actions → Guest / Public → /login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /login
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/login.rs
│
└─ LoginPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/login
component=login
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #28 — /register

**树路径：** `UI-only Actions → Guest / Public → /register`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /register
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/crates/client-register/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/register
component=register
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #29 — /forgot-password

**树路径：** `UI-only Actions → Guest / Public → /forgot-password`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /forgot-password
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/forgot_password.rs
│
└─ ForgotPasswordPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/forgot-password
component=forgot-password
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #30 — /reset-password?:token

**树路径：** `UI-only Actions → Guest / Public → /reset-password?:token`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /reset-password?:token
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/reset_password.rs
│
└─ ResetPasswordPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/reset-password?:token
component=reset-password?:token
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #31 — /console/dashboard

**树路径：** `UI-only Actions → Console → /console/dashboard`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/dashboard
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/dashboard.rs
│
└─ Dashboard re-export
│
▼
FILE: crates/client/crates/client-dashboard/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/dashboard
component=console/dashboard
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #32 — /console/deploy

**树路径：** `UI-only Actions → Console → /console/deploy`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/deploy
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/deploy.rs
│
└─ DeployConfig re-export
│
▼
FILE: crates/client/crates/client-deploy/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/deploy
component=console/deploy
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #33 — /console/monitor

**树路径：** `UI-only Actions → Console → /console/monitor`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/monitor
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/monitor.rs
│
└─ ServiceMonitor re-export
│
▼
FILE: crates/client/crates/client-monitor/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/monitor
component=console/monitor
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #34 — /console/access

**树路径：** `UI-only Actions → Console → /console/access`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/access
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/api.rs
│
└─ AccessPage re-export
│
▼
FILE: crates/client/crates/client-access/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/access
component=console/access
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #35 — /console/models

**树路径：** `UI-only Actions → Console → /console/models`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/models
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/models.rs
│
└─ ChannelPage re-export
│
▼
FILE: crates/client/crates/client-models/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/models
component=console/models
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #36 — /console/users

**树路径：** `UI-only Actions → Console → /console/users`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/users
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/user.rs
│
└─ UsersPage re-export
│
▼
FILE: crates/client/crates/client-users/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/users
component=console/users
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #37 — /console/settings

**树路径：** `UI-only Actions → Console → /console/settings`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/settings
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/settings.rs
│
└─ SystemSettings re-export
│
▼
FILE: crates/client/crates/client-settings/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/settings
component=console/settings
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #38 — /console/finance

**树路径：** `UI-only Actions → Console → /console/finance`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/finance
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/billing.rs
│
└─ FinancePage re-export
│
▼
FILE: crates/client/crates/client-finance/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/finance
component=console/finance
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #39 — /console/logs

**树路径：** `UI-only Actions → Console → /console/logs`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/logs
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/logs.rs
│
└─ LogPage re-export
│
▼
FILE: crates/client/crates/client-log/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/logs
component=console/logs
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #40 — /console/connect

**树路径：** `UI-only Actions → Console → /console/connect`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/connect
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/connect.rs
│
└─ ConnectPage re-export
│
▼
FILE: crates/client/crates/client-connect/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/connect
component=console/connect
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #41 — /console/playground

**树路径：** `UI-only Actions → Console → /console/playground`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/playground
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/playground.rs
│
└─ PlaygroundPage re-export
│
▼
FILE: crates/client/crates/client-playground/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/playground
component=console/playground
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #42 — /console/:..segments → NotFound

**树路径：** `UI-only Actions → Console → /console/:..segments → NotFound`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/:..segments → NotFound
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/not_found.rs
│
└─ NotFoundPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/:..segments → NotFound
component=NotFoundPage
rendered=true
```

### E2E FLOW #43 — /preview/home

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/home
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/home
component=preview/home
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #44 — /preview/login

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/login
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/login
component=preview/login
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #45 — /preview/console/dashboard

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/dashboard`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/dashboard
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/dashboard
component=preview/console/dashboard
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #46 — /preview/console/models

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/models`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/models
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/models
component=preview/console/models
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #47 — /preview/console/access

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/access`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/access
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/access
component=preview/console/access
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #48 — /preview/console/settings

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/settings`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/settings
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/settings
component=preview/console/settings
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #49 — /preview/console/finance

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/finance`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/finance
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/finance
component=preview/console/finance
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #50 — /preview/console/monitor

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/monitor`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/monitor
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/monitor
component=preview/console/monitor
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #51 — /preview/console/playground

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/playground`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/playground
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/playground
component=preview/console/playground
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #52 — i18n context

**树路径：** `UI-only Actions → Local UI State → i18n context`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ App root initialization
│    └─ state/context: i18n context
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Create initial state
│    ├─ derive default/configured value
│    └─ store in Dioxus signal/context
│
├─ Provide context
│    ├─ descendant components can read/subscribe
│    └─ current render reads latest value
│
├─ Update path
│    └─ DECISION: UI event changes this state?
│         ├─ NO → keep current value
│         └─ YES
│              ├─ mutate signal/context
│              └─ mark dependent subtree dirty
│
├─ Re-render
│    └─ Dioxus reconciles affected component tree
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI EVENT LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```json
{
  "locale": "zh-CN",
  "fallback_locale": "en-US",
  "ready": true
}
```

### E2E FLOW #53 — Toast state / ToastContainer

**树路径：** `UI-only Actions → Local UI State → Toast state / ToastContainer`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ App root initialization
│    └─ state/context: Toast state / ToastContainer
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Create initial state
│    ├─ derive default/configured value
│    └─ store in Dioxus signal/context
│
├─ Provide context
│    ├─ descendant components can read/subscribe
│    └─ current render reads latest value
│
├─ Update path
│    └─ DECISION: UI event changes this state?
│         ├─ NO → keep current value
│         └─ YES
│              ├─ mutate signal/context
│              └─ mark dependent subtree dirty
│
├─ Re-render
│    └─ Dioxus reconciles affected component tree
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI EVENT LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```json
{
  "visible": true,
  "level": "success",
  "message": "操作成功"
}
```

### E2E FLOW #54 — Auth context

**树路径：** `UI-only Actions → Local UI State → Auth context`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ App root initialization
│    └─ state/context: Auth context
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Create initial state
│    ├─ derive default/configured value
│    └─ store in Dioxus signal/context
│
├─ Provide context
│    ├─ descendant components can read/subscribe
│    └─ current render reads latest value
│
├─ Update path
│    └─ DECISION: UI event changes this state?
│         ├─ NO → keep current value
│         └─ YES
│              ├─ mutate signal/context
│              └─ mark dependent subtree dirty
│
├─ Re-render
│    └─ Dioxus reconciles affected component tree
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI EVENT LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```json
{
  "authenticated": true,
  "user_id": 10001,
  "username": "demo_user"
}
```

### E2E FLOW #55 — Theme state

**树路径：** `UI-only Actions → Local UI State → Theme state`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ App root initialization
│    └─ state/context: Theme state
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Create initial state
│    ├─ derive default/configured value
│    └─ store in Dioxus signal/context
│
├─ Provide context
│    ├─ descendant components can read/subscribe
│    └─ current render reads latest value
│
├─ Update path
│    └─ DECISION: UI event changes this state?
│         ├─ NO → keep current value
│         └─ YES
│              ├─ mutate signal/context
│              └─ mark dependent subtree dirty
│
├─ Re-render
│    └─ Dioxus reconciles affected component tree
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI EVENT LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```json
{
  "theme": "system",
  "resolved": "dark"
}
```

### E2E FLOW #56 — window maximize

**树路径：** `UI-only Actions → Desktop UI → window maximize`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Desktop event/state input
│    └─ action: window maximize
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Platform branch
│    └─ DECISION: current platform/runtime supports desktop action?
│         ├─ NO → skip/unsupported branch → END
│         └─ YES → obtain desktop window/tray handle
│
├─ Current UI state
│    ├─ read visibility/focus/maximized/tray state as needed
│    └─ decide desired state
│
├─ Apply desktop side effect
│    ├─ maximize / show / hide / focus / tray startup
│    └─ DECISION: OS/window operation succeeds?
│         ├─ NO → log/ignore according to UI path
│         └─ YES → state visible to user
│
├─ Event loop handoff
│    └─ return control to Dioxus/desktop event loop
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
window.maximized=true
window.visible=true
```

### E2E FLOW #57 — Windows tray startup

**树路径：** `UI-only Actions → Desktop UI → Windows tray startup`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Desktop event/state input
│    └─ action: Windows tray startup
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Platform branch
│    └─ DECISION: current platform/runtime supports desktop action?
│         ├─ NO → skip/unsupported branch → END
│         └─ YES → obtain desktop window/tray handle
│
├─ Current UI state
│    ├─ read visibility/focus/maximized/tray state as needed
│    └─ decide desired state
│
├─ Apply desktop side effect
│    ├─ maximize / show / hide / focus / tray startup
│    └─ DECISION: OS/window operation succeeds?
│         ├─ NO → log/ignore according to UI path
│         └─ YES → state visible to user
│
├─ Event loop handoff
│    └─ return control to Dioxus/desktop event loop
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
tray.visible=true
tray.status=running
```

### E2E FLOW #58 — show / hide / focus

**树路径：** `UI-only Actions → Desktop UI → show / hide / focus`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Desktop event/state input
│    └─ action: show / hide / focus
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Platform branch
│    └─ DECISION: current platform/runtime supports desktop action?
│         ├─ NO → skip/unsupported branch → END
│         └─ YES → obtain desktop window/tray handle
│
├─ Current UI state
│    ├─ read visibility/focus/maximized/tray state as needed
│    └─ decide desired state
│
├─ Apply desktop side effect
│    ├─ maximize / show / hide / focus / tray startup
│    └─ DECISION: OS/window operation succeeds?
│         ├─ NO → log/ignore according to UI path
│         └─ YES → state visible to user
│
├─ Event loop handoff
│    └─ return control to Dioxus/desktop event loop
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -1,159 +0,0 @@
```

BEFORE / REMOVED:
```diff
-use dioxus::prelude::*;
-
-#[cfg(feature = "desktop")]
-#[path = "desktop_chrome.rs"]
-mod desktop_chrome;
-
-use crate::{
-    auth_gate::AuthGate,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
show_window=true
window.visible=true
window.focused=true
```

## 未映射 Changed Files

- `crates/client/Cargo.toml`
- `crates/client/assets/burncloud.rc`
- `crates/client/assets/favicon.ico`
- `crates/client/build.rs`
- `crates/client/scripts/check-functional-wiring.sh`
- `crates/client/scripts/check-product-ux.sh`
- `crates/client/scripts/check-ui-conventions.sh`
- `crates/client/scripts/check-visual-system.sh`
- `crates/client/src/api/admin/mod.rs`
- `crates/client/src/api/buyer/mod.rs`
- `crates/client/src/api/client.rs`
- `crates/client/src/api/error.rs`
- `crates/client/src/api/mod.rs`
- `crates/client/src/api/request.rs`
- `crates/client/src/api/response.rs`
- `crates/client/src/api/shared/mod.rs`
- `crates/client/src/api/supplier/mod.rs`
- `crates/client/src/app/app.rs`
- `crates/client/src/app/bootstrap/auth.rs`
- `crates/client/src/app/bootstrap/i18n.rs`
- `crates/client/src/app/bootstrap/mod.rs`
- `crates/client/src/app/bootstrap/observability.rs`
- `crates/client/src/app/mod.rs`
- `crates/client/src/app/router/console.rs`
- `crates/client/src/app/router/mod.rs`
- `crates/client/src/app/router/not_found.rs`
- `crates/client/src/app/router/public.rs`
- `crates/client/src/app/router/routes.rs`
- `crates/client/src/auth/auth_gate.rs`
- `crates/client/src/auth/authorization.rs`
- `crates/client/src/auth/capabilities.rs`
- `crates/client/src/auth/mod.rs`
- `crates/client/src/auth/return_to.rs`
- `crates/client/src/auth/session.rs`
- `crates/client/src/auth/workspace_gate.rs`
- `crates/client/src/auth_gate.rs`
- `crates/client/src/backend.rs`
- `crates/client/src/components.rs`
- `crates/client/src/critical_pages.css`
- `crates/client/src/critical_pages.rs`
- `crates/client/src/critical_pages/auth.rs`
- `crates/client/src/critical_pages/customers_portable.rs`
- `crates/client/src/critical_pages/dashboard.rs`
- `crates/client/src/critical_pages/logs.rs`
- `crates/client/src/data.rs`
- `crates/client/src/design/breakpoints.rs`
- `crates/client/src/design/mod.rs`
- `crates/client/src/design/radius.rs`
- `crates/client/src/design/spacing.rs`
- `crates/client/src/design/tokens.rs`
- `crates/client/src/design/typography.rs`
- `crates/client/src/desktop_chrome.css`
- `crates/client/src/desktop_chrome.rs`
- `crates/client/src/domains/admin/advanced/evaluation/actions.rs`
- `crates/client/src/domains/admin/advanced/evaluation/components/mod.rs`
- `crates/client/src/domains/admin/advanced/evaluation/mod.rs`
- `crates/client/src/domains/admin/advanced/evaluation/model.rs`
- `crates/client/src/domains/admin/advanced/evaluation/page.rs`
- `crates/client/src/domains/admin/advanced/evaluation/state.rs`
- `crates/client/src/domains/admin/advanced/evaluation/tests.rs`
- `crates/client/src/domains/admin/advanced/guardrails/actions.rs`
- `crates/client/src/domains/admin/advanced/guardrails/components/mod.rs`
- `crates/client/src/domains/admin/advanced/guardrails/mod.rs`
- `crates/client/src/domains/admin/advanced/guardrails/model.rs`
- `crates/client/src/domains/admin/advanced/guardrails/page.rs`
- `crates/client/src/domains/admin/advanced/guardrails/state.rs`
- `crates/client/src/domains/admin/advanced/guardrails/tests.rs`
- `crates/client/src/domains/admin/advanced/logs/actions.rs`
- `crates/client/src/domains/admin/advanced/logs/components/mod.rs`
- `crates/client/src/domains/admin/advanced/logs/mod.rs`
- `crates/client/src/domains/admin/advanced/logs/model.rs`
- `crates/client/src/domains/admin/advanced/logs/page.rs`
- `crates/client/src/domains/admin/advanced/logs/state.rs`
- `crates/client/src/domains/admin/advanced/logs/tests.rs`
- `crates/client/src/domains/admin/advanced/mod.rs`
- `crates/client/src/domains/admin/advanced/providers/actions.rs`
- `crates/client/src/domains/admin/advanced/providers/components/mod.rs`
- `crates/client/src/domains/admin/advanced/providers/mod.rs`
- `crates/client/src/domains/admin/advanced/providers/model.rs`
- `crates/client/src/domains/admin/advanced/providers/page.rs`
- `crates/client/src/domains/admin/advanced/providers/state.rs`
- `crates/client/src/domains/admin/advanced/providers/tests.rs`
- `crates/client/src/domains/admin/advanced/routes/actions.rs`
- `crates/client/src/domains/admin/advanced/routes/components/mod.rs`
- `crates/client/src/domains/admin/advanced/routes/mod.rs`
- `crates/client/src/domains/admin/advanced/routes/model.rs`
- `crates/client/src/domains/admin/advanced/routes/page.rs`
- `crates/client/src/domains/admin/advanced/routes/state.rs`
- `crates/client/src/domains/admin/advanced/routes/tests.rs`
- `crates/client/src/domains/admin/advanced/team/actions.rs`
- `crates/client/src/domains/admin/advanced/team/components/mod.rs`
- `crates/client/src/domains/admin/advanced/team/mod.rs`
- `crates/client/src/domains/admin/advanced/team/model.rs`
- `crates/client/src/domains/admin/advanced/team/page.rs`
- `crates/client/src/domains/admin/advanced/team/state.rs`
- `crates/client/src/domains/admin/advanced/team/tests.rs`
- `crates/client/src/domains/admin/billing/actions.rs`
- `crates/client/src/domains/admin/billing/components/mod.rs`
- `crates/client/src/domains/admin/billing/mod.rs`
- `crates/client/src/domains/admin/billing/model.rs`
- `crates/client/src/domains/admin/billing/page.rs`
- `crates/client/src/domains/admin/billing/state.rs`
- `crates/client/src/domains/admin/billing/tests.rs`
- `crates/client/src/domains/admin/capacity/actions.rs`
- `crates/client/src/domains/admin/capacity/components/mod.rs`
- `crates/client/src/domains/admin/capacity/mod.rs`
- `crates/client/src/domains/admin/capacity/model.rs`
- `crates/client/src/domains/admin/capacity/page.rs`
- `crates/client/src/domains/admin/capacity/state.rs`
- `crates/client/src/domains/admin/capacity/tests.rs`
- `crates/client/src/domains/admin/customers/actions.rs`
- `crates/client/src/domains/admin/customers/components/mod.rs`
- `crates/client/src/domains/admin/customers/mod.rs`
- `crates/client/src/domains/admin/customers/model.rs`
- `crates/client/src/domains/admin/customers/page.rs`
- `crates/client/src/domains/admin/customers/state.rs`
- `crates/client/src/domains/admin/customers/tests.rs`
- `crates/client/src/domains/admin/demand/actions.rs`
- `crates/client/src/domains/admin/demand/components/mod.rs`
- `crates/client/src/domains/admin/demand/mod.rs`
- `crates/client/src/domains/admin/demand/model.rs`
- `crates/client/src/domains/admin/demand/page.rs`
- `crates/client/src/domains/admin/demand/state.rs`
- `crates/client/src/domains/admin/demand/tests.rs`
- `crates/client/src/domains/admin/mod.rs`
- `crates/client/src/domains/admin/models/actions.rs`
- `crates/client/src/domains/admin/models/components/mod.rs`
- `crates/client/src/domains/admin/models/mod.rs`
- `crates/client/src/domains/admin/models/model.rs`
- `crates/client/src/domains/admin/models/page.rs`
- `crates/client/src/domains/admin/models/state.rs`
- `crates/client/src/domains/admin/models/tests.rs`
- `crates/client/src/domains/admin/navigation.rs`
- `crates/client/src/domains/admin/operations/actions.rs`
- `crates/client/src/domains/admin/operations/components/mod.rs`
- `crates/client/src/domains/admin/operations/mod.rs`
- `crates/client/src/domains/admin/operations/model.rs`
- `crates/client/src/domains/admin/operations/page.rs`
- `crates/client/src/domains/admin/operations/state.rs`
- `crates/client/src/domains/admin/operations/tests.rs`
- `crates/client/src/domains/admin/overview/actions.rs`
- `crates/client/src/domains/admin/overview/components/mod.rs`
- `crates/client/src/domains/admin/overview/mod.rs`
- `crates/client/src/domains/admin/overview/model.rs`
- `crates/client/src/domains/admin/overview/page.rs`
- `crates/client/src/domains/admin/overview/state.rs`
- `crates/client/src/domains/admin/overview/tests.rs`
- `crates/client/src/domains/admin/revenue/actions.rs`
- `crates/client/src/domains/admin/revenue/components/mod.rs`
- `crates/client/src/domains/admin/revenue/mod.rs`
- `crates/client/src/domains/admin/revenue/model.rs`
- `crates/client/src/domains/admin/revenue/page.rs`
- `crates/client/src/domains/admin/revenue/state.rs`
- `crates/client/src/domains/admin/revenue/tests.rs`
- `crates/client/src/domains/admin/routes.rs`
- `crates/client/src/domains/admin/settings/actions.rs`
- `crates/client/src/domains/admin/settings/components/mod.rs`
- `crates/client/src/domains/admin/settings/mod.rs`
- `crates/client/src/domains/admin/settings/model.rs`
- `crates/client/src/domains/admin/settings/page.rs`
- `crates/client/src/domains/admin/settings/state.rs`
- `crates/client/src/domains/admin/settings/tests.rs`
- `crates/client/src/domains/admin/settlements/actions.rs`
- `crates/client/src/domains/admin/settlements/components/mod.rs`
- `crates/client/src/domains/admin/settlements/mod.rs`
- `crates/client/src/domains/admin/settlements/model.rs`
- `crates/client/src/domains/admin/settlements/page.rs`
- `crates/client/src/domains/admin/settlements/state.rs`
- `crates/client/src/domains/admin/settlements/tests.rs`
- `crates/client/src/domains/admin/suppliers/actions.rs`
- `crates/client/src/domains/admin/suppliers/components/mod.rs`
- `crates/client/src/domains/admin/suppliers/mod.rs`
- `crates/client/src/domains/admin/suppliers/model.rs`
- `crates/client/src/domains/admin/suppliers/page.rs`
- `crates/client/src/domains/admin/suppliers/state.rs`
- `crates/client/src/domains/admin/suppliers/tests.rs`
- `crates/client/src/domains/admin/supply/actions.rs`
- `crates/client/src/domains/admin/supply/components/mod.rs`
- `crates/client/src/domains/admin/supply/mod.rs`
- `crates/client/src/domains/admin/supply/model.rs`
- `crates/client/src/domains/admin/supply/page.rs`
- `crates/client/src/domains/admin/supply/state.rs`
- `crates/client/src/domains/admin/supply/tests.rs`
- `crates/client/src/domains/buyer/api_keys/actions.rs`
- `crates/client/src/domains/buyer/api_keys/components/mod.rs`
- `crates/client/src/domains/buyer/api_keys/mod.rs`
- `crates/client/src/domains/buyer/api_keys/model.rs`
- `crates/client/src/domains/buyer/api_keys/page.rs`
- `crates/client/src/domains/buyer/api_keys/state.rs`
- `crates/client/src/domains/buyer/api_keys/tests.rs`
- `crates/client/src/domains/buyer/billing/actions.rs`
- `crates/client/src/domains/buyer/billing/components/mod.rs`
- `crates/client/src/domains/buyer/billing/mod.rs`
- `crates/client/src/domains/buyer/billing/model.rs`
- `crates/client/src/domains/buyer/billing/page.rs`
- `crates/client/src/domains/buyer/billing/state.rs`
- `crates/client/src/domains/buyer/billing/tests.rs`
- `crates/client/src/domains/buyer/logs/actions.rs`
- `crates/client/src/domains/buyer/logs/components/mod.rs`
- `crates/client/src/domains/buyer/logs/mod.rs`
- `crates/client/src/domains/buyer/logs/model.rs`
- `crates/client/src/domains/buyer/logs/page.rs`
- `crates/client/src/domains/buyer/logs/state.rs`
- `crates/client/src/domains/buyer/logs/tests.rs`
- `crates/client/src/domains/buyer/marketplace/actions.rs`
- `crates/client/src/domains/buyer/marketplace/components/mod.rs`
- `crates/client/src/domains/buyer/marketplace/mod.rs`
- `crates/client/src/domains/buyer/marketplace/model.rs`
- `crates/client/src/domains/buyer/marketplace/page.rs`
- `crates/client/src/domains/buyer/marketplace/state.rs`
- `crates/client/src/domains/buyer/marketplace/tests.rs`
- `crates/client/src/domains/buyer/mod.rs`
- `crates/client/src/domains/buyer/navigation.rs`
- `crates/client/src/domains/buyer/overview/actions.rs`
- `crates/client/src/domains/buyer/overview/components/mod.rs`
- `crates/client/src/domains/buyer/overview/mod.rs`
- `crates/client/src/domains/buyer/overview/model.rs`
- `crates/client/src/domains/buyer/overview/page.rs`
- `crates/client/src/domains/buyer/overview/state.rs`
- `crates/client/src/domains/buyer/overview/tests.rs`
- `crates/client/src/domains/buyer/playground/actions.rs`
- `crates/client/src/domains/buyer/playground/components/mod.rs`
- `crates/client/src/domains/buyer/playground/mod.rs`
- `crates/client/src/domains/buyer/playground/model.rs`
- `crates/client/src/domains/buyer/playground/page.rs`
- `crates/client/src/domains/buyer/playground/state.rs`
- `crates/client/src/domains/buyer/playground/tests.rs`
- `crates/client/src/domains/buyer/routes.rs`
- `crates/client/src/domains/buyer/usage/actions.rs`
- `crates/client/src/domains/buyer/usage/components/mod.rs`
- `crates/client/src/domains/buyer/usage/mod.rs`
- `crates/client/src/domains/buyer/usage/model.rs`
- `crates/client/src/domains/buyer/usage/page.rs`
- `crates/client/src/domains/buyer/usage/state.rs`
- `crates/client/src/domains/buyer/usage/tests.rs`
- `crates/client/src/domains/mod.rs`
- `crates/client/src/domains/supplier/deployments/actions.rs`
- `crates/client/src/domains/supplier/deployments/components/mod.rs`
- `crates/client/src/domains/supplier/deployments/mod.rs`
- `crates/client/src/domains/supplier/deployments/model.rs`
- `crates/client/src/domains/supplier/deployments/page.rs`
- `crates/client/src/domains/supplier/deployments/state.rs`
- `crates/client/src/domains/supplier/deployments/tests.rs`
- `crates/client/src/domains/supplier/earnings/actions.rs`
- `crates/client/src/domains/supplier/earnings/components/mod.rs`
- `crates/client/src/domains/supplier/earnings/mod.rs`
- `crates/client/src/domains/supplier/earnings/model.rs`
- `crates/client/src/domains/supplier/earnings/page.rs`
- `crates/client/src/domains/supplier/earnings/state.rs`
- `crates/client/src/domains/supplier/earnings/tests.rs`
- `crates/client/src/domains/supplier/mod.rs`
- `crates/client/src/domains/supplier/navigation.rs`
- `crates/client/src/domains/supplier/overview/actions.rs`
- `crates/client/src/domains/supplier/overview/components/mod.rs`
- `crates/client/src/domains/supplier/overview/mod.rs`
- `crates/client/src/domains/supplier/overview/model.rs`
- `crates/client/src/domains/supplier/overview/page.rs`
- `crates/client/src/domains/supplier/overview/state.rs`
- `crates/client/src/domains/supplier/overview/tests.rs`
- `crates/client/src/domains/supplier/reliability/actions.rs`
- `crates/client/src/domains/supplier/reliability/components/mod.rs`
- `crates/client/src/domains/supplier/reliability/mod.rs`
- `crates/client/src/domains/supplier/reliability/model.rs`
- `crates/client/src/domains/supplier/reliability/page.rs`
- `crates/client/src/domains/supplier/reliability/state.rs`
- `crates/client/src/domains/supplier/reliability/tests.rs`
- `crates/client/src/domains/supplier/resources/actions.rs`
- `crates/client/src/domains/supplier/resources/components/mod.rs`
- `crates/client/src/domains/supplier/resources/mod.rs`
- `crates/client/src/domains/supplier/resources/model.rs`
- `crates/client/src/domains/supplier/resources/page.rs`
- `crates/client/src/domains/supplier/resources/state.rs`
- `crates/client/src/domains/supplier/resources/tests.rs`
- `crates/client/src/domains/supplier/routes.rs`
- `crates/client/src/domains/supplier/settings/actions.rs`
- `crates/client/src/domains/supplier/settings/components/mod.rs`
- `crates/client/src/domains/supplier/settings/mod.rs`
- `crates/client/src/domains/supplier/settings/model.rs`
- `crates/client/src/domains/supplier/settings/page.rs`
- `crates/client/src/domains/supplier/settings/state.rs`
- `crates/client/src/domains/supplier/settings/tests.rs`
- `crates/client/src/domains/supplier/settlements/actions.rs`
- `crates/client/src/domains/supplier/settlements/components/mod.rs`
- `crates/client/src/domains/supplier/settlements/mod.rs`
- `crates/client/src/domains/supplier/settlements/model.rs`
- `crates/client/src/domains/supplier/settlements/page.rs`
- `crates/client/src/domains/supplier/settlements/state.rs`
- `crates/client/src/domains/supplier/settlements/tests.rs`
- `crates/client/src/functional_api.rs`
- `crates/client/src/functional_layout.rs`
- `crates/client/src/functional_pages/access.rs`
- `crates/client/src/functional_pages/access_live.rs`
- `crates/client/src/functional_pages/analytics.rs`
- `crates/client/src/functional_pages/analytics_full.rs`
- `crates/client/src/functional_pages/api_keys_live.rs`
- `crates/client/src/functional_pages/catalog.rs`
- `crates/client/src/functional_pages/guardrails.rs`
- `crates/client/src/functional_pages/guardrails_live.rs`
- `crates/client/src/functional_pages/logs_full.rs`
- `crates/client/src/functional_pages/mod.rs`
- `crates/client/src/functional_pages/platform.rs`
- `crates/client/src/functional_pages/platform_live.rs`
- `crates/client/src/functional_pages/playground.rs`
- `crates/client/src/functional_pages/playground_live.rs`
- `crates/client/src/functional_pages/providers.rs`
- `crates/client/src/functional_pages/settings.rs`
- `crates/client/src/i18n/formatter.rs`
- `crates/client/src/i18n/locale.rs`
- `crates/client/src/i18n/locales/en.rs`
- `crates/client/src/i18n/locales/ja.rs`
- `crates/client/src/i18n/locales/mod.rs`
- `crates/client/src/i18n/locales/zh.rs`
- `crates/client/src/i18n/locales/zh_tw.rs`
- `crates/client/src/i18n/machine_values.rs`
- `crates/client/src/i18n/mod.rs`
- `crates/client/src/observability.rs`
- `crates/client/src/pages.rs`
- `crates/client/src/platform/desktop.rs`
- `crates/client/src/platform/liveview.rs`
- `crates/client/src/platform/mod.rs`
- `crates/client/src/platform/web.rs`
- `crates/client/src/product_ui.css`
- `crates/client/src/route_aliases.rs`
- `crates/client/src/shared/hooks/mod.rs`
- `crates/client/src/shared/layout/mod.rs`
- `crates/client/src/shared/mod.rs`
- `crates/client/src/shared/states/mod.rs`
- `crates/client/src/shared/types/mod.rs`
- `crates/client/src/shared/ui/mod.rs`
- `crates/client/src/shared/utils/mod.rs`
- `crates/client/src/shared/validation/mod.rs`
- `crates/client/src/styles.css`
- `crates/client/src/visual_system.css`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
