---
title: "PR #561 — fix(client): support Dioxus 0.7.5 icon API"
slug: /pr/pr-561
---

# PR #561 — fix(client): support Dioxus 0.7.5 icon API

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-18T05:42:54Z`  
> **Base：** `main@7fae4588d5e8`  
> **Head：** `codex/fix-dioxus-icon-api@616155697c1c`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `2`
- **Additions / Deletions：** `+2 / -13`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `2`

**PR 说明摘要：** ## Summary - remove calls to `dioxus::desktop::icon_from_memory`, which is unavailable in the locked `dioxus-desktop 0.7.5` - use the Windows icon resource embedded by `build.rs` for the system tray - let Dioxus 0.7.5 apply its supported default window icon behavior ## Verification - `cargo check -p burncloud-client` - `git diff --check`

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/interfaces/client/src/desktop_chrome.rs` | modified | 2 | 2 | 0 |
| `crates/interfaces/client/src/lib.rs` | modified | 0 | 11 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/interfaces/client/src/desktop_chrome.rs`
- `crates/interfaces/client/src/lib.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
