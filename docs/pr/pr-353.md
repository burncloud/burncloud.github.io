---
title: "PR #353 — refactor(client): unify design system CSS injection and console styling"
slug: /pr/pr-353
---

# PR #353 — refactor(client): unify design system CSS injection and console styling

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-07-05T16:51:50Z`  
> **Base：** `main@5c0772cea399`  
> **Head：** `refactor/ui-design-system-unification@e51fe258c163`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `34`
- **Additions / Deletions：** `+701 / -1432`
- **受影响的完整 E2E Flow：** `37`
- **未映射 changed files：** `21`

**PR 说明摘要：** ## Summary - Introduce a single stylesheet injection point (AppStyles / liveview_style_tags) that loads Tailwind utilities, DESIGN_SYSTEM_CSS, and SVG defaults in a fixed order across layout, guest layout, and LiveView shells. - Remove ~500 lines of duplicate JIT CSS from guest_layout.rs and lib.rs, prune dead DaisyUI/legacy layout rules from styles.rs, and migrate RSX color classes from ext-primary/secondary to Tailwind ext-bc-* tokens. - Deduplicate console navigation via shared ConsoleSidebar + ConsoleNav trait; align console pages with PageHeader / page-content, BCModal, and updated layout rhythm (full-bleed headers, content padding via page-content). ## Test plan - [x] cargo build -p burncloud-client - [x] cargo test -p burncloud-client-shared --lib - [ ] Smoke-test console pages: dashboard, access, models, users, monitor (emergency modal), deploy, playground, settings - [ ] Verify guest pages (login, home) and dark mode toggle still render correctly - [ ] Run E2E design token tests if browser infra is available (design_tokens.rs) Made with [Cursor](https://cursor.com)

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/client/crates/client-access/src/lib.rs` | modified | 54 | 54 | 1 |
| `crates/client/crates/client-api/src/api.rs` | modified | 102 | 155 | 0 |
| `crates/client/crates/client-connect/src/lib.rs` | modified | 7 | 7 | 1 |
| `crates/client/crates/client-deploy/src/deploy.rs` | modified | 8 | 8 | 0 |
| `crates/client/crates/client-finance/src/lib.rs` | modified | 2 | 2 | 1 |
| `crates/client/crates/client-log/src/lib.rs` | modified | 2 | 2 | 1 |
| `crates/client/crates/client-models/src/models.rs` | modified | 4 | 4 | 0 |
| `crates/client/crates/client-monitor/src/monitor.rs` | modified | 37 | 61 | 0 |
| `crates/client/crates/client-playground/src/lib.rs` | modified | 10 | 9 | 1 |
| `crates/client/crates/client-settings/src/groups.rs` | modified | 5 | 5 | 0 |
| `crates/client/crates/client-settings/src/settings.rs` | modified | 1 | 1 | 0 |
| `crates/client/crates/client-shared/src/app_styles.rs` | added | 27 | 0 | 1 |
| `crates/client/crates/client-shared/src/components/bc_button.rs` | modified | 1 | 1 | 0 |
| `crates/client/crates/client-shared/src/components/bc_card.rs` | modified | 1 | 1 | 0 |
| `crates/client/crates/client-shared/src/components/bc_input.rs` | modified | 1 | 1 | 0 |
| `crates/client/crates/client-shared/src/components/bc_modal.rs` | modified | 1 | 1 | 0 |
| `crates/client/crates/client-shared/src/components/bc_table.rs` | modified | 1 | 1 | 0 |
| `crates/client/crates/client-shared/src/components/console_sidebar.rs` | added | 220 | 0 | 0 |
| `crates/client/crates/client-shared/src/components/layout.rs` | modified | 23 | 8 | 12 |
| `crates/client/crates/client-shared/src/components/mod.rs` | modified | 2 | 0 | 0 |
| `crates/client/crates/client-shared/src/components/schema_table.rs` | modified | 5 | 5 | 0 |
| `crates/client/crates/client-shared/src/components/sidebar.rs` | modified | 20 | 120 | 0 |
| `crates/client/crates/client-shared/src/lib.rs` | modified | 2 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles.rs` | modified | 40 | 256 | 0 |
| `crates/client/crates/client-users/src/lib.rs` | modified | 77 | 80 | 1 |
| `crates/client/src/bin/screenshot_gen.rs` | modified | 5 | 7 | 1 |
| `crates/client/src/components/app_styles.rs` | added | 2 | 0 | 0 |
| `crates/client/src/components/guest_layout.rs` | modified | 3 | 316 | 5 |
| `crates/client/src/components/layout.rs` | modified | 8 | 22 | 12 |
| `crates/client/src/components/mod.rs` | modified | 1 | 0 | 0 |
| `crates/client/src/components/sidebar.rs` | modified | 19 | 114 | 0 |
| `crates/client/src/lib.rs` | modified | 6 | 180 | 19 |
| `crates/client/src/pages/not_found.rs` | modified | 3 | 3 | 1 |
| `crates/client/tailwind.config.js` | modified | 1 | 8 | 0 |

## Affected E2E Impact Matrix

| # | 完整 E2E / Entry Point | 类型 | PR 命中的源码文件 |
|---:|---|---|---|
| 1 | `GET /` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 2 | `GET /home` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 3 | `GET /login` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 4 | `GET /register` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 5 | `GET /forgot-password` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 6 | `GET /reset-password` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 7 | `GET /console` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 8 | `GET /console/` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 9 | `GET /console/{*path}` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 10 | `GET /favicon.ico` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 11 | `GET /preview/home` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 12 | `GET /preview/login` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 13 | `GET /preview/console` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 14 | `GET /preview/console/` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 15 | `GET /preview/console/{*path}` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 16 | `GET /ws` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs` |
| 17 | `server` | CLI / Executables / burncloud | `crates/client/src/lib.rs` |
| 18 | `router` | CLI / Executables / burncloud | `crates/client/src/lib.rs` |
| 19 | `screenshot_gen` | CLI / Executables / Workspace Binaries | `crates/client/src/bin/screenshot_gen.rs` |
| 20 | `create_app` | Startup / Startup Chain | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/app_styles.rs` |
| 21 | `/home` | UI-only Actions / Guest / Public | `crates/client/src/components/guest_layout.rs` |
| 22 | `/login` | UI-only Actions / Guest / Public | `crates/client/src/components/guest_layout.rs` |
| 23 | `/register` | UI-only Actions / Guest / Public | `crates/client/src/components/guest_layout.rs` |
| 24 | `/forgot-password` | UI-only Actions / Guest / Public | `crates/client/src/components/guest_layout.rs` |
| 25 | `/reset-password?:token` | UI-only Actions / Guest / Public | `crates/client/src/components/guest_layout.rs` |
| 26 | `/console/dashboard` | UI-only Actions / Console | `crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs` |
| 27 | `/console/deploy` | UI-only Actions / Console | `crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs` |
| 28 | `/console/monitor` | UI-only Actions / Console | `crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs` |
| 29 | `/console/access` | UI-only Actions / Console | `crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-access/src/lib.rs` |
| 30 | `/console/models` | UI-only Actions / Console | `crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs` |
| 31 | `/console/users` | UI-only Actions / Console | `crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-users/src/lib.rs` |
| 32 | `/console/settings` | UI-only Actions / Console | `crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs` |
| 33 | `/console/finance` | UI-only Actions / Console | `crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-finance/src/lib.rs` |
| 34 | `/console/logs` | UI-only Actions / Console | `crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-log/src/lib.rs` |
| 35 | `/console/connect` | UI-only Actions / Console | `crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-connect/src/lib.rs` |
| 36 | `/console/playground` | UI-only Actions / Console | `crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-playground/src/lib.rs` |
| 37 | `/console/:..segments → NotFound` | UI-only Actions / Console | `crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/src/pages/not_found.rs` |

## 完整受影响 E2E Request Flow

### E2E FLOW #1 — GET /

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #2 — GET /home

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /home
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #3 — GET /login

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /login
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #4 — GET /register

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /register`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /register
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #5 — GET /forgot-password

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /forgot-password`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /forgot-password
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #6 — GET /reset-password

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /reset-password`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /reset-password
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #7 — GET /console

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /console`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /console
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #8 — GET /console/

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /console/`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /console/
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #9 — GET /console/&#123;*path&#125;

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /console/{*path}`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /console/{*path}
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #10 — GET /favicon.ico

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /favicon.ico`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /favicon.ico
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: image/x-icon
Content-Length: 5430

<binary favicon bytes>
```

### E2E FLOW #11 — GET /preview/home

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/home
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #12 — GET /preview/login

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/login
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #13 — GET /preview/console

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/console`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/console
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #14 — GET /preview/console/

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/console/`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/console/
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #15 — GET /preview/console/&#123;*path&#125;

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/console/{*path}`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/console/{*path}
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #16 — GET /ws

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /ws`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /ws
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView router
│         └─ NO → fallback routing
│
├─ Feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → /ws not served by LiveView path
│         └─ YES → websocket route available
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ WebSocket upgrade
│    ├─ validate Upgrade/Connection/Sec-WebSocket-* headers
│    └─ DECISION: WebSocket handshake valid?
│         ├─ NO → HTTP handshake error → END
│         └─ YES → 101 Switching Protocols
│
├─ LiveView session
│    ├─ create/attach Dioxus LiveView session
│    ├─ receive browser UI events
│    ├─ update Virtual DOM/server-side UI state
│    └─ send render patches/messages to browser
│
├─ Connection loop
│    └─ DECISION: socket still connected?
│         ├─ YES → await next UI message → repeat
│         └─ NO → cleanup session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Accept: <computed-value>

# 随后进入双向 WebSocket / LiveView 消息通道
```

### E2E FLOW #17 — server

**树路径：** `CLI / Executables → burncloud → server`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ Shell / OS 入口
│    └─ burncloud server
│    └─ 显式 Server 模式；当前实现同时 enable_liveview=true
│
▼
FILE: src/main.rs
│
├─ main()
│    ├─ dotenvy::dotenv().ok()
│    ├─ ensure_master_key()
│    │    ├─ is_valid_master_key()
│    │    │    ├─ env::var("MASTER_KEY")
│    │    │    ├─ hex::decode(...)
│    │    │    └─ DECISION: decoded key length == 32 bytes?
│    │    │         ├─ YES → keep existing key
│    │    │         └─ NO  → generate replacement
│    │    ├─ rand::random::<[u8;32]>()
│    │    ├─ hex::encode()
│    │    ├─ resolve .env path from CWD / current_exe()
│    │    ├─ read_to_string() when file exists
│    │    ├─ fs::write(.env)
│    │    └─ env::set_var("MASTER_KEY", ...)
│    ├─ env::args().collect()
│    ├─ is_server = args[1] in [server, router, client]
│    └─ burncloud_server::logging::init_logging()
│
▼
FILE: crates/server/src/logging.rs
│
├─ init_logging()
│    ├─ LOG_DIR / LOG_MAX_FILES / RUST_LOG
│    ├─ fs::create_dir_all(log_dir)
│    ├─ tracing_log::LogTracer::init()
│    ├─ file_appender(server/service/database/router)
│    │    └─ RollingFileAppender::builder() → non_blocking(...)
│    ├─ module_filter(...)
│    └─ tracing::subscriber::set_global_default(...)
│
▼
FILE: src/main.rs
│
├─ match args.as_slice()
│    └─ DECISION: subcommand == "server" OR "router"?
│         ├─ NO  → other direct/CLI branch
│         └─ YES → run_async_server()
│
├─ run_async_server() #[tokio::main]
│    ├─ HOST env or "127.0.0.1"
│    ├─ PORT env or DEFAULT_PORT
│    └─ burncloud_server::start_server(host, port, true)
│
├─ IMPORTANT
│    ├─ 当前源码中 `server` 与 `router` 都走同一个 run_async_server()
│    └─ 此 direct branch 不进入 Clap CLI dispatch
│
▼
FILE: crates/server/src/lib.rs
│
├─ start_server(host, port, enable_liveview=true)
│    ├─ create_default_database().await
│    ├─ RouterDatabase::init(&db).await
│    ├─ UserDatabase::init(&db).await
│    ├─ create_app(db, true).await
│    ├─ SocketAddr::parse()
│    ├─ TcpListener::bind(addr).await
│    └─ axum::serve(listener, app).await
│
▼
FILE: crates/database/src/database.rs
│
├─ create_default_database() → Database::new()
├─ Database::new()
│    ├─ DECISION: BURNCLOUD_DATABASE_URL exists?
│    │    ├─ YES → use configured URL
│    │    └─ NO
│    │         ├─ get_default_database_path()
│    │         ├─ create_directory_if_not_exists()
│    │         ├─ DECISION: BURNCLOUD_FRESH_DB == "1" and DB exists?
│    │         │    ├─ YES → fs::remove_file(default DB)
│    │         │    └─ NO  → preserve DB
│    │         └─ build sqlite://...?...mode=rwc
│    └─ Database::initialize().await
│
├─ Database::initialize()
│    ├─ sqlx::any::install_default_drivers()
│    ├─ DatabaseConnection::new(database_url)
│    │    ├─ AnyConnectOptions::from_str()
│    │    └─ AnyPoolOptions::new().max_connections(10).connect_with(...)
│    ├─ DECISION: db.kind() == sqlite?
│    │    └─ YES → PRAGMA journal_mode=WAL
│    ├─ MigrationRunner::run(self)
│    └─ Schema::init(self)
│
▼
FILE: crates/database/src/migration/mod.rs
│
├─ MigrationRunner::run()
│    ├─ ensure _schema_migrations table
│    ├─ choose SQLite / PostgreSQL migration catalogue
│    ├─ inspect already-applied versions
│    └─ execute pending versioned DDL migrations
│
▼
FILE: crates/database/src/schema/mod.rs
│
├─ Schema::init()
│    ├─ rename::migrate_table_renames()
│    ├─ router::migrate_router_logs()
│    ├─ price::migrate_prices()
│    └─ user::migrate_users_and_seed()
│
▼
FILE: crates/server/src/lib.rs
│
└─ start_server() continues → RouterDatabase::init(&db)
│
▼
FILE: crates/database/crates/router/src/lib.rs
│
├─ RouterDatabase::init()
│    ├─ db.get_connection()
│    ├─ db.kind()
│    ├─ CREATE TABLE IF NOT EXISTS router_tokens
│    └─ SQLite-only compatibility ALTER TABLE statements
│
▼
FILE: crates/server/src/lib.rs
│
└─ start_server() continues → UserDatabase::init(&db)
│
▼
FILE: crates/database/crates/user/src/lib.rs
│
├─ UserDatabase::init()
│    ├─ CREATE / verify user_roles
│    ├─ CREATE / verify user_role_bindings
│    ├─ CREATE / verify user_recharges
│    ├─ SQLite/PostgreSQL compatibility migrations
│    ├─ SELECT COUNT(*) FROM user_roles
│    ├─ DECISION: no roles exist?
│    │    └─ YES → seed admin/user roles
│    ├─ query users without role bindings
│    └─ assign_role(...) for orphan users when required
│
▼
FILE: crates/server/src/lib.rs
│
├─ create_app(db, enable_liveview=true)
│    ├─ SystemMonitorService::new()
│    ├─ monitor.start_auto_update().await
│    ├─ CacheService::new().await
│    ├─ cache.is_available().await
│    ├─ create_router_app(db.clone()).await
│    ├─ UserService::new()
│    ├─ api::routes(state.clone())
│    ├─ build /health + merge Management/Internal routers
│    ├─ DECISION: enable_liveview?
│    │    └─ YES → burncloud_client::liveview_router(db.clone()) → merge
│    ├─ fallback_service(router_app)
│    └─ SetRequestId / PropagateRequestId / Trace / CORS layers
│
▼
FILE: crates/service/crates/monitor/src/service.rs
│
├─ SystemMonitorService::new()
├─ start_auto_update()
│    └─ tokio::spawn
│         └─ LOOP interval(1s)
│              └─ collect_metrics_internal()
│                   ├─ CpuCollector::collect()
│                   ├─ MemoryCollector::collect()
│                   └─ DiskCollector::collect_all()
│
▼
FILE: crates/service/crates/cache/src/service.rs
│
├─ CacheService::new() → with_config(CacheConfig::default())
├─ DECISION: CACHE_ENABLED?
│    ├─ NO  → return disabled cache
│    └─ YES
│         ├─ require REDIS_URL
│         ├─ redis::Client::open()
│         ├─ get_connection_manager().await
│         └─ PING
│
▼
FILE: crates/router/src/lib.rs
│
├─ create_router_app(db)
│    ├─ reqwest::Client::builder() + timeouts/pool settings
│    ├─ RoundRobinBalancer::new()
│    ├─ RateLimiter::new(100, 10)
│    ├─ CircuitBreaker::new(...)
│    ├─ ModelRouter::new(db.clone())
│    ├─ ChannelStateTracker::new()
│    ├─ DynamicAdaptorFactory::new(db.clone())
│    ├─ ApiVersionDetector::new(db.clone())
│    ├─ PriceCache::load(&db).await
│    │    └─ DECISION: load fails? → PriceCache::empty()
│    ├─ CostCalculator::new(price_cache.clone())
│    ├─ ExchangeRateService::new(db.clone())
│    ├─ load_rates_from_db().await
│    ├─ tokio::spawn → ExchangeRateService::start_sync_task()
│    ├─ scheduler::load_scheduler_config()
│    ├─ AffinityCache::default()
│    ├─ InMemoryBudget::new()
│    ├─ configure_rate_budget_from_db(...).await
│    ├─ read BILLING_STRICT_MODE
│    ├─ read REQUEST_LOG_STORAGE_POLICY
│    ├─ tokio::spawn → AIMD budget-update consumer
│    ├─ price_sync::start_price_sync_task(...)
│    ├─ tokio::spawn → RouterLog writer → RouterDatabase::insert_log()
│    ├─ tokio::spawn → RequestLog writer → RouterDatabase::insert_request_log()
│    ├─ ChannelHealthManager::new()
│    ├─ build internal_app routes
│    │    ├─ /console/internal/health
│    │    ├─ /console/internal/prices/sync
│    │    ├─ /console/internal/circuit-breaker/trip-all
│    │    └─ /console/internal/metrics
│    └─ build data-plane app
│         ├─ GET /v1/models → models_handler [REGISTER ONLY]
│         ├─ GET /api/v1/usage → usage_handler [REGISTER ONLY]
│         ├─ GET /api/v1/usage/models → usage_models_handler [REGISTER ONLY]
│         └─ fallback(proxy_handler) [REGISTER ONLY]
│
├─ IMPORTANT: 上述 Handler 在启动时只被注册；收到对应 HTTP 请求后才执行
│
▼
FILE: crates/service/crates/billing/src/cache.rs
│
├─ PriceCache::load(db)
│    ├─ PriceCache::empty()
│    └─ refresh(db)
│         └─ BillingPriceModel::list(...)
│
▼
FILE: crates/router/src/exchange_rate.rs
│
├─ ExchangeRateService::new()
├─ load_rates_from_db()
└─ start_sync_task()
     └─ tokio::spawn hourly loop
│
▼
FILE: crates/router/src/price_sync.rs
│
├─ start_price_sync_task(...)
│    └─ tokio::spawn
│         ├─ PriceSyncService::new()/with_config()
│         ├─ startup sync_all(false) unless SKIP_INITIAL_PRICE_SYNC
│         └─ LOOP select(periodic tick, force-sync receiver)
│
▼
FILE: crates/server/src/api/mod.rs
│
├─ api::routes(state)
│    ├─ auth::public_routes() [REGISTER]
│    ├─ auth::protected_routes() [REGISTER]
│    ├─ billing::routes() [REGISTER]
│    ├─ channel::routes() [REGISTER]
│    ├─ token::routes() [REGISTER]
│    ├─ log::routes() [REGISTER]
│    ├─ monitor::routes() [REGISTER]
│    ├─ user::routes() [REGISTER]
│    ├─ security::security_routes() [REGISTER]
│    ├─ openapi::routes() [REGISTER]
│    ├─ cache::routes() [REGISTER]
│    └─ auth_middleware layered over protected router [REGISTER]
│
├─ IMPORTANT: routes() 函数在启动期执行；具体 HTTP Handler 此刻不执行
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ liveview_router(db)
│    ├─ LiveViewPool::new()
│    ├─ register HTML shell routes /, /home, /login, /register, ...
│    ├─ register /console and /console/*
│    ├─ register /favicon.ico
│    └─ register WS_PATH
│         └─ app::App is passed as future WebSocket-session callback [REGISTER ONLY]
│
▼
FILE: crates/server/src/lib.rs
│
├─ create_app() returns Unified Router
├─ SocketAddr::parse(host:port)
├─ TcpListener::bind(addr).await
├─ axum::serve(listener, app).await
│
└─ LONG-RUNNING STATE
     ├─ HTTP listener waits for Management / Internal / Data Plane / LiveView traffic
     ├─ monitor updater keeps running
     ├─ exchange-rate task keeps running
     ├─ price-sync task keeps running
     ├─ AIMD feedback consumer keeps running
     ├─ RouterLog writer keeps running
     └─ RequestLog writer keeps running
│
▼
END = server shutdown or fatal serve error
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
Unified Gateway listening on 127.0.0.1:3000
- Dashboard: http://127.0.0.1:3000/
- LLM API:   http://127.0.0.1:3000/v1/...

# 随后 axum::serve 持续运行，命令不会立即返回到 Shell。
```

### E2E FLOW #18 — router

**树路径：** `CLI / Executables → burncloud → router`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ Shell / OS 入口
│    └─ burncloud router
│    └─ 源码命令名为 router，但当前实现与 server 一样直接进入 run_async_server()
│
▼
FILE: src/main.rs
│
├─ main()
│    ├─ dotenvy::dotenv().ok()
│    ├─ ensure_master_key()
│    │    ├─ is_valid_master_key()
│    │    │    ├─ env::var("MASTER_KEY")
│    │    │    ├─ hex::decode(...)
│    │    │    └─ DECISION: decoded key length == 32 bytes?
│    │    │         ├─ YES → keep existing key
│    │    │         └─ NO  → generate replacement
│    │    ├─ rand::random::<[u8;32]>()
│    │    ├─ hex::encode()
│    │    ├─ resolve .env path from CWD / current_exe()
│    │    ├─ read_to_string() when file exists
│    │    ├─ fs::write(.env)
│    │    └─ env::set_var("MASTER_KEY", ...)
│    ├─ env::args().collect()
│    ├─ is_server = args[1] in [server, router, client]
│    └─ burncloud_server::logging::init_logging()
│
▼
FILE: crates/server/src/logging.rs
│
├─ init_logging()
│    ├─ LOG_DIR / LOG_MAX_FILES / RUST_LOG
│    ├─ fs::create_dir_all(log_dir)
│    ├─ tracing_log::LogTracer::init()
│    ├─ file_appender(server/service/database/router)
│    │    └─ RollingFileAppender::builder() → non_blocking(...)
│    ├─ module_filter(...)
│    └─ tracing::subscriber::set_global_default(...)
│
▼
FILE: src/main.rs
│
├─ match args.as_slice()
│    └─ DECISION: subcommand == "server" OR "router"?
│         ├─ NO  → other direct/CLI branch
│         └─ YES → run_async_server()
│
├─ run_async_server() #[tokio::main]
│    ├─ HOST env or "127.0.0.1"
│    ├─ PORT env or DEFAULT_PORT
│    └─ burncloud_server::start_server(host, port, true)
│
├─ IMPORTANT
│    ├─ 当前源码中 `server` 与 `router` 都走同一个 run_async_server()
│    └─ 此 direct branch 不进入 Clap CLI dispatch
│
▼
FILE: crates/server/src/lib.rs
│
├─ start_server(host, port, enable_liveview=true)
│    ├─ create_default_database().await
│    ├─ RouterDatabase::init(&db).await
│    ├─ UserDatabase::init(&db).await
│    ├─ create_app(db, true).await
│    ├─ SocketAddr::parse()
│    ├─ TcpListener::bind(addr).await
│    └─ axum::serve(listener, app).await
│
▼
FILE: crates/database/src/database.rs
│
├─ create_default_database() → Database::new()
├─ Database::new()
│    ├─ DECISION: BURNCLOUD_DATABASE_URL exists?
│    │    ├─ YES → use configured URL
│    │    └─ NO
│    │         ├─ get_default_database_path()
│    │         ├─ create_directory_if_not_exists()
│    │         ├─ DECISION: BURNCLOUD_FRESH_DB == "1" and DB exists?
│    │         │    ├─ YES → fs::remove_file(default DB)
│    │         │    └─ NO  → preserve DB
│    │         └─ build sqlite://...?...mode=rwc
│    └─ Database::initialize().await
│
├─ Database::initialize()
│    ├─ sqlx::any::install_default_drivers()
│    ├─ DatabaseConnection::new(database_url)
│    │    ├─ AnyConnectOptions::from_str()
│    │    └─ AnyPoolOptions::new().max_connections(10).connect_with(...)
│    ├─ DECISION: db.kind() == sqlite?
│    │    └─ YES → PRAGMA journal_mode=WAL
│    ├─ MigrationRunner::run(self)
│    └─ Schema::init(self)
│
▼
FILE: crates/database/src/migration/mod.rs
│
├─ MigrationRunner::run()
│    ├─ ensure _schema_migrations table
│    ├─ choose SQLite / PostgreSQL migration catalogue
│    ├─ inspect already-applied versions
│    └─ execute pending versioned DDL migrations
│
▼
FILE: crates/database/src/schema/mod.rs
│
├─ Schema::init()
│    ├─ rename::migrate_table_renames()
│    ├─ router::migrate_router_logs()
│    ├─ price::migrate_prices()
│    └─ user::migrate_users_and_seed()
│
▼
FILE: crates/server/src/lib.rs
│
└─ start_server() continues → RouterDatabase::init(&db)
│
▼
FILE: crates/database/crates/router/src/lib.rs
│
├─ RouterDatabase::init()
│    ├─ db.get_connection()
│    ├─ db.kind()
│    ├─ CREATE TABLE IF NOT EXISTS router_tokens
│    └─ SQLite-only compatibility ALTER TABLE statements
│
▼
FILE: crates/server/src/lib.rs
│
└─ start_server() continues → UserDatabase::init(&db)
│
▼
FILE: crates/database/crates/user/src/lib.rs
│
├─ UserDatabase::init()
│    ├─ CREATE / verify user_roles
│    ├─ CREATE / verify user_role_bindings
│    ├─ CREATE / verify user_recharges
│    ├─ SQLite/PostgreSQL compatibility migrations
│    ├─ SELECT COUNT(*) FROM user_roles
│    ├─ DECISION: no roles exist?
│    │    └─ YES → seed admin/user roles
│    ├─ query users without role bindings
│    └─ assign_role(...) for orphan users when required
│
▼
FILE: crates/server/src/lib.rs
│
├─ create_app(db, enable_liveview=true)
│    ├─ SystemMonitorService::new()
│    ├─ monitor.start_auto_update().await
│    ├─ CacheService::new().await
│    ├─ cache.is_available().await
│    ├─ create_router_app(db.clone()).await
│    ├─ UserService::new()
│    ├─ api::routes(state.clone())
│    ├─ build /health + merge Management/Internal routers
│    ├─ DECISION: enable_liveview?
│    │    └─ YES → burncloud_client::liveview_router(db.clone()) → merge
│    ├─ fallback_service(router_app)
│    └─ SetRequestId / PropagateRequestId / Trace / CORS layers
│
▼
FILE: crates/service/crates/monitor/src/service.rs
│
├─ SystemMonitorService::new()
├─ start_auto_update()
│    └─ tokio::spawn
│         └─ LOOP interval(1s)
│              └─ collect_metrics_internal()
│                   ├─ CpuCollector::collect()
│                   ├─ MemoryCollector::collect()
│                   └─ DiskCollector::collect_all()
│
▼
FILE: crates/service/crates/cache/src/service.rs
│
├─ CacheService::new() → with_config(CacheConfig::default())
├─ DECISION: CACHE_ENABLED?
│    ├─ NO  → return disabled cache
│    └─ YES
│         ├─ require REDIS_URL
│         ├─ redis::Client::open()
│         ├─ get_connection_manager().await
│         └─ PING
│
▼
FILE: crates/router/src/lib.rs
│
├─ create_router_app(db)
│    ├─ reqwest::Client::builder() + timeouts/pool settings
│    ├─ RoundRobinBalancer::new()
│    ├─ RateLimiter::new(100, 10)
│    ├─ CircuitBreaker::new(...)
│    ├─ ModelRouter::new(db.clone())
│    ├─ ChannelStateTracker::new()
│    ├─ DynamicAdaptorFactory::new(db.clone())
│    ├─ ApiVersionDetector::new(db.clone())
│    ├─ PriceCache::load(&db).await
│    │    └─ DECISION: load fails? → PriceCache::empty()
│    ├─ CostCalculator::new(price_cache.clone())
│    ├─ ExchangeRateService::new(db.clone())
│    ├─ load_rates_from_db().await
│    ├─ tokio::spawn → ExchangeRateService::start_sync_task()
│    ├─ scheduler::load_scheduler_config()
│    ├─ AffinityCache::default()
│    ├─ InMemoryBudget::new()
│    ├─ configure_rate_budget_from_db(...).await
│    ├─ read BILLING_STRICT_MODE
│    ├─ read REQUEST_LOG_STORAGE_POLICY
│    ├─ tokio::spawn → AIMD budget-update consumer
│    ├─ price_sync::start_price_sync_task(...)
│    ├─ tokio::spawn → RouterLog writer → RouterDatabase::insert_log()
│    ├─ tokio::spawn → RequestLog writer → RouterDatabase::insert_request_log()
│    ├─ ChannelHealthManager::new()
│    ├─ build internal_app routes
│    │    ├─ /console/internal/health
│    │    ├─ /console/internal/prices/sync
│    │    ├─ /console/internal/circuit-breaker/trip-all
│    │    └─ /console/internal/metrics
│    └─ build data-plane app
│         ├─ GET /v1/models → models_handler [REGISTER ONLY]
│         ├─ GET /api/v1/usage → usage_handler [REGISTER ONLY]
│         ├─ GET /api/v1/usage/models → usage_models_handler [REGISTER ONLY]
│         └─ fallback(proxy_handler) [REGISTER ONLY]
│
├─ IMPORTANT: 上述 Handler 在启动时只被注册；收到对应 HTTP 请求后才执行
│
▼
FILE: crates/service/crates/billing/src/cache.rs
│
├─ PriceCache::load(db)
│    ├─ PriceCache::empty()
│    └─ refresh(db)
│         └─ BillingPriceModel::list(...)
│
▼
FILE: crates/router/src/exchange_rate.rs
│
├─ ExchangeRateService::new()
├─ load_rates_from_db()
└─ start_sync_task()
     └─ tokio::spawn hourly loop
│
▼
FILE: crates/router/src/price_sync.rs
│
├─ start_price_sync_task(...)
│    └─ tokio::spawn
│         ├─ PriceSyncService::new()/with_config()
│         ├─ startup sync_all(false) unless SKIP_INITIAL_PRICE_SYNC
│         └─ LOOP select(periodic tick, force-sync receiver)
│
▼
FILE: crates/server/src/api/mod.rs
│
├─ api::routes(state)
│    ├─ auth::public_routes() [REGISTER]
│    ├─ auth::protected_routes() [REGISTER]
│    ├─ billing::routes() [REGISTER]
│    ├─ channel::routes() [REGISTER]
│    ├─ token::routes() [REGISTER]
│    ├─ log::routes() [REGISTER]
│    ├─ monitor::routes() [REGISTER]
│    ├─ user::routes() [REGISTER]
│    ├─ security::security_routes() [REGISTER]
│    ├─ openapi::routes() [REGISTER]
│    ├─ cache::routes() [REGISTER]
│    └─ auth_middleware layered over protected router [REGISTER]
│
├─ IMPORTANT: routes() 函数在启动期执行；具体 HTTP Handler 此刻不执行
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ liveview_router(db)
│    ├─ LiveViewPool::new()
│    ├─ register HTML shell routes /, /home, /login, /register, ...
│    ├─ register /console and /console/*
│    ├─ register /favicon.ico
│    └─ register WS_PATH
│         └─ app::App is passed as future WebSocket-session callback [REGISTER ONLY]
│
▼
FILE: crates/server/src/lib.rs
│
├─ create_app() returns Unified Router
├─ SocketAddr::parse(host:port)
├─ TcpListener::bind(addr).await
├─ axum::serve(listener, app).await
│
└─ LONG-RUNNING STATE
     ├─ HTTP listener waits for Management / Internal / Data Plane / LiveView traffic
     ├─ monitor updater keeps running
     ├─ exchange-rate task keeps running
     ├─ price-sync task keeps running
     ├─ AIMD feedback consumer keeps running
     ├─ RouterLog writer keeps running
     └─ RequestLog writer keeps running
│
▼
END = server shutdown or fatal serve error
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
Unified Gateway listening on 127.0.0.1:3000
- Dashboard: http://127.0.0.1:3000/
- LLM API:   http://127.0.0.1:3000/v1/...

# 随后 axum::serve 持续运行，命令不会立即返回到 Shell。
```

### E2E FLOW #19 — screenshot_gen

**树路径：** `CLI / Executables → Workspace Binaries → screenshot_gen`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/bin/screenshot_gen.rs`

```text
START
│
├─ OS process launch
│    ├─ executable: screenshot_gen
│    ├─ argv / cwd / environment inherited from OS
│    └─ DECISION: executable can be loaded/launched?
│         ├─ NO → OS-level error → END
│         └─ YES → main()
│
▼
FILE: crates/client/src/bin/screenshot_gen.rs  [PR CHANGED]
│
├─ main() initialization
│    ├─ initialize executable-specific runtime/services
│    ├─ parse any supported arguments
│    └─ DECISION: platform/arguments/initialization valid?
│         ├─ NO → print/return error → process exit
│         └─ YES → continue
│
├─ Runtime work
│    ├─ create client/download/loop/tray structures as applicable
│    ├─ start event loop or execute one-shot job
│    └─ DECISION: long-running executable?
│         ├─ YES → enter event/service loop
│         └─ NO → produce output and exit
│
├─ Error boundary
│    └─ runtime error → log/return non-success according to executable implementation
│
▼
END / RUNNING LOOP
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/bin/screenshot_gen.rs`**

HUNKS:
```text
@@ -1,5 +1,5 @@
@@ -24,24 +24,22 @@ fn main() {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::styles::DESIGN_SYSTEM_CSS;
-    let tailwind_css = include_str!("../assets/tailwind.css");
-
-    <style>{}</style>
-    <style>{}</style>
-    {}
-        tailwind_css, DESIGN_SYSTEM_CSS, html_content
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::liveview_style_tags;
+    {styles}
+    {body}
+        styles = liveview_style_tags(),
+        body = html_content
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
VirtualDom rendered
output=screenshot.html
status=success
```

### E2E FLOW #20 — create_app

**树路径：** `Startup → Startup Chain → create_app`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/crates/client-shared/src/app_styles.rs`

```text
START
│
├─ Process/environment input
│    ├─ startup target: create_app
│    ├─ environment variables / dotenv
│    ├─ CLI/platform mode
│    └─ filesystem/database availability
│
├─ Enter startup function
│    └─ execute create_app
│
├─ Dependency initialization
│    ├─ construct required DB/services/runtime state
│    ├─ register routes/tasks as applicable
│    └─ DECISION: dependency initialization succeeds?
│         ├─ NO → propagate startup error → process/runtime not ready → END
│         └─ YES → next dependency
│
├─ Runtime composition
│    ├─ wire shared Arc/State/services
│    ├─ compose routers/middleware/background jobs
│    └─ make dependencies reachable from runtime entrypoints
│
├─ Readiness boundary
│    └─ DECISION: all required startup stages complete?
│         ├─ NO → startup fails/returns Err
│         └─ YES → expose listener/client/event loop/runtime
│
├─ Steady-state handoff
│    ├─ long-running loops take ownership of runtime
│    └─ requests/events can now enter documented entrypoints
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/server/src/lib.rs
│    │    ├─ start_server()
│    │    │    └─ CALL → create_default_database() @ crates/database/src/database.rs
│    │    │    └─ CALL → RouterDatabase::init() @ crates/database/crates/router/src/lib.rs
│    │    │    └─ CALL → UserDatabase::init() @ crates/database/crates/user/src/lib.rs
│    │    │    └─ CALL → create_app() @ crates/server/src/lib.rs
│    │    ├─ create_app()
│    │    │    └─ CALL → SystemMonitorService::new() @ crates/service/crates/monitor/src/service.rs
│    │    │    └─ CALL → CacheService::new() @ crates/service/crates/cache/src/service.rs
│    │    │    └─ CALL → create_router_app() @ crates/router/src/lib.rs
│    │    │    └─ CALL → UserService::new() @ crates/service/crates/user/src/lib.rs
│    │    │    └─ CALL → routes() @ crates/server/src/api/mod.rs
│    ├─ FILE: crates/server/src/api/mod.rs
│    │    ├─ routes()
│    │    │    └─ CALL → public_routes() @ crates/server/src/api/auth.rs
│    │    │    └─ CALL → protected_routes() @ crates/server/src/api/auth.rs
│    │    │    └─ CALL → security_routes() @ crates/server/src/api/security.rs
│    ├─ FILE: crates/client/src/lib.rs  [PR CHANGED]
│    │    ├─ liveview_router()
│    │    │    └─ CALL → liveview_style_tags() @ crates/client/crates/client-shared/src/app_styles.rs  [PR CHANGED]
│    ├─ FILE: crates/router/src/lib.rs
│    │    ├─ create_router_app()
│    │    │    └─ CALL → RoundRobinBalancer::new() @ crates/router/src/balancer/mod.rs
│    │    │    └─ CALL → RateLimiter::new() @ crates/router/src/limiter.rs
│    │    │    └─ CALL → CircuitBreaker::new() @ crates/router/src/circuit_breaker.rs
│    │    │    └─ CALL → ModelRouter::new() @ crates/router/src/model_router.rs
│    │    │    └─ CALL → ChannelStateTracker::new() @ crates/router/src/channel_state.rs
│    │    │    └─ CALL → DynamicAdaptorFactory::new() @ crates/router/src/adaptor/factory.rs
│    │    │    └─ CALL → ApiVersionDetector::new() @ crates/router/src/adaptor/detector.rs
│    │    │    └─ CALL → PriceCache::load() @ crates/service/crates/billing/src/cache.rs
│    │    │    └─ CALL → PriceCache::empty() @ crates/service/crates/billing/src/cache.rs
│    │    │    └─ CALL → CostCalculator::new() @ crates/service/crates/billing/src/calculator.rs
│    │    ├─ proxy_handler()
│    │    │    └─ CALL → normalize_doubled_path() @ crates/router/src/lib.rs
│    │    │    └─ CALL → build_response_with_header() @ crates/router/src/lib.rs
│    │    │    └─ CALL → RouterDatabase::validate_token_and_get_info() @ crates/database/crates/router/src/lib.rs
│    │    │    └─ CALL → RouterDatabase::update_token_accessed_time() @ crates/database/crates/router/src/lib.rs
│    │    │    └─ CALL → RouterDatabase::validate_token_detailed() @ crates/database/crates/router/src/lib.rs
│    │    │    └─ CALL → extract_model_from_gemini_path() @ crates/router/src/passthrough.rs
│    │    │    └─ CALL → RouterVideoTaskModel::get_by_task_id() @ crates/database/crates/router/src/router_video_task.rs
│    │    │    └─ CALL → ChannelProviderModel::get_by_id() @ crates/database/crates/channel/src/channel_provider.rs
│    │    │    └─ CALL → UnifiedTokenCounter::new() @ crates/service/crates/billing/src/counter.rs
│    │    │    └─ CALL → proxy_logic() @ crates/router/src/lib.rs
│    │    ├─ proxy_logic()
│    │    │    └─ CALL → sanitize_request_body() @ crates/router/src/lib.rs
│    │    │    └─ CALL → sanitize_request_headers() @ crates/router/src/lib.rs
│    │    │    └─ CALL → extract_model_from_gemini_path() @ crates/router/src/passthrough.rs
│    │    │    └─ CALL → UserService::resolve_traffic_class() @ crates/service/crates/user/src/lib.rs
│    │    │    └─ CALL → OrderType::from_db_row() @ crates/router/src/order_type.rs
│    │    │    └─ CALL → build_response_with_header() @ crates/router/src/lib.rs
│    │    │    └─ CALL → record_failover_attempt() @ crates/router/src/lib.rs
│    │    │    └─ CALL → BudgetGuard::new() @ crates/router/src/rate_budget.rs
│    │    │    └─ CALL → should_passthrough() @ crates/router/src/passthrough.rs
│    │    │    └─ CALL → build_gemini_passthrough_url() @ crates/router/src/passthrough.rs
│    ├─ FILE: crates/database/src/database.rs
│    │    ├─ create_default_database()
│    │    │    └─ CALL → Database::new() @ crates/database/src/database.rs
│    ├─ FILE: crates/database/crates/router/src/lib.rs
│    │    ├─ RouterDatabase::init()
│    ├─ FILE: crates/database/crates/user/src/lib.rs
│    │    ├─ UserDatabase::init()
│    │    │    └─ CALL → UserDatabase::assign_role() @ crates/database/crates/user/src/lib.rs
│    ├─ FILE: crates/service/crates/monitor/src/service.rs
│    │    ├─ SystemMonitorService::new()
│    ├─ FILE: crates/service/crates/cache/src/service.rs
│    │    ├─ CacheService::new()
│    │    │    └─ CALL → CacheService::with_config() @ crates/service/crates/cache/src/service.rs
│    ├─ FILE: crates/service/crates/user/src/lib.rs
│    │    ├─ UserService::new()
│    ├─ FILE: crates/server/src/api/auth.rs
│    │    ├─ public_routes()
│    │    ├─ protected_routes()
│    ├─ FILE: crates/server/src/api/security.rs
│    │    ├─ security_routes()
│    ├─ FILE: crates/client/crates/client-shared/src/app_styles.rs  [PR CHANGED]
│    │    ├─ liveview_style_tags()
│    ├─ FILE: crates/router/src/balancer/mod.rs
│    │    ├─ RoundRobinBalancer::new()
│    ├─ FILE: crates/router/src/limiter.rs
│    │    ├─ RateLimiter::new()
│    ├─ FILE: crates/router/src/circuit_breaker.rs
│    │    ├─ CircuitBreaker::new()
│    ├─ FILE: crates/router/src/model_router.rs
│    │    ├─ ModelRouter::new()
│    ├─ FILE: crates/router/src/channel_state.rs
│    │    ├─ ChannelStateTracker::new()
│    ├─ FILE: crates/router/src/adaptor/factory.rs
│    │    ├─ DynamicAdaptorFactory::new()
│    ├─ FILE: crates/router/src/adaptor/detector.rs
│    │    ├─ ApiVersionDetector::new()
│    ├─ FILE: crates/service/crates/billing/src/cache.rs
│    │    ├─ PriceCache::load()
│    │    │    └─ CALL → PriceCache::empty() @ crates/service/crates/billing/src/cache.rs
│    │    ├─ PriceCache::empty()
│    ├─ FILE: crates/service/crates/billing/src/calculator.rs
│    │    ├─ CostCalculator::new()
│    └─ FILE: crates/router/src/exchange_rate.rs
│    │    ├─ ExchangeRateService::new()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ component is READY / RUNNING
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/app_styles.rs`**

HUNKS:
```text
@@ -0,0 +1,27 @@
```

AFTER / ADDED:
```diff
+//! Single injection point for Tailwind (layout) + BurnCloud design system (visual).
+//!
+//! `tailwind.css` is generated by `crates/client/build.rs` into `crates/client/src/assets/`.
+
+use crate::styles::DESIGN_SYSTEM_CSS;
+use dioxus::prelude::*;
+
+/// Tailwind utilities built by the parent `burncloud-client` crate's `build.rs`.
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -23,6 +23,8 @@ use burncloud_common::constants::WS_PATH;
@@ -39,192 +41,16 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-                <style>{}</style>
-                <style>
-                    /* Custom JIT Shims for LiveView match Desktop Layout */
-                    .text-xxs {{ font-size: 10px; }}
-                    .text-xxxs {{ font-size: 8px; }}
-                    .bg-macos-red {{ background-color: #FF5F56; }}
-                    .bg-macos-yellow {{ background-color: #FFBD2E; }}
-                    .bg-macos-green {{ background-color: #27C93F; }}
```

AFTER / ADDED:
```diff
+    use burncloud_client_shared::liveview_style_tags;
+
+                {styles}
+                {glue}
+            styles = liveview_style_tags(),
+            glue = dioxus_liveview::interpreter_glue(&format!("ws://{}{}", host, WS_PATH))
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
monitor=started
cache=ready
management_routes=mounted
internal_routes=mounted
liveview=enabled
data_plane_fallback=mounted
```

### E2E FLOW #21 — /home

**树路径：** `UI-only Actions → Guest / Public → /home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/guest_layout.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /home
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/home.rs
│
└─ HomePage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/components/guest_layout.rs`**

HUNKS:
```text
@@ -2,331 +2,18 @@ use dioxus::prelude::*;
```

BEFORE / REMOVED:
```diff
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
-            // BurnCloud Design System CSS (single source of truth)
-            style { "{burncloud_client_shared::styles::DESIGN_SYSTEM_CSS}" }
-
-            // Tailwind v2 JIT Shims (static build compatibility for arbitrary values)
-            // These will be gradually removed as pages migrate to --bc-* tokens
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            AppStyles {}
+        div { class: "h-screen w-screen overflow-hidden flex flex-col bg-bc-canvas text-bc-text relative", "data-theme": "light",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/home
component=home
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #22 — /login

**树路径：** `UI-only Actions → Guest / Public → /login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/guest_layout.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /login
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/login.rs
│
└─ LoginPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/components/guest_layout.rs`**

HUNKS:
```text
@@ -2,331 +2,18 @@ use dioxus::prelude::*;
```

BEFORE / REMOVED:
```diff
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
-            // BurnCloud Design System CSS (single source of truth)
-            style { "{burncloud_client_shared::styles::DESIGN_SYSTEM_CSS}" }
-
-            // Tailwind v2 JIT Shims (static build compatibility for arbitrary values)
-            // These will be gradually removed as pages migrate to --bc-* tokens
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            AppStyles {}
+        div { class: "h-screen w-screen overflow-hidden flex flex-col bg-bc-canvas text-bc-text relative", "data-theme": "light",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/login
component=login
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #23 — /register

**树路径：** `UI-only Actions → Guest / Public → /register`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/guest_layout.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /register
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/crates/client-register/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/components/guest_layout.rs`**

HUNKS:
```text
@@ -2,331 +2,18 @@ use dioxus::prelude::*;
```

BEFORE / REMOVED:
```diff
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
-            // BurnCloud Design System CSS (single source of truth)
-            style { "{burncloud_client_shared::styles::DESIGN_SYSTEM_CSS}" }
-
-            // Tailwind v2 JIT Shims (static build compatibility for arbitrary values)
-            // These will be gradually removed as pages migrate to --bc-* tokens
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            AppStyles {}
+        div { class: "h-screen w-screen overflow-hidden flex flex-col bg-bc-canvas text-bc-text relative", "data-theme": "light",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/register
component=register
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #24 — /forgot-password

**树路径：** `UI-only Actions → Guest / Public → /forgot-password`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/guest_layout.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /forgot-password
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/forgot_password.rs
│
└─ ForgotPasswordPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/components/guest_layout.rs`**

HUNKS:
```text
@@ -2,331 +2,18 @@ use dioxus::prelude::*;
```

BEFORE / REMOVED:
```diff
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
-            // BurnCloud Design System CSS (single source of truth)
-            style { "{burncloud_client_shared::styles::DESIGN_SYSTEM_CSS}" }
-
-            // Tailwind v2 JIT Shims (static build compatibility for arbitrary values)
-            // These will be gradually removed as pages migrate to --bc-* tokens
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            AppStyles {}
+        div { class: "h-screen w-screen overflow-hidden flex flex-col bg-bc-canvas text-bc-text relative", "data-theme": "light",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/forgot-password
component=forgot-password
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #25 — /reset-password?:token

**树路径：** `UI-only Actions → Guest / Public → /reset-password?:token`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/guest_layout.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /reset-password?:token
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/reset_password.rs
│
└─ ResetPasswordPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/components/guest_layout.rs`**

HUNKS:
```text
@@ -2,331 +2,18 @@ use dioxus::prelude::*;
```

BEFORE / REMOVED:
```diff
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
-            // BurnCloud Design System CSS (single source of truth)
-            style { "{burncloud_client_shared::styles::DESIGN_SYSTEM_CSS}" }
-
-            // Tailwind v2 JIT Shims (static build compatibility for arbitrary values)
-            // These will be gradually removed as pages migrate to --bc-* tokens
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            AppStyles {}
+        div { class: "h-screen w-screen overflow-hidden flex flex-col bg-bc-canvas text-bc-text relative", "data-theme": "light",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/reset-password?:token
component=reset-password?:token
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #26 — /console/dashboard

**树路径：** `UI-only Actions → Console → /console/dashboard`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/dashboard
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/dashboard.rs
│
└─ Dashboard re-export
│
▼
FILE: crates/client/crates/client-dashboard/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -9,7 +9,7 @@ use super::placeholders::{
@@ -44,14 +44,29 @@ pub enum CoreRoute {
```

BEFORE / REMOVED:
```diff
-use crate::styles::DESIGN_SYSTEM_CSS;
-            style { "{DESIGN_SYSTEM_CSS}" }
-        div { class: "app-container",
-            TitleBar {}
-            div { class: "app-body",
-                Sidebar {}
-                main { class: "main-content",
-                    Outlet::<CoreRoute> {}
```

AFTER / ADDED:
```diff
+use crate::app_styles::AppStyles;
+            AppStyles {}
+
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "light",
+
+            div { class: "absolute top-0 left-0 w-full z-50 pointer-events-none",
+                div { class: "pointer-events-auto",
+                    TitleBar {}
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -1,6 +1,7 @@
@@ -17,39 +18,26 @@ pub fn Layout() -> Element {
@@ -61,16 +49,14 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-    // Route guard: Check if user is authenticated
-    // Only redirect on initial mount, not on every render
-    // Prevent rendering protected content if not authenticated
-            div { class: "h-screen w-screen flex items-center justify-center bg-base-100",
-                span { class: "loading loading-spinner loading-lg text-primary" }
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            div { class: "h-screen w-screen flex items-center justify-center bg-bc-canvas",
+                span { class: "bc-spinner bc-spinner--lg" }
+            AppStyles {}
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "{theme_str}",
+                div { class: "w-64 shrink-0 flex flex-col border-r border-bc-border/50 bg-bc-canvas/80 backdrop-blur-xl",
+                div { class: "flex-1 flex flex-col bg-bc-canvas relative min-w-0",
+                    main { class: "flex-1 overflow-y-auto overflow-x-hidden flex flex-col min-h-0",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/dashboard
component=console/dashboard
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #27 — /console/deploy

**树路径：** `UI-only Actions → Console → /console/deploy`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/deploy
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/deploy.rs
│
└─ DeployConfig re-export
│
▼
FILE: crates/client/crates/client-deploy/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -9,7 +9,7 @@ use super::placeholders::{
@@ -44,14 +44,29 @@ pub enum CoreRoute {
```

BEFORE / REMOVED:
```diff
-use crate::styles::DESIGN_SYSTEM_CSS;
-            style { "{DESIGN_SYSTEM_CSS}" }
-        div { class: "app-container",
-            TitleBar {}
-            div { class: "app-body",
-                Sidebar {}
-                main { class: "main-content",
-                    Outlet::<CoreRoute> {}
```

AFTER / ADDED:
```diff
+use crate::app_styles::AppStyles;
+            AppStyles {}
+
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "light",
+
+            div { class: "absolute top-0 left-0 w-full z-50 pointer-events-none",
+                div { class: "pointer-events-auto",
+                    TitleBar {}
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -1,6 +1,7 @@
@@ -17,39 +18,26 @@ pub fn Layout() -> Element {
@@ -61,16 +49,14 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-    // Route guard: Check if user is authenticated
-    // Only redirect on initial mount, not on every render
-    // Prevent rendering protected content if not authenticated
-            div { class: "h-screen w-screen flex items-center justify-center bg-base-100",
-                span { class: "loading loading-spinner loading-lg text-primary" }
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            div { class: "h-screen w-screen flex items-center justify-center bg-bc-canvas",
+                span { class: "bc-spinner bc-spinner--lg" }
+            AppStyles {}
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "{theme_str}",
+                div { class: "w-64 shrink-0 flex flex-col border-r border-bc-border/50 bg-bc-canvas/80 backdrop-blur-xl",
+                div { class: "flex-1 flex flex-col bg-bc-canvas relative min-w-0",
+                    main { class: "flex-1 overflow-y-auto overflow-x-hidden flex flex-col min-h-0",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/deploy
component=console/deploy
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #28 — /console/monitor

**树路径：** `UI-only Actions → Console → /console/monitor`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/monitor
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/monitor.rs
│
└─ ServiceMonitor re-export
│
▼
FILE: crates/client/crates/client-monitor/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -9,7 +9,7 @@ use super::placeholders::{
@@ -44,14 +44,29 @@ pub enum CoreRoute {
```

BEFORE / REMOVED:
```diff
-use crate::styles::DESIGN_SYSTEM_CSS;
-            style { "{DESIGN_SYSTEM_CSS}" }
-        div { class: "app-container",
-            TitleBar {}
-            div { class: "app-body",
-                Sidebar {}
-                main { class: "main-content",
-                    Outlet::<CoreRoute> {}
```

AFTER / ADDED:
```diff
+use crate::app_styles::AppStyles;
+            AppStyles {}
+
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "light",
+
+            div { class: "absolute top-0 left-0 w-full z-50 pointer-events-none",
+                div { class: "pointer-events-auto",
+                    TitleBar {}
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -1,6 +1,7 @@
@@ -17,39 +18,26 @@ pub fn Layout() -> Element {
@@ -61,16 +49,14 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-    // Route guard: Check if user is authenticated
-    // Only redirect on initial mount, not on every render
-    // Prevent rendering protected content if not authenticated
-            div { class: "h-screen w-screen flex items-center justify-center bg-base-100",
-                span { class: "loading loading-spinner loading-lg text-primary" }
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            div { class: "h-screen w-screen flex items-center justify-center bg-bc-canvas",
+                span { class: "bc-spinner bc-spinner--lg" }
+            AppStyles {}
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "{theme_str}",
+                div { class: "w-64 shrink-0 flex flex-col border-r border-bc-border/50 bg-bc-canvas/80 backdrop-blur-xl",
+                div { class: "flex-1 flex flex-col bg-bc-canvas relative min-w-0",
+                    main { class: "flex-1 overflow-y-auto overflow-x-hidden flex flex-col min-h-0",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/monitor
component=console/monitor
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #29 — /console/access

**树路径：** `UI-only Actions → Console → /console/access`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-access/src/lib.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/access
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/api.rs
│
└─ AccessPage re-export
│
▼
FILE: crates/client/crates/client-access/src/lib.rs  [PR CHANGED]
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-access/src/lib.rs`**

HUNKS:
```text
@@ -131,7 +131,7 @@ pub fn AccessPage() -> Element {
@@ -154,9 +154,24 @@ pub fn AccessPage() -> Element {
@@ -169,22 +184,6 @@ pub fn AccessPage() -> Element {
@@ -193,13 +192,21 @@ pub fn AccessPage() -> Element {
@@ -222,13 +229,6 @@ pub fn AccessPage() -> Element {
@@ -237,48 +237,48 @@ pub fn AccessPage() -> Element {
```

BEFORE / REMOVED:
```diff
-                                                class: "btn-icon text-tertiary",
-            div { class: "flex flex-col gap-lg p-lg",
-                div { class: "bc-hint-text mt-xs", {t(*lang.read(), "access.create_modal.desc")} }
-
-                div { class: "flex justify-end gap-md mt-md",
-                    BCButton {
-                        variant: ButtonVariant::Ghost,
-                        onclick: move |_| show_create.set(false),
```

AFTER / ADDED:
```diff
+                                                class: "btn-icon text-bc-text-tertiary",
+            footer: rsx! {
+                BCButton {
+                    variant: ButtonVariant::Ghost,
+                    onclick: move |_| show_create.set(false),
+                    {t(*lang.read(), "common.cancel")}
+                }
+                BCButton {
```

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -9,7 +9,7 @@ use super::placeholders::{
@@ -44,14 +44,29 @@ pub enum CoreRoute {
```

BEFORE / REMOVED:
```diff
-use crate::styles::DESIGN_SYSTEM_CSS;
-            style { "{DESIGN_SYSTEM_CSS}" }
-        div { class: "app-container",
-            TitleBar {}
-            div { class: "app-body",
-                Sidebar {}
-                main { class: "main-content",
-                    Outlet::<CoreRoute> {}
```

AFTER / ADDED:
```diff
+use crate::app_styles::AppStyles;
+            AppStyles {}
+
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "light",
+
+            div { class: "absolute top-0 left-0 w-full z-50 pointer-events-none",
+                div { class: "pointer-events-auto",
+                    TitleBar {}
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -1,6 +1,7 @@
@@ -17,39 +18,26 @@ pub fn Layout() -> Element {
@@ -61,16 +49,14 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-    // Route guard: Check if user is authenticated
-    // Only redirect on initial mount, not on every render
-    // Prevent rendering protected content if not authenticated
-            div { class: "h-screen w-screen flex items-center justify-center bg-base-100",
-                span { class: "loading loading-spinner loading-lg text-primary" }
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            div { class: "h-screen w-screen flex items-center justify-center bg-bc-canvas",
+                span { class: "bc-spinner bc-spinner--lg" }
+            AppStyles {}
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "{theme_str}",
+                div { class: "w-64 shrink-0 flex flex-col border-r border-bc-border/50 bg-bc-canvas/80 backdrop-blur-xl",
+                div { class: "flex-1 flex flex-col bg-bc-canvas relative min-w-0",
+                    main { class: "flex-1 overflow-y-auto overflow-x-hidden flex flex-col min-h-0",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/access
component=console/access
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #30 — /console/models

**树路径：** `UI-only Actions → Console → /console/models`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/models
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/models.rs
│
└─ ChannelPage re-export
│
▼
FILE: crates/client/crates/client-models/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -9,7 +9,7 @@ use super::placeholders::{
@@ -44,14 +44,29 @@ pub enum CoreRoute {
```

BEFORE / REMOVED:
```diff
-use crate::styles::DESIGN_SYSTEM_CSS;
-            style { "{DESIGN_SYSTEM_CSS}" }
-        div { class: "app-container",
-            TitleBar {}
-            div { class: "app-body",
-                Sidebar {}
-                main { class: "main-content",
-                    Outlet::<CoreRoute> {}
```

AFTER / ADDED:
```diff
+use crate::app_styles::AppStyles;
+            AppStyles {}
+
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "light",
+
+            div { class: "absolute top-0 left-0 w-full z-50 pointer-events-none",
+                div { class: "pointer-events-auto",
+                    TitleBar {}
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -1,6 +1,7 @@
@@ -17,39 +18,26 @@ pub fn Layout() -> Element {
@@ -61,16 +49,14 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-    // Route guard: Check if user is authenticated
-    // Only redirect on initial mount, not on every render
-    // Prevent rendering protected content if not authenticated
-            div { class: "h-screen w-screen flex items-center justify-center bg-base-100",
-                span { class: "loading loading-spinner loading-lg text-primary" }
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            div { class: "h-screen w-screen flex items-center justify-center bg-bc-canvas",
+                span { class: "bc-spinner bc-spinner--lg" }
+            AppStyles {}
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "{theme_str}",
+                div { class: "w-64 shrink-0 flex flex-col border-r border-bc-border/50 bg-bc-canvas/80 backdrop-blur-xl",
+                div { class: "flex-1 flex flex-col bg-bc-canvas relative min-w-0",
+                    main { class: "flex-1 overflow-y-auto overflow-x-hidden flex flex-col min-h-0",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/models
component=console/models
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #31 — /console/users

**树路径：** `UI-only Actions → Console → /console/users`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-users/src/lib.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/users
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/user.rs
│
└─ UsersPage re-export
│
▼
FILE: crates/client/crates/client-users/src/lib.rs  [PR CHANGED]
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -9,7 +9,7 @@ use super::placeholders::{
@@ -44,14 +44,29 @@ pub enum CoreRoute {
```

BEFORE / REMOVED:
```diff
-use crate::styles::DESIGN_SYSTEM_CSS;
-            style { "{DESIGN_SYSTEM_CSS}" }
-        div { class: "app-container",
-            TitleBar {}
-            div { class: "app-body",
-                Sidebar {}
-                main { class: "main-content",
-                    Outlet::<CoreRoute> {}
```

AFTER / ADDED:
```diff
+use crate::app_styles::AppStyles;
+            AppStyles {}
+
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "light",
+
+            div { class: "absolute top-0 left-0 w-full z-50 pointer-events-none",
+                div { class: "pointer-events-auto",
+                    TitleBar {}
```

**FILE: `crates/client/crates/client-users/src/lib.rs`**

HUNKS:
```text
@@ -128,22 +128,22 @@ pub fn UsersPage() -> Element {
@@ -154,10 +154,9 @@ pub fn UsersPage() -> Element {
@@ -175,82 +174,80 @@ pub fn UsersPage() -> Element {
@@ -263,7 +260,7 @@ pub fn UsersPage() -> Element {
```

BEFORE / REMOVED:
```diff
-                                th { style: "text-align:right", {t(*lang.read(), "users.col.actions")} }
-                                    td { span { class: "mono", style: "font-size:12px", "{u.id}" } }
-                                    td { style: "font-weight:600", "{u.username}" }
-                                    td { class: "mono", style: "color:var(--bc-text-primary); font-variant-numeric:tabular-nums",
-                                            span { class: "pill", style: "background:var(--bc-primary-light); color:var(--bc-primary)", "VIP" }
-                                    td { style: "text-align:right",
-                                            class: "btn btn-ghost",
-                                            style: "color:var(--bc-primary); font-weight:600",
```

AFTER / ADDED:
```diff
+                                th { class: "text-right", {t(*lang.read(), "users.col.actions")} }
+                                    td { span { class: "mono bc-font-11", "{u.id}" } }
+                                    td { class: "font-semibold", "{u.username}" }
+                                    td { class: "mono bc-tabular-primary",
+                                            span { class: "pill vip", "VIP" }
+                                    td { class: "text-right",
+                                            class: "btn btn-ghost bc-btn-primary-text",
+        BCModal {
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -1,6 +1,7 @@
@@ -17,39 +18,26 @@ pub fn Layout() -> Element {
@@ -61,16 +49,14 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-    // Route guard: Check if user is authenticated
-    // Only redirect on initial mount, not on every render
-    // Prevent rendering protected content if not authenticated
-            div { class: "h-screen w-screen flex items-center justify-center bg-base-100",
-                span { class: "loading loading-spinner loading-lg text-primary" }
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            div { class: "h-screen w-screen flex items-center justify-center bg-bc-canvas",
+                span { class: "bc-spinner bc-spinner--lg" }
+            AppStyles {}
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "{theme_str}",
+                div { class: "w-64 shrink-0 flex flex-col border-r border-bc-border/50 bg-bc-canvas/80 backdrop-blur-xl",
+                div { class: "flex-1 flex flex-col bg-bc-canvas relative min-w-0",
+                    main { class: "flex-1 overflow-y-auto overflow-x-hidden flex flex-col min-h-0",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/users
component=console/users
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #32 — /console/settings

**树路径：** `UI-only Actions → Console → /console/settings`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/settings
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/settings.rs
│
└─ SystemSettings re-export
│
▼
FILE: crates/client/crates/client-settings/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -9,7 +9,7 @@ use super::placeholders::{
@@ -44,14 +44,29 @@ pub enum CoreRoute {
```

BEFORE / REMOVED:
```diff
-use crate::styles::DESIGN_SYSTEM_CSS;
-            style { "{DESIGN_SYSTEM_CSS}" }
-        div { class: "app-container",
-            TitleBar {}
-            div { class: "app-body",
-                Sidebar {}
-                main { class: "main-content",
-                    Outlet::<CoreRoute> {}
```

AFTER / ADDED:
```diff
+use crate::app_styles::AppStyles;
+            AppStyles {}
+
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "light",
+
+            div { class: "absolute top-0 left-0 w-full z-50 pointer-events-none",
+                div { class: "pointer-events-auto",
+                    TitleBar {}
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -1,6 +1,7 @@
@@ -17,39 +18,26 @@ pub fn Layout() -> Element {
@@ -61,16 +49,14 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-    // Route guard: Check if user is authenticated
-    // Only redirect on initial mount, not on every render
-    // Prevent rendering protected content if not authenticated
-            div { class: "h-screen w-screen flex items-center justify-center bg-base-100",
-                span { class: "loading loading-spinner loading-lg text-primary" }
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            div { class: "h-screen w-screen flex items-center justify-center bg-bc-canvas",
+                span { class: "bc-spinner bc-spinner--lg" }
+            AppStyles {}
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "{theme_str}",
+                div { class: "w-64 shrink-0 flex flex-col border-r border-bc-border/50 bg-bc-canvas/80 backdrop-blur-xl",
+                div { class: "flex-1 flex flex-col bg-bc-canvas relative min-w-0",
+                    main { class: "flex-1 overflow-y-auto overflow-x-hidden flex flex-col min-h-0",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/settings
component=console/settings
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #33 — /console/finance

**树路径：** `UI-only Actions → Console → /console/finance`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-finance/src/lib.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/finance
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/billing.rs
│
└─ FinancePage re-export
│
▼
FILE: crates/client/crates/client-finance/src/lib.rs  [PR CHANGED]
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-finance/src/lib.rs`**

HUNKS:
```text
@@ -239,10 +239,10 @@ pub fn Finance() -> Element {
```

BEFORE / REMOVED:
```diff
-                                    td { class: "text-secondary",
-                                    td { class: "mono text-secondary",
```

AFTER / ADDED:
```diff
+                                    td { class: "text-bc-text-secondary",
+                                    td { class: "mono text-bc-text-secondary",
```

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -9,7 +9,7 @@ use super::placeholders::{
@@ -44,14 +44,29 @@ pub enum CoreRoute {
```

BEFORE / REMOVED:
```diff
-use crate::styles::DESIGN_SYSTEM_CSS;
-            style { "{DESIGN_SYSTEM_CSS}" }
-        div { class: "app-container",
-            TitleBar {}
-            div { class: "app-body",
-                Sidebar {}
-                main { class: "main-content",
-                    Outlet::<CoreRoute> {}
```

AFTER / ADDED:
```diff
+use crate::app_styles::AppStyles;
+            AppStyles {}
+
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "light",
+
+            div { class: "absolute top-0 left-0 w-full z-50 pointer-events-none",
+                div { class: "pointer-events-auto",
+                    TitleBar {}
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -1,6 +1,7 @@
@@ -17,39 +18,26 @@ pub fn Layout() -> Element {
@@ -61,16 +49,14 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-    // Route guard: Check if user is authenticated
-    // Only redirect on initial mount, not on every render
-    // Prevent rendering protected content if not authenticated
-            div { class: "h-screen w-screen flex items-center justify-center bg-base-100",
-                span { class: "loading loading-spinner loading-lg text-primary" }
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            div { class: "h-screen w-screen flex items-center justify-center bg-bc-canvas",
+                span { class: "bc-spinner bc-spinner--lg" }
+            AppStyles {}
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "{theme_str}",
+                div { class: "w-64 shrink-0 flex flex-col border-r border-bc-border/50 bg-bc-canvas/80 backdrop-blur-xl",
+                div { class: "flex-1 flex flex-col bg-bc-canvas relative min-w-0",
+                    main { class: "flex-1 overflow-y-auto overflow-x-hidden flex flex-col min-h-0",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/finance
component=console/finance
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #34 — /console/logs

**树路径：** `UI-only Actions → Console → /console/logs`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-log/src/lib.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/logs
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/logs.rs
│
└─ LogPage re-export
│
▼
FILE: crates/client/crates/client-log/src/lib.rs  [PR CHANGED]
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-log/src/lib.rs`**

HUNKS:
```text
@@ -196,14 +196,14 @@ pub fn LogPage() -> Element {
```

BEFORE / REMOVED:
```diff
-                        div { class: "stat-value text-danger",
-                        div { class: "stat-value text-warning",
```

AFTER / ADDED:
```diff
+                        div { class: "stat-value text-bc-danger",
+                        div { class: "stat-value text-bc-warning",
```

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -9,7 +9,7 @@ use super::placeholders::{
@@ -44,14 +44,29 @@ pub enum CoreRoute {
```

BEFORE / REMOVED:
```diff
-use crate::styles::DESIGN_SYSTEM_CSS;
-            style { "{DESIGN_SYSTEM_CSS}" }
-        div { class: "app-container",
-            TitleBar {}
-            div { class: "app-body",
-                Sidebar {}
-                main { class: "main-content",
-                    Outlet::<CoreRoute> {}
```

AFTER / ADDED:
```diff
+use crate::app_styles::AppStyles;
+            AppStyles {}
+
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "light",
+
+            div { class: "absolute top-0 left-0 w-full z-50 pointer-events-none",
+                div { class: "pointer-events-auto",
+                    TitleBar {}
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -1,6 +1,7 @@
@@ -17,39 +18,26 @@ pub fn Layout() -> Element {
@@ -61,16 +49,14 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-    // Route guard: Check if user is authenticated
-    // Only redirect on initial mount, not on every render
-    // Prevent rendering protected content if not authenticated
-            div { class: "h-screen w-screen flex items-center justify-center bg-base-100",
-                span { class: "loading loading-spinner loading-lg text-primary" }
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            div { class: "h-screen w-screen flex items-center justify-center bg-bc-canvas",
+                span { class: "bc-spinner bc-spinner--lg" }
+            AppStyles {}
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "{theme_str}",
+                div { class: "w-64 shrink-0 flex flex-col border-r border-bc-border/50 bg-bc-canvas/80 backdrop-blur-xl",
+                div { class: "flex-1 flex flex-col bg-bc-canvas relative min-w-0",
+                    main { class: "flex-1 overflow-y-auto overflow-x-hidden flex flex-col min-h-0",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/logs
component=console/logs
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #35 — /console/connect

**树路径：** `UI-only Actions → Console → /console/connect`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-connect/src/lib.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/connect
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/connect.rs
│
└─ ConnectPage re-export
│
▼
FILE: crates/client/crates/client-connect/src/lib.rs  [PR CHANGED]
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-connect/src/lib.rs`**

HUNKS:
```text
@@ -243,7 +243,7 @@ pub fn ConnectPage() -> Element {
@@ -296,7 +296,7 @@ pub fn ConnectPage() -> Element {
@@ -327,7 +327,7 @@ pub fn ConnectPage() -> Element {
@@ -380,7 +380,7 @@ pub fn ConnectPage() -> Element {
@@ -415,15 +415,15 @@ fn MarketplaceCard(
```

BEFORE / REMOVED:
```diff
-                                                td { class: "mono text-caption text-secondary", "{ch.base_url}" }
-                                        div { class: "mono text-caption text-secondary mt-xs", "https://pool.skynet-ops.io" }
-                                div { class: "config-label mb-md text-secondary font-bold", {t(*lang.read(), "connect.pool.available")} }
-                p { class: "text-secondary text-caption",
-                    div { class: "mono text-xxs text-tertiary bc-mt-2", "{latency} · {nodes} nodes" }
-                    div { class: "text-xxs text-tertiary", "/ 1K tok" }
-                span { class: "bc-font-11 text-secondary", "trust ", span { class: "mono font-semibold text-primary", "{trust}" } }
```

AFTER / ADDED:
```diff
+                                                td { class: "mono text-caption text-bc-text-secondary", "{ch.base_url}" }
+                                        div { class: "mono text-caption text-bc-text-secondary mt-xs", "https://pool.skynet-ops.io" }
+                                div { class: "config-label mb-md text-bc-text-secondary font-bold", {t(*lang.read(), "connect.pool.available")} }
+                p { class: "bc-detail-line m-0",
+                    div { class: "mono text-xxs text-bc-text-tertiary bc-mt-2", "{latency} · {nodes} nodes" }
+                    div { class: "text-xxs text-bc-text-tertiary", "/ 1K tok" }
+                span { class: "bc-font-11 text-bc-text-secondary", "trust ", span { class: "mono font-semibold text-bc-text", "{trust}" } }
```

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -9,7 +9,7 @@ use super::placeholders::{
@@ -44,14 +44,29 @@ pub enum CoreRoute {
```

BEFORE / REMOVED:
```diff
-use crate::styles::DESIGN_SYSTEM_CSS;
-            style { "{DESIGN_SYSTEM_CSS}" }
-        div { class: "app-container",
-            TitleBar {}
-            div { class: "app-body",
-                Sidebar {}
-                main { class: "main-content",
-                    Outlet::<CoreRoute> {}
```

AFTER / ADDED:
```diff
+use crate::app_styles::AppStyles;
+            AppStyles {}
+
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "light",
+
+            div { class: "absolute top-0 left-0 w-full z-50 pointer-events-none",
+                div { class: "pointer-events-auto",
+                    TitleBar {}
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -1,6 +1,7 @@
@@ -17,39 +18,26 @@ pub fn Layout() -> Element {
@@ -61,16 +49,14 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-    // Route guard: Check if user is authenticated
-    // Only redirect on initial mount, not on every render
-    // Prevent rendering protected content if not authenticated
-            div { class: "h-screen w-screen flex items-center justify-center bg-base-100",
-                span { class: "loading loading-spinner loading-lg text-primary" }
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            div { class: "h-screen w-screen flex items-center justify-center bg-bc-canvas",
+                span { class: "bc-spinner bc-spinner--lg" }
+            AppStyles {}
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "{theme_str}",
+                div { class: "w-64 shrink-0 flex flex-col border-r border-bc-border/50 bg-bc-canvas/80 backdrop-blur-xl",
+                div { class: "flex-1 flex flex-col bg-bc-canvas relative min-w-0",
+                    main { class: "flex-1 overflow-y-auto overflow-x-hidden flex flex-col min-h-0",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/connect
component=console/connect
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #36 — /console/playground

**树路径：** `UI-only Actions → Console → /console/playground`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-playground/src/lib.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/playground
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/playground.rs
│
└─ PlaygroundPage re-export
│
▼
FILE: crates/client/crates/client-playground/src/lib.rs  [PR CHANGED]
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-playground/src/lib.rs`**

HUNKS:
```text
@@ -321,14 +321,14 @@ pub fn Playground() -> Element {
@@ -375,7 +375,7 @@ pub fn Playground() -> Element {
@@ -396,7 +396,7 @@ pub fn Playground() -> Element {
@@ -445,7 +445,7 @@ pub fn Playground() -> Element {
@@ -522,5 +522,6 @@ pub fn Playground() -> Element {
```

BEFORE / REMOVED:
```diff
-        // Error banner
-        if let Some(err) = error_msg() {
-            div { class: "bc-error-banner-warning mb-sm",
-                "{err}"
-        }
-        div { class: "bc-playground-layout",
-                    div { class: "mono bc-body-13px text-secondary",
-                        span { class: "mono bc-body-13px text-secondary bc-temp-value", "{temperature():.1}" }
```

AFTER / ADDED:
```diff
+        div { class: "page-content flex flex-col flex-1 min-h-0 gap-sm",
+            if let Some(err) = error_msg() {
+                div { class: "bc-error-banner-warning",
+                    "{err}"
+                }
+            div { class: "bc-playground-layout flex-1 min-h-0",
+                    div { class: "mono bc-body-13px text-bc-text-secondary",
+                        span { class: "mono bc-body-13px text-bc-text-secondary bc-temp-value", "{temperature():.1}" }
```

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -9,7 +9,7 @@ use super::placeholders::{
@@ -44,14 +44,29 @@ pub enum CoreRoute {
```

BEFORE / REMOVED:
```diff
-use crate::styles::DESIGN_SYSTEM_CSS;
-            style { "{DESIGN_SYSTEM_CSS}" }
-        div { class: "app-container",
-            TitleBar {}
-            div { class: "app-body",
-                Sidebar {}
-                main { class: "main-content",
-                    Outlet::<CoreRoute> {}
```

AFTER / ADDED:
```diff
+use crate::app_styles::AppStyles;
+            AppStyles {}
+
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "light",
+
+            div { class: "absolute top-0 left-0 w-full z-50 pointer-events-none",
+                div { class: "pointer-events-auto",
+                    TitleBar {}
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -1,6 +1,7 @@
@@ -17,39 +18,26 @@ pub fn Layout() -> Element {
@@ -61,16 +49,14 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-    // Route guard: Check if user is authenticated
-    // Only redirect on initial mount, not on every render
-    // Prevent rendering protected content if not authenticated
-            div { class: "h-screen w-screen flex items-center justify-center bg-base-100",
-                span { class: "loading loading-spinner loading-lg text-primary" }
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            div { class: "h-screen w-screen flex items-center justify-center bg-bc-canvas",
+                span { class: "bc-spinner bc-spinner--lg" }
+            AppStyles {}
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "{theme_str}",
+                div { class: "w-64 shrink-0 flex flex-col border-r border-bc-border/50 bg-bc-canvas/80 backdrop-blur-xl",
+                div { class: "flex-1 flex flex-col bg-bc-canvas relative min-w-0",
+                    main { class: "flex-1 overflow-y-auto overflow-x-hidden flex flex-col min-h-0",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/playground
component=console/playground
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #37 — /console/:..segments → NotFound

**树路径：** `UI-only Actions → Console → /console/:..segments → NotFound`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/src/pages/not_found.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/:..segments → NotFound
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/not_found.rs  [PR CHANGED]
│
└─ NotFoundPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -9,7 +9,7 @@ use super::placeholders::{
@@ -44,14 +44,29 @@ pub enum CoreRoute {
```

BEFORE / REMOVED:
```diff
-use crate::styles::DESIGN_SYSTEM_CSS;
-            style { "{DESIGN_SYSTEM_CSS}" }
-        div { class: "app-container",
-            TitleBar {}
-            div { class: "app-body",
-                Sidebar {}
-                main { class: "main-content",
-                    Outlet::<CoreRoute> {}
```

AFTER / ADDED:
```diff
+use crate::app_styles::AppStyles;
+            AppStyles {}
+
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "light",
+
+            div { class: "absolute top-0 left-0 w-full z-50 pointer-events-none",
+                div { class: "pointer-events-auto",
+                    TitleBar {}
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -1,6 +1,7 @@
@@ -17,39 +18,26 @@ pub fn Layout() -> Element {
@@ -61,16 +49,14 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-    // Route guard: Check if user is authenticated
-    // Only redirect on initial mount, not on every render
-    // Prevent rendering protected content if not authenticated
-            div { class: "h-screen w-screen flex items-center justify-center bg-base-100",
-                span { class: "loading loading-spinner loading-lg text-primary" }
-            // Tailwind utilities generated by build.rs from tailwind.config.js
-            style { "{include_str!(\"../assets/tailwind.css\")}" }
-
```

AFTER / ADDED:
```diff
+use crate::components::app_styles::AppStyles;
+            div { class: "h-screen w-screen flex items-center justify-center bg-bc-canvas",
+                span { class: "bc-spinner bc-spinner--lg" }
+            AppStyles {}
+        div { class: "relative flex flex-col h-screen w-screen bg-bc-canvas text-bc-text overflow-hidden select-none", "data-theme": "{theme_str}",
+                div { class: "w-64 shrink-0 flex flex-col border-r border-bc-border/50 bg-bc-canvas/80 backdrop-blur-xl",
+                div { class: "flex-1 flex flex-col bg-bc-canvas relative min-w-0",
+                    main { class: "flex-1 overflow-y-auto overflow-x-hidden flex flex-col min-h-0",
```

**FILE: `crates/client/src/pages/not_found.rs`**

HUNKS:
```text
@@ -7,9 +7,9 @@ pub fn NotFoundPage(segments: Vec<String>) -> Element {
```

BEFORE / REMOVED:
```diff
-        div { class: "flex flex-col items-center justify-center h-full",
-            h1 { class: "text-display font-bold mb-lg text-primary", "404" }
-            p { class: "text-subtitle text-secondary mb-xxxl", "页面未找到 (Page Not Found)" }
```

AFTER / ADDED:
```diff
+        div { class: "page-content flex flex-col items-center justify-center h-full",
+            h1 { class: "text-display font-bold mb-lg text-bc-text", "404" }
+            p { class: "text-subtitle text-bc-text-secondary mb-xxxl", "页面未找到 (Page Not Found)" }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/:..segments → NotFound
component=NotFoundPage
rendered=true
```

## 未映射 Changed Files

- `crates/client/crates/client-api/src/api.rs`
- `crates/client/crates/client-deploy/src/deploy.rs`
- `crates/client/crates/client-models/src/models.rs`
- `crates/client/crates/client-monitor/src/monitor.rs`
- `crates/client/crates/client-settings/src/groups.rs`
- `crates/client/crates/client-settings/src/settings.rs`
- `crates/client/crates/client-shared/src/components/bc_button.rs`
- `crates/client/crates/client-shared/src/components/bc_card.rs`
- `crates/client/crates/client-shared/src/components/bc_input.rs`
- `crates/client/crates/client-shared/src/components/bc_modal.rs`
- `crates/client/crates/client-shared/src/components/bc_table.rs`
- `crates/client/crates/client-shared/src/components/console_sidebar.rs`
- `crates/client/crates/client-shared/src/components/mod.rs`
- `crates/client/crates/client-shared/src/components/schema_table.rs`
- `crates/client/crates/client-shared/src/components/sidebar.rs`
- `crates/client/crates/client-shared/src/lib.rs`
- `crates/client/crates/client-shared/src/styles.rs`
- `crates/client/src/components/app_styles.rs`
- `crates/client/src/components/mod.rs`
- `crates/client/src/components/sidebar.rs`
- `crates/client/tailwind.config.js`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
