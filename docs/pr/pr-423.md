---
title: "PR #423 — Run Graphs migration for Overview"
slug: /pr/pr-423
---

# PR #423 — Run Graphs migration for Overview

> **状态：** OPEN  
> **作者：** `rustburn`  
> **创建：** `2026-08-15T05:03:10Z`  
> **Base：** `main@c314bff9646f`  
> **Head：** `graphs/ui-migration-overview@5255582be496`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `3`
- **Additions / Deletions：** `+690 / -2`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `3`

**PR 说明摘要：** ## Graph run: Overview This PR is the first real execution of the `burncloud/graphs` UI migration contract against `rustburn/burncloud-ui -> burncloud/burncloud`. ### Graph nodes executed - Source audit: `rustburn/burncloud-ui/src/pages/Overview.tsx` - Target audit: real BurnCloud Dioxus Overview and backend APIs - Implementation: new truth-safe Overview surface - Scope gate: only `crates/client/src/**` - Product gate: Overview remains the top-level system summary - Truth gate: no unconditional verification/attestation claims, no mock telemetry - Visual gate: native Dioxus + BurnCloud CSS; no React/Tailwind transliteration - Build/cross-page gates: delegated to repository CI on this PR ### New Overview hierarchy ### Important truth changes - Removes fake silicon/TPM verification language. - Removes fake global success/uptime/latency metrics. - Does not convert loading/errors into zero values. - Labels Logs-backed data as operational/request metadata, not cryptographic attestation. - Billing cost is shown as observed cost only; no invented revenue, savings, or margin. - Recent HTTP success is explicitly scoped to the loaded recent request sample. This PR should remain draft until CI/build gates pass.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/client/src/critical_pages.css` | modified | 207 | 0 | 0 |
| `crates/client/src/critical_pages.rs` | modified | 2 | 2 | 0 |
| `crates/client/src/critical_pages/overview_graph.rs` | added | 481 | 0 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/client/src/critical_pages.css`
- `crates/client/src/critical_pages.rs`
- `crates/client/src/critical_pages/overview_graph.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
