---
title: "PR #542 — test(node): close fake golden path with supply resolver"
slug: /pr/pr-542
---

# PR #542 — test(node): close fake golden path with supply resolver

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-17T02:06:20Z`  
> **Base：** `main@cf990b44b865`  
> **Head：** `test/node-wire-supply-resolver@921dfcae1921`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `SOURCE / TOOLING`
- **Changed Files：** `2`
- **Additions / Deletions：** `+26 / -25`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `2`

**PR 说明摘要：** ## Phase 0.5 — Fake Skeleton Completion, final wiring Closes the last manual evidence gap in the fake golden path. Before this PR, tests directly emitted `Resolved`. Now they must first call the Supply-owned `FakeModelResolver`: `qwen-4b -> FakeModelResolver -> GGUF + llama.cpp -> Resolved -> FakeArtifactPreparer -> FakeRuntimePreparer -> FakeProcessManager -> FakeReadinessProbe -> Ready -> RouterAttached -> Routable` Key boundaries: - `ModelResolver` remains owned by Supply. - `platform/node` only consumes the resolver in dev/acceptance tests; it does not take ownership of model selection. - The resolved artifact/runtime outputs feed the next fake ports instead of hard-coded independent values. - No real model/runtime download or process/network side effect is added. After this PR, the Phase 0.5 fake chain has no direct success-evidence shortcut for Resolve, Artifact, Runtime, Process, or Readiness. Router attachment remains separately proven against the real existing ModelRouter/SQLite path.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/platform/node/Cargo.toml` | modified | 4 | 0 | 0 |
| `crates/platform/node/tests/golden_path.rs` | modified | 22 | 25 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/platform/node/Cargo.toml`
- `crates/platform/node/tests/golden_path.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
