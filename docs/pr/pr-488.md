---
title: "PR #488 — test(ui): remove retired cache admin contracts"
slug: /pr/pr-488
---

# PR #488 — test(ui): remove retired cache admin contracts

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-14T08:39:56Z`  
> **Base：** `main@32b1e6e6ec1d`  
> **Head：** `cleanup/remove-stale-cache-ui-contract-487@cf8fb50a4509`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `SOURCE / TOOLING`
- **Changed Files：** `1`
- **Additions / Deletions：** `+0 / -2`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `1`

**PR 说明摘要：** Closes #487 ## 变更 - 删除对已退役 Cache Settings UI 的 `confirm_clear` 契约检查 - 删除对已退役 Cache Settings UI 的 `MAINTENANCE` 契约检查 - 保留 Guardrails 危险操作的确认与 Danger Zone 契约 ## 验证 - `git diff --check` - `bash crates/client/scripts/check-product-ux.sh` - `bash crates/client/scripts/check-functional-wiring.sh` 这是 #484 的契约检查同步修复，也是 #486 当前 CI 阻塞项的前置修复。

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/client/scripts/check-product-ux.sh` | modified | 0 | 2 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/client/scripts/check-product-ux.sh`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
