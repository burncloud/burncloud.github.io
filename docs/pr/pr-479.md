---
title: "PR #479 — cleanup(cache): remove unused direct dependencies"
slug: /pr/pr-479
---

# PR #479 — cleanup(cache): remove unused direct dependencies

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-14T05:18:37Z`  
> **Base：** `main@29389cb37787`  
> **Head：** `cleanup/remove-unused-cache-deps-478@3e8c97a98f0d`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `SOURCE / TOOLING`
- **Changed Files：** `1`
- **Additions / Deletions：** `+0 / -3`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `1`

**PR 说明摘要：** ## Summary Closes #478. Remove three direct dependencies no longer referenced by `burncloud-service-cache`: - `burncloud-database` - `serde_json` - `chrono` ## Boundary Changed only: - `crates/service/crates/cache/Cargo.toml` No source, `Cargo.lock`, workspace dependency, version, feature, API, runtime behavior, or directory change. The dependencies still used by Cache remain unchanged: - `thiserror` - `tokio` - `serde` - `tracing` - `redis` ## Verification Completed: - source-level usage review after PR #477; - `git diff --check main...HEAD`; - exact diff review: three lines deleted. Required before merge in a Rust-enabled environment: The current execution environment does not contain a Rust toolchain. Dead error-variant cleanup is intentionally excluded.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/service/crates/cache/Cargo.toml` | modified | 0 | 3 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/service/crates/cache/Cargo.toml`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
