---
title: "PR #414 — Polish Billing spend hierarchy"
slug: /pr/pr-414
---

# PR #414 — Polish Billing spend hierarchy

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-08-14T02:56:24Z`  
> **Base：** `main@ec04b233e0e5`  
> **Head：** `agent/phase-2-billing-polish@d32fa655e87d`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `1`
- **Additions / Deletions：** `+92 / -18`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `1`

**PR 说明摘要：** ## Goal Complete the Phase 2 Observe pass by making Billing answer the primary financial question first: how much was spent in the loaded billing period, then what usage and model concentration drove that spend. ## What changed ### Spend is now primary - promote total billing-period Spend into a dedicated top financial conclusion instead of treating it as one of four equal KPIs - keep the period attached to that conclusion - surface the top spend-driving model and its share as supporting context ### Supporting KPIs After Spend, show neutral factual KPIs for: - Requests - Tokens - Models Used - Top Model Share Financial amount/concentration is not treated as warning/error state merely because it is large. ### Truthful loading - stop showing a fake `Current billing period` badge while billing data is still loading - disable Refresh while loading - use a dedicated billing loading state before financial conclusions render ### Precision - show normal dollar totals with two decimals - preserve six-decimal precision for sub-dollar values so small API spend does not collapse to `$0.00` - use the same adaptive currency formatting in the model table ### Attribution clarity - distinguish a genuinely empty billing period from activity that exists without a current model-level breakdown - explain when pre-migration requests are included in total request count without model attribution - mak…

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/client/src/functional_pages/analytics.rs` | modified | 92 | 18 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/client/src/functional_pages/analytics.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
