---
title: "PR #346 — test(router): add comprehensive E2E tests for Router functionality (Issue #332)"
slug: /pr/pr-346
---

# PR #346 — test(router): add comprehensive E2E tests for Router functionality (Issue #332)

> **状态：** OPEN  
> **作者：** `rustburn`  
> **创建：** `2026-06-10T19:19:15Z`  
> **Base：** `main@66985337b455`  
> **Head：** `issue-332@96accaf9c9f0`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `TEST ONLY`
- **Changed Files：** `2`
- **Additions / Deletions：** `+535 / -0`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `2`

**PR 说明摘要：** ## Summary - Add router_flow.rs with 15 E2E test cases for Router core functionality - Cover non-streaming requests: chat completions, embeddings, models API - Cover streaming requests: SSE format validation, [DONE] marker - Cover OpenAI format: /v1/chat/completions, /v1/embeddings, /v1/models - Cover Anthropic format: /v1/messages (streaming and non-streaming) - Cover authentication: invalid token, missing token - Cover error scenarios: model not found, invalid request, empty messages - Cover JSON error format validation - Cover health/status endpoints ## Test Cases Added ### Non-streaming Request Tests - test_openai_chat_completion_non_streaming - test_embeddings_api - test_models_list ### Streaming Request Tests - test_openai_chat_completion_streaming ### Anthropic Format Tests - test_anthropic_messages_non_streaming - test_anthropic_messages_streaming ### Authentication Tests - test_auth_invalid_token - test_auth_missing_token ### Error Scenario Tests - test_model_not_found_error - test_invalid_request_body - test_empty_messages_error ### JSON Error Format Tests - test_json_error_format ### Health Check Tests - test_health_check - test_status_endpoint ## Technical Implementation - All tests use #[ignore] for external infrastructure requirement - Tests use reqwest for HTTP client (consistent with e2e_smoke.rs) - Follow project E2E test conventions and clippy allowances - Use…

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/tests/tests/e2e/mod.rs` | modified | 1 | 0 | 0 |
| `crates/tests/tests/e2e/router_flow.rs` | added | 534 | 0 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/tests/tests/e2e/mod.rs`
- `crates/tests/tests/e2e/router_flow.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
