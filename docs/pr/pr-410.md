---
title: "PR #410 — Polish Playground smoke test workflow"
slug: /pr/pr-410
---

# PR #410 — Polish Playground smoke test workflow

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-08-14T02:30:17Z`  
> **Base：** `main@281884cbae74`  
> **Head：** `agent/phase-2-playground-workflow-polish@4d52132a1bf7`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `2`
- **Additions / Deletions：** `+459 / -174`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `2`

**PR 说明摘要：** ## Goal Apply the Phase 2 page-by-page polish pass to Playground after Providers (#407), Models (#408), and Routes (#409). Playground should behave like an operational smoke-test workbench: verify prerequisites, run a real routed request, surface success/failure clearly, and point the operator to request evidence. ## What changed ### Truthful readiness loading - distinguish prerequisite loading from genuine missing setup - stop initial provider/key fetches from flashing false "connect a provider" states - when provider or API-key readiness data fails to load, show an explicit verification error instead of guessing that the path is not configured - disable readiness refresh while a readiness check or request is running ### Smoke-test lifecycle The top conclusion now distinguishes: - prerequisites missing - test in progress - last test failed - end-to-end test passed - ready to run the first test A successful response is promoted from hidden conversation output to an explicit `End-to-end test passed` conclusion. ### Safer failed-request behavior - preserve the conversation as it existed before a failed request - restore the failed prompt into the composer for correction/retry - prevent a failed user-only message from silently becoming part of the next request context - reset route trace when a new test begins so stale trace data is not presented as current evidence ### Request co…

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/client/src/functional_pages/playground_live.rs` | modified | 295 | 172 | 0 |
| `crates/client/src/product_ui.css` | modified | 164 | 2 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/client/src/functional_pages/playground_live.rs`
- `crates/client/src/product_ui.css`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
