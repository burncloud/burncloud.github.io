---
title: "PR #429 — docs(agent): establish engineering issue standard"
slug: /pr/pr-429
---

# PR #429 — docs(agent): establish engineering issue standard

> **状态：** OPEN  
> **作者：** `rustburn`  
> **创建：** `2026-09-01T01:51:47Z`  
> **Base：** `main@c314bff9646f`  
> **Head：** `docs/node-issue-standard@bf5d985ce2b1`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `DOCS / CI ONLY`
- **Changed Files：** `4`
- **Additions / Deletions：** `+409 / -3`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `4`

**PR 说明摘要：** ## 目标 建立 BurnCloud 的工程 Issue 标准，使 Issue 成为进入 Task Contract / Harness 执行链之前的明确边界合同。 ## 变更 - 新增 `docs/agent/ISSUE_STANDARD.md` - 新增 `.github/ISSUE_TEMPLATE/engineering_task.yml` - 禁用空白 Issue，要求通过结构化 Issue Form 创建工程任务 - 更新 Agent Docs 执行链：Implementation Plan / User Task → Engineering Issue → Task Contract → Implementation → PR ## 核心规则 - 一个 Issue 只交付一个主要可验证能力 - 当前事实必须有证据或标记 UNKNOWN - 必须写 Allowed / Avoid 范围 - 必须判断 Invariants 与影响面 - 必须定义 Verification 与 Done When - 架构/Invariant 修改不能隐藏在普通功能 Issue 中 - 所有实现通过分支 + Pull Request 进入 main ## BurnCloud Node Issue 标准额外锁定：复用现有 Server / Router / Database / Models / Download / Monitor，不创建第二套 NodeRouter / NodeDownloader / NodeDatabase；本地模型通过现有 Channel / Ability 接入 Router。

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `.github/ISSUE_TEMPLATE/config.yml` | added | 1 | 0 | 0 |
| `.github/ISSUE_TEMPLATE/engineering_task.yml` | added | 167 | 0 | 0 |
| `docs/agent/ISSUE_STANDARD.md` | added | 232 | 0 | 0 |
| `docs/agent/README.md` | modified | 9 | 3 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `.github/ISSUE_TEMPLATE/config.yml`
- `.github/ISSUE_TEMPLATE/engineering_task.yml`
- `docs/agent/ISSUE_STANDARD.md`
- `docs/agent/README.md`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
