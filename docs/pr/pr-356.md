---
title: "PR #356 — Cursor/cloud agent 1783489011499 bzl1g"
slug: /pr/pr-356
---

# PR #356 — Cursor/cloud agent 1783489011499 bzl1g

> **状态：** CLOSED / NOT MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-07-08T06:00:25Z`  
> **Base：** `main@c7107382b847`  
> **Head：** `cursor/cloud-agent-1783489011499-bzl1g@4538793d4936`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `123`
- **Additions / Deletions：** `+9078 / -3017`
- **受影响的完整 E2E Flow：** `59`
- **未映射 changed files：** `108`

**PR 说明摘要：** PR 未提供正文说明。

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `.github/workflows/client-ui.yml` | added | 23 | 0 | 0 |
| `Cargo.toml` | modified | 1 | 0 | 0 |
| `crates/client/Cargo.toml` | modified | 2 | 0 | 0 |
| `crates/client/crates/client-access/src/lib.rs` | modified | 17 | 16 | 1 |
| `crates/client/crates/client-api/docs/06-css-design-system.md` | modified | 6 | 0 | 0 |
| `crates/client/crates/client-connect/src/lib.rs` | modified | 36 | 34 | 1 |
| `crates/client/crates/client-dashboard/src/dashboard.rs` | modified | 43 | 138 | 0 |
| `crates/client/crates/client-deploy/src/deploy.rs` | modified | 6 | 3 | 0 |
| `crates/client/crates/client-finance/src/lib.rs` | modified | 3 | 3 | 1 |
| `crates/client/crates/client-log/src/lib.rs` | modified | 4 | 4 | 1 |
| `crates/client/crates/client-models/src/models.rs` | modified | 20 | 9 | 0 |
| `crates/client/crates/client-monitor/src/monitor.rs` | modified | 31 | 24 | 0 |
| `crates/client/crates/client-playground/src/lib.rs` | modified | 28 | 26 | 1 |
| `crates/client/crates/client-settings/src/groups.rs` | modified | 36 | 26 | 0 |
| `crates/client/crates/client-settings/src/settings.rs` | modified | 26 | 43 | 0 |
| `crates/client/crates/client-settings/src/tokens.rs` | modified | 5 | 5 | 0 |
| `crates/client/crates/client-shared/Cargo.toml` | modified | 2 | 0 | 0 |
| `crates/client/crates/client-shared/scripts/split_styles.py` | added | 30 | 0 | 0 |
| `crates/client/crates/client-shared/src/components/bc_button.rs` | modified | 4 | 0 | 0 |
| `crates/client/crates/client-shared/src/components/bc_card.rs` | modified | 2 | 2 | 0 |
| `crates/client/crates/client-shared/src/components/bc_magic_button.rs` | modified | 1 | 1 | 0 |
| `crates/client/crates/client-shared/src/components/bc_modal.rs` | modified | 9 | 6 | 0 |
| `crates/client/crates/client-shared/src/components/bc_table.rs` | modified | 8 | 6 | 0 |
| `crates/client/crates/client-shared/src/components/console_sidebar.rs` | modified | 9 | 9 | 0 |
| `crates/client/crates/client-shared/src/components/glass_card.rs` | modified | 1 | 1 | 0 |
| `crates/client/crates/client-shared/src/components/layout.rs` | modified | 9 | 2 | 12 |
| `crates/client/crates/client-shared/src/components/page_header.rs` | modified | 2 | 2 | 0 |
| `crates/client/crates/client-shared/src/components/schema_form.rs` | modified | 8 | 7 | 0 |
| `crates/client/crates/client-shared/src/components/schema_table.rs` | modified | 12 | 11 | 0 |
| `crates/client/crates/client-shared/src/components/standard_crud_page.rs` | modified | 7 | 7 | 0 |
| `crates/client/crates/client-shared/src/components/title_bar.rs` | modified | 1 | 1 | 0 |
| `crates/client/crates/client-shared/src/components/toast.rs` | modified | 2 | 2 | 0 |
| `crates/client/crates/client-shared/src/e2e_mock/fixtures.rs` | added | 198 | 0 | 0 |
| `crates/client/crates/client-shared/src/e2e_mock/mod.rs` | added | 219 | 0 | 0 |
| `crates/client/crates/client-shared/src/e2e_mock/shell.rs` | added | 37 | 0 | 0 |
| `crates/client/crates/client-shared/src/i18n.rs` | modified | 49 | 0 | 53 |
| `crates/client/crates/client-shared/src/lib.rs` | modified | 3 | 0 | 0 |
| `crates/client/crates/client-shared/src/services/billing_service.rs` | modified | 4 | 0 | 0 |
| `crates/client/crates/client-shared/src/services/channel_service.rs` | modified | 4 | 0 | 0 |
| `crates/client/crates/client-shared/src/services/log_service.rs` | modified | 4 | 0 | 0 |
| `crates/client/crates/client-shared/src/services/monitor_service.rs` | modified | 13 | 0 | 0 |
| `crates/client/crates/client-shared/src/services/token_service.rs` | modified | 3 | 0 | 0 |
| `crates/client/crates/client-shared/src/services/usage_service.rs` | modified | 8 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles.rs` | removed | 0 | 2355 | 0 |
| `crates/client/crates/client-shared/src/styles/00_burncloud_design_system_apple_inspired.css` | added | 189 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/01_base_element_styles.css` | added | 19 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/02_acrylic_glass_effects.css` | added | 18 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/03_card_styles.css` | added | 44 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/04_button_styles.css` | added | 117 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/05_input_styles.css` | added | 134 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/06_progress_bar.css` | added | 19 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/07_status_indicators.css` | added | 26 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/08_animations.css` | added | 97 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/09_layout_helpers_burncloud_specific_not_tailwind_d.css` | added | 56 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/10_typography.css` | added | 29 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/11_app_layout.css` | added | 28 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/12_navigation.css` | added | 40 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/13_model_cards_metrics.css` | added | 59 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/14_log_viewer.css` | added | 23 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/15_macos_style_scrollbars_hidden_by_default_show_on.css` | added | 77 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/16_macos_window_control_colors.css` | added | 109 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/17_scrollbar_theme.css` | added | 143 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/18_login_register_page_50_50_split_screen.css` | added | 313 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/19_component_utility_classes_migrated_from_inline_s.css` | added | 65 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/20_landing_marketing_page.css` | added | 161 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/21_landing_page_semantic_component_classes_home_rs.css` | added | 189 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/22_legacy_daisyui_class_aliases_migrate_to_bc_over.css` | added | 117 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/23_page_level_helpers_from_design_kit_styles_css.css` | added | 270 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/24_semantic_component_classes_register_users_api_mi.css` | added | 167 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/25_token_compliant_utility_classes_issue_179_migrat.css` | added | 109 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/README.md` | added | 15 | 0 | 0 |
| `crates/client/crates/client-shared/src/styles/mod.rs` | added | 34 | 0 | 0 |
| `crates/client/crates/client-shared/src/theme_context.rs` | added | 41 | 0 | 0 |
| `crates/client/crates/client-users/src/lib.rs` | modified | 14 | 13 | 1 |
| `crates/client/scripts/check-ui-conventions.ps1` | added | 57 | 0 | 0 |
| `crates/client/scripts/check-ui-conventions.sh` | added | 57 | 0 | 0 |
| `crates/client/src/app.rs` | modified | 35 | 0 | 55 |
| `crates/client/src/components/layout.rs` | modified | 4 | 6 | 12 |
| `crates/client/src/lib.rs` | modified | 11 | 1 | 19 |
| `crates/client/src/pages/e2e_preview.rs` | added | 139 | 0 | 9 |
| `crates/client/src/pages/login.rs` | modified | 12 | 16 | 1 |
| `crates/client/src/pages/mod.rs` | modified | 1 | 0 | 0 |
| `crates/client/src/pages/not_found.rs` | modified | 2 | 2 | 1 |
| `crates/client/tailwind.config.js` | modified | 1 | 0 | 0 |
| `crates/loops/Cargo.toml` | added | 24 | 0 | 0 |
| `crates/loops/README.md` | added | 54 | 0 | 0 |
| `crates/loops/acceptance/aesthetic-optimize-acceptance.md` | added | 300 | 0 | 0 |
| `crates/loops/acceptance/aesthetic-optimize-agent-prompt.md` | added | 69 | 0 | 0 |
| `crates/loops/acceptance/css-optimize-acceptance.md` | added | 163 | 0 | 0 |
| `crates/loops/acceptance/css-optimize-agent-prompt.md` | added | 48 | 0 | 0 |
| `crates/loops/acceptance/jobs-aesthetic-agent-prompt.md` | added | 74 | 0 | 0 |
| `crates/loops/src/cli.rs` | added | 139 | 0 | 0 |
| `crates/loops/src/gates/aesthetic_review.rs` | added | 169 | 0 | 0 |
| `crates/loops/src/gates/cargo_test.rs` | added | 245 | 0 | 0 |
| `crates/loops/src/gates/css_naming.rs` | added | 224 | 0 | 0 |
| `crates/loops/src/gates/mod.rs` | added | 247 | 0 | 0 |
| `crates/loops/src/gates/ui_conventions.rs` | added | 76 | 0 | 0 |
| `crates/loops/src/lib.rs` | added | 13 | 0 | 0 |
| `crates/loops/src/lock.rs` | added | 57 | 0 | 0 |
| `crates/loops/src/log.rs` | added | 91 | 0 | 0 |
| `crates/loops/src/loops/css_optimize.rs` | added | 134 | 0 | 0 |
| `crates/loops/src/loops/jobs_aesthetic.rs` | added | 324 | 0 | 0 |
| `crates/loops/src/loops/mod.rs` | added | 2 | 0 | 0 |
| `crates/loops/src/main.rs` | added | 8 | 0 | 1 |
| `crates/loops/src/page_progress.rs` | added | 145 | 0 | 0 |
| `crates/loops/src/paths.rs` | added | 54 | 0 | 0 |
| `crates/loops/src/process.rs` | added | 39 | 0 | 0 |
| `crates/loops/src/prompt/jobs_aesthetic.rs` | added | 545 | 0 | 0 |
| `crates/loops/src/prompt/mod.rs` | added | 1 | 0 | 0 |
| `crates/loops/src/server.rs` | added | 89 | 0 | 0 |
| `crates/loops/src/state.rs` | added | 57 | 0 | 0 |
| `crates/tests/tests/common/mod.rs` | modified | 23 | 5 | 0 |
| `crates/tests/tests/e2e/aesthetic_acceptance.rs` | added | 442 | 0 | 0 |
| `crates/tests/tests/e2e/agent_browser.rs` | modified | 121 | 25 | 0 |
| `crates/tests/tests/e2e/css_visual_acceptance.rs` | added | 227 | 0 | 0 |
| `crates/tests/tests/e2e/mod.rs` | modified | 234 | 179 | 0 |
| `docs/CLAUDE.md` | modified | 23 | 0 | 0 |
| `docs/ui/README.md` | modified | 24 | 7 | 0 |
| `docs/ui/components.md` | added | 177 | 0 | 0 |
| `docs/ui/naming.md` | added | 216 | 0 | 0 |
| `docs/ui/pages.md` | added | 163 | 0 | 0 |
| `docs/ui/system.md` | modified | 24 | 15 | 0 |
| `docs/ui/tokens.md` | modified | 36 | 5 | 0 |

## Affected E2E Impact Matrix

| # | 完整 E2E / Entry Point | 类型 | PR 命中的源码文件 |
|---:|---|---|---|
| 1 | `GET /` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 2 | `GET /home` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 3 | `GET /login` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 4 | `GET /register` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 5 | `GET /forgot-password` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 6 | `GET /reset-password` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 7 | `GET /console` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 8 | `GET /console/` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 9 | `GET /console/{*path}` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 10 | `GET /favicon.ico` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 11 | `GET /preview/home` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 12 | `GET /preview/login` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 13 | `GET /preview/console` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 14 | `GET /preview/console/` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 15 | `GET /preview/console/{*path}` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 16 | `GET /ws` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/lib.rs`&lt;br/&gt;`crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 17 | `burncloud` | CLI / Executables / burncloud | `crates/client/src/app.rs` |
| 18 | `server` | CLI / Executables / burncloud | `crates/client/src/lib.rs` |
| 19 | `router` | CLI / Executables / burncloud | `crates/client/src/lib.rs` |
| 20 | `client` | CLI / Executables / burncloud | `crates/client/src/app.rs` |
| 21 | `burncloud-client` | CLI / Executables / Workspace Binaries | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 22 | `burncloud-loop` | CLI / Executables / Workspace Binaries | `crates/loops/src/main.rs` |
| 23 | `Windows tray thread` | Background Jobs / Async Side Effects / Desktop Background Work | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 24 | `Show-window poll loop` | Background Jobs / Async Side Effects / Desktop Background Work | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 25 | `create_app` | Startup / Startup Chain | `crates/client/src/lib.rs` |
| 26 | `/` | UI-only Actions / Guest / Public | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 27 | `/home` | UI-only Actions / Guest / Public | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 28 | `/login` | UI-only Actions / Guest / Public | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/pages/login.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 29 | `/register` | UI-only Actions / Guest / Public | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 30 | `/forgot-password` | UI-only Actions / Guest / Public | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 31 | `/reset-password?:token` | UI-only Actions / Guest / Public | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 32 | `/console/dashboard` | UI-only Actions / Console | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 33 | `/console/deploy` | UI-only Actions / Console | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 34 | `/console/monitor` | UI-only Actions / Console | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 35 | `/console/access` | UI-only Actions / Console | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-access/src/lib.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 36 | `/console/models` | UI-only Actions / Console | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 37 | `/console/users` | UI-only Actions / Console | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-users/src/lib.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 38 | `/console/settings` | UI-only Actions / Console | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 39 | `/console/finance` | UI-only Actions / Console | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-finance/src/lib.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 40 | `/console/logs` | UI-only Actions / Console | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-log/src/lib.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 41 | `/console/connect` | UI-only Actions / Console | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-connect/src/lib.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 42 | `/console/playground` | UI-only Actions / Console | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-playground/src/lib.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 43 | `/console/:..segments → NotFound` | UI-only Actions / Console | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/components/layout.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/components/layout.rs`&lt;br/&gt;`crates/client/src/pages/not_found.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 44 | `/preview/home` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/pages/e2e_preview.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 45 | `/preview/login` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/pages/e2e_preview.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 46 | `/preview/console/dashboard` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/pages/e2e_preview.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 47 | `/preview/console/models` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/pages/e2e_preview.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 48 | `/preview/console/access` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/pages/e2e_preview.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 49 | `/preview/console/settings` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/pages/e2e_preview.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 50 | `/preview/console/finance` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/pages/e2e_preview.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 51 | `/preview/console/monitor` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/pages/e2e_preview.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 52 | `/preview/console/playground` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/src/pages/e2e_preview.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 53 | `i18n context` | UI-only Actions / Local UI State | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 54 | `Toast state / ToastContainer` | UI-only Actions / Local UI State | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 55 | `Auth context` | UI-only Actions / Local UI State | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 56 | `Theme state` | UI-only Actions / Local UI State | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 57 | `window maximize` | UI-only Actions / Desktop UI | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 58 | `Windows tray startup` | UI-only Actions / Desktop UI | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |
| 59 | `show / hide / focus` | UI-only Actions / Desktop UI | `crates/client/src/app.rs`&lt;br/&gt;`crates/client/crates/client-shared/src/i18n.rs` |

## 完整受影响 E2E Request Flow

### E2E FLOW #1 — GET /

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #2 — GET /home

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /home
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #3 — GET /login

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /login
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #4 — GET /register

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /register`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /register
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #5 — GET /forgot-password

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /forgot-password`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /forgot-password
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #6 — GET /reset-password

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /reset-password`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /reset-password
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #7 — GET /console

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /console`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /console
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #8 — GET /console/

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /console/`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /console/
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #9 — GET /console/&#123;*path&#125;

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /console/{*path}`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /console/{*path}
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #10 — GET /favicon.ico

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /favicon.ico`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /favicon.ico
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: image/x-icon
Content-Length: 5430

<binary favicon bytes>
```

### E2E FLOW #11 — GET /preview/home

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/home
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #12 — GET /preview/login

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/login
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #13 — GET /preview/console

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/console`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/console
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #14 — GET /preview/console/

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/console/`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/console/
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #15 — GET /preview/console/&#123;*path&#125;

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/console/{*path}`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/console/{*path}
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #16 — GET /ws

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /ws`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`, `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /ws
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView router
│         └─ NO → fallback routing
│
├─ Feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → /ws not served by LiveView path
│         └─ YES → websocket route available
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ WebSocket upgrade
│    ├─ validate Upgrade/Connection/Sec-WebSocket-* headers
│    └─ DECISION: WebSocket handshake valid?
│         ├─ NO → HTTP handshake error → END
│         └─ YES → 101 Switching Protocols
│
├─ LiveView session
│    ├─ create/attach Dioxus LiveView session
│    ├─ receive browser UI events
│    ├─ update Virtual DOM/server-side UI state
│    └─ send render patches/messages to browser
│
├─ Connection loop
│    └─ DECISION: socket still connected?
│         ├─ YES → await next UI message → repeat
│         └─ NO → cleanup session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Accept: <computed-value>

# 随后进入双向 WebSocket / LiveView 消息通道
```

### E2E FLOW #17 — burncloud

**树路径：** `CLI / Executables → burncloud → burncloud`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Shell / OS: burncloud（无参数）
│
▼
FILE: src/main.rs
│
├─ main()
│    ├─ dotenvy::dotenv()
│    ├─ ensure_master_key()
│    ├─ init_logging()
│    └─ match args == [binary]
│
├─ DECISION: target_os == windows?
│    ├─ YES
│    │    ├─ std::thread::spawn(background server thread)
│    │    │    ├─ tokio::runtime::Runtime::new()
│    │    │    ├─ HOST / PORT
│    │    │    └─ burncloud_server::start_server(host, port, false)
│    │    │         └─ enable_liveview=false：后台 Server 不挂 LiveView Router
│    │    └─ main thread → burncloud_client::launch_gui_with_tray()
│    │
│    └─ NO（Linux/macOS 等）
│         ├─ print Headless Mode startup line
│         └─ run_async_server()
│              └─ burncloud_server::start_server(host, port, true)
│
├─ Windows server thread 后续主链与 `burncloud server` 的 start_server/create_app/create_router_app 相同
├─ Non-Windows 后续主链与 `burncloud server` 完全相同
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Windows GUI branch: launch_gui_with_tray()
│    ├─ WindowBuilder
│    ├─ Config::new().with_window(...).with_data_directory(...)
│    └─ LaunchBuilder::desktop().launch(AppWithTray)
│
├─ AppWithTray()
│    ├─ DesktopMode context
│    ├─ maximize window
│    ├─ std::thread::spawn(start_tray)
│    ├─ spawn async show-window poll loop
│    └─ render App()
│
└─ App()
     ├─ use_init_i18n()
     ├─ use_init_toast()
     ├─ use_init_auth()
     ├─ use_init_theme()
     └─ Router<Route>
│
▼
END / LONG-RUNNING SERVER + UI LOOPS
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
# 非 Windows
Starting BurnCloud Server with LiveView (Headless Mode)...
Unified Gateway listening on 127.0.0.1:3000
- Dashboard: http://127.0.0.1:3000/
- LLM API:   http://127.0.0.1:3000/v1/...

# Windows
background_server_thread=running (enable_liveview=false)
desktop_gui_event_loop=running
system_tray_thread=running
```

### E2E FLOW #18 — server

**树路径：** `CLI / Executables → burncloud → server`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ Shell / OS 入口
│    └─ burncloud server
│    └─ 显式 Server 模式；当前实现同时 enable_liveview=true
│
▼
FILE: src/main.rs
│
├─ main()
│    ├─ dotenvy::dotenv().ok()
│    ├─ ensure_master_key()
│    │    ├─ is_valid_master_key()
│    │    │    ├─ env::var("MASTER_KEY")
│    │    │    ├─ hex::decode(...)
│    │    │    └─ DECISION: decoded key length == 32 bytes?
│    │    │         ├─ YES → keep existing key
│    │    │         └─ NO  → generate replacement
│    │    ├─ rand::random::<[u8;32]>()
│    │    ├─ hex::encode()
│    │    ├─ resolve .env path from CWD / current_exe()
│    │    ├─ read_to_string() when file exists
│    │    ├─ fs::write(.env)
│    │    └─ env::set_var("MASTER_KEY", ...)
│    ├─ env::args().collect()
│    ├─ is_server = args[1] in [server, router, client]
│    └─ burncloud_server::logging::init_logging()
│
▼
FILE: crates/server/src/logging.rs
│
├─ init_logging()
│    ├─ LOG_DIR / LOG_MAX_FILES / RUST_LOG
│    ├─ fs::create_dir_all(log_dir)
│    ├─ tracing_log::LogTracer::init()
│    ├─ file_appender(server/service/database/router)
│    │    └─ RollingFileAppender::builder() → non_blocking(...)
│    ├─ module_filter(...)
│    └─ tracing::subscriber::set_global_default(...)
│
▼
FILE: src/main.rs
│
├─ match args.as_slice()
│    └─ DECISION: subcommand == "server" OR "router"?
│         ├─ NO  → other direct/CLI branch
│         └─ YES → run_async_server()
│
├─ run_async_server() #[tokio::main]
│    ├─ HOST env or "127.0.0.1"
│    ├─ PORT env or DEFAULT_PORT
│    └─ burncloud_server::start_server(host, port, true)
│
├─ IMPORTANT
│    ├─ 当前源码中 `server` 与 `router` 都走同一个 run_async_server()
│    └─ 此 direct branch 不进入 Clap CLI dispatch
│
▼
FILE: crates/server/src/lib.rs
│
├─ start_server(host, port, enable_liveview=true)
│    ├─ create_default_database().await
│    ├─ RouterDatabase::init(&db).await
│    ├─ UserDatabase::init(&db).await
│    ├─ create_app(db, true).await
│    ├─ SocketAddr::parse()
│    ├─ TcpListener::bind(addr).await
│    └─ axum::serve(listener, app).await
│
▼
FILE: crates/database/src/database.rs
│
├─ create_default_database() → Database::new()
├─ Database::new()
│    ├─ DECISION: BURNCLOUD_DATABASE_URL exists?
│    │    ├─ YES → use configured URL
│    │    └─ NO
│    │         ├─ get_default_database_path()
│    │         ├─ create_directory_if_not_exists()
│    │         ├─ DECISION: BURNCLOUD_FRESH_DB == "1" and DB exists?
│    │         │    ├─ YES → fs::remove_file(default DB)
│    │         │    └─ NO  → preserve DB
│    │         └─ build sqlite://...?...mode=rwc
│    └─ Database::initialize().await
│
├─ Database::initialize()
│    ├─ sqlx::any::install_default_drivers()
│    ├─ DatabaseConnection::new(database_url)
│    │    ├─ AnyConnectOptions::from_str()
│    │    └─ AnyPoolOptions::new().max_connections(10).connect_with(...)
│    ├─ DECISION: db.kind() == sqlite?
│    │    └─ YES → PRAGMA journal_mode=WAL
│    ├─ MigrationRunner::run(self)
│    └─ Schema::init(self)
│
▼
FILE: crates/database/src/migration/mod.rs
│
├─ MigrationRunner::run()
│    ├─ ensure _schema_migrations table
│    ├─ choose SQLite / PostgreSQL migration catalogue
│    ├─ inspect already-applied versions
│    └─ execute pending versioned DDL migrations
│
▼
FILE: crates/database/src/schema/mod.rs
│
├─ Schema::init()
│    ├─ rename::migrate_table_renames()
│    ├─ router::migrate_router_logs()
│    ├─ price::migrate_prices()
│    └─ user::migrate_users_and_seed()
│
▼
FILE: crates/server/src/lib.rs
│
└─ start_server() continues → RouterDatabase::init(&db)
│
▼
FILE: crates/database/crates/router/src/lib.rs
│
├─ RouterDatabase::init()
│    ├─ db.get_connection()
│    ├─ db.kind()
│    ├─ CREATE TABLE IF NOT EXISTS router_tokens
│    └─ SQLite-only compatibility ALTER TABLE statements
│
▼
FILE: crates/server/src/lib.rs
│
└─ start_server() continues → UserDatabase::init(&db)
│
▼
FILE: crates/database/crates/user/src/lib.rs
│
├─ UserDatabase::init()
│    ├─ CREATE / verify user_roles
│    ├─ CREATE / verify user_role_bindings
│    ├─ CREATE / verify user_recharges
│    ├─ SQLite/PostgreSQL compatibility migrations
│    ├─ SELECT COUNT(*) FROM user_roles
│    ├─ DECISION: no roles exist?
│    │    └─ YES → seed admin/user roles
│    ├─ query users without role bindings
│    └─ assign_role(...) for orphan users when required
│
▼
FILE: crates/server/src/lib.rs
│
├─ create_app(db, enable_liveview=true)
│    ├─ SystemMonitorService::new()
│    ├─ monitor.start_auto_update().await
│    ├─ CacheService::new().await
│    ├─ cache.is_available().await
│    ├─ create_router_app(db.clone()).await
│    ├─ UserService::new()
│    ├─ api::routes(state.clone())
│    ├─ build /health + merge Management/Internal routers
│    ├─ DECISION: enable_liveview?
│    │    └─ YES → burncloud_client::liveview_router(db.clone()) → merge
│    ├─ fallback_service(router_app)
│    └─ SetRequestId / PropagateRequestId / Trace / CORS layers
│
▼
FILE: crates/service/crates/monitor/src/service.rs
│
├─ SystemMonitorService::new()
├─ start_auto_update()
│    └─ tokio::spawn
│         └─ LOOP interval(1s)
│              └─ collect_metrics_internal()
│                   ├─ CpuCollector::collect()
│                   ├─ MemoryCollector::collect()
│                   └─ DiskCollector::collect_all()
│
▼
FILE: crates/service/crates/cache/src/service.rs
│
├─ CacheService::new() → with_config(CacheConfig::default())
├─ DECISION: CACHE_ENABLED?
│    ├─ NO  → return disabled cache
│    └─ YES
│         ├─ require REDIS_URL
│         ├─ redis::Client::open()
│         ├─ get_connection_manager().await
│         └─ PING
│
▼
FILE: crates/router/src/lib.rs
│
├─ create_router_app(db)
│    ├─ reqwest::Client::builder() + timeouts/pool settings
│    ├─ RoundRobinBalancer::new()
│    ├─ RateLimiter::new(100, 10)
│    ├─ CircuitBreaker::new(...)
│    ├─ ModelRouter::new(db.clone())
│    ├─ ChannelStateTracker::new()
│    ├─ DynamicAdaptorFactory::new(db.clone())
│    ├─ ApiVersionDetector::new(db.clone())
│    ├─ PriceCache::load(&db).await
│    │    └─ DECISION: load fails? → PriceCache::empty()
│    ├─ CostCalculator::new(price_cache.clone())
│    ├─ ExchangeRateService::new(db.clone())
│    ├─ load_rates_from_db().await
│    ├─ tokio::spawn → ExchangeRateService::start_sync_task()
│    ├─ scheduler::load_scheduler_config()
│    ├─ AffinityCache::default()
│    ├─ InMemoryBudget::new()
│    ├─ configure_rate_budget_from_db(...).await
│    ├─ read BILLING_STRICT_MODE
│    ├─ read REQUEST_LOG_STORAGE_POLICY
│    ├─ tokio::spawn → AIMD budget-update consumer
│    ├─ price_sync::start_price_sync_task(...)
│    ├─ tokio::spawn → RouterLog writer → RouterDatabase::insert_log()
│    ├─ tokio::spawn → RequestLog writer → RouterDatabase::insert_request_log()
│    ├─ ChannelHealthManager::new()
│    ├─ build internal_app routes
│    │    ├─ /console/internal/health
│    │    ├─ /console/internal/prices/sync
│    │    ├─ /console/internal/circuit-breaker/trip-all
│    │    └─ /console/internal/metrics
│    └─ build data-plane app
│         ├─ GET /v1/models → models_handler [REGISTER ONLY]
│         ├─ GET /api/v1/usage → usage_handler [REGISTER ONLY]
│         ├─ GET /api/v1/usage/models → usage_models_handler [REGISTER ONLY]
│         └─ fallback(proxy_handler) [REGISTER ONLY]
│
├─ IMPORTANT: 上述 Handler 在启动时只被注册；收到对应 HTTP 请求后才执行
│
▼
FILE: crates/service/crates/billing/src/cache.rs
│
├─ PriceCache::load(db)
│    ├─ PriceCache::empty()
│    └─ refresh(db)
│         └─ BillingPriceModel::list(...)
│
▼
FILE: crates/router/src/exchange_rate.rs
│
├─ ExchangeRateService::new()
├─ load_rates_from_db()
└─ start_sync_task()
     └─ tokio::spawn hourly loop
│
▼
FILE: crates/router/src/price_sync.rs
│
├─ start_price_sync_task(...)
│    └─ tokio::spawn
│         ├─ PriceSyncService::new()/with_config()
│         ├─ startup sync_all(false) unless SKIP_INITIAL_PRICE_SYNC
│         └─ LOOP select(periodic tick, force-sync receiver)
│
▼
FILE: crates/server/src/api/mod.rs
│
├─ api::routes(state)
│    ├─ auth::public_routes() [REGISTER]
│    ├─ auth::protected_routes() [REGISTER]
│    ├─ billing::routes() [REGISTER]
│    ├─ channel::routes() [REGISTER]
│    ├─ token::routes() [REGISTER]
│    ├─ log::routes() [REGISTER]
│    ├─ monitor::routes() [REGISTER]
│    ├─ user::routes() [REGISTER]
│    ├─ security::security_routes() [REGISTER]
│    ├─ openapi::routes() [REGISTER]
│    ├─ cache::routes() [REGISTER]
│    └─ auth_middleware layered over protected router [REGISTER]
│
├─ IMPORTANT: routes() 函数在启动期执行；具体 HTTP Handler 此刻不执行
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ liveview_router(db)
│    ├─ LiveViewPool::new()
│    ├─ register HTML shell routes /, /home, /login, /register, ...
│    ├─ register /console and /console/*
│    ├─ register /favicon.ico
│    └─ register WS_PATH
│         └─ app::App is passed as future WebSocket-session callback [REGISTER ONLY]
│
▼
FILE: crates/server/src/lib.rs
│
├─ create_app() returns Unified Router
├─ SocketAddr::parse(host:port)
├─ TcpListener::bind(addr).await
├─ axum::serve(listener, app).await
│
└─ LONG-RUNNING STATE
     ├─ HTTP listener waits for Management / Internal / Data Plane / LiveView traffic
     ├─ monitor updater keeps running
     ├─ exchange-rate task keeps running
     ├─ price-sync task keeps running
     ├─ AIMD feedback consumer keeps running
     ├─ RouterLog writer keeps running
     └─ RequestLog writer keeps running
│
▼
END = server shutdown or fatal serve error
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
Unified Gateway listening on 127.0.0.1:3000
- Dashboard: http://127.0.0.1:3000/
- LLM API:   http://127.0.0.1:3000/v1/...

# 随后 axum::serve 持续运行，命令不会立即返回到 Shell。
```

### E2E FLOW #19 — router

**树路径：** `CLI / Executables → burncloud → router`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ Shell / OS 入口
│    └─ burncloud router
│    └─ 源码命令名为 router，但当前实现与 server 一样直接进入 run_async_server()
│
▼
FILE: src/main.rs
│
├─ main()
│    ├─ dotenvy::dotenv().ok()
│    ├─ ensure_master_key()
│    │    ├─ is_valid_master_key()
│    │    │    ├─ env::var("MASTER_KEY")
│    │    │    ├─ hex::decode(...)
│    │    │    └─ DECISION: decoded key length == 32 bytes?
│    │    │         ├─ YES → keep existing key
│    │    │         └─ NO  → generate replacement
│    │    ├─ rand::random::<[u8;32]>()
│    │    ├─ hex::encode()
│    │    ├─ resolve .env path from CWD / current_exe()
│    │    ├─ read_to_string() when file exists
│    │    ├─ fs::write(.env)
│    │    └─ env::set_var("MASTER_KEY", ...)
│    ├─ env::args().collect()
│    ├─ is_server = args[1] in [server, router, client]
│    └─ burncloud_server::logging::init_logging()
│
▼
FILE: crates/server/src/logging.rs
│
├─ init_logging()
│    ├─ LOG_DIR / LOG_MAX_FILES / RUST_LOG
│    ├─ fs::create_dir_all(log_dir)
│    ├─ tracing_log::LogTracer::init()
│    ├─ file_appender(server/service/database/router)
│    │    └─ RollingFileAppender::builder() → non_blocking(...)
│    ├─ module_filter(...)
│    └─ tracing::subscriber::set_global_default(...)
│
▼
FILE: src/main.rs
│
├─ match args.as_slice()
│    └─ DECISION: subcommand == "server" OR "router"?
│         ├─ NO  → other direct/CLI branch
│         └─ YES → run_async_server()
│
├─ run_async_server() #[tokio::main]
│    ├─ HOST env or "127.0.0.1"
│    ├─ PORT env or DEFAULT_PORT
│    └─ burncloud_server::start_server(host, port, true)
│
├─ IMPORTANT
│    ├─ 当前源码中 `server` 与 `router` 都走同一个 run_async_server()
│    └─ 此 direct branch 不进入 Clap CLI dispatch
│
▼
FILE: crates/server/src/lib.rs
│
├─ start_server(host, port, enable_liveview=true)
│    ├─ create_default_database().await
│    ├─ RouterDatabase::init(&db).await
│    ├─ UserDatabase::init(&db).await
│    ├─ create_app(db, true).await
│    ├─ SocketAddr::parse()
│    ├─ TcpListener::bind(addr).await
│    └─ axum::serve(listener, app).await
│
▼
FILE: crates/database/src/database.rs
│
├─ create_default_database() → Database::new()
├─ Database::new()
│    ├─ DECISION: BURNCLOUD_DATABASE_URL exists?
│    │    ├─ YES → use configured URL
│    │    └─ NO
│    │         ├─ get_default_database_path()
│    │         ├─ create_directory_if_not_exists()
│    │         ├─ DECISION: BURNCLOUD_FRESH_DB == "1" and DB exists?
│    │         │    ├─ YES → fs::remove_file(default DB)
│    │         │    └─ NO  → preserve DB
│    │         └─ build sqlite://...?...mode=rwc
│    └─ Database::initialize().await
│
├─ Database::initialize()
│    ├─ sqlx::any::install_default_drivers()
│    ├─ DatabaseConnection::new(database_url)
│    │    ├─ AnyConnectOptions::from_str()
│    │    └─ AnyPoolOptions::new().max_connections(10).connect_with(...)
│    ├─ DECISION: db.kind() == sqlite?
│    │    └─ YES → PRAGMA journal_mode=WAL
│    ├─ MigrationRunner::run(self)
│    └─ Schema::init(self)
│
▼
FILE: crates/database/src/migration/mod.rs
│
├─ MigrationRunner::run()
│    ├─ ensure _schema_migrations table
│    ├─ choose SQLite / PostgreSQL migration catalogue
│    ├─ inspect already-applied versions
│    └─ execute pending versioned DDL migrations
│
▼
FILE: crates/database/src/schema/mod.rs
│
├─ Schema::init()
│    ├─ rename::migrate_table_renames()
│    ├─ router::migrate_router_logs()
│    ├─ price::migrate_prices()
│    └─ user::migrate_users_and_seed()
│
▼
FILE: crates/server/src/lib.rs
│
└─ start_server() continues → RouterDatabase::init(&db)
│
▼
FILE: crates/database/crates/router/src/lib.rs
│
├─ RouterDatabase::init()
│    ├─ db.get_connection()
│    ├─ db.kind()
│    ├─ CREATE TABLE IF NOT EXISTS router_tokens
│    └─ SQLite-only compatibility ALTER TABLE statements
│
▼
FILE: crates/server/src/lib.rs
│
└─ start_server() continues → UserDatabase::init(&db)
│
▼
FILE: crates/database/crates/user/src/lib.rs
│
├─ UserDatabase::init()
│    ├─ CREATE / verify user_roles
│    ├─ CREATE / verify user_role_bindings
│    ├─ CREATE / verify user_recharges
│    ├─ SQLite/PostgreSQL compatibility migrations
│    ├─ SELECT COUNT(*) FROM user_roles
│    ├─ DECISION: no roles exist?
│    │    └─ YES → seed admin/user roles
│    ├─ query users without role bindings
│    └─ assign_role(...) for orphan users when required
│
▼
FILE: crates/server/src/lib.rs
│
├─ create_app(db, enable_liveview=true)
│    ├─ SystemMonitorService::new()
│    ├─ monitor.start_auto_update().await
│    ├─ CacheService::new().await
│    ├─ cache.is_available().await
│    ├─ create_router_app(db.clone()).await
│    ├─ UserService::new()
│    ├─ api::routes(state.clone())
│    ├─ build /health + merge Management/Internal routers
│    ├─ DECISION: enable_liveview?
│    │    └─ YES → burncloud_client::liveview_router(db.clone()) → merge
│    ├─ fallback_service(router_app)
│    └─ SetRequestId / PropagateRequestId / Trace / CORS layers
│
▼
FILE: crates/service/crates/monitor/src/service.rs
│
├─ SystemMonitorService::new()
├─ start_auto_update()
│    └─ tokio::spawn
│         └─ LOOP interval(1s)
│              └─ collect_metrics_internal()
│                   ├─ CpuCollector::collect()
│                   ├─ MemoryCollector::collect()
│                   └─ DiskCollector::collect_all()
│
▼
FILE: crates/service/crates/cache/src/service.rs
│
├─ CacheService::new() → with_config(CacheConfig::default())
├─ DECISION: CACHE_ENABLED?
│    ├─ NO  → return disabled cache
│    └─ YES
│         ├─ require REDIS_URL
│         ├─ redis::Client::open()
│         ├─ get_connection_manager().await
│         └─ PING
│
▼
FILE: crates/router/src/lib.rs
│
├─ create_router_app(db)
│    ├─ reqwest::Client::builder() + timeouts/pool settings
│    ├─ RoundRobinBalancer::new()
│    ├─ RateLimiter::new(100, 10)
│    ├─ CircuitBreaker::new(...)
│    ├─ ModelRouter::new(db.clone())
│    ├─ ChannelStateTracker::new()
│    ├─ DynamicAdaptorFactory::new(db.clone())
│    ├─ ApiVersionDetector::new(db.clone())
│    ├─ PriceCache::load(&db).await
│    │    └─ DECISION: load fails? → PriceCache::empty()
│    ├─ CostCalculator::new(price_cache.clone())
│    ├─ ExchangeRateService::new(db.clone())
│    ├─ load_rates_from_db().await
│    ├─ tokio::spawn → ExchangeRateService::start_sync_task()
│    ├─ scheduler::load_scheduler_config()
│    ├─ AffinityCache::default()
│    ├─ InMemoryBudget::new()
│    ├─ configure_rate_budget_from_db(...).await
│    ├─ read BILLING_STRICT_MODE
│    ├─ read REQUEST_LOG_STORAGE_POLICY
│    ├─ tokio::spawn → AIMD budget-update consumer
│    ├─ price_sync::start_price_sync_task(...)
│    ├─ tokio::spawn → RouterLog writer → RouterDatabase::insert_log()
│    ├─ tokio::spawn → RequestLog writer → RouterDatabase::insert_request_log()
│    ├─ ChannelHealthManager::new()
│    ├─ build internal_app routes
│    │    ├─ /console/internal/health
│    │    ├─ /console/internal/prices/sync
│    │    ├─ /console/internal/circuit-breaker/trip-all
│    │    └─ /console/internal/metrics
│    └─ build data-plane app
│         ├─ GET /v1/models → models_handler [REGISTER ONLY]
│         ├─ GET /api/v1/usage → usage_handler [REGISTER ONLY]
│         ├─ GET /api/v1/usage/models → usage_models_handler [REGISTER ONLY]
│         └─ fallback(proxy_handler) [REGISTER ONLY]
│
├─ IMPORTANT: 上述 Handler 在启动时只被注册；收到对应 HTTP 请求后才执行
│
▼
FILE: crates/service/crates/billing/src/cache.rs
│
├─ PriceCache::load(db)
│    ├─ PriceCache::empty()
│    └─ refresh(db)
│         └─ BillingPriceModel::list(...)
│
▼
FILE: crates/router/src/exchange_rate.rs
│
├─ ExchangeRateService::new()
├─ load_rates_from_db()
└─ start_sync_task()
     └─ tokio::spawn hourly loop
│
▼
FILE: crates/router/src/price_sync.rs
│
├─ start_price_sync_task(...)
│    └─ tokio::spawn
│         ├─ PriceSyncService::new()/with_config()
│         ├─ startup sync_all(false) unless SKIP_INITIAL_PRICE_SYNC
│         └─ LOOP select(periodic tick, force-sync receiver)
│
▼
FILE: crates/server/src/api/mod.rs
│
├─ api::routes(state)
│    ├─ auth::public_routes() [REGISTER]
│    ├─ auth::protected_routes() [REGISTER]
│    ├─ billing::routes() [REGISTER]
│    ├─ channel::routes() [REGISTER]
│    ├─ token::routes() [REGISTER]
│    ├─ log::routes() [REGISTER]
│    ├─ monitor::routes() [REGISTER]
│    ├─ user::routes() [REGISTER]
│    ├─ security::security_routes() [REGISTER]
│    ├─ openapi::routes() [REGISTER]
│    ├─ cache::routes() [REGISTER]
│    └─ auth_middleware layered over protected router [REGISTER]
│
├─ IMPORTANT: routes() 函数在启动期执行；具体 HTTP Handler 此刻不执行
│
▼
FILE: crates/client/src/lib.rs  [PR CHANGED]
│
├─ liveview_router(db)
│    ├─ LiveViewPool::new()
│    ├─ register HTML shell routes /, /home, /login, /register, ...
│    ├─ register /console and /console/*
│    ├─ register /favicon.ico
│    └─ register WS_PATH
│         └─ app::App is passed as future WebSocket-session callback [REGISTER ONLY]
│
▼
FILE: crates/server/src/lib.rs
│
├─ create_app() returns Unified Router
├─ SocketAddr::parse(host:port)
├─ TcpListener::bind(addr).await
├─ axum::serve(listener, app).await
│
└─ LONG-RUNNING STATE
     ├─ HTTP listener waits for Management / Internal / Data Plane / LiveView traffic
     ├─ monitor updater keeps running
     ├─ exchange-rate task keeps running
     ├─ price-sync task keeps running
     ├─ AIMD feedback consumer keeps running
     ├─ RouterLog writer keeps running
     └─ RequestLog writer keeps running
│
▼
END = server shutdown or fatal serve error
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
Unified Gateway listening on 127.0.0.1:3000
- Dashboard: http://127.0.0.1:3000/
- LLM API:   http://127.0.0.1:3000/v1/...

# 随后 axum::serve 持续运行，命令不会立即返回到 Shell。
```

### E2E FLOW #20 — client

**树路径：** `CLI / Executables → burncloud → client`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Shell: burncloud client
│
▼
FILE: src/main.rs
│
├─ main()
│    ├─ dotenvy::dotenv()
│    ├─ ensure_master_key()
│    ├─ init_logging()
│    └─ match subcommand == "client"
│
├─ DECISION: target_os == windows?
│    ├─ YES → burncloud_client::launch_gui_with_tray()
│    └─ NO
│         ├─ println("Desktop GUI is only available on Windows.")
│         ├─ println("On Linux, use 'burncloud server' ...")
│         └─ return Ok(()) → END
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ launch_gui_with_tray()
│    ├─ WindowBuilder::new()
│    ├─ configure title / size / resizable / decorations
│    ├─ load Windows icon when available
│    ├─ temp_dir()/burncloud_webview_data
│    ├─ Config::new().with_window(...).with_data_directory(...)
│    └─ LaunchBuilder::desktop().with_cfg(config).launch(AppWithTray)
│
├─ AppWithTray()
│    ├─ use_context_provider(DesktopMode)
│    ├─ use_window()
│    ├─ use_effect → set_maximized(true)
│    ├─ std::thread::spawn → start_tray()
│    ├─ use_effect → spawn async 100ms show-window poll
│    │    └─ should_show_window() → set_visible / set_focus
│    └─ rsx! { App {} }
│
├─ App()
│    ├─ use_init_i18n()
│    ├─ use_init_toast()
│    ├─ use_init_auth()
│    ├─ use_init_theme()
│    ├─ ToastContainer
│    └─ Router<Route>
│
▼
END / DESKTOP EVENT LOOP
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
# Windows
BurnCloud desktop window + tray enter the long-running GUI event loop.

# 非 Windows（源码真实 stdout）
Desktop GUI is only available on Windows.
On Linux, use 'burncloud server' to start the web dashboard.
```

### E2E FLOW #21 — burncloud-client

**树路径：** `CLI / Executables → Workspace Binaries → burncloud-client`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ OS process launch
│    ├─ executable: burncloud-client
│    ├─ argv / cwd / environment inherited from OS
│    └─ DECISION: executable can be loaded/launched?
│         ├─ NO → OS-level error → END
│         └─ YES → main()
│
▼
FILE: crates/client/src/main.rs
│
├─ main() initialization
│    ├─ initialize executable-specific runtime/services
│    ├─ parse any supported arguments
│    └─ DECISION: platform/arguments/initialization valid?
│         ├─ NO → print/return error → process exit
│         └─ YES → continue
│
├─ Runtime work
│    ├─ create client/download/loop/tray structures as applicable
│    ├─ start event loop or execute one-shot job
│    └─ DECISION: long-running executable?
│         ├─ YES → enter event/service loop
│         └─ NO → produce output and exit
│
├─ Error boundary
│    └─ runtime error → log/return non-success according to executable implementation
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / RUNNING LOOP
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
BurnCloud Client
ui=initialized
route=/
status=running
```

### E2E FLOW #22 — burncloud-loop

**树路径：** `CLI / Executables → Workspace Binaries → burncloud-loop`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/loops/src/main.rs`

```text
START
│
├─ OS process launch
│    ├─ executable: burncloud-loop
│    ├─ argv / cwd / environment inherited from OS
│    └─ DECISION: executable can be loaded/launched?
│         ├─ NO → OS-level error → END
│         └─ YES → main()
│
▼
FILE: crates/loops/src/main.rs  [PR CHANGED]
│
├─ main() initialization
│    ├─ initialize executable-specific runtime/services
│    ├─ parse any supported arguments
│    └─ DECISION: platform/arguments/initialization valid?
│         ├─ NO → print/return error → process exit
│         └─ YES → continue
│
├─ Runtime work
│    ├─ create client/download/loop/tray structures as applicable
│    ├─ start event loop or execute one-shot job
│    └─ DECISION: long-running executable?
│         ├─ YES → enter event/service loop
│         └─ NO → produce output and exit
│
├─ Error boundary
│    └─ runtime error → log/return non-success according to executable implementation
│
▼
END / RUNNING LOOP
```

#### PR Before / After（patch 证据）

**FILE: `crates/loops/src/main.rs`**

HUNKS:
```text
@@ -0,0 +1,8 @@
```

AFTER / ADDED:
```diff
+use burncloud_loops::cli::Cli;
+use clap::Parser;
+
+fn main() -> anyhow::Result<()> {
+    let cli = Cli::parse();
+    let code = burncloud_loops::cli::run(cli)?;
+    std::process::exit(code);
+}
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
burncloud-loop
subcommands: jobs-aesthetic | css-optimize | gate | gates | list-gates
```

### E2E FLOW #23 — Windows tray thread

**树路径：** `Background Jobs / Async Side Effects → Desktop Background Work → Windows tray thread`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START / TRIGGER
│
├─ Trigger source
│    ├─ startup spawn / request-time tokio::spawn / manager restoration / channel message
│    └─ task: Windows tray thread
│
├─ Spawn / registration
│    ├─ parent runtime creates async task/thread/loop
│    └─ DECISION: spawn/runtime handle available?
│         ├─ NO → task never starts; parent may log error
│         └─ YES → task owns/borrows required shared state
│
├─ Wait boundary
│    ├─ timer sleep / mpsc receive / polling interval / restored work item
│    └─ DECISION: trigger/event available?
│         ├─ NO → continue waiting
│         └─ YES → one iteration begins
│
├─ Input snapshot
│    ├─ read latest runtime/DB/request-derived state needed by job
│    └─ freeze iteration context
│
├─ Core job operation
│    ├─ execute Windows tray thread
│    └─ may call DB / HTTP / filesystem / runtime service depending on task
│
├─ Operation result
│    └─ DECISION: iteration succeeds?
│         ├─ NO
│         │    ├─ log/record failure
│         │    ├─ preserve parent request availability when task is fail-open
│         │    └─ decide retry on next event/interval
│         └─ YES
│              ├─ update in-memory state and/or persistent state
│              └─ emit success telemetry/log
│
├─ Cancellation / lifetime
│    └─ DECISION: parent runtime still alive AND task should continue?
│         ├─ YES → 返回等待边界
│         └─ NO → release task resources
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / NEXT ITERATION
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
tray=started icon=visible menu_items=3 status=running
```

### E2E FLOW #24 — Show-window poll loop

**树路径：** `Background Jobs / Async Side Effects → Desktop Background Work → Show-window poll loop`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START / TRIGGER
│
├─ Trigger source
│    ├─ startup spawn / request-time tokio::spawn / manager restoration / channel message
│    └─ task: Show-window poll loop
│
├─ Spawn / registration
│    ├─ parent runtime creates async task/thread/loop
│    └─ DECISION: spawn/runtime handle available?
│         ├─ NO → task never starts; parent may log error
│         └─ YES → task owns/borrows required shared state
│
├─ Wait boundary
│    ├─ timer sleep / mpsc receive / polling interval / restored work item
│    └─ DECISION: trigger/event available?
│         ├─ NO → continue waiting
│         └─ YES → one iteration begins
│
├─ Input snapshot
│    ├─ read latest runtime/DB/request-derived state needed by job
│    └─ freeze iteration context
│
├─ Core job operation
│    ├─ execute Show-window poll loop
│    └─ may call DB / HTTP / filesystem / runtime service depending on task
│
├─ Operation result
│    └─ DECISION: iteration succeeds?
│         ├─ NO
│         │    ├─ log/record failure
│         │    ├─ preserve parent request availability when task is fail-open
│         │    └─ decide retry on next event/interval
│         └─ YES
│              ├─ update in-memory state and/or persistent state
│              └─ emit success telemetry/log
│
├─ Cancellation / lifetime
│    └─ DECISION: parent runtime still alive AND task should continue?
│         ├─ YES → 返回等待边界
│         └─ NO → release task resources
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / NEXT ITERATION
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
show_window=true visible=true focused=true poll_interval_ms=100
```

### E2E FLOW #25 — create_app

**树路径：** `Startup → Startup Chain → create_app`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/lib.rs`

```text
START
│
├─ Process/environment input
│    ├─ startup target: create_app
│    ├─ environment variables / dotenv
│    ├─ CLI/platform mode
│    └─ filesystem/database availability
│
├─ Enter startup function
│    └─ execute create_app
│
├─ Dependency initialization
│    ├─ construct required DB/services/runtime state
│    ├─ register routes/tasks as applicable
│    └─ DECISION: dependency initialization succeeds?
│         ├─ NO → propagate startup error → process/runtime not ready → END
│         └─ YES → next dependency
│
├─ Runtime composition
│    ├─ wire shared Arc/State/services
│    ├─ compose routers/middleware/background jobs
│    └─ make dependencies reachable from runtime entrypoints
│
├─ Readiness boundary
│    └─ DECISION: all required startup stages complete?
│         ├─ NO → startup fails/returns Err
│         └─ YES → expose listener/client/event loop/runtime
│
├─ Steady-state handoff
│    ├─ long-running loops take ownership of runtime
│    └─ requests/events can now enter documented entrypoints
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/server/src/lib.rs
│    │    ├─ start_server()
│    │    │    └─ CALL → create_default_database() @ crates/database/src/database.rs
│    │    │    └─ CALL → RouterDatabase::init() @ crates/database/crates/router/src/lib.rs
│    │    │    └─ CALL → UserDatabase::init() @ crates/database/crates/user/src/lib.rs
│    │    │    └─ CALL → create_app() @ crates/server/src/lib.rs
│    │    ├─ create_app()
│    │    │    └─ CALL → SystemMonitorService::new() @ crates/service/crates/monitor/src/service.rs
│    │    │    └─ CALL → CacheService::new() @ crates/service/crates/cache/src/service.rs
│    │    │    └─ CALL → create_router_app() @ crates/router/src/lib.rs
│    │    │    └─ CALL → UserService::new() @ crates/service/crates/user/src/lib.rs
│    │    │    └─ CALL → routes() @ crates/server/src/api/mod.rs
│    ├─ FILE: crates/server/src/api/mod.rs
│    │    ├─ routes()
│    │    │    └─ CALL → public_routes() @ crates/server/src/api/auth.rs
│    │    │    └─ CALL → protected_routes() @ crates/server/src/api/auth.rs
│    │    │    └─ CALL → security_routes() @ crates/server/src/api/security.rs
│    ├─ FILE: crates/client/src/lib.rs  [PR CHANGED]
│    │    ├─ liveview_router()
│    │    │    └─ CALL → liveview_style_tags() @ crates/client/crates/client-shared/src/app_styles.rs
│    ├─ FILE: crates/router/src/lib.rs
│    │    ├─ create_router_app()
│    │    │    └─ CALL → RoundRobinBalancer::new() @ crates/router/src/balancer/mod.rs
│    │    │    └─ CALL → RateLimiter::new() @ crates/router/src/limiter.rs
│    │    │    └─ CALL → CircuitBreaker::new() @ crates/router/src/circuit_breaker.rs
│    │    │    └─ CALL → ModelRouter::new() @ crates/router/src/model_router.rs
│    │    │    └─ CALL → ChannelStateTracker::new() @ crates/router/src/channel_state.rs
│    │    │    └─ CALL → DynamicAdaptorFactory::new() @ crates/router/src/adaptor/factory.rs
│    │    │    └─ CALL → ApiVersionDetector::new() @ crates/router/src/adaptor/detector.rs
│    │    │    └─ CALL → PriceCache::load() @ crates/service/crates/billing/src/cache.rs
│    │    │    └─ CALL → PriceCache::empty() @ crates/service/crates/billing/src/cache.rs
│    │    │    └─ CALL → CostCalculator::new() @ crates/service/crates/billing/src/calculator.rs
│    │    ├─ proxy_handler()
│    │    │    └─ CALL → normalize_doubled_path() @ crates/router/src/lib.rs
│    │    │    └─ CALL → build_response_with_header() @ crates/router/src/lib.rs
│    │    │    └─ CALL → RouterDatabase::validate_token_and_get_info() @ crates/database/crates/router/src/lib.rs
│    │    │    └─ CALL → RouterDatabase::update_token_accessed_time() @ crates/database/crates/router/src/lib.rs
│    │    │    └─ CALL → RouterDatabase::validate_token_detailed() @ crates/database/crates/router/src/lib.rs
│    │    │    └─ CALL → extract_model_from_gemini_path() @ crates/router/src/passthrough.rs
│    │    │    └─ CALL → RouterVideoTaskModel::get_by_task_id() @ crates/database/crates/router/src/router_video_task.rs
│    │    │    └─ CALL → ChannelProviderModel::get_by_id() @ crates/database/crates/channel/src/channel_provider.rs
│    │    │    └─ CALL → UnifiedTokenCounter::new() @ crates/service/crates/billing/src/counter.rs
│    │    │    └─ CALL → proxy_logic() @ crates/router/src/lib.rs
│    │    ├─ proxy_logic()
│    │    │    └─ CALL → sanitize_request_body() @ crates/router/src/lib.rs
│    │    │    └─ CALL → sanitize_request_headers() @ crates/router/src/lib.rs
│    │    │    └─ CALL → extract_model_from_gemini_path() @ crates/router/src/passthrough.rs
│    │    │    └─ CALL → UserService::resolve_traffic_class() @ crates/service/crates/user/src/lib.rs
│    │    │    └─ CALL → OrderType::from_db_row() @ crates/router/src/order_type.rs
│    │    │    └─ CALL → build_response_with_header() @ crates/router/src/lib.rs
│    │    │    └─ CALL → record_failover_attempt() @ crates/router/src/lib.rs
│    │    │    └─ CALL → BudgetGuard::new() @ crates/router/src/rate_budget.rs
│    │    │    └─ CALL → should_passthrough() @ crates/router/src/passthrough.rs
│    │    │    └─ CALL → build_gemini_passthrough_url() @ crates/router/src/passthrough.rs
│    ├─ FILE: crates/database/src/database.rs
│    │    ├─ create_default_database()
│    │    │    └─ CALL → Database::new() @ crates/database/src/database.rs
│    ├─ FILE: crates/database/crates/router/src/lib.rs
│    │    ├─ RouterDatabase::init()
│    ├─ FILE: crates/database/crates/user/src/lib.rs
│    │    ├─ UserDatabase::init()
│    │    │    └─ CALL → UserDatabase::assign_role() @ crates/database/crates/user/src/lib.rs
│    ├─ FILE: crates/service/crates/monitor/src/service.rs
│    │    ├─ SystemMonitorService::new()
│    ├─ FILE: crates/service/crates/cache/src/service.rs
│    │    ├─ CacheService::new()
│    │    │    └─ CALL → CacheService::with_config() @ crates/service/crates/cache/src/service.rs
│    ├─ FILE: crates/service/crates/user/src/lib.rs
│    │    ├─ UserService::new()
│    ├─ FILE: crates/server/src/api/auth.rs
│    │    ├─ public_routes()
│    │    ├─ protected_routes()
│    ├─ FILE: crates/server/src/api/security.rs
│    │    ├─ security_routes()
│    ├─ FILE: crates/client/crates/client-shared/src/app_styles.rs
│    │    ├─ liveview_style_tags()
│    ├─ FILE: crates/router/src/balancer/mod.rs
│    │    ├─ RoundRobinBalancer::new()
│    ├─ FILE: crates/router/src/limiter.rs
│    │    ├─ RateLimiter::new()
│    ├─ FILE: crates/router/src/circuit_breaker.rs
│    │    ├─ CircuitBreaker::new()
│    ├─ FILE: crates/router/src/model_router.rs
│    │    ├─ ModelRouter::new()
│    ├─ FILE: crates/router/src/channel_state.rs
│    │    ├─ ChannelStateTracker::new()
│    ├─ FILE: crates/router/src/adaptor/factory.rs
│    │    ├─ DynamicAdaptorFactory::new()
│    ├─ FILE: crates/router/src/adaptor/detector.rs
│    │    ├─ ApiVersionDetector::new()
│    ├─ FILE: crates/service/crates/billing/src/cache.rs
│    │    ├─ PriceCache::load()
│    │    │    └─ CALL → PriceCache::empty() @ crates/service/crates/billing/src/cache.rs
│    │    ├─ PriceCache::empty()
│    ├─ FILE: crates/service/crates/billing/src/calculator.rs
│    │    ├─ CostCalculator::new()
│    └─ FILE: crates/router/src/exchange_rate.rs
│    │    ├─ ExchangeRateService::new()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ component is READY / RUNNING
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/lib.rs`**

HUNKS:
```text
@@ -68,7 +68,17 @@ pub fn liveview_router(_db: Arc<Database>) -> Router {
```

BEFORE / REMOVED:
```diff
-        .route("/console/{*path}", html_handler.clone())
```

AFTER / ADDED:
```diff
+        .route("/console/{*path}", html_handler.clone());
+
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    let app = app
+        .route("/preview/home", html_handler.clone())
+        .route("/preview/login", html_handler.clone())
+        .route("/preview/console", html_handler.clone())
+        .route("/preview/console/", html_handler.clone())
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
monitor=started
cache=ready
management_routes=mounted
internal_routes=mounted
liveview=enabled
data_plane_fallback=mounted
```

### E2E FLOW #26 — /

**树路径：** `UI-only Actions → Guest / Public → /`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/home.rs
│
└─ Root / Home page module
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/
component=home
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #27 — /home

**树路径：** `UI-only Actions → Guest / Public → /home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /home
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/home.rs
│
└─ HomePage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/home
component=home
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #28 — /login

**树路径：** `UI-only Actions → Guest / Public → /login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/pages/login.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /login
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/login.rs  [PR CHANGED]
│
└─ LoginPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/pages/login.rs`**

HUNKS:
```text
@@ -88,8 +88,7 @@ pub fn LoginPage() -> Element {
@@ -100,21 +99,17 @@ pub fn LoginPage() -> Element {
@@ -130,7 +125,7 @@ pub fn LoginPage() -> Element {
@@ -173,7 +168,7 @@ pub fn LoginPage() -> Element {
@@ -183,9 +178,9 @@ pub fn LoginPage() -> Element {
@@ -208,6 +203,7 @@ pub fn LoginPage() -> Element {
```

BEFORE / REMOVED:
```diff
-                // Center content
-                div {
-
-                // Version
-                div { class: "login-brand-version",
-                    "v0.3.1 \u{00b7} build 2026.04.27 \u{00b7} burncloud.io"
-                }
-                div { class: "mb-xxxl",
```

AFTER / ADDED:
```diff
+                div { class: "login-brand-body",
+                div { class: "login-form-inner",
+                    div { class: "login-form-header",
+                        h2 { class: "login-form-title", {t(*lang.read(), "login.form.title")} }
+                        p { class: "login-form-subtitle", {t(*lang.read(), "login.form.subtitle")} }
+                    }
+                    div { class: "login-form-fields",
+                        div { class: "flex items-center justify-between mb-bc-2",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/login
component=login
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #29 — /register

**树路径：** `UI-only Actions → Guest / Public → /register`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /register
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/crates/client-register/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/register
component=register
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #30 — /forgot-password

**树路径：** `UI-only Actions → Guest / Public → /forgot-password`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /forgot-password
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/forgot_password.rs
│
└─ ForgotPasswordPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/forgot-password
component=forgot-password
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #31 — /reset-password?:token

**树路径：** `UI-only Actions → Guest / Public → /reset-password?:token`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /reset-password?:token
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/reset_password.rs
│
└─ ResetPasswordPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/reset-password?:token
component=reset-password?:token
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #32 — /console/dashboard

**树路径：** `UI-only Actions → Console → /console/dashboard`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/dashboard
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/dashboard.rs
│
└─ Dashboard re-export
│
▼
FILE: crates/client/crates/client-dashboard/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -10,6 +10,7 @@ use super::placeholders::{
@@ -42,6 +43,12 @@ pub enum CoreRoute {
@@ -55,10 +62,10 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-            div { class: "flex flex-1 min-h-0 overflow-hidden w-full pt-8",
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use crate::DesktopMode;
+    let titlebar_pad = if try_use_context::<DesktopMode>().is_some() {
+        "pt-bc-8"
+    } else {
+        ""
+    };
+
+            div { class: "flex flex-1 min-h-0 overflow-hidden w-full {titlebar_pad}",
```

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -5,18 +5,16 @@ use crate::components::app_styles::AppStyles;
@@ -50,7 +48,7 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::utils::storage::ClientState;
-
-    let client_state = ClientState::load();
-    let theme = client_state.theme.unwrap_or_default();
-    let theme_str = theme.as_str();
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::use_theme;
+    let theme_ctx = use_theme();
+    let theme_str = theme_ctx.data_theme_attr();
+                    div { class: "flex-1 overflow-y-auto px-bc-2 py-bc-4",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/dashboard
component=console/dashboard
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #33 — /console/deploy

**树路径：** `UI-only Actions → Console → /console/deploy`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/deploy
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/deploy.rs
│
└─ DeployConfig re-export
│
▼
FILE: crates/client/crates/client-deploy/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -10,6 +10,7 @@ use super::placeholders::{
@@ -42,6 +43,12 @@ pub enum CoreRoute {
@@ -55,10 +62,10 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-            div { class: "flex flex-1 min-h-0 overflow-hidden w-full pt-8",
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use crate::DesktopMode;
+    let titlebar_pad = if try_use_context::<DesktopMode>().is_some() {
+        "pt-bc-8"
+    } else {
+        ""
+    };
+
+            div { class: "flex flex-1 min-h-0 overflow-hidden w-full {titlebar_pad}",
```

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -5,18 +5,16 @@ use crate::components::app_styles::AppStyles;
@@ -50,7 +48,7 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::utils::storage::ClientState;
-
-    let client_state = ClientState::load();
-    let theme = client_state.theme.unwrap_or_default();
-    let theme_str = theme.as_str();
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::use_theme;
+    let theme_ctx = use_theme();
+    let theme_str = theme_ctx.data_theme_attr();
+                    div { class: "flex-1 overflow-y-auto px-bc-2 py-bc-4",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/deploy
component=console/deploy
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #34 — /console/monitor

**树路径：** `UI-only Actions → Console → /console/monitor`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/monitor
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/monitor.rs
│
└─ ServiceMonitor re-export
│
▼
FILE: crates/client/crates/client-monitor/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -10,6 +10,7 @@ use super::placeholders::{
@@ -42,6 +43,12 @@ pub enum CoreRoute {
@@ -55,10 +62,10 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-            div { class: "flex flex-1 min-h-0 overflow-hidden w-full pt-8",
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use crate::DesktopMode;
+    let titlebar_pad = if try_use_context::<DesktopMode>().is_some() {
+        "pt-bc-8"
+    } else {
+        ""
+    };
+
+            div { class: "flex flex-1 min-h-0 overflow-hidden w-full {titlebar_pad}",
```

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -5,18 +5,16 @@ use crate::components::app_styles::AppStyles;
@@ -50,7 +48,7 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::utils::storage::ClientState;
-
-    let client_state = ClientState::load();
-    let theme = client_state.theme.unwrap_or_default();
-    let theme_str = theme.as_str();
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::use_theme;
+    let theme_ctx = use_theme();
+    let theme_str = theme_ctx.data_theme_attr();
+                    div { class: "flex-1 overflow-y-auto px-bc-2 py-bc-4",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/monitor
component=console/monitor
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #35 — /console/access

**树路径：** `UI-only Actions → Console → /console/access`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-access/src/lib.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/access
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/api.rs
│
└─ AccessPage re-export
│
▼
FILE: crates/client/crates/client-access/src/lib.rs  [PR CHANGED]
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-access/src/lib.rs`**

HUNKS:
```text
@@ -72,49 +72,50 @@ pub fn AccessPage() -> Element {
@@ -129,7 +130,7 @@ pub fn AccessPage() -> Element {
@@ -170,7 +171,7 @@ pub fn AccessPage() -> Element {
@@ -244,7 +245,7 @@ pub fn AccessPage() -> Element {
@@ -262,14 +263,14 @@ pub fn AccessPage() -> Element {
```

BEFORE / REMOVED:
```diff
-                    class: "btn-black",
-        div { class: "page-content flex flex-col gap-lg",
-                    icon: rsx! { span { class: "text-display", "🔑" } },
-                            class: "btn-primary",
-                div { class: "grid gap-md",
-                                div { key: "{tk_id}", class: "row-card p-xl",
-                                    div { class: "flex items-start justify-between gap-lg",
-                                        div { class: "flex items-start gap-lg",
```

AFTER / ADDED:
```diff
+            subtitle: Some(t(*lang.read(), "access.subtitle").to_string()),
+                    variant: ButtonVariant::Black,
+        div { class: "page-content flex flex-col gap-bc-4 flex-1 min-h-0 min-w-0",
+                    icon: rsx! { span { class: "text-large-title", "🔑" } },
+                            variant: ButtonVariant::Black,
+                div { class: "grid gap-bc-3",
+                                div { key: "{tk_id}", class: "row-card p-bc-5",
+                                    div { class: "flex items-center justify-between gap-bc-4 min-w-0",
```

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -10,6 +10,7 @@ use super::placeholders::{
@@ -42,6 +43,12 @@ pub enum CoreRoute {
@@ -55,10 +62,10 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-            div { class: "flex flex-1 min-h-0 overflow-hidden w-full pt-8",
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use crate::DesktopMode;
+    let titlebar_pad = if try_use_context::<DesktopMode>().is_some() {
+        "pt-bc-8"
+    } else {
+        ""
+    };
+
+            div { class: "flex flex-1 min-h-0 overflow-hidden w-full {titlebar_pad}",
```

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -5,18 +5,16 @@ use crate::components::app_styles::AppStyles;
@@ -50,7 +48,7 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::utils::storage::ClientState;
-
-    let client_state = ClientState::load();
-    let theme = client_state.theme.unwrap_or_default();
-    let theme_str = theme.as_str();
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::use_theme;
+    let theme_ctx = use_theme();
+    let theme_str = theme_ctx.data_theme_attr();
+                    div { class: "flex-1 overflow-y-auto px-bc-2 py-bc-4",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/access
component=console/access
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #36 — /console/models

**树路径：** `UI-only Actions → Console → /console/models`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/models
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/models.rs
│
└─ ChannelPage re-export
│
▼
FILE: crates/client/crates/client-models/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -10,6 +10,7 @@ use super::placeholders::{
@@ -42,6 +43,12 @@ pub enum CoreRoute {
@@ -55,10 +62,10 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-            div { class: "flex flex-1 min-h-0 overflow-hidden w-full pt-8",
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use crate::DesktopMode;
+    let titlebar_pad = if try_use_context::<DesktopMode>().is_some() {
+        "pt-bc-8"
+    } else {
+        ""
+    };
+
+            div { class: "flex flex-1 min-h-0 overflow-hidden w-full {titlebar_pad}",
```

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -5,18 +5,16 @@ use crate::components::app_styles::AppStyles;
@@ -50,7 +48,7 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::utils::storage::ClientState;
-
-    let client_state = ClientState::load();
-    let theme = client_state.theme.unwrap_or_default();
-    let theme_str = theme.as_str();
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::use_theme;
+    let theme_ctx = use_theme();
+    let theme_str = theme_ctx.data_theme_attr();
+                    div { class: "flex-1 overflow-y-auto px-bc-2 py-bc-4",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/models
component=console/models
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #37 — /console/users

**树路径：** `UI-only Actions → Console → /console/users`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-users/src/lib.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/users
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/user.rs
│
└─ UsersPage re-export
│
▼
FILE: crates/client/crates/client-users/src/lib.rs  [PR CHANGED]
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -10,6 +10,7 @@ use super::placeholders::{
@@ -42,6 +43,12 @@ pub enum CoreRoute {
@@ -55,10 +62,10 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-            div { class: "flex flex-1 min-h-0 overflow-hidden w-full pt-8",
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use crate::DesktopMode;
+    let titlebar_pad = if try_use_context::<DesktopMode>().is_some() {
+        "pt-bc-8"
+    } else {
+        ""
+    };
+
+            div { class: "flex flex-1 min-h-0 overflow-hidden w-full {titlebar_pad}",
```

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/crates/client-users/src/lib.rs`**

HUNKS:
```text
@@ -51,7 +51,7 @@ pub fn UsersPage() -> Element {
@@ -62,7 +62,7 @@ pub fn UsersPage() -> Element {
@@ -155,8 +155,9 @@ pub fn UsersPage() -> Element {
@@ -219,7 +220,7 @@ pub fn UsersPage() -> Element {
@@ -234,18 +235,18 @@ pub fn UsersPage() -> Element {
@@ -259,7 +260,7 @@ pub fn UsersPage() -> Element {
```

BEFORE / REMOVED:
```diff
-                    class: "btn-black",
-        div { class: "page-content bc-flex-col-gap-xl",
-                                        button {
-                                            class: "btn btn-ghost bc-btn-primary-text",
-            div { class: "flex flex-col gap-md",
-                    button {
-                        class: "btn btn-secondary",
-                    button {
```

AFTER / ADDED:
```diff
+                    variant: ButtonVariant::Black,
+        div { class: "page-content flex flex-col gap-bc-6",
+                                        BCButton {
+                                            variant: ButtonVariant::Ghost,
+                                            class: "bc-btn-primary-text".to_string(),
+            div { class: "flex flex-col gap-bc-3",
+                    BCButton {
+                        variant: ButtonVariant::Secondary,
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -5,18 +5,16 @@ use crate::components::app_styles::AppStyles;
@@ -50,7 +48,7 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::utils::storage::ClientState;
-
-    let client_state = ClientState::load();
-    let theme = client_state.theme.unwrap_or_default();
-    let theme_str = theme.as_str();
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::use_theme;
+    let theme_ctx = use_theme();
+    let theme_str = theme_ctx.data_theme_attr();
+                    div { class: "flex-1 overflow-y-auto px-bc-2 py-bc-4",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/users
component=console/users
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #38 — /console/settings

**树路径：** `UI-only Actions → Console → /console/settings`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/settings
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/settings.rs
│
└─ SystemSettings re-export
│
▼
FILE: crates/client/crates/client-settings/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -10,6 +10,7 @@ use super::placeholders::{
@@ -42,6 +43,12 @@ pub enum CoreRoute {
@@ -55,10 +62,10 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-            div { class: "flex flex-1 min-h-0 overflow-hidden w-full pt-8",
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use crate::DesktopMode;
+    let titlebar_pad = if try_use_context::<DesktopMode>().is_some() {
+        "pt-bc-8"
+    } else {
+        ""
+    };
+
+            div { class: "flex flex-1 min-h-0 overflow-hidden w-full {titlebar_pad}",
```

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -5,18 +5,16 @@ use crate::components::app_styles::AppStyles;
@@ -50,7 +48,7 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::utils::storage::ClientState;
-
-    let client_state = ClientState::load();
-    let theme = client_state.theme.unwrap_or_default();
-    let theme_str = theme.as_str();
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::use_theme;
+    let theme_ctx = use_theme();
+    let theme_str = theme_ctx.data_theme_attr();
+                    div { class: "flex-1 overflow-y-auto px-bc-2 py-bc-4",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/settings
component=console/settings
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #39 — /console/finance

**树路径：** `UI-only Actions → Console → /console/finance`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-finance/src/lib.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/finance
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/billing.rs
│
└─ FinancePage re-export
│
▼
FILE: crates/client/crates/client-finance/src/lib.rs  [PR CHANGED]
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-finance/src/lib.rs`**

HUNKS:
```text
@@ -117,7 +117,7 @@ pub fn Finance() -> Element {
@@ -140,7 +140,7 @@ pub fn Finance() -> Element {
@@ -215,7 +215,7 @@ pub fn Finance() -> Element {
```

BEFORE / REMOVED:
```diff
-        div { class: "page-content flex flex-col gap-xxxl",
-                        icon: rsx! { span { class: "text-xxl", "💰" } },
-                        icon: rsx! { span { class: "text-xxl", "💳" } },
```

AFTER / ADDED:
```diff
+        div { class: "page-content flex flex-col gap-bc-8",
+                        icon: rsx! { span { class: "bc-font-emoji", "💰" } },
+                        icon: rsx! { span { class: "bc-font-emoji", "💳" } },
```

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -10,6 +10,7 @@ use super::placeholders::{
@@ -42,6 +43,12 @@ pub enum CoreRoute {
@@ -55,10 +62,10 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-            div { class: "flex flex-1 min-h-0 overflow-hidden w-full pt-8",
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use crate::DesktopMode;
+    let titlebar_pad = if try_use_context::<DesktopMode>().is_some() {
+        "pt-bc-8"
+    } else {
+        ""
+    };
+
+            div { class: "flex flex-1 min-h-0 overflow-hidden w-full {titlebar_pad}",
```

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -5,18 +5,16 @@ use crate::components::app_styles::AppStyles;
@@ -50,7 +48,7 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::utils::storage::ClientState;
-
-    let client_state = ClientState::load();
-    let theme = client_state.theme.unwrap_or_default();
-    let theme_str = theme.as_str();
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::use_theme;
+    let theme_ctx = use_theme();
+    let theme_str = theme_ctx.data_theme_attr();
+                    div { class: "flex-1 overflow-y-auto px-bc-2 py-bc-4",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/finance
component=console/finance
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #40 — /console/logs

**树路径：** `UI-only Actions → Console → /console/logs`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-log/src/lib.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/logs
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/logs.rs
│
└─ LogPage re-export
│
▼
FILE: crates/client/crates/client-log/src/lib.rs  [PR CHANGED]
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-log/src/lib.rs`**

HUNKS:
```text
@@ -1,5 +1,5 @@
@@ -160,8 +160,8 @@ pub fn LogPage() -> Element {
@@ -177,7 +177,7 @@ pub fn LogPage() -> Element {
```

BEFORE / REMOVED:
```diff
-    Chip, EmptyState, LevelBadge, PageHeader, SkeletonCard, SkeletonVariant,
-                        button {
-                            class: "btn btn-secondary",
-        div { class: "page-content flex flex-col gap-xxxl",
```

AFTER / ADDED:
```diff
+    BCButton, ButtonVariant, Chip, EmptyState, LevelBadge, PageHeader, SkeletonCard, SkeletonVariant,
+                        BCButton {
+                            variant: ButtonVariant::Secondary,
+        div { class: "page-content flex flex-col gap-bc-8",
```

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -10,6 +10,7 @@ use super::placeholders::{
@@ -42,6 +43,12 @@ pub enum CoreRoute {
@@ -55,10 +62,10 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-            div { class: "flex flex-1 min-h-0 overflow-hidden w-full pt-8",
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use crate::DesktopMode;
+    let titlebar_pad = if try_use_context::<DesktopMode>().is_some() {
+        "pt-bc-8"
+    } else {
+        ""
+    };
+
+            div { class: "flex flex-1 min-h-0 overflow-hidden w-full {titlebar_pad}",
```

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -5,18 +5,16 @@ use crate::components::app_styles::AppStyles;
@@ -50,7 +48,7 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::utils::storage::ClientState;
-
-    let client_state = ClientState::load();
-    let theme = client_state.theme.unwrap_or_default();
-    let theme_str = theme.as_str();
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::use_theme;
+    let theme_ctx = use_theme();
+    let theme_str = theme_ctx.data_theme_attr();
+                    div { class: "flex-1 overflow-y-auto px-bc-2 py-bc-4",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/logs
component=console/logs
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #41 — /console/connect

**树路径：** `UI-only Actions → Console → /console/connect`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-connect/src/lib.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/connect
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/connect.rs
│
└─ ConnectPage re-export
│
▼
FILE: crates/client/crates/client-connect/src/lib.rs  [PR CHANGED]
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-connect/src/lib.rs`**

HUNKS:
```text
@@ -136,14 +136,14 @@ pub fn ConnectPage() -> Element {
@@ -158,7 +158,7 @@ pub fn ConnectPage() -> Element {
@@ -192,7 +192,7 @@ pub fn ConnectPage() -> Element {
@@ -212,7 +212,7 @@ pub fn ConnectPage() -> Element {
@@ -245,8 +245,9 @@ pub fn ConnectPage() -> Element {
@@ -274,51 +275,51 @@ pub fn ConnectPage() -> Element {
```

BEFORE / REMOVED:
```diff
-                    class: "btn-primary",
-        div { class: "page-content flex flex-col bc-gap-7",
-                        div { class: "stat-value bc-text-brand", "1.2 PFlops" }
-                    div { class: "flex flex-col bc-gap-7 mt-xxl",
-                                            class: "btn-secondary",
-                                                    button {
-                                                        class: "btn btn-ghost bc-text-danger font-semibold",
-                        div { class: "mt-xxl border-t",
```

AFTER / ADDED:
```diff
+                    variant: ButtonVariant::Primary,
+        div { class: "page-content flex flex-col gap-bc-7",
+                        div { class: "stat-value text-bc-primary", "1.2 PFlops" }
+                    div { class: "flex flex-col gap-bc-7 mt-bc-6",
+                                            variant: ButtonVariant::Secondary,
+                                                    BCButton {
+                                                        variant: ButtonVariant::Ghost,
+                                                        class: "bc-text-danger font-semibold".to_string(),
```

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -10,6 +10,7 @@ use super::placeholders::{
@@ -42,6 +43,12 @@ pub enum CoreRoute {
@@ -55,10 +62,10 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-            div { class: "flex flex-1 min-h-0 overflow-hidden w-full pt-8",
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use crate::DesktopMode;
+    let titlebar_pad = if try_use_context::<DesktopMode>().is_some() {
+        "pt-bc-8"
+    } else {
+        ""
+    };
+
+            div { class: "flex flex-1 min-h-0 overflow-hidden w-full {titlebar_pad}",
```

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -5,18 +5,16 @@ use crate::components::app_styles::AppStyles;
@@ -50,7 +48,7 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::utils::storage::ClientState;
-
-    let client_state = ClientState::load();
-    let theme = client_state.theme.unwrap_or_default();
-    let theme_str = theme.as_str();
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::use_theme;
+    let theme_ctx = use_theme();
+    let theme_str = theme_ctx.data_theme_attr();
+                    div { class: "flex-1 overflow-y-auto px-bc-2 py-bc-4",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/connect
component=console/connect
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #42 — /console/playground

**树路径：** `UI-only Actions → Console → /console/playground`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/crates/client-playground/src/lib.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/playground
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/playground.rs
│
└─ PlaygroundPage re-export
│
▼
FILE: crates/client/crates/client-playground/src/lib.rs  [PR CHANGED]
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-playground/src/lib.rs`**

HUNKS:
```text
@@ -1,5 +1,5 @@
@@ -316,12 +316,12 @@ pub fn Playground() -> Element {
@@ -382,7 +382,7 @@ pub fn Playground() -> Element {
@@ -417,25 +417,27 @@ pub fn Playground() -> Element {
@@ -455,7 +457,7 @@ pub fn Playground() -> Element {
@@ -481,8 +483,8 @@ pub fn Playground() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::components::PageHeader;
-                button { class: "btn btn-secondary", onclick: on_clear, {t(*lang.read(), "playground.clear")} }
-                button { class: "btn btn-secondary", onclick: on_export, {t(*lang.read(), "playground.export")} }
-        div { class: "page-content flex flex-col flex-1 min-h-0 gap-sm",
-                    div { class: "flex items-center gap-md",
-                    label { class: "bc-option-row",
-                        span { {t(*lang.read(), "playground.stream_response")} }
-                        label { class: "switch",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::components::{BCButton, ButtonVariant, PageHeader};
+                BCButton { variant: ButtonVariant::Secondary, onclick: on_clear, {t(*lang.read(), "playground.clear")} }
+                BCButton { variant: ButtonVariant::Secondary, onclick: on_export, {t(*lang.read(), "playground.export")} }
+        div { class: "page-content flex flex-col flex-1 min-h-0 gap-bc-2",
+                    div { class: "flex items-center gap-bc-3",
+                    div { class: "bc-option-rows",
+                        label { class: "bc-option-row",
+                            span { {t(*lang.read(), "playground.stream_response")} }
```

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -10,6 +10,7 @@ use super::placeholders::{
@@ -42,6 +43,12 @@ pub enum CoreRoute {
@@ -55,10 +62,10 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-            div { class: "flex flex-1 min-h-0 overflow-hidden w-full pt-8",
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use crate::DesktopMode;
+    let titlebar_pad = if try_use_context::<DesktopMode>().is_some() {
+        "pt-bc-8"
+    } else {
+        ""
+    };
+
+            div { class: "flex flex-1 min-h-0 overflow-hidden w-full {titlebar_pad}",
```

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -5,18 +5,16 @@ use crate::components::app_styles::AppStyles;
@@ -50,7 +48,7 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::utils::storage::ClientState;
-
-    let client_state = ClientState::load();
-    let theme = client_state.theme.unwrap_or_default();
-    let theme_str = theme.as_str();
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::use_theme;
+    let theme_ctx = use_theme();
+    let theme_str = theme_ctx.data_theme_attr();
+                    div { class: "flex-1 overflow-y-auto px-bc-2 py-bc-4",
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/playground
component=console/playground
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #43 — /console/:..segments → NotFound

**树路径：** `UI-only Actions → Console → /console/:..segments → NotFound`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/components/layout.rs`, `crates/client/crates/client-shared/src/components/layout.rs`, `crates/client/src/pages/not_found.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/:..segments → NotFound
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs  [PR CHANGED]
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/not_found.rs  [PR CHANGED]
│
└─ NotFoundPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/components/layout.rs`**

HUNKS:
```text
@@ -10,6 +10,7 @@ use super::placeholders::{
@@ -42,6 +43,12 @@ pub enum CoreRoute {
@@ -55,10 +62,10 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-            div { class: "flex flex-1 min-h-0 overflow-hidden w-full pt-8",
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use crate::DesktopMode;
+    let titlebar_pad = if try_use_context::<DesktopMode>().is_some() {
+        "pt-bc-8"
+    } else {
+        ""
+    };
+
+            div { class: "flex flex-1 min-h-0 overflow-hidden w-full {titlebar_pad}",
```

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/components/layout.rs`**

HUNKS:
```text
@@ -5,18 +5,16 @@ use crate::components::app_styles::AppStyles;
@@ -50,7 +48,7 @@ pub fn Layout() -> Element {
```

BEFORE / REMOVED:
```diff
-use burncloud_client_shared::utils::storage::ClientState;
-
-    let client_state = ClientState::load();
-    let theme = client_state.theme.unwrap_or_default();
-    let theme_str = theme.as_str();
-                    div { class: "flex-1 overflow-y-auto px-2 py-4",
```

AFTER / ADDED:
```diff
+use burncloud_client_shared::use_theme;
+    let theme_ctx = use_theme();
+    let theme_str = theme_ctx.data_theme_attr();
+                    div { class: "flex-1 overflow-y-auto px-bc-2 py-bc-4",
```

**FILE: `crates/client/src/pages/not_found.rs`**

HUNKS:
```text
@@ -8,8 +8,8 @@ pub fn NotFoundPage(segments: Vec<String>) -> Element {
```

BEFORE / REMOVED:
```diff
-            h1 { class: "text-display font-bold mb-lg text-bc-text", "404" }
-            p { class: "text-subtitle text-bc-text-secondary mb-xxxl", "页面未找到 (Page Not Found)" }
```

AFTER / ADDED:
```diff
+            h1 { class: "text-large-title font-bold mb-bc-4 text-bc-text", "404" }
+            p { class: "text-subtitle text-bc-text-secondary mb-bc-8", "页面未找到 (Page Not Found)" }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/:..segments → NotFound
component=NotFoundPage
rendered=true
```

### E2E FLOW #44 — /preview/home

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/pages/e2e_preview.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/home
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs  [PR CHANGED]
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/pages/e2e_preview.rs`**

HUNKS:
```text
@@ -0,0 +1,139 @@
```

AFTER / ADDED:
```diff
+//! E2E preview routes (`/preview/*`) — mock data, no login flow.
+//! Compiled only in debug or with `e2e-preview` feature. Production default routes unchanged.
+
+use crate::components::app_styles::AppStyles;
+use crate::components::sidebar::Sidebar;
+use crate::pages::{
+    api::AccessPage, billing::FinancePage, dashboard::Dashboard, home::HomePage, login::LoginPage,
+    models::ChannelPage, monitor::ServiceMonitor, playground::PlaygroundPage,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/home
component=preview/home
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #45 — /preview/login

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/pages/e2e_preview.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/login
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs  [PR CHANGED]
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/pages/e2e_preview.rs`**

HUNKS:
```text
@@ -0,0 +1,139 @@
```

AFTER / ADDED:
```diff
+//! E2E preview routes (`/preview/*`) — mock data, no login flow.
+//! Compiled only in debug or with `e2e-preview` feature. Production default routes unchanged.
+
+use crate::components::app_styles::AppStyles;
+use crate::components::sidebar::Sidebar;
+use crate::pages::{
+    api::AccessPage, billing::FinancePage, dashboard::Dashboard, home::HomePage, login::LoginPage,
+    models::ChannelPage, monitor::ServiceMonitor, playground::PlaygroundPage,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/login
component=preview/login
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #46 — /preview/console/dashboard

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/dashboard`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/pages/e2e_preview.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/dashboard
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs  [PR CHANGED]
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/pages/e2e_preview.rs`**

HUNKS:
```text
@@ -0,0 +1,139 @@
```

AFTER / ADDED:
```diff
+//! E2E preview routes (`/preview/*`) — mock data, no login flow.
+//! Compiled only in debug or with `e2e-preview` feature. Production default routes unchanged.
+
+use crate::components::app_styles::AppStyles;
+use crate::components::sidebar::Sidebar;
+use crate::pages::{
+    api::AccessPage, billing::FinancePage, dashboard::Dashboard, home::HomePage, login::LoginPage,
+    models::ChannelPage, monitor::ServiceMonitor, playground::PlaygroundPage,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/dashboard
component=preview/console/dashboard
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #47 — /preview/console/models

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/models`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/pages/e2e_preview.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/models
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs  [PR CHANGED]
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/pages/e2e_preview.rs`**

HUNKS:
```text
@@ -0,0 +1,139 @@
```

AFTER / ADDED:
```diff
+//! E2E preview routes (`/preview/*`) — mock data, no login flow.
+//! Compiled only in debug or with `e2e-preview` feature. Production default routes unchanged.
+
+use crate::components::app_styles::AppStyles;
+use crate::components::sidebar::Sidebar;
+use crate::pages::{
+    api::AccessPage, billing::FinancePage, dashboard::Dashboard, home::HomePage, login::LoginPage,
+    models::ChannelPage, monitor::ServiceMonitor, playground::PlaygroundPage,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/models
component=preview/console/models
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #48 — /preview/console/access

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/access`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/pages/e2e_preview.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/access
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs  [PR CHANGED]
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/pages/e2e_preview.rs`**

HUNKS:
```text
@@ -0,0 +1,139 @@
```

AFTER / ADDED:
```diff
+//! E2E preview routes (`/preview/*`) — mock data, no login flow.
+//! Compiled only in debug or with `e2e-preview` feature. Production default routes unchanged.
+
+use crate::components::app_styles::AppStyles;
+use crate::components::sidebar::Sidebar;
+use crate::pages::{
+    api::AccessPage, billing::FinancePage, dashboard::Dashboard, home::HomePage, login::LoginPage,
+    models::ChannelPage, monitor::ServiceMonitor, playground::PlaygroundPage,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/access
component=preview/console/access
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #49 — /preview/console/settings

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/settings`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/pages/e2e_preview.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/settings
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs  [PR CHANGED]
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/pages/e2e_preview.rs`**

HUNKS:
```text
@@ -0,0 +1,139 @@
```

AFTER / ADDED:
```diff
+//! E2E preview routes (`/preview/*`) — mock data, no login flow.
+//! Compiled only in debug or with `e2e-preview` feature. Production default routes unchanged.
+
+use crate::components::app_styles::AppStyles;
+use crate::components::sidebar::Sidebar;
+use crate::pages::{
+    api::AccessPage, billing::FinancePage, dashboard::Dashboard, home::HomePage, login::LoginPage,
+    models::ChannelPage, monitor::ServiceMonitor, playground::PlaygroundPage,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/settings
component=preview/console/settings
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #50 — /preview/console/finance

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/finance`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/pages/e2e_preview.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/finance
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs  [PR CHANGED]
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/pages/e2e_preview.rs`**

HUNKS:
```text
@@ -0,0 +1,139 @@
```

AFTER / ADDED:
```diff
+//! E2E preview routes (`/preview/*`) — mock data, no login flow.
+//! Compiled only in debug or with `e2e-preview` feature. Production default routes unchanged.
+
+use crate::components::app_styles::AppStyles;
+use crate::components::sidebar::Sidebar;
+use crate::pages::{
+    api::AccessPage, billing::FinancePage, dashboard::Dashboard, home::HomePage, login::LoginPage,
+    models::ChannelPage, monitor::ServiceMonitor, playground::PlaygroundPage,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/finance
component=preview/console/finance
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #51 — /preview/console/monitor

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/monitor`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/pages/e2e_preview.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/monitor
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs  [PR CHANGED]
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/pages/e2e_preview.rs`**

HUNKS:
```text
@@ -0,0 +1,139 @@
```

AFTER / ADDED:
```diff
+//! E2E preview routes (`/preview/*`) — mock data, no login flow.
+//! Compiled only in debug or with `e2e-preview` feature. Production default routes unchanged.
+
+use crate::components::app_styles::AppStyles;
+use crate::components::sidebar::Sidebar;
+use crate::pages::{
+    api::AccessPage, billing::FinancePage, dashboard::Dashboard, home::HomePage, login::LoginPage,
+    models::ChannelPage, monitor::ServiceMonitor, playground::PlaygroundPage,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/monitor
component=preview/console/monitor
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #52 — /preview/console/playground

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/playground`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/src/pages/e2e_preview.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/playground
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs  [PR CHANGED]
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

**FILE: `crates/client/src/pages/e2e_preview.rs`**

HUNKS:
```text
@@ -0,0 +1,139 @@
```

AFTER / ADDED:
```diff
+//! E2E preview routes (`/preview/*`) — mock data, no login flow.
+//! Compiled only in debug or with `e2e-preview` feature. Production default routes unchanged.
+
+use crate::components::app_styles::AppStyles;
+use crate::components::sidebar::Sidebar;
+use crate::pages::{
+    api::AccessPage, billing::FinancePage, dashboard::Dashboard, home::HomePage, login::LoginPage,
+    models::ChannelPage, monitor::ServiceMonitor, playground::PlaygroundPage,
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/playground
component=preview/console/playground
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #53 — i18n context

**树路径：** `UI-only Actions → Local UI State → i18n context`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ App root initialization
│    └─ state/context: i18n context
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Create initial state
│    ├─ derive default/configured value
│    └─ store in Dioxus signal/context
│
├─ Provide context
│    ├─ descendant components can read/subscribe
│    └─ current render reads latest value
│
├─ Update path
│    └─ DECISION: UI event changes this state?
│         ├─ NO → keep current value
│         └─ YES
│              ├─ mutate signal/context
│              └─ mark dependent subtree dirty
│
├─ Re-render
│    └─ Dioxus reconciles affected component tree
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI EVENT LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```json
{
  "locale": "zh-CN",
  "fallback_locale": "en-US",
  "ready": true
}
```

### E2E FLOW #54 — Toast state / ToastContainer

**树路径：** `UI-only Actions → Local UI State → Toast state / ToastContainer`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ App root initialization
│    └─ state/context: Toast state / ToastContainer
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Create initial state
│    ├─ derive default/configured value
│    └─ store in Dioxus signal/context
│
├─ Provide context
│    ├─ descendant components can read/subscribe
│    └─ current render reads latest value
│
├─ Update path
│    └─ DECISION: UI event changes this state?
│         ├─ NO → keep current value
│         └─ YES
│              ├─ mutate signal/context
│              └─ mark dependent subtree dirty
│
├─ Re-render
│    └─ Dioxus reconciles affected component tree
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI EVENT LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```json
{
  "visible": true,
  "level": "success",
  "message": "操作成功"
}
```

### E2E FLOW #55 — Auth context

**树路径：** `UI-only Actions → Local UI State → Auth context`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ App root initialization
│    └─ state/context: Auth context
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Create initial state
│    ├─ derive default/configured value
│    └─ store in Dioxus signal/context
│
├─ Provide context
│    ├─ descendant components can read/subscribe
│    └─ current render reads latest value
│
├─ Update path
│    └─ DECISION: UI event changes this state?
│         ├─ NO → keep current value
│         └─ YES
│              ├─ mutate signal/context
│              └─ mark dependent subtree dirty
│
├─ Re-render
│    └─ Dioxus reconciles affected component tree
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI EVENT LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```json
{
  "authenticated": true,
  "user_id": 10001,
  "username": "demo_user"
}
```

### E2E FLOW #56 — Theme state

**树路径：** `UI-only Actions → Local UI State → Theme state`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ App root initialization
│    └─ state/context: Theme state
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Create initial state
│    ├─ derive default/configured value
│    └─ store in Dioxus signal/context
│
├─ Provide context
│    ├─ descendant components can read/subscribe
│    └─ current render reads latest value
│
├─ Update path
│    └─ DECISION: UI event changes this state?
│         ├─ NO → keep current value
│         └─ YES
│              ├─ mutate signal/context
│              └─ mark dependent subtree dirty
│
├─ Re-render
│    └─ Dioxus reconciles affected component tree
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI EVENT LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```json
{
  "theme": "system",
  "resolved": "dark"
}
```

### E2E FLOW #57 — window maximize

**树路径：** `UI-only Actions → Desktop UI → window maximize`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Desktop event/state input
│    └─ action: window maximize
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Platform branch
│    └─ DECISION: current platform/runtime supports desktop action?
│         ├─ NO → skip/unsupported branch → END
│         └─ YES → obtain desktop window/tray handle
│
├─ Current UI state
│    ├─ read visibility/focus/maximized/tray state as needed
│    └─ decide desired state
│
├─ Apply desktop side effect
│    ├─ maximize / show / hide / focus / tray startup
│    └─ DECISION: OS/window operation succeeds?
│         ├─ NO → log/ignore according to UI path
│         └─ YES → state visible to user
│
├─ Event loop handoff
│    └─ return control to Dioxus/desktop event loop
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
window.maximized=true
window.visible=true
```

### E2E FLOW #58 — Windows tray startup

**树路径：** `UI-only Actions → Desktop UI → Windows tray startup`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Desktop event/state input
│    └─ action: Windows tray startup
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Platform branch
│    └─ DECISION: current platform/runtime supports desktop action?
│         ├─ NO → skip/unsupported branch → END
│         └─ YES → obtain desktop window/tray handle
│
├─ Current UI state
│    ├─ read visibility/focus/maximized/tray state as needed
│    └─ decide desired state
│
├─ Apply desktop side effect
│    ├─ maximize / show / hide / focus / tray startup
│    └─ DECISION: OS/window operation succeeds?
│         ├─ NO → log/ignore according to UI path
│         └─ YES → state visible to user
│
├─ Event loop handoff
│    └─ return control to Dioxus/desktop event loop
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
tray.visible=true
tray.status=running
```

### E2E FLOW #59 — show / hide / focus

**树路径：** `UI-only Actions → Desktop UI → show / hide / focus`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`, `crates/client/crates/client-shared/src/i18n.rs`

```text
START
│
├─ Desktop event/state input
│    └─ action: show / hide / focus
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Platform branch
│    └─ DECISION: current platform/runtime supports desktop action?
│         ├─ NO → skip/unsupported branch → END
│         └─ YES → obtain desktop window/tray handle
│
├─ Current UI state
│    ├─ read visibility/focus/maximized/tray state as needed
│    └─ decide desired state
│
├─ Apply desktop side effect
│    ├─ maximize / show / hide / focus / tray startup
│    └─ DECISION: OS/window operation succeeds?
│         ├─ NO → log/ignore according to UI path
│         └─ YES → state visible to user
│
├─ Event loop handoff
│    └─ return control to Dioxus/desktop event loop
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs  [PR CHANGED]
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/crates/client-shared/src/i18n.rs`**

HUNKS:
```text
@@ -37,6 +37,11 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -532,6 +537,8 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -756,6 +763,14 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -887,6 +902,12 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
@@ -904,6 +925,34 @@ pub fn t(lang: Language, key: &'static str) -> &'static str {
```

AFTER / ADDED:
```diff
+        (Language::Zh, "deploy.title") => "模型部署",
+        (Language::En, "deploy.title") => "Model Deployment",
+        (Language::Zh, "deploy.subtitle") => "从多种来源部署新模型渠道",
+        (Language::En, "deploy.subtitle") => "Deploy new models from various sources.",
+
+        (Language::Zh, "access.subtitle") => "管理 API 密钥与访问配额",
+        (Language::En, "access.subtitle") => "Manage API keys and access quotas",
+        (Language::Zh, "settings.subtitle") => "配置系统偏好与外观",
```

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -19,6 +19,12 @@ use crate::pages::{
@@ -41,6 +47,33 @@ pub enum Route {
@@ -76,6 +109,8 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+#[cfg(any(debug_assertions, feature = "e2e-preview"))]
+use crate::pages::e2e_preview::{
+    PreviewAccessPage, PreviewDashboardPage, PreviewFinancePage, PreviewHomePage,
+    PreviewLoginPage, PreviewModelsPage, PreviewMonitorPage, PreviewPlaygroundPage,
+    PreviewSettingsPage,
+};
+    #[cfg(any(debug_assertions, feature = "e2e-preview"))]
+    #[route("/preview/home")]
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
show_window=true
window.visible=true
window.focused=true
```

## 未映射 Changed Files

- `.github/workflows/client-ui.yml`
- `Cargo.toml`
- `crates/client/Cargo.toml`
- `crates/client/crates/client-api/docs/06-css-design-system.md`
- `crates/client/crates/client-dashboard/src/dashboard.rs`
- `crates/client/crates/client-deploy/src/deploy.rs`
- `crates/client/crates/client-models/src/models.rs`
- `crates/client/crates/client-monitor/src/monitor.rs`
- `crates/client/crates/client-settings/src/groups.rs`
- `crates/client/crates/client-settings/src/settings.rs`
- `crates/client/crates/client-settings/src/tokens.rs`
- `crates/client/crates/client-shared/Cargo.toml`
- `crates/client/crates/client-shared/scripts/split_styles.py`
- `crates/client/crates/client-shared/src/components/bc_button.rs`
- `crates/client/crates/client-shared/src/components/bc_card.rs`
- `crates/client/crates/client-shared/src/components/bc_magic_button.rs`
- `crates/client/crates/client-shared/src/components/bc_modal.rs`
- `crates/client/crates/client-shared/src/components/bc_table.rs`
- `crates/client/crates/client-shared/src/components/console_sidebar.rs`
- `crates/client/crates/client-shared/src/components/glass_card.rs`
- `crates/client/crates/client-shared/src/components/page_header.rs`
- `crates/client/crates/client-shared/src/components/schema_form.rs`
- `crates/client/crates/client-shared/src/components/schema_table.rs`
- `crates/client/crates/client-shared/src/components/standard_crud_page.rs`
- `crates/client/crates/client-shared/src/components/title_bar.rs`
- `crates/client/crates/client-shared/src/components/toast.rs`
- `crates/client/crates/client-shared/src/e2e_mock/fixtures.rs`
- `crates/client/crates/client-shared/src/e2e_mock/mod.rs`
- `crates/client/crates/client-shared/src/e2e_mock/shell.rs`
- `crates/client/crates/client-shared/src/lib.rs`
- `crates/client/crates/client-shared/src/services/billing_service.rs`
- `crates/client/crates/client-shared/src/services/channel_service.rs`
- `crates/client/crates/client-shared/src/services/log_service.rs`
- `crates/client/crates/client-shared/src/services/monitor_service.rs`
- `crates/client/crates/client-shared/src/services/token_service.rs`
- `crates/client/crates/client-shared/src/services/usage_service.rs`
- `crates/client/crates/client-shared/src/styles.rs`
- `crates/client/crates/client-shared/src/styles/00_burncloud_design_system_apple_inspired.css`
- `crates/client/crates/client-shared/src/styles/01_base_element_styles.css`
- `crates/client/crates/client-shared/src/styles/02_acrylic_glass_effects.css`
- `crates/client/crates/client-shared/src/styles/03_card_styles.css`
- `crates/client/crates/client-shared/src/styles/04_button_styles.css`
- `crates/client/crates/client-shared/src/styles/05_input_styles.css`
- `crates/client/crates/client-shared/src/styles/06_progress_bar.css`
- `crates/client/crates/client-shared/src/styles/07_status_indicators.css`
- `crates/client/crates/client-shared/src/styles/08_animations.css`
- `crates/client/crates/client-shared/src/styles/09_layout_helpers_burncloud_specific_not_tailwind_d.css`
- `crates/client/crates/client-shared/src/styles/10_typography.css`
- `crates/client/crates/client-shared/src/styles/11_app_layout.css`
- `crates/client/crates/client-shared/src/styles/12_navigation.css`
- `crates/client/crates/client-shared/src/styles/13_model_cards_metrics.css`
- `crates/client/crates/client-shared/src/styles/14_log_viewer.css`
- `crates/client/crates/client-shared/src/styles/15_macos_style_scrollbars_hidden_by_default_show_on.css`
- `crates/client/crates/client-shared/src/styles/16_macos_window_control_colors.css`
- `crates/client/crates/client-shared/src/styles/17_scrollbar_theme.css`
- `crates/client/crates/client-shared/src/styles/18_login_register_page_50_50_split_screen.css`
- `crates/client/crates/client-shared/src/styles/19_component_utility_classes_migrated_from_inline_s.css`
- `crates/client/crates/client-shared/src/styles/20_landing_marketing_page.css`
- `crates/client/crates/client-shared/src/styles/21_landing_page_semantic_component_classes_home_rs.css`
- `crates/client/crates/client-shared/src/styles/22_legacy_daisyui_class_aliases_migrate_to_bc_over.css`
- `crates/client/crates/client-shared/src/styles/23_page_level_helpers_from_design_kit_styles_css.css`
- `crates/client/crates/client-shared/src/styles/24_semantic_component_classes_register_users_api_mi.css`
- `crates/client/crates/client-shared/src/styles/25_token_compliant_utility_classes_issue_179_migrat.css`
- `crates/client/crates/client-shared/src/styles/README.md`
- `crates/client/crates/client-shared/src/styles/mod.rs`
- `crates/client/crates/client-shared/src/theme_context.rs`
- `crates/client/scripts/check-ui-conventions.ps1`
- `crates/client/scripts/check-ui-conventions.sh`
- `crates/client/src/pages/mod.rs`
- `crates/client/tailwind.config.js`
- `crates/loops/Cargo.toml`
- `crates/loops/README.md`
- `crates/loops/acceptance/aesthetic-optimize-acceptance.md`
- `crates/loops/acceptance/aesthetic-optimize-agent-prompt.md`
- `crates/loops/acceptance/css-optimize-acceptance.md`
- `crates/loops/acceptance/css-optimize-agent-prompt.md`
- `crates/loops/acceptance/jobs-aesthetic-agent-prompt.md`
- `crates/loops/src/cli.rs`
- `crates/loops/src/gates/aesthetic_review.rs`
- `crates/loops/src/gates/cargo_test.rs`
- `crates/loops/src/gates/css_naming.rs`
- `crates/loops/src/gates/mod.rs`
- `crates/loops/src/gates/ui_conventions.rs`
- `crates/loops/src/lib.rs`
- `crates/loops/src/lock.rs`
- `crates/loops/src/log.rs`
- `crates/loops/src/loops/css_optimize.rs`
- `crates/loops/src/loops/jobs_aesthetic.rs`
- `crates/loops/src/loops/mod.rs`
- `crates/loops/src/page_progress.rs`
- `crates/loops/src/paths.rs`
- `crates/loops/src/process.rs`
- `crates/loops/src/prompt/jobs_aesthetic.rs`
- `crates/loops/src/prompt/mod.rs`
- `crates/loops/src/server.rs`
- `crates/loops/src/state.rs`
- `crates/tests/tests/common/mod.rs`
- `crates/tests/tests/e2e/aesthetic_acceptance.rs`
- `crates/tests/tests/e2e/agent_browser.rs`
- `crates/tests/tests/e2e/css_visual_acceptance.rs`
- `crates/tests/tests/e2e/mod.rs`
- `docs/CLAUDE.md`
- `docs/ui/README.md`
- `docs/ui/components.md`
- `docs/ui/naming.md`
- `docs/ui/pages.md`
- `docs/ui/system.md`
- `docs/ui/tokens.md`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
