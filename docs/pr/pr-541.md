---
title: "PR #541 — feat(supply): add model resolver contract and fake"
slug: /pr/pr-541
---

# PR #541 — feat(supply): add model resolver contract and fake

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-17T02:01:41Z`  
> **Base：** `main@05632cb6c930`  
> **Head：** `feat/supply-model-resolver-contract@bbc5a478b4b3`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME`
- **Changed Files：** `4`
- **Additions / Deletions：** `+83 / -18`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `4`

**PR 说明摘要：** ## Phase 0.5 — Fake Skeleton Completion, step 3 Moves the remaining fake-resolution responsibility into the Supply domain instead of `platform/node`. Adds to `crates/supply/service-models`: - `ModelResolutionRequest` - `ResolvedModel` - `ModelResolutionError` - `ModelResolver` trait - `FakeModelResolver` The fake returns a deterministic GGUF + `llama.cpp` choice for a requested model such as `qwen-4b`. Boundary rules: - Supply owns model/artifact/runtime variant choice. - Platform still owns only machine/runtime primitives. - Resolver does not download artifacts, install runtimes, start processes, or route requests. - No second model catalog, downloader, router, or database is introduced. Next step: wire this Supply-owned fake resolver into the application-level Node golden path so `Resolved` evidence is emitted only after `ModelResolver::resolve()` succeeds.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/supply/service-models/Cargo.toml` | modified | 2 | 0 | 0 |
| `crates/supply/service-models/src/lib.rs` | modified | 6 | 18 | 0 |
| `crates/supply/service-models/src/resolver.rs` | added | 56 | 0 | 0 |
| `crates/supply/service-models/tests/resolver.rs` | added | 19 | 0 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/supply/service-models/Cargo.toml`
- `crates/supply/service-models/src/lib.rs`
- `crates/supply/service-models/src/resolver.rs`
- `crates/supply/service-models/tests/resolver.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
