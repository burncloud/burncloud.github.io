---
title: "PR #540 — test(node): wire fake preparation capabilities into golden path"
slug: /pr/pr-540
---

# PR #540 — test(node): wire fake preparation capabilities into golden path

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-17T01:30:39Z`  
> **Base：** `main@47adce14051e`  
> **Head：** `test/node-wire-fake-preparation@de0069abe5a2`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `TEST ONLY`
- **Changed Files：** `8`
- **Additions / Deletions：** `+295 / -13`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `8`

**PR 说明摘要：** ## Phase 0.5 — Fake Skeleton Completion, step 2 Turns the newly added ports into real fake-stage calls in the Node golden path. The test no longer claims artifact/runtime/readiness work happened without first calling the corresponding fake capability: - `FakeArtifactPreparer::prepare` -&gt; then `ArtifactPrepared` - `FakeRuntimePreparer::prepare` -&gt; then `RuntimePrepared` - `FakeProcessManager::start` -&gt; then `ProcessStarted` - `FakeReadinessProbe::wait_ready` -&gt; then `ReadinessVerified` - `FakeHealthProbe::is_healthy` is kept as a separate observation Also adds focused tests proving: - preparation ports are replaceable by independent implementations - a failed artifact preparation does not advance reconciler state - fakes remain side-effect free and return `/fake/...` paths - llama.cpp remains behind the runtime preparation port Important boundary: `Resolved` is still external evidence because model/variant selection must not be owned by `platform/node`. Router attachment remains covered by the existing application/traffic integration tests.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/platform/node/tests/evidence_after_capability.rs` | added | 46 | 0 | 0 |
| `crates/platform/node/tests/fake_failure_gate.rs` | added | 34 | 0 | 0 |
| `crates/platform/node/tests/golden_path.rs` | modified | 75 | 13 | 0 |
| `crates/platform/node/tests/health_gate.rs` | added | 13 | 0 | 0 |
| `crates/platform/node/tests/no_real_side_effects.rs` | added | 19 | 0 | 0 |
| `crates/platform/node/tests/preparation_contracts.rs` | added | 52 | 0 | 0 |
| `crates/platform/node/tests/preparation_fakes.rs` | added | 42 | 0 | 0 |
| `crates/platform/node/tests/runtime_is_internal.rs` | added | 14 | 0 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/platform/node/tests/evidence_after_capability.rs`
- `crates/platform/node/tests/fake_failure_gate.rs`
- `crates/platform/node/tests/golden_path.rs`
- `crates/platform/node/tests/health_gate.rs`
- `crates/platform/node/tests/no_real_side_effects.rs`
- `crates/platform/node/tests/preparation_contracts.rs`
- `crates/platform/node/tests/preparation_fakes.rs`
- `crates/platform/node/tests/runtime_is_internal.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
