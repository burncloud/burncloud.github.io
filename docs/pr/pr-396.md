---
title: "PR #396 — Refine BurnCloud console UX around operator workflows"
slug: /pr/pr-396
---

# PR #396 — Refine BurnCloud console UX around operator workflows

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-08-12T12:34:39Z`  
> **Base：** `main@7dce6ec09dd8`  
> **Head：** `agent/product-ui-rationalization@85633f55653f`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `17`
- **Additions / Deletions：** `+3140 / -1191`
- **受影响的完整 E2E Flow：** `55`
- **未映射 changed files：** `16`

**PR 说明摘要：** ## Goal This PR is a product/UX rationalization pass on top of merged #394. It keeps the real backend wiring introduced there, but changes the console so it follows how an operator actually sets up, verifies, observes, and safely manages BurnCloud. The product rule for this pass is simple: every page should answer **why am I here, what is the current state, and what should I do next?** Backend implementation details should stay secondary unless they are genuinely needed for an advanced operation. ## Product principles applied - show system conclusions before implementation details - order navigation by real task sequence - make prerequisites and next actions explicit - translate backend fields into user-facing concepts - separate customer accounts from staff/operator identities - keep dangerous operations away from routine actions and require confirmation - use empty states to guide users into the next valid setup step - preserve real BurnCloud API behavior instead of inventing unsupported features - never claim a runtime state that the UI has not actually verified ## Main changes ### Navigation / console chrome - reorganized around **Workspace → Traffic Setup → Observe → Access & Customers → Security & System** - Traffic Setup now follows **Providers → Models → Routes → Playground**, matching the real setup/test sequence - Observe follows **Logs → Evaluation → Billing** - top …

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `.github/workflows/client-ui.yml` | modified | 3 | 0 | 0 |
| `crates/client/scripts/check-product-ux.sh` | added | 114 | 0 | 0 |
| `crates/client/src/app.rs` | modified | 1 | 0 | 55 |
| `crates/client/src/critical_pages/auth.rs` | modified | 200 | 267 | 0 |
| `crates/client/src/critical_pages/customers_portable.rs` | modified | 161 | 157 | 0 |
| `crates/client/src/critical_pages/dashboard.rs` | modified | 219 | 93 | 0 |
| `crates/client/src/functional_layout.rs` | modified | 31 | 29 | 0 |
| `crates/client/src/functional_pages/access_live.rs` | modified | 474 | 104 | 0 |
| `crates/client/src/functional_pages/analytics.rs` | modified | 118 | 119 | 0 |
| `crates/client/src/functional_pages/analytics_full.rs` | modified | 151 | 47 | 0 |
| `crates/client/src/functional_pages/catalog.rs` | modified | 220 | 75 | 0 |
| `crates/client/src/functional_pages/guardrails_live.rs` | modified | 313 | 28 | 0 |
| `crates/client/src/functional_pages/logs_full.rs` | modified | 125 | 49 | 0 |
| `crates/client/src/functional_pages/playground_live.rs` | modified | 269 | 19 | 0 |
| `crates/client/src/functional_pages/providers.rs` | modified | 387 | 142 | 0 |
| `crates/client/src/functional_pages/settings.rs` | modified | 91 | 62 | 0 |
| `crates/client/src/product_ui.css` | added | 263 | 0 | 0 |

## Affected E2E Impact Matrix

| # | 完整 E2E / Entry Point | 类型 | PR 命中的源码文件 |
|---:|---|---|---|
| 1 | `GET /` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 2 | `GET /home` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 3 | `GET /login` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 4 | `GET /register` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 5 | `GET /forgot-password` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 6 | `GET /reset-password` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 7 | `GET /console` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 8 | `GET /console/` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 9 | `GET /console/{*path}` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 10 | `GET /favicon.ico` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 11 | `GET /preview/home` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 12 | `GET /preview/login` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 13 | `GET /preview/console` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 14 | `GET /preview/console/` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 15 | `GET /preview/console/{*path}` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 16 | `GET /ws` | HTTP / API / Web UI / LiveView / WebSocket | `crates/client/src/app.rs` |
| 17 | `burncloud` | CLI / Executables / burncloud | `crates/client/src/app.rs` |
| 18 | `client` | CLI / Executables / burncloud | `crates/client/src/app.rs` |
| 19 | `burncloud-client` | CLI / Executables / Workspace Binaries | `crates/client/src/app.rs` |
| 20 | `Windows tray thread` | Background Jobs / Async Side Effects / Desktop Background Work | `crates/client/src/app.rs` |
| 21 | `Show-window poll loop` | Background Jobs / Async Side Effects / Desktop Background Work | `crates/client/src/app.rs` |
| 22 | `/` | UI-only Actions / Guest / Public | `crates/client/src/app.rs` |
| 23 | `/home` | UI-only Actions / Guest / Public | `crates/client/src/app.rs` |
| 24 | `/login` | UI-only Actions / Guest / Public | `crates/client/src/app.rs` |
| 25 | `/register` | UI-only Actions / Guest / Public | `crates/client/src/app.rs` |
| 26 | `/forgot-password` | UI-only Actions / Guest / Public | `crates/client/src/app.rs` |
| 27 | `/reset-password?:token` | UI-only Actions / Guest / Public | `crates/client/src/app.rs` |
| 28 | `/console/dashboard` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 29 | `/console/deploy` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 30 | `/console/monitor` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 31 | `/console/access` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 32 | `/console/models` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 33 | `/console/users` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 34 | `/console/settings` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 35 | `/console/finance` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 36 | `/console/logs` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 37 | `/console/connect` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 38 | `/console/playground` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 39 | `/console/:..segments → NotFound` | UI-only Actions / Console | `crates/client/src/app.rs` |
| 40 | `/preview/home` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 41 | `/preview/login` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 42 | `/preview/console/dashboard` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 43 | `/preview/console/models` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 44 | `/preview/console/access` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 45 | `/preview/console/settings` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 46 | `/preview/console/finance` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 47 | `/preview/console/monitor` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 48 | `/preview/console/playground` | UI-only Actions / Debug / e2e-preview | `crates/client/src/app.rs` |
| 49 | `i18n context` | UI-only Actions / Local UI State | `crates/client/src/app.rs` |
| 50 | `Toast state / ToastContainer` | UI-only Actions / Local UI State | `crates/client/src/app.rs` |
| 51 | `Auth context` | UI-only Actions / Local UI State | `crates/client/src/app.rs` |
| 52 | `Theme state` | UI-only Actions / Local UI State | `crates/client/src/app.rs` |
| 53 | `window maximize` | UI-only Actions / Desktop UI | `crates/client/src/app.rs` |
| 54 | `Windows tray startup` | UI-only Actions / Desktop UI | `crates/client/src/app.rs` |
| 55 | `show / hide / focus` | UI-only Actions / Desktop UI | `crates/client/src/app.rs` |

## 完整受影响 E2E Request Flow

### E2E FLOW #1 — GET /

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #2 — GET /home

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /home
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #3 — GET /login

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /login
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #4 — GET /register

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /register`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /register
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #5 — GET /forgot-password

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /forgot-password`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /forgot-password
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #6 — GET /reset-password

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /reset-password`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /reset-password
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #7 — GET /console

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /console`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /console
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #8 — GET /console/

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /console/`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /console/
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #9 — GET /console/&#123;*path&#125;

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /console/{*path}`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /console/{*path}
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #10 — GET /favicon.ico

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /favicon.ico`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /favicon.ico
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: image/x-icon
Content-Length: 5430

<binary favicon bytes>
```

### E2E FLOW #11 — GET /preview/home

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/home
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #12 — GET /preview/login

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/login
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #13 — GET /preview/console

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/console`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/console
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #14 — GET /preview/console/

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/console/`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/console/
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #15 — GET /preview/console/&#123;*path&#125;

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /preview/console/{*path}`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /preview/console/{*path}
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView/static route candidate
│         └─ NO → other route/fallback
│
├─ LiveView feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → LiveView route unavailable; routing continues/falls back
│         └─ YES → match LiveView Router
│
▼
FILE: crates/client/src/lib.rs
│
├─ HTTP shell/static handler
│    ├─ route shell / preview / favicon according to path
│    └─ DECISION: requested LiveView/static route recognized?
│         ├─ NO → route miss
│         └─ YES → construct HTML/static response
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Client route model
│    ├─ Dioxus Route enum represents browser-side view
│    ├─ App contexts: auth/theme/i18n/toast
│    └─ page-specific component selected after client session establishes
│
├─ Follow-up interactive transport
│    ├─ initial HTTP response delivers shell
│    └─ interactive events move to /ws LiveView session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
     └─ Browser receives HTML/static shell; UI lifecycle continues separately
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 200 OK
Content-Type: text/html; charset=utf-8

<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>BurnCloud</title></head>
  <body><div id="main">Dioxus LiveView shell</div></body>
</html>
```

### E2E FLOW #16 — GET /ws

**树路径：** `HTTP / API → Web UI / LiveView / WebSocket → GET /ws`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ 调用方与输入边界
│    ├─ Actor: User / SDK / Browser / Operator
│    ├─ Entry: GET /ws
│    ├─ Input sources
│    │    ├─ Method + URI path
│    │    ├─ Query string（如有）
│    │    ├─ HTTP headers
│    │    └─ Request body（如有）
│    └─ DECISION: TCP/HTTP 请求能否到达 BurnCloud listener?
│         ├─ NO  → 网络层失败；应用代码未执行 → END
│         └─ YES → 进入 Axum
│
▼
FILE: crates/server/src/lib.rs
│
├─ 统一 HTTP Server
│    ├─ start_server() 已在进程启动时完成
│    │    ├─ database 初始化
│    │    ├─ RouterDatabase::init()
│    │    ├─ UserDatabase::init()
│    │    ├─ create_app(...)
│    │    ├─ TcpListener::bind(...)
│    │    └─ axum::serve(listener, app)
│    ├─ 当前请求进入 Unified Axum App
│    └─ 全局 middleware
│         ├─ CORS
│         ├─ TraceLayer
│         ├─ SetRequestIdLayer
│         └─ PropagateRequestIdLayer
│
├─ 顶层 Route 决策
│    └─ DECISION: Unified App 是否已有显式/合并路由命中当前 Method + Path?
│         ├─ YES → merged LiveView router
│         └─ NO → fallback routing
│
├─ Feature gate
│    └─ DECISION: enable_liveview == true?
│         ├─ NO → /ws not served by LiveView path
│         └─ YES → websocket route available
│
▼
FILE: crates/client/src/lib.rs
│
├─ WebSocket upgrade
│    ├─ validate Upgrade/Connection/Sec-WebSocket-* headers
│    └─ DECISION: WebSocket handshake valid?
│         ├─ NO → HTTP handshake error → END
│         └─ YES → 101 Switching Protocols
│
├─ LiveView session
│    ├─ create/attach Dioxus LiveView session
│    ├─ receive browser UI events
│    ├─ update Virtual DOM/server-side UI state
│    └─ send render patches/messages to browser
│
├─ Connection loop
│    └─ DECISION: socket still connected?
│         ├─ YES → await next UI message → repeat
│         └─ NO → cleanup session
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```http
HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Accept: <computed-value>

# 随后进入双向 WebSocket / LiveView 消息通道
```

### E2E FLOW #17 — burncloud

**树路径：** `CLI / Executables → burncloud → burncloud`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Shell / OS: burncloud（无参数）
│
▼
FILE: src/main.rs
│
├─ main()
│    ├─ dotenvy::dotenv()
│    ├─ ensure_master_key()
│    ├─ init_logging()
│    └─ match args == [binary]
│
├─ DECISION: target_os == windows?
│    ├─ YES
│    │    ├─ std::thread::spawn(background server thread)
│    │    │    ├─ tokio::runtime::Runtime::new()
│    │    │    ├─ HOST / PORT
│    │    │    └─ burncloud_server::start_server(host, port, false)
│    │    │         └─ enable_liveview=false：后台 Server 不挂 LiveView Router
│    │    └─ main thread → burncloud_client::launch_gui_with_tray()
│    │
│    └─ NO（Linux/macOS 等）
│         ├─ print Headless Mode startup line
│         └─ run_async_server()
│              └─ burncloud_server::start_server(host, port, true)
│
├─ Windows server thread 后续主链与 `burncloud server` 的 start_server/create_app/create_router_app 相同
├─ Non-Windows 后续主链与 `burncloud server` 完全相同
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Windows GUI branch: launch_gui_with_tray()
│    ├─ WindowBuilder
│    ├─ Config::new().with_window(...).with_data_directory(...)
│    └─ LaunchBuilder::desktop().launch(AppWithTray)
│
├─ AppWithTray()
│    ├─ DesktopMode context
│    ├─ maximize window
│    ├─ std::thread::spawn(start_tray)
│    ├─ spawn async show-window poll loop
│    └─ render App()
│
└─ App()
     ├─ use_init_i18n()
     ├─ use_init_toast()
     ├─ use_init_auth()
     ├─ use_init_theme()
     └─ Router<Route>
│
▼
END / LONG-RUNNING SERVER + UI LOOPS
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
# 非 Windows
Starting BurnCloud Server with LiveView (Headless Mode)...
Unified Gateway listening on 127.0.0.1:3000
- Dashboard: http://127.0.0.1:3000/
- LLM API:   http://127.0.0.1:3000/v1/...

# Windows
background_server_thread=running (enable_liveview=false)
desktop_gui_event_loop=running
system_tray_thread=running
```

### E2E FLOW #18 — client

**树路径：** `CLI / Executables → burncloud → client`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Shell: burncloud client
│
▼
FILE: src/main.rs
│
├─ main()
│    ├─ dotenvy::dotenv()
│    ├─ ensure_master_key()
│    ├─ init_logging()
│    └─ match subcommand == "client"
│
├─ DECISION: target_os == windows?
│    ├─ YES → burncloud_client::launch_gui_with_tray()
│    └─ NO
│         ├─ println("Desktop GUI is only available on Windows.")
│         ├─ println("On Linux, use 'burncloud server' ...")
│         └─ return Ok(()) → END
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ launch_gui_with_tray()
│    ├─ WindowBuilder::new()
│    ├─ configure title / size / resizable / decorations
│    ├─ load Windows icon when available
│    ├─ temp_dir()/burncloud_webview_data
│    ├─ Config::new().with_window(...).with_data_directory(...)
│    └─ LaunchBuilder::desktop().with_cfg(config).launch(AppWithTray)
│
├─ AppWithTray()
│    ├─ use_context_provider(DesktopMode)
│    ├─ use_window()
│    ├─ use_effect → set_maximized(true)
│    ├─ std::thread::spawn → start_tray()
│    ├─ use_effect → spawn async 100ms show-window poll
│    │    └─ should_show_window() → set_visible / set_focus
│    └─ rsx! { App {} }
│
├─ App()
│    ├─ use_init_i18n()
│    ├─ use_init_toast()
│    ├─ use_init_auth()
│    ├─ use_init_theme()
│    ├─ ToastContainer
│    └─ Router<Route>
│
▼
END / DESKTOP EVENT LOOP
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
# Windows
BurnCloud desktop window + tray enter the long-running GUI event loop.

# 非 Windows（源码真实 stdout）
Desktop GUI is only available on Windows.
On Linux, use 'burncloud server' to start the web dashboard.
```

### E2E FLOW #19 — burncloud-client

**树路径：** `CLI / Executables → Workspace Binaries → burncloud-client`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ OS process launch
│    ├─ executable: burncloud-client
│    ├─ argv / cwd / environment inherited from OS
│    └─ DECISION: executable can be loaded/launched?
│         ├─ NO → OS-level error → END
│         └─ YES → main()
│
▼
FILE: crates/client/src/main.rs
│
├─ main() initialization
│    ├─ initialize executable-specific runtime/services
│    ├─ parse any supported arguments
│    └─ DECISION: platform/arguments/initialization valid?
│         ├─ NO → print/return error → process exit
│         └─ YES → continue
│
├─ Runtime work
│    ├─ create client/download/loop/tray structures as applicable
│    ├─ start event loop or execute one-shot job
│    └─ DECISION: long-running executable?
│         ├─ YES → enter event/service loop
│         └─ NO → produce output and exit
│
├─ Error boundary
│    └─ runtime error → log/return non-success according to executable implementation
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / RUNNING LOOP
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
BurnCloud Client
ui=initialized
route=/
status=running
```

### E2E FLOW #20 — Windows tray thread

**树路径：** `Background Jobs / Async Side Effects → Desktop Background Work → Windows tray thread`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START / TRIGGER
│
├─ Trigger source
│    ├─ startup spawn / request-time tokio::spawn / manager restoration / channel message
│    └─ task: Windows tray thread
│
├─ Spawn / registration
│    ├─ parent runtime creates async task/thread/loop
│    └─ DECISION: spawn/runtime handle available?
│         ├─ NO → task never starts; parent may log error
│         └─ YES → task owns/borrows required shared state
│
├─ Wait boundary
│    ├─ timer sleep / mpsc receive / polling interval / restored work item
│    └─ DECISION: trigger/event available?
│         ├─ NO → continue waiting
│         └─ YES → one iteration begins
│
├─ Input snapshot
│    ├─ read latest runtime/DB/request-derived state needed by job
│    └─ freeze iteration context
│
├─ Core job operation
│    ├─ execute Windows tray thread
│    └─ may call DB / HTTP / filesystem / runtime service depending on task
│
├─ Operation result
│    └─ DECISION: iteration succeeds?
│         ├─ NO
│         │    ├─ log/record failure
│         │    ├─ preserve parent request availability when task is fail-open
│         │    └─ decide retry on next event/interval
│         └─ YES
│              ├─ update in-memory state and/or persistent state
│              └─ emit success telemetry/log
│
├─ Cancellation / lifetime
│    └─ DECISION: parent runtime still alive AND task should continue?
│         ├─ YES → 返回等待边界
│         └─ NO → release task resources
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / NEXT ITERATION
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
tray=started icon=visible menu_items=3 status=running
```

### E2E FLOW #21 — Show-window poll loop

**树路径：** `Background Jobs / Async Side Effects → Desktop Background Work → Show-window poll loop`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START / TRIGGER
│
├─ Trigger source
│    ├─ startup spawn / request-time tokio::spawn / manager restoration / channel message
│    └─ task: Show-window poll loop
│
├─ Spawn / registration
│    ├─ parent runtime creates async task/thread/loop
│    └─ DECISION: spawn/runtime handle available?
│         ├─ NO → task never starts; parent may log error
│         └─ YES → task owns/borrows required shared state
│
├─ Wait boundary
│    ├─ timer sleep / mpsc receive / polling interval / restored work item
│    └─ DECISION: trigger/event available?
│         ├─ NO → continue waiting
│         └─ YES → one iteration begins
│
├─ Input snapshot
│    ├─ read latest runtime/DB/request-derived state needed by job
│    └─ freeze iteration context
│
├─ Core job operation
│    ├─ execute Show-window poll loop
│    └─ may call DB / HTTP / filesystem / runtime service depending on task
│
├─ Operation result
│    └─ DECISION: iteration succeeds?
│         ├─ NO
│         │    ├─ log/record failure
│         │    ├─ preserve parent request availability when task is fail-open
│         │    └─ decide retry on next event/interval
│         └─ YES
│              ├─ update in-memory state and/or persistent state
│              └─ emit success telemetry/log
│
├─ Cancellation / lifetime
│    └─ DECISION: parent runtime still alive AND task should continue?
│         ├─ YES → 返回等待边界
│         └─ NO → release task resources
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / NEXT ITERATION
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
show_window=true visible=true focused=true poll_interval_ms=100
```

### E2E FLOW #22 — /

**树路径：** `UI-only Actions → Guest / Public → /`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/home.rs
│
└─ Root / Home page module
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/
component=home
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #23 — /home

**树路径：** `UI-only Actions → Guest / Public → /home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /home
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/home.rs
│
└─ HomePage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/home
component=home
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #24 — /login

**树路径：** `UI-only Actions → Guest / Public → /login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /login
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/login.rs
│
└─ LoginPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/login
component=login
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #25 — /register

**树路径：** `UI-only Actions → Guest / Public → /register`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /register
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/crates/client-register/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/register
component=register
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #26 — /forgot-password

**树路径：** `UI-only Actions → Guest / Public → /forgot-password`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /forgot-password
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/forgot_password.rs
│
└─ ForgotPasswordPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/forgot-password
component=forgot-password
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #27 — /reset-password?:token

**树路径：** `UI-only Actions → Guest / Public → /reset-password?:token`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /reset-password?:token
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/reset_password.rs
│
└─ ResetPasswordPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/reset-password?:token
component=reset-password?:token
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #28 — /console/dashboard

**树路径：** `UI-only Actions → Console → /console/dashboard`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/dashboard
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/dashboard.rs
│
└─ Dashboard re-export
│
▼
FILE: crates/client/crates/client-dashboard/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/dashboard
component=console/dashboard
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #29 — /console/deploy

**树路径：** `UI-only Actions → Console → /console/deploy`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/deploy
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/deploy.rs
│
└─ DeployConfig re-export
│
▼
FILE: crates/client/crates/client-deploy/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/deploy
component=console/deploy
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #30 — /console/monitor

**树路径：** `UI-only Actions → Console → /console/monitor`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/monitor
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/monitor.rs
│
└─ ServiceMonitor re-export
│
▼
FILE: crates/client/crates/client-monitor/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/monitor
component=console/monitor
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #31 — /console/access

**树路径：** `UI-only Actions → Console → /console/access`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/access
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/api.rs
│
└─ AccessPage re-export
│
▼
FILE: crates/client/crates/client-access/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/access
component=console/access
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #32 — /console/models

**树路径：** `UI-only Actions → Console → /console/models`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/models
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/models.rs
│
└─ ChannelPage re-export
│
▼
FILE: crates/client/crates/client-models/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/models
component=console/models
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #33 — /console/users

**树路径：** `UI-only Actions → Console → /console/users`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/users
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/user.rs
│
└─ UsersPage re-export
│
▼
FILE: crates/client/crates/client-users/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/users
component=console/users
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #34 — /console/settings

**树路径：** `UI-only Actions → Console → /console/settings`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/settings
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/settings.rs
│
└─ SystemSettings re-export
│
▼
FILE: crates/client/crates/client-settings/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/settings
component=console/settings
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #35 — /console/finance

**树路径：** `UI-only Actions → Console → /console/finance`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/finance
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/billing.rs
│
└─ FinancePage re-export
│
▼
FILE: crates/client/crates/client-finance/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/finance
component=console/finance
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #36 — /console/logs

**树路径：** `UI-only Actions → Console → /console/logs`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/logs
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/logs.rs
│
└─ LogPage re-export
│
▼
FILE: crates/client/crates/client-log/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/logs
component=console/logs
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #37 — /console/connect

**树路径：** `UI-only Actions → Console → /console/connect`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/connect
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/connect.rs
│
└─ ConnectPage re-export
│
▼
FILE: crates/client/crates/client-connect/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/connect
component=console/connect
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #38 — /console/playground

**树路径：** `UI-only Actions → Console → /console/playground`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/playground
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/playground.rs
│
└─ PlaygroundPage re-export
│
▼
FILE: crates/client/crates/client-playground/src/lib.rs
│
└─ actual page component implementation / local effects
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/playground
component=console/playground
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #39 — /console/:..segments → NotFound

**树路径：** `UI-only Actions → Console → /console/:..segments → NotFound`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /console/:..segments → NotFound
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/components/layout.rs
│
└─ Layout wraps console page / navigation / shared contexts
│
▼
FILE: crates/client/src/pages/not_found.rs
│
└─ NotFoundPage
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/console/:..segments → NotFound
component=NotFoundPage
rendered=true
```

### E2E FLOW #40 — /preview/home

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/home`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/home
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/home
component=preview/home
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #41 — /preview/login

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/login`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/login
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/login
component=preview/login
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #42 — /preview/console/dashboard

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/dashboard`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/dashboard
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/dashboard
component=preview/console/dashboard
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #43 — /preview/console/models

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/models`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/models
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/models
component=preview/console/models
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #44 — /preview/console/access

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/access`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/access
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/access
component=preview/console/access
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #45 — /preview/console/settings

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/settings`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/settings
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/settings
component=preview/console/settings
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #46 — /preview/console/finance

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/finance`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/finance
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/finance
component=preview/console/finance
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #47 — /preview/console/monitor

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/monitor`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/monitor
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/monitor
component=preview/console/monitor
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #48 — /preview/console/playground

**树路径：** `UI-only Actions → Debug / e2e-preview → /preview/console/playground`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Navigation input
│    ├─ browser/client route: /preview/console/playground
│    ├─ current Auth context
│    ├─ current Theme/i18n/Toast context
│    └─ optional route params/query
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Dioxus Router match
│    └─ DECISION: Route enum/path matches?
│         ├─ NO → NotFound route/component
│         └─ YES → select mapped page component
│
├─ Route guards / context read
│    ├─ component can read Auth context
│    ├─ component can read Theme/i18n state
│    └─ DECISION: page requires authenticated state and context satisfies it?
│         ├─ NO → login/guard behavior as implemented by component/router
│         └─ YES → render page
│
▼
FILE: crates/client/src/pages/e2e_preview.rs
│
└─ selected Preview*Page component renders deterministic preview state
│
├─ Component construction
│    ├─ initialize local signals/state
│    ├─ render initial VDOM
│    └─ register click/input/effect handlers
│
├─ Data boundary
│    └─ DECISION: component action/effect needs server data?
│         ├─ NO → remain local UI-only path
│         └─ YES → issue separate HTTP/API request
│              └─ that request is documented under HTTP / API, not hidden in this flow
│
├─ User-visible result
│    ├─ Dioxus reconciles VDOM
│    └─ page/render state becomes visible
│
├─ Event loop
│    └─ wait for next UI event/navigation/state update
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
route=/preview/console/playground
component=preview/console/playground
rendered=true
locale=zh-CN
theme=system
```

### E2E FLOW #49 — i18n context

**树路径：** `UI-only Actions → Local UI State → i18n context`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ App root initialization
│    └─ state/context: i18n context
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Create initial state
│    ├─ derive default/configured value
│    └─ store in Dioxus signal/context
│
├─ Provide context
│    ├─ descendant components can read/subscribe
│    └─ current render reads latest value
│
├─ Update path
│    └─ DECISION: UI event changes this state?
│         ├─ NO → keep current value
│         └─ YES
│              ├─ mutate signal/context
│              └─ mark dependent subtree dirty
│
├─ Re-render
│    └─ Dioxus reconciles affected component tree
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI EVENT LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```json
{
  "locale": "zh-CN",
  "fallback_locale": "en-US",
  "ready": true
}
```

### E2E FLOW #50 — Toast state / ToastContainer

**树路径：** `UI-only Actions → Local UI State → Toast state / ToastContainer`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ App root initialization
│    └─ state/context: Toast state / ToastContainer
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Create initial state
│    ├─ derive default/configured value
│    └─ store in Dioxus signal/context
│
├─ Provide context
│    ├─ descendant components can read/subscribe
│    └─ current render reads latest value
│
├─ Update path
│    └─ DECISION: UI event changes this state?
│         ├─ NO → keep current value
│         └─ YES
│              ├─ mutate signal/context
│              └─ mark dependent subtree dirty
│
├─ Re-render
│    └─ Dioxus reconciles affected component tree
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI EVENT LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```json
{
  "visible": true,
  "level": "success",
  "message": "操作成功"
}
```

### E2E FLOW #51 — Auth context

**树路径：** `UI-only Actions → Local UI State → Auth context`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ App root initialization
│    └─ state/context: Auth context
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Create initial state
│    ├─ derive default/configured value
│    └─ store in Dioxus signal/context
│
├─ Provide context
│    ├─ descendant components can read/subscribe
│    └─ current render reads latest value
│
├─ Update path
│    └─ DECISION: UI event changes this state?
│         ├─ NO → keep current value
│         └─ YES
│              ├─ mutate signal/context
│              └─ mark dependent subtree dirty
│
├─ Re-render
│    └─ Dioxus reconciles affected component tree
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI EVENT LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```json
{
  "authenticated": true,
  "user_id": 10001,
  "username": "demo_user"
}
```

### E2E FLOW #52 — Theme state

**树路径：** `UI-only Actions → Local UI State → Theme state`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ App root initialization
│    └─ state/context: Theme state
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Create initial state
│    ├─ derive default/configured value
│    └─ store in Dioxus signal/context
│
├─ Provide context
│    ├─ descendant components can read/subscribe
│    └─ current render reads latest value
│
├─ Update path
│    └─ DECISION: UI event changes this state?
│         ├─ NO → keep current value
│         └─ YES
│              ├─ mutate signal/context
│              └─ mark dependent subtree dirty
│
├─ Re-render
│    └─ Dioxus reconciles affected component tree
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / UI EVENT LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```json
{
  "theme": "system",
  "resolved": "dark"
}
```

### E2E FLOW #53 — window maximize

**树路径：** `UI-only Actions → Desktop UI → window maximize`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Desktop event/state input
│    └─ action: window maximize
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Platform branch
│    └─ DECISION: current platform/runtime supports desktop action?
│         ├─ NO → skip/unsupported branch → END
│         └─ YES → obtain desktop window/tray handle
│
├─ Current UI state
│    ├─ read visibility/focus/maximized/tray state as needed
│    └─ decide desired state
│
├─ Apply desktop side effect
│    ├─ maximize / show / hide / focus / tray startup
│    └─ DECISION: OS/window operation succeeds?
│         ├─ NO → log/ignore according to UI path
│         └─ YES → state visible to user
│
├─ Event loop handoff
│    └─ return control to Dioxus/desktop event loop
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
window.maximized=true
window.visible=true
```

### E2E FLOW #54 — Windows tray startup

**树路径：** `UI-only Actions → Desktop UI → Windows tray startup`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Desktop event/state input
│    └─ action: Windows tray startup
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Platform branch
│    └─ DECISION: current platform/runtime supports desktop action?
│         ├─ NO → skip/unsupported branch → END
│         └─ YES → obtain desktop window/tray handle
│
├─ Current UI state
│    ├─ read visibility/focus/maximized/tray state as needed
│    └─ decide desired state
│
├─ Apply desktop side effect
│    ├─ maximize / show / hide / focus / tray startup
│    └─ DECISION: OS/window operation succeeds?
│         ├─ NO → log/ignore according to UI path
│         └─ YES → state visible to user
│
├─ Event loop handoff
│    └─ return control to Dioxus/desktop event loop
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
tray.visible=true
tray.status=running
```

### E2E FLOW #55 — show / hide / focus

**树路径：** `UI-only Actions → Desktop UI → show / hide / focus`  
**PR Impact：** `STATIC SOURCE-PATH MATCH`  
**本 Flow 命中的 changed files：** `crates/client/src/app.rs`

```text
START
│
├─ Desktop event/state input
│    └─ action: show / hide / focus
│
▼
FILE: crates/client/src/app.rs  [PR CHANGED]
│
├─ Platform branch
│    └─ DECISION: current platform/runtime supports desktop action?
│         ├─ NO → skip/unsupported branch → END
│         └─ YES → obtain desktop window/tray handle
│
├─ Current UI state
│    ├─ read visibility/focus/maximized/tray state as needed
│    └─ decide desired state
│
├─ Apply desktop side effect
│    ├─ maximize / show / hide / focus / tray startup
│    └─ DECISION: OS/window operation succeeds?
│         ├─ NO → log/ignore according to UI path
│         └─ YES → state visible to user
│
├─ Event loop handoff
│    └─ return control to Dioxus/desktop event loop
│
│
├─ 源码函数展开（静态扫描确认）
│    ├─ FILE: crates/client/src/app.rs  [PR CHANGED]
│    │    ├─ App()
│    │    │    └─ CALL → use_init_i18n() @ crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ launch_gui_with_tray()
│    └─ FILE: crates/client/crates/client-shared/src/i18n.rs
│    │    ├─ use_init_i18n()
│
├─ 规则：只展开能够解析到 BurnCloud 仓库内部真实函数定义的调用；第三方库调用保留在主 E2E 中，不伪造源码目标文件
│

▼
END / LOOP CONTINUES
```

#### PR Before / After（patch 证据）

**FILE: `crates/client/src/app.rs`**

HUNKS:
```text
@@ -83,6 +83,7 @@ pub fn App() -> Element {
```

AFTER / ADDED:
```diff
+            style { dangerous_inner_html: include_str!("product_ui.css") }
```

#### 当前 Atlas 返回结果基线

> 这是当前审计基线的结果示例，不代表该历史 PR 的 response 一定完全相同；PR-specific response 变化以 patch 证据为准。

```text
show_window=true
window.visible=true
window.focused=true
```

## 未映射 Changed Files

- `.github/workflows/client-ui.yml`
- `crates/client/scripts/check-product-ux.sh`
- `crates/client/src/critical_pages/auth.rs`
- `crates/client/src/critical_pages/customers_portable.rs`
- `crates/client/src/critical_pages/dashboard.rs`
- `crates/client/src/functional_layout.rs`
- `crates/client/src/functional_pages/access_live.rs`
- `crates/client/src/functional_pages/analytics.rs`
- `crates/client/src/functional_pages/analytics_full.rs`
- `crates/client/src/functional_pages/catalog.rs`
- `crates/client/src/functional_pages/guardrails_live.rs`
- `crates/client/src/functional_pages/logs_full.rs`
- `crates/client/src/functional_pages/playground_live.rs`
- `crates/client/src/functional_pages/providers.rs`
- `crates/client/src/functional_pages/settings.rs`
- `crates/client/src/product_ui.css`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
