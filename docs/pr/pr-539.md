---
title: "PR #539 — feat(node): add missing fakeable preparation capability ports"
slug: /pr/pr-539
---

# PR #539 — feat(node): add missing fakeable preparation capability ports

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-16T20:05:35Z`  
> **Base：** `main@a349ea6e1d37`  
> **Head：** `feat/node-fake-skeleton-completion@245c3db8afcb`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME`
- **Changed Files：** `3`
- **Additions / Deletions：** `+154 / -3`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `3`

**PR 说明摘要：** ## Phase 0.5 — Fake Skeleton Completion, step 1 Fills the machine-level ports that were previously represented only by manually injected reconciler evidence. Adds: - `ArtifactPreparer` + `FakeArtifactPreparer` - `RuntimePreparer` + `FakeRuntimePreparer` - `ReadinessProbe` + `FakeReadinessProbe` - `HealthProbe` + `FakeHealthProbe` Boundary rules: - no real download - no llama.cpp installation - no network readiness call - no model/provider/router business semantics in `platform/node` - no second Downloader / Server / Router / Database This deliberately does NOT add `ModelResolver` to `platform/node`; model selection is a business/domain responsibility and must be owned outside platform. Next step after this PR: wire these fake ports into the golden path so `ArtifactPrepared`, `RuntimePrepared`, and `ReadinessVerified` are produced only after the corresponding fake side effect succeeds, instead of tests injecting evidence directly.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/platform/node/src/fake.rs` | modified | 60 | 2 | 0 |
| `crates/platform/node/src/lib.rs` | modified | 10 | 1 | 0 |
| `crates/platform/node/src/preparation.rs` | added | 84 | 0 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/platform/node/src/fake.rs`
- `crates/platform/node/src/lib.rs`
- `crates/platform/node/src/preparation.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
