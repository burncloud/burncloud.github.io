---
title: "PR #395 — docs: add repository-local runtime atlas for chat completions"
slug: /pr/pr-395
---

# PR #395 — docs: add repository-local runtime atlas for chat completions

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-08-11T03:12:59Z`  
> **Base：** `main@956041a8b54d`  
> **Head：** `agent/runtime-atlas-chat-completions@0fed7bae06dd`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `DOCS / CI ONLY`
- **Changed Files：** `5`
- **Additions / Deletions：** `+825 / -35`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `5`

**PR 说明摘要：** ## What changed - add a root `AGENTS.md` as the repository-level operating contract for coding agents - add `docs/runtime/FLOW_TEMPLATE.md` for incremental End-to-End Request Flow + progressive ICFG documentation - add a source-derived `POST /v1/chat/completions` runtime atlas covering entry, authentication/admission, model routing, candidate filtering, billing preflight, failover, upstream execution, settlement/logging, and response behavior - update the docs indexes so repository-local runtime Markdown becomes the versioned truth/navigation layer ## Why BurnCloud already has a strong agent engineering harness, but runtime-flow truth was still primarily delegated to the external Docusaurus site. This change starts moving source-derived runtime knowledge into the same repository and review lifecycle as the code. The flow intentionally stays incremental and reviewable instead of generating a repository-wide call graph. Important edges are classified as STATIC CONFIRMED, DYNAMIC, or INFERRED, and contradictions are recorded as verification gaps instead of being guessed away. ## Verification - docs-only diff; no runtime behavior changed - audited source paths and symbols against current `main` at `956041a8b54d8c6964e57fa2284f825cc322b0d2` - checked the current router fallback, authentication/admission chain, scheduler/candidate construction, OpenAI passthrough branch, billing/logg…

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `AGENTS.md` | added | 73 | 0 | 0 |
| `docs/README.md` | modified | 45 | 21 | 0 |
| `docs/runtime/CHAT_COMPLETIONS.md` | added | 514 | 0 | 0 |
| `docs/runtime/FLOW_TEMPLATE.md` | added | 140 | 0 | 0 |
| `docs/runtime/README.md` | modified | 53 | 14 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `AGENTS.md`
- `docs/README.md`
- `docs/runtime/CHAT_COMPLETIONS.md`
- `docs/runtime/FLOW_TEMPLATE.md`
- `docs/runtime/README.md`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
