---
title: "PR #563 — feat(node): close live request lifecycle gaps"
slug: /pr/pr-563
---

# PR #563 — feat(node): close live request lifecycle gaps

> **状态：** OPEN  
> **作者：** `rustburn`  
> **创建：** `2026-09-18T13:56:32Z`  
> **Base：** `main@0a9dfa1c307d`  
> **Head：** `feat/node-live-request-closure@a9fe1ef4fbd1`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME`
- **Changed Files：** `5`
- **Additions / Deletions：** `+261 / -55`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `5`

**PR 说明摘要：** Closes only the three agreed Node framework gaps, without changing routing preference or scheduler behavior. 1. Keep Node runtime/request-visible state alive with the unified server. 2. Allow the existing router to ask the application for a specialized response only after a true zero-candidate route miss; Node uses this seam for MODEL_PREPARING. 3. Converge Node preparation/recovery stage failures to Failed instead of leaving Preparing state stale. Boundaries: - no LocalFirst / ExternalFirst policy - no scheduler scoring changes - no second router/server/routing table - no real GPU/downloader/runtime adapters - no automatic request-triggered model preparation

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/interfaces/server/src/lib.rs` | modified | 17 | 10 | 0 |
| `crates/interfaces/server/src/node_orchestrator.rs` | modified | 164 | 44 | 0 |
| `crates/interfaces/server/src/node_request.rs` | modified | 42 | 0 | 0 |
| `crates/traffic/router/src/lib.rs` | modified | 32 | 1 | 0 |
| `crates/traffic/router/src/state.rs` | modified | 6 | 0 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/interfaces/server/src/lib.rs`
- `crates/interfaces/server/src/node_orchestrator.rs`
- `crates/interfaces/server/src/node_request.rs`
- `crates/traffic/router/src/lib.rs`
- `crates/traffic/router/src/state.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
