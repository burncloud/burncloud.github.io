---
title: "PR #544 — feat(node): add application-level orchestration root"
slug: /pr/pr-544
---

# PR #544 — feat(node): add application-level orchestration root

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-17T04:23:22Z`  
> **Base：** `main@bb83989801eb`  
> **Head：** `feat/node-application-orchestrator@70160bf11ea2`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME`
- **Changed Files：** `3`
- **Additions / Deletions：** `+78 / -1`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `3`

**PR 说明摘要：** ## S0-10 — Application-level Node composition Adds the cross-domain composition root that sits above machine-level `NodeComposition`. `NodeOrchestrator` now owns the wiring between: - Supply-owned `ModelResolver` - Platform-owned `NodeComposition` It deliberately does not implement model selection, artifact/runtime preparation, process lifecycle, health, or routing. Those remain replaceable owner contracts. Traffic attachment is intentionally still an application seam targeting the existing BurnCloud router; no second Router/Server is introduced. This PR establishes the correct place for the next framework steps: 1. S0-11 `ModelDemand` / desired-state input 2. S0-12 Action -&gt; owner contract -&gt; evidence automatic dispatch 3. S0-13 unhealthy/failure -&gt; detach/recover wiring 4. S0-14 fake E2E driven only by demand No real Node capability is implemented here.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/interfaces/server/Cargo.toml` | modified | 2 | 1 | 0 |
| `crates/interfaces/server/src/lib.rs` | modified | 1 | 0 | 0 |
| `crates/interfaces/server/src/node_orchestrator.rs` | added | 75 | 0 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/interfaces/server/Cargo.toml`
- `crates/interfaces/server/src/lib.rs`
- `crates/interfaces/server/src/node_orchestrator.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
