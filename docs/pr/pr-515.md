---
title: "PR #515 — cleanup(observability): retire unused alert crate"
slug: /pr/pr-515
---

# PR #515 — cleanup(observability): retire unused alert crate

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-15T01:03:21Z`  
> **Base：** `main@e5359c3e20d8`  
> **Head：** `cleanup/retire-alert-510@4e265b96bc42`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME`
- **Changed Files：** `12`
- **Additions / Deletions：** `+0 / -1149`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `12`

**PR 说明摘要：** Closes #510 ## 结果 - 删除没有任何生产消费者的 `burncloud-service-alert` crate； - 删除根 workspace dependency 和 `burncloud-service` 的未使用依赖； - 不创建替代告警系统，不改变其他 Service API； - #502/#503 记录的凭据暴露与重复环境读取风险随未使用实现一并消除。 ## 验证 - 全仓库已无 `burncloud-service-alert` / `burncloud_service_alert` 引用； - `git diff --check` 通过； - 仅修改 #510 Allowed Paths； - 编译与测试由 GitHub Actions 验证。

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `Cargo.toml` | modified | 0 | 1 | 0 |
| `crates/service/Cargo.toml` | modified | 0 | 1 | 0 |
| `crates/service/crates/alert/Cargo.toml` | removed | 0 | 26 | 0 |
| `crates/service/crates/alert/src/channels/email.rs` | removed | 0 | 52 | 0 |
| `crates/service/crates/alert/src/channels/mod.rs` | removed | 0 | 27 | 0 |
| `crates/service/crates/alert/src/channels/slack.rs` | removed | 0 | 81 | 0 |
| `crates/service/crates/alert/src/channels/telegram.rs` | removed | 0 | 86 | 0 |
| `crates/service/crates/alert/src/channels/webhook.rs` | removed | 0 | 65 | 0 |
| `crates/service/crates/alert/src/lib.rs` | removed | 0 | 16 | 0 |
| `crates/service/crates/alert/src/rules.rs` | removed | 0 | 279 | 0 |
| `crates/service/crates/alert/src/service.rs` | removed | 0 | 280 | 0 |
| `crates/service/crates/alert/src/types.rs` | removed | 0 | 235 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `Cargo.toml`
- `crates/service/Cargo.toml`
- `crates/service/crates/alert/Cargo.toml`
- `crates/service/crates/alert/src/channels/email.rs`
- `crates/service/crates/alert/src/channels/mod.rs`
- `crates/service/crates/alert/src/channels/slack.rs`
- `crates/service/crates/alert/src/channels/telegram.rs`
- `crates/service/crates/alert/src/channels/webhook.rs`
- `crates/service/crates/alert/src/lib.rs`
- `crates/service/crates/alert/src/rules.rs`
- `crates/service/crates/alert/src/service.rs`
- `crates/service/crates/alert/src/types.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
