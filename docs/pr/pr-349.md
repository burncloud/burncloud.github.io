---
title: "PR #349 — fix(db): register missing migrations for router_request_logs (Issue #334)"
slug: /pr/pr-349
---

# PR #349 — fix(db): register missing migrations for router_request_logs (Issue #334)

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-06-13T17:55:10Z`  
> **Base：** `main@0f51207790b4`  
> **Head：** `issue-334@624d3cabb2f8`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + STATE/DB`
- **Changed Files：** `4`
- **Additions / Deletions：** `+44 / -4`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `4`

**PR 说明摘要：** ## Summary This PR fixes the missing migration registration for the `router_request_logs` table, which was already implemented in code but never registered in `migration.rs`. ## Problem Issue #334 requested implementation of detailed request/response logging. Investigation revealed that: 1. The `RouterRequestLog` model and `RouterRequestLogModel` database operations were already implemented 2. The migration file `0017_router_request_logs.sql` existed but was never registered 3. There was a version conflict: two migrations had the same version number (0017) ## Changes 1. **Renamed migration file**: `0017_router_request_logs.sql` → `0018_router_request_logs.sql` (to fix version conflict) 2. **Registered missing migrations** in `migration.rs`: - 0015_monthly_quota - 0016_token_rotation - 0017_fix_bool_columns - 0018_router_request_logs 3. **Created PostgreSQL placeholder**: `0017_fix_bool_columns.sql` (no-op, PostgreSQL already uses proper BOOLEAN type) ## Features Already Implemented The following features from Issue #334 were already implemented in the codebase: | Feature | Status | |---------|--------| | `router_request_logs` table schema | ✅ | | Request body logging with truncation (64KB max) | ✅ | | Response body logging with truncation | ✅ | | Sensitive header redaction (authorization, api-key, etc.) | ✅ | | Sensitive field redaction in JSON bodies | ✅ | | Storage policy con…

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/database/migrations/postgres/0017_fix_bool_columns.sql` | added | 8 | 0 | 0 |
| `crates/database/migrations/postgres/0018_router_request_logs.sql` | renamed | 1 | 1 | 0 |
| `crates/database/migrations/sqlite/0018_router_request_logs.sql` | renamed | 1 | 1 | 0 |
| `crates/database/src/migration.rs` | modified | 34 | 2 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/database/migrations/postgres/0017_fix_bool_columns.sql`
- `crates/database/migrations/postgres/0018_router_request_logs.sql`
- `crates/database/migrations/sqlite/0018_router_request_logs.sql`
- `crates/database/src/migration.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
