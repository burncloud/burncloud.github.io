---
title: "PR #416 — Polish API key lifecycle around secure management refs"
slug: /pr/pr-416
---

# PR #416 — Polish API key lifecycle around secure management refs

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-08-14T05:34:40Z`  
> **Base：** `main@eae8758facf1`  
> **Head：** `agent/phase-2-api-keys-polish@c52c4fd9fe68`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME + UI`
- **Changed Files：** `5`
- **Additions / Deletions：** `+879 / -441`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `5`

**PR 说明摘要：** ## Goal Polish the Phase 2 API Keys experience on top of #415 without weakening the bearer-secret redaction contract. The page should make it obvious that management identifiers are not API credentials, show secrets only at creation/rotation, and make owner, spend, network, status, rotation, and deletion decisions explicit. ## What changed - route `/keys` to a dedicated `api_keys_live` page while keeping Team responsibilities separate - remove the superseded API Keys implementation from `access_live.rs`, leaving that module responsible only for Team - show `tok_…` values explicitly as opaque management references, never as masked bearer secrets - keep bearer-secret disclosure one-time only after create or rotate - fail closed when the owner directory is unavailable; no free-form owner-ID fallback - expose finite quota as a USD spend limit while converting to the server's nanodollar contract - show settled spend vs limit and surface active-key owner/quota attention states - validate exact IPv4/IPv6 allowlists client-side and reject CIDR because the current router performs exact matching only - require acknowledgement before removing an existing IP restriction - add explicit confirmations for enable/disable, rotation, immediate old-secret revocation, and deletion - update functional-wiring and product-UX regression gates so the API Keys/Team module split and management-ref/secret…

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/client/scripts/check-functional-wiring.sh` | modified | 8 | 1 | 0 |
| `crates/client/scripts/check-product-ux.sh` | modified | 12 | 5 | 0 |
| `crates/client/src/functional_pages/access_live.rs` | modified | 13 | 434 | 0 |
| `crates/client/src/functional_pages/api_keys_live.rs` | added | 843 | 0 | 0 |
| `crates/client/src/functional_pages/mod.rs` | modified | 3 | 1 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/client/scripts/check-functional-wiring.sh`
- `crates/client/scripts/check-product-ux.sh`
- `crates/client/src/functional_pages/access_live.rs`
- `crates/client/src/functional_pages/api_keys_live.rs`
- `crates/client/src/functional_pages/mod.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
