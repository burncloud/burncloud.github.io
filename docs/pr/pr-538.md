---
title: "PR #538 — test(node): complete Phase-0 existing-router acceptance"
slug: /pr/pr-538
---

# PR #538 — test(node): complete Phase-0 existing-router acceptance

> **状态：** MERGED  
> **作者：** `rustburn`  
> **创建：** `2026-09-16T19:32:20Z`  
> **Base：** `main@9410d665299f`  
> **Head：** `feat/node-router-attachment-evidence@82b3d0258178`  
> **完整 E2E 拓扑基线：** `burncloud/burncloud@aa54e21393c6d46a6b09555ffd3661c1f22484f3`  
> **证据规则：** PR changed files / patch 为 `STATIC CONFIRMED`；下方完整 E2E 使用当前已审计 Atlas 拓扑，并在命中 PR changed file 的节点标记 `[PR CHANGED]`。旧 PR 的历史 head 可能与当前拓扑存在后续演化，因此未把当前拓扑伪装成历史源码逐行复现。

## PR 影响总览

- **Change Class：** `RUNTIME`
- **Changed Files：** `3`
- **Additions / Deletions：** `+131 / -0`
- **受影响的完整 E2E Flow：** `0`
- **未映射 changed files：** `3`

**PR 说明摘要：** ## Phase 0 final acceptance Completes the two remaining S0-08 requirements. 1. `RouterAttached` is now recorded only after the application-layer attachment side effect returns success. A failed database/traffic attachment leaves the reconciler in `Ready`, never `Routable`. 2. A real SQLite integration test uses `ExistingRouterLocalAttacher` plus the existing `ModelRouter` to prove: - before attach: model is not discoverable - READY endpoint attach creates the existing Channel/Ability truth - existing `ModelRouter::get_candidates` discovers that exact channel - detach by stable channel id removes it - existing `ModelRouter` no longer discovers it No second server, listener, ModelRouter, or routing table is introduced. No real GPU detection, model download, or inference runtime is added; those are outside Phase 0.

## Changed Files

| 文件 | 状态 | + | - | Atlas 命中 |
|---|---:|---:|---:|---:|
| `crates/interfaces/server/src/lib.rs` | modified | 1 | 0 | 0 |
| `crates/interfaces/server/src/node_attachment.rs` | added | 70 | 0 | 0 |
| `crates/traffic/router/tests/node_existing_router_integration.rs` | added | 60 | 0 | 0 |

## Affected E2E Impact Matrix

当前 PR 的 changed files 与当前 204+ 页 Atlas 的已审计源码路径没有精确交集，因此本页**不会虚构受影响 E2E**。这通常表示 docs/test/CI-only 变更，或旧 PR 使用了当前基线已经重命名/删除的路径。

## 完整受影响 E2E Request Flow

**Affected Runtime E2E: NONE CONFIRMED BY CURRENT ATLAS PATH MAPPING.**

## 未映射 Changed Files

- `crates/interfaces/server/src/lib.rs`
- `crates/interfaces/server/src/node_attachment.rs`
- `crates/traffic/router/tests/node_existing_router_integration.rs`

## Execution Classification

- **PR metadata / changed filenames / patch hunks：STATIC CONFIRMED**
- **Affected Flow 判定：STATIC CONFIRMED（当前 Atlas source-path 精确交集）**
- **完整 Flow 拓扑：CURRENT AUDITED BASELINE**
- **历史 PR head 的逐行精确 Runtime 拓扑：NOT CLAIMED，除非 patch 本身直接证明**
