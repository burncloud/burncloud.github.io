---
title: "PR #340 · fix（router）: Improve empty streaming response detection to check for actual content"
slug: /commits/2026-06-08-pr-340-1e23f149/
sidebar_position: 15
doc_type: commit-change-atlas-v2
truth: source-diff-derived
language: zh-CN
diagram_style: plaintext
repository: burncloud/burncloud
pr_number: 340
base_commit: 3469d3e5650e7b9ce700ac1aa96a9a1641ad1d3f
target_commit: 1e23f1493342676226aece129710ca8c0fd30ea5
risk: 高
merged_at: 2026-06-08T01:57:53Z
---

# PR #340：这次到底改了什么？

## 一句话先看懂

这次主要改了这些事：**流式返回**、**上游失败后换下一个渠道**、**模型请求选渠道**。

**风险：高（15 分）** · 改了 **1** 个文件 · 先看 **3** 个真正的变化块

> **作者写的目的（AUTHOR-STATED INTENT）：** fix（router）: Improve empty streaming response detection to check for actual content

> **作者补充说明（原文）：** ＃＃ Problem The previous implementation of empty streaming response detection had a critical flaw: - It only checked for ＊＊usage information （token counts）＊＊ to determine if a streaming response was empty - Many providers send content in fields ＊＊WITHOUT usage info＊＊ in intermediate chunks - Some providers only include……（原 PR 里还有更多说明）

**完整源码差异：** [BASE `3469d3e5650e` → TARGET `1e23f1493342`](https://github.com/burncloud/burncloud/compare/3469d3e5650e7b9ce700ac1aa96a9a1641ad1d3f...1e23f1493342676226aece129710ca8c0fd30ea5)

---

## 这次主要改了什么

不要先看 0 个符号。先把这次提交当成 **3 件独立的事情** 看：**流式返回、上游失败后换下一个渠道、模型请求选渠道**。

### 变化 1：流式返回

**大白话：** 流式回复不会一拿到连接就马上交给用户了。现在会先看第一块数据；如果第一块已经是上游错误，就还有机会换一个渠道。

**谁会感觉到：** 用户发起流式模型请求

#### 以前 → 现在

```text
[以前]
用户发起流式请求
   │
   ▼
上游返回一个流
   │
   ▼
马上把流交给客户端
   │
   ▼
第一块就是错误时也很难再换渠道

                 变成
                  │
                  ▼

[现在]
用户发起流式请求
   │
   ▼
上游返回一个流
   │
   ▼
先看第一块数据
   │
   ▼
第一块正常 → 继续流给用户
   │
   ▼
第一块报错 → 进入失败 / 重试处理
```

#### 这块主要改了哪些文件

- `crates/router/src/lib.rs`

#### 源码证据

- **修改后:** [`crates/router/src/lib.rs:L2563-L2563`](https://github.com/burncloud/burncloud/blob/1e23f1493342676226aece129710ca8c0fd30ea5/crates/router/src/lib.rs#L2563-L2563)
- **修改前:** [`crates/router/src/lib.rs:L2563-L2563`](https://github.com/burncloud/burncloud/blob/3469d3e5650e7b9ce700ac1aa96a9a1641ad1d3f/crates/router/src/lib.rs#L2563-L2563)
- **修改后:** [`crates/router/src/lib.rs:L2574-L2602`](https://github.com/burncloud/burncloud/blob/1e23f1493342676226aece129710ca8c0fd30ea5/crates/router/src/lib.rs#L2574-L2602)

### 变化 2：上游失败后换下一个渠道

**大白话：** 上游出错以后，BurnCloud 决定『直接报错』还是『换下一个渠道再试』的规则变了。

**谁会感觉到：** 上游失败后的重试 / Failover

#### 以前 → 现在

```text
[以前]
请求发给当前 Channel
   │
   ▼
上游失败
   │
   ▼
按旧规则决定是否结束

                 变成
                  │
                  ▼

[现在]
请求发给当前 Channel
   │
   ▼
上游失败
   │
   ▼
记录失败状态
   │
   ▼
符合条件 → 换下一个候选 Channel
   │
   ▼
不符合条件 → 返回错误
```

#### 这块主要改了哪些文件

- `crates/router/src/lib.rs`

#### 源码证据

- **修改后:** [`crates/router/src/lib.rs:L3171-L3196`](https://github.com/burncloud/burncloud/blob/1e23f1493342676226aece129710ca8c0fd30ea5/crates/router/src/lib.rs#L3171-L3196)
- **修改后:** [`crates/router/src/lib.rs:L3304-L3329`](https://github.com/burncloud/burncloud/blob/1e23f1493342676226aece129710ca8c0fd30ea5/crates/router/src/lib.rs#L3304-L3329)

### 变化 3：模型请求选渠道

**大白话：** 模型请求挑选 Channel 的规则变了。要关心的是：哪些渠道能进候选、怎么排序、最后先试谁。

**谁会感觉到：** 模型请求选择 Channel

#### 以前 → 现在

```text
[以前]
模型请求进来
   │
   ▼
按旧规则找候选 Channel
   │
   ▼
选出要先尝试的 Channel

                 变成
                  │
                  ▼

[现在]
模型请求进来
   │
   ▼
按新规则过滤 / 排序候选 Channel
   │
   ▼
选出新的尝试顺序
```

#### 这块主要改了哪些文件

- `crates/router/src/lib.rs`

#### 源码证据

- **修改后:** [`crates/router/src/lib.rs:L2604-L2604`](https://github.com/burncloud/burncloud/blob/1e23f1493342676226aece129710ca8c0fd30ea5/crates/router/src/lib.rs#L2604-L2604)
- **修改前:** [`crates/router/src/lib.rs:L2575-L2575`](https://github.com/burncloud/burncloud/blob/3469d3e5650e7b9ce700ac1aa96a9a1641ad1d3f/crates/router/src/lib.rs#L2575-L2575)


---

## 10 个修改维度

这 10 个维度是**检查清单**，不是阅读顺序。上面的“变化块”才是人真正应该先看的。

| 维度 | 结果 | 大白话 |
|---|---|---|
| 1. 修改范围 | 🟡 有变化 | 代码文件和符号有变化。 |
| 2. 用户流程 | 🟡 有变化 | 有用户或管理员能直接感觉到的流程变化。 |
| 3. 运行过程 | 🟡 有变化 | 程序实际运行时的步骤有变化。 |
| 4. 控制流程 | 🟡 有变化 | 有 if / match / loop / return 等控制逻辑变化。 |
| 5. 状态变化 | ⚪ 没发现变化 | 没发现明确的状态写入变化。 |
| 6. API 契约 | ⚪ 没发现变化 | 没发现服务器公开 API 契约变化；客户端自己加请求头不自动算契约变化。 |
| 7. 数据库 / 持久化 | ⚪ 没发现变化 | 没发现数据库 / 持久化变化。 |
| 8. 外部依赖 | 🟡 有变化 | BurnCloud 调外部 Provider 的方式有变化。 |
| 9. 影响范围 | 🟡 有变化 | 下面会把直接修改和可能受影响的地方分开列出来。 |
| 10. 测试与证据 | ⚠️ 本提交没改测试 | 这个提交没有同步修改测试文件；不等于没有旧测试，只表示本次 diff 没加/改测试。 |

---

## 1. 修改范围

**改了什么：** 1 个文件，静态解析到 0 个 Rust 符号，其中 0 个函数。

**先看这些变化块：** 流式返回、上游失败后换下一个渠道、模型请求选渠道。

### 主要文件（最多 20 个）

- 🟡 修改 `crates/router/src/lib.rs`

### 主要代码入口（最多 12 个）

- 没有安全解析出的 Rust 符号；不会硬猜。

---

## 2. 用户流程

- **流式返回：** 用户发起流式模型请求
- **上游失败后换下一个渠道：** 上游失败后的重试 / Failover
- **模型请求选渠道：** 模型请求选择 Channel

> 只有能从这次 diff 安全对应到用户 / 管理员操作的变化才写在这里。数据库迁移、CI、文档不会被硬说成“用户流程”。

---

## 3. 运行过程

- **流式返回：** 流式回复不会一拿到连接就马上交给用户了。现在会先看第一块数据；如果第一块已经是上游错误，就还有机会换一个渠道。
- **上游失败后换下一个渠道：** 上游出错以后，BurnCloud 决定『直接报错』还是『换下一个渠道再试』的规则变了。
- **模型请求选渠道：** 模型请求挑选 Channel 的规则变了。要关心的是：哪些渠道能进候选、怎么排序、最后先试谁。

**真正的 BEFORE → AFTER 已经放在每个“变化块”的 plaintext 图里。不同功能不会被强行接成一条假 E2E。**

---

## 4. 控制流程

这里**不把不同函数里的 if 强行串成一条假 ICFG**。只列真正改过的控制语句：


**修改后新增 / 改过的控制点：**
- `if !seen_tokens_clone.load（std::sync::atomic::Ordering::Relaxed） ｛`
- `for line in text.lines（） ｛`
- `if !line.starts_with（'data: '） ｛`
- `continue;`
- `if data.trim（） == '［DONE］' ｛`
- `if let Ok（json） = serde_json::from_str::‹serde_json::Value›（data） ｛`

---

## 5. 状态变化

- 没发现能安全概括的关键状态变化。

---

## 6. API 契约

**⚪ 没发现服务器公开 API 契约变化。**

---

## 7. 数据库 / 持久化

**⚪ 没发现数据库 / 持久化变化。**

---

## 8. 外部依赖

**🟡 外部调用相关行为有变化。**

- 流式回复不会一拿到连接就马上交给用户了。现在会先看第一块数据；如果第一块已经是上游错误，就还有机会换一个渠道。
- 上游出错以后，BurnCloud 决定『直接报错』还是『换下一个渠道再试』的规则变了。
- 模型请求挑选 Channel 的规则变了。要关心的是：哪些渠道能进候选、怎么排序、最后先试谁。

---

## 9. 影响范围

```text
这次 Commit
 ├─ 流式返回
 │     └─ 用户发起流式模型请求
 ├─ 上游失败后换下一个渠道
 │     └─ 上游失败后的重试 / Failover
 └─ 模型请求选渠道
       └─ 模型请求选择 Channel
```

> “可能受影响（词法引用）”只说明代码里找到了同名引用，**不等于已经证明是一条真实调用链**。

---

## 10. 测试与证据

**⚠️ 本提交没有同步修改测试文件。** 这不等于项目没有旧测试，只表示这次提交本身没有加 / 改测试。

### 本次提交的历史源码证据

- **修改后:** [`crates/router/src/lib.rs:L2563-L2563`](https://github.com/burncloud/burncloud/blob/1e23f1493342676226aece129710ca8c0fd30ea5/crates/router/src/lib.rs#L2563-L2563)
- **修改前:** [`crates/router/src/lib.rs:L2563-L2563`](https://github.com/burncloud/burncloud/blob/3469d3e5650e7b9ce700ac1aa96a9a1641ad1d3f/crates/router/src/lib.rs#L2563-L2563)
- **修改后:** [`crates/router/src/lib.rs:L2574-L2602`](https://github.com/burncloud/burncloud/blob/1e23f1493342676226aece129710ca8c0fd30ea5/crates/router/src/lib.rs#L2574-L2602)
- **修改后:** [`crates/router/src/lib.rs:L2604-L2604`](https://github.com/burncloud/burncloud/blob/1e23f1493342676226aece129710ca8c0fd30ea5/crates/router/src/lib.rs#L2604-L2604)
- **修改前:** [`crates/router/src/lib.rs:L2575-L2575`](https://github.com/burncloud/burncloud/blob/3469d3e5650e7b9ce700ac1aa96a9a1641ad1d3f/crates/router/src/lib.rs#L2575-L2575)
- **修改后:** [`crates/router/src/lib.rs:L3171-L3196`](https://github.com/burncloud/burncloud/blob/1e23f1493342676226aece129710ca8c0fd30ea5/crates/router/src/lib.rs#L3171-L3196)
- **修改后:** [`crates/router/src/lib.rs:L3304-L3329`](https://github.com/burncloud/burncloud/blob/1e23f1493342676226aece129710ca8c0fd30ea5/crates/router/src/lib.rs#L3304-L3329)

---

## 风险判断

**总分：15 → 高风险**

- 路由 / Channel 选择：`+4`
- 流式响应：`+4`
- 重试 / Failover：`+4`
- 改了生产代码但这个提交没有同步改测试：`+3`

这个分数来自固定规则，不是 AI 凭感觉打分。

## 程序员 Review 清单

- [ ] 我能用一句话说清这次改了哪几件事
- [ ] 每个变化块的“以前 → 现在”符合真实源码
- [ ] 没把两个不相关的功能画成一条假流程
- [ ] 登录 / 权限变化已经单独确认
- [ ] 数据库迁移变化已经单独确认
- [ ] API 契约和“客户端请求行为”没有混为一谈
- [ ] 外部 Provider 调用变化已经确认
- [ ] 影响范围里的“可能”没有被当成“确定”
- [ ] 测试是否覆盖关键变化已经人工确认
- [ ] 所有重要结论都能点回 BASE / TARGET 历史源码

## 直接看源码

- [完整 BASE → TARGET Diff](https://github.com/burncloud/burncloud/compare/3469d3e5650e7b9ce700ac1aa96a9a1641ad1d3f...1e23f1493342676226aece129710ca8c0fd30ea5)
- [原 Pull Request #340](https://github.com/burncloud/burncloud/pull/340)
