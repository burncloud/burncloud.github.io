---
title: "PR #348 · fix: HTTP error responses should return JSON format instead of plain text"
slug: /commits/2026-06-13-pr-348-0f512077/
sidebar_position: 8
doc_type: commit-change-atlas-v2
truth: source-diff-derived
language: zh-CN
diagram_style: plaintext
repository: burncloud/burncloud
pr_number: 348
base_commit: 3ff4ca580ab4294372c40ad6b4b6ce11a1bb7ee1
target_commit: 0f51207790b4e5beaf890ea378f19a80f2a0bcbc
risk: 低
merged_at: 2026-06-13T16:57:28Z
---

# PR #348：这次到底改了什么？

## 一句话先看懂

这次主要改了一件事：**没有实际代码差异**。

**风险：低（0 分）** · 改了 **0** 个文件 · 先看 **1** 个真正的变化块

> **作者写的目的（AUTHOR-STATED INTENT）：** fix: HTTP error responses should return JSON format instead of plain text

> **作者补充说明（原文）：** ＃＃ 问题描述 代码中存在多处 HTTP 响应直接输出纯文本字符串，而不是正确的 JSON 格式。这违反了 API 规范，客户端无法正确解析错误信息。 ＃＃ 修复内容 ＃＃＃ 1. 流式响应构建失败 （3处） - 第 2728 行 - 第 3368 行 - 第 3532 行 修改前: 修改后: ＃＃＃ 2. 上游读取错误 （2处） - 第 2900 行 - 第 3661 行 修改前: 修改后: ＃＃ 测试验证 - 编译测试通过 - 端点正常 - 端点正常 - 错误响应均为 JSON 格式 ＃＃ 相关 Issue - Kanban 任务: 修复HTTP响应直接输出文本而非JSON格式的问题

**完整源码差异：** [BASE `3ff4ca580ab4` → TARGET `0f51207790b4`](https://github.com/burncloud/burncloud/compare/3ff4ca580ab4294372c40ad6b4b6ce11a1bb7ee1...0f51207790b4e5beaf890ea378f19a80f2a0bcbc)

---

## 这次主要改了什么

不要先看 0 个符号。先把这次提交当成 **1 件独立的事情** 看：**没有实际代码差异**。

### 变化 1：没有实际代码差异

**大白话：** 这个 merge commit 的第一父提交和 TARGET 之间没有文件差异，所以这里明确写『没有代码变化』，不为了凑图编故事。

**谁会感觉到：** 没有用户流程变化

#### 以前 → 现在

```text
[以前]
BASE 版本代码

                 变成
                  │
                  ▼

[现在]
TARGET 版本代码
   │
   ▼
BASE → TARGET 没有新增文件差异
```

#### 这块主要改了哪些文件

- 没有实际变化文件

#### 源码证据

- **完整差异扫描：** 这个变化块来自空 diff，没有历史行号可列。


---

## 10 个修改维度

这 10 个维度是**检查清单**，不是阅读顺序。上面的“变化块”才是人真正应该先看的。

| 维度 | 结果 | 大白话 |
|---|---|---|
| 1. 修改范围 | ⚪ 没发现变化 | BASE → TARGET 是空 diff，没有实际文件变化。 |
| 2. 用户流程 | ⚪ 没发现变化 | 没发现能安全映射到用户操作的变化。 |
| 3. 运行过程 | ⚪ 没发现变化 | 没发现生产运行链路变化。 |
| 4. 控制流程 | ⚪ 没发现变化 | 没发现明确的控制分支变化。 |
| 5. 状态变化 | ⚪ 没发现变化 | 没发现明确的状态写入变化。 |
| 6. API 契约 | ⚪ 没发现变化 | 没发现服务器公开 API 契约变化；客户端自己加请求头不自动算契约变化。 |
| 7. 数据库 / 持久化 | ⚪ 没发现变化 | 没发现数据库 / 持久化变化。 |
| 8. 外部依赖 | ⚪ 没发现变化 | 没发现明确的外部 Provider 调用变化。 |
| 9. 影响范围 | ⚪ 没发现变化 | 没有代码 diff，因此没有代码 Blast Radius。 |
| 10. 测试与证据 | ⚪ 没发现变化 | 这个提交没有同步修改测试文件；不等于没有旧测试，只表示本次 diff 没加/改测试。 |

---

## 1. 修改范围

**改了什么：** 0 个文件，静态解析到 0 个 Rust 符号，其中 0 个函数。

**先看这些变化块：** 没有实际代码差异。

### 主要文件（最多 20 个）

- BASE → TARGET 没有文件差异。

### 主要代码入口（最多 12 个）

- 没有安全解析出的 Rust 符号；不会硬猜。

---

## 2. 用户流程

- 没发现可以安全说成『用户流程改变』的变化；不会为了凑图硬猜。

> 只有能从这次 diff 安全对应到用户 / 管理员操作的变化才写在这里。数据库迁移、CI、文档不会被硬说成“用户流程”。

---

## 3. 运行过程

- 没发现线上运行步骤改变；如果这是空 diff，就明确保持为空，不制造变化。

**真正的 BEFORE → AFTER 已经放在每个“变化块”的 plaintext 图里。不同功能不会被强行接成一条假 E2E。**

---

## 4. 控制流程

**⚪ 没发现明确的 if / match / loop / return / spawn 控制语句变化。**

---

## 5. 状态变化

- 没发现能安全概括的关键状态变化。

---

## 6. API 契约

**⚪ 没发现服务器公开 API 契约变化。**

---

## 7. 数据库 / 持久化

**⚪ 没发现数据库 / 持久化变化。**

---

## 8. 外部依赖

**⚪ 没发现 BurnCloud 调外部 Provider 的方式发生明确变化。**

---

## 9. 影响范围

```text
这次 Commit
 └─ 没有实际代码差异
       └─ 没有用户流程变化
```

> “可能受影响（词法引用）”只说明代码里找到了同名引用，**不等于已经证明是一条真实调用链**。

---

## 10. 测试与证据

**⚪ 这是空 diff。** 没有生产代码变化，也没有需要为这个 diff 新增的测试变化。

### 本次提交的历史源码证据

- **完整差异扫描：** 完整 BASE → TARGET diff 是空的，没有历史行号证据。

---

## 风险判断

**总分：0 → 低风险**

- 没命中高风险规则。

这个分数来自固定规则，不是 AI 凭感觉打分。

## 程序员 Review 清单

- [ ] 我能用一句话说清这次改了哪几件事
- [ ] 每个变化块的“以前 → 现在”符合真实源码
- [ ] 没把两个不相关的功能画成一条假流程
- [ ] 登录 / 权限变化已经单独确认
- [ ] 数据库迁移变化已经单独确认
- [ ] API 契约和“客户端请求行为”没有混为一谈
- [ ] 外部 Provider 调用变化已经确认
- [ ] 影响范围里的“可能”没有被当成“确定”
- [ ] 测试是否覆盖关键变化已经人工确认
- [ ] 所有重要结论都能点回 BASE / TARGET 历史源码

## 直接看源码

- [完整 BASE → TARGET Diff](https://github.com/burncloud/burncloud/compare/3ff4ca580ab4294372c40ad6b4b6ce11a1bb7ee1...0f51207790b4e5beaf890ea378f19a80f2a0bcbc)
- [原 Pull Request #348](https://github.com/burncloud/burncloud/pull/348)
