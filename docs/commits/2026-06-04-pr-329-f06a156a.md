---
title: "PR #329 · fix（router）: failover on auth errors （401/402） and add xunfei channel type"
slug: /commits/2026-06-04-pr-329-f06a156a/
sidebar_position: 29
doc_type: commit-change-atlas-v2
truth: source-diff-derived
language: zh-CN
diagram_style: plaintext
repository: burncloud/burncloud
pr_number: 329
base_commit: 018855c3c571e7eb42520db46e70bc8fc5ba726e
target_commit: f06a156afd7f4c49ccbe08fe6e8b4a00f4168787
risk: 高
merged_at: 2026-06-04T10:14:27Z
---

# PR #329：这次到底改了什么？

## 一句话先看懂

这次主要改了这些事：**上游失败后换下一个渠道**、**模型请求选渠道**、**调用上游模型服务**、**其他代码整理**。

**风险：高（20 分）** · 改了 **2** 个文件 · 先看 **4** 个真正的变化块

> **作者写的目的（AUTHOR-STATED INTENT）：** fix（router）: failover on auth errors （401/402） and add xunfei channel type

> **作者补充说明（原文）：** ＃＃ 问题 当渠道返回 401 （Unauthorized） 或 402 （Payment Required） 错误时，请求直接返回错误给客户端，不会尝试 failover 到其他渠道。这导致即使有其他渠道配置了正确的 API key，请求仍然失败。 ＃＃ 修复 1. ＊＊Failover 逻辑改进＊＊: 将 401 和 402 错误加入 failover 触发条件 - 之前：只有 429 （rate limit） 触发 failover - 现在：429、401、402 都会触发 failover 到下一个候选渠道 2. ＊＊CLI 支持 xunfei 渠道类型＊＊: 添加讯飞星火渠道类型支持 - 支持 ′xunfei′ 和 ′x……（原 PR 里还有更多说明）

**完整源码差异：** [BASE `018855c3c571` → TARGET `f06a156afd7f`](https://github.com/burncloud/burncloud/compare/018855c3c571e7eb42520db46e70bc8fc5ba726e...f06a156afd7f4c49ccbe08fe6e8b4a00f4168787)

---

## 这次主要改了什么

不要先看 3 个符号。先把这次提交当成 **4 件独立的事情** 看：**上游失败后换下一个渠道、模型请求选渠道、调用上游模型服务、其他代码整理**。

### 变化 1：上游失败后换下一个渠道

**大白话：** 上游出错以后，BurnCloud 决定『直接报错』还是『换下一个渠道再试』的规则变了。 这次代码里还明确碰到了 429 限流处理。

**谁会感觉到：** 上游失败后的重试 / Failover

#### 以前 → 现在

```text
[以前]
请求发给当前 Channel
   │
   ▼
上游失败
   │
   ▼
按旧规则决定是否结束

                 变成
                  │
                  ▼

[现在]
请求发给当前 Channel
   │
   ▼
上游失败
   │
   ▼
记录失败状态
   │
   ▼
符合条件 → 换下一个候选 Channel
   │
   ▼
不符合条件 → 返回错误
```

#### 这块主要改了哪些文件

- `crates/router/src/lib.rs`

#### 源码证据

- **修改后:** [`crates/router/src/lib.rs:L2295-L2300`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/crates/router/src/lib.rs#L2295-L2300)
- **修改前:** [`crates/router/src/lib.rs:L2295-L2296`](https://github.com/burncloud/burncloud/blob/018855c3c571e7eb42520db46e70bc8fc5ba726e/crates/router/src/lib.rs#L2295-L2296)
- **修改后:** [`crates/router/src/lib.rs:L2302-L2304`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/crates/router/src/lib.rs#L2302-L2304)
- **修改前:** [`crates/router/src/lib.rs:L2298-L2299`](https://github.com/burncloud/burncloud/blob/018855c3c571e7eb42520db46e70bc8fc5ba726e/crates/router/src/lib.rs#L2298-L2299)

### 变化 2：模型请求选渠道

**大白话：** 模型请求挑选 Channel 的规则变了。要关心的是：哪些渠道能进候选、怎么排序、最后先试谁。

**谁会感觉到：** 模型请求选择 Channel

#### 以前 → 现在

```text
[以前]
模型请求进来
   │
   ▼
按旧规则找候选 Channel
   │
   ▼
选出要先尝试的 Channel

                 变成
                  │
                  ▼

[现在]
模型请求进来
   │
   ▼
按新规则过滤 / 排序候选 Channel
   │
   ▼
选出新的尝试顺序
```

#### 这块主要改了哪些文件

- `crates/router/src/lib.rs`

#### 源码证据

- **修改后:** [`crates/router/src/lib.rs:L2306-L2306`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/crates/router/src/lib.rs#L2306-L2306)
- **修改后:** [`crates/router/src/lib.rs:L2310-L2310`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/crates/router/src/lib.rs#L2310-L2310)
- **修改前:** [`crates/router/src/lib.rs:L2304-L2304`](https://github.com/burncloud/burncloud/blob/018855c3c571e7eb42520db46e70bc8fc5ba726e/crates/router/src/lib.rs#L2304-L2304)

### 变化 3：调用上游模型服务

**大白话：** BurnCloud 调上游模型服务的代码变了。可能涉及请求地址、请求头、协议转换或返回结果处理。

**谁会感觉到：** BurnCloud 调用上游 Provider

#### 以前 → 现在

```text
[以前]
BurnCloud 准备上游请求
   │
   ▼
按旧方式调用 Provider
   │
   ▼
读取 Provider 返回

                 变成
                  │
                  ▼

[现在]
BurnCloud 准备上游请求
   │
   ▼
按这次修改后的方式调用 Provider
   │
   ▼
读取并处理 Provider 返回
```

#### 这块主要改了哪些文件

- `src/cli/channel.rs`

#### 源码证据

- **修改后:** [`src/cli/channel.rs:L32-L32`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/src/cli/channel.rs#L32-L32)
- **修改后:** [`src/cli/channel.rs:L52-L52`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/src/cli/channel.rs#L52-L52)
- **修改后:** [`src/cli/channel.rs:L91-L91`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/src/cli/channel.rs#L91-L91)

### 变化 4：其他代码整理

**大白话：** 这部分代码有变化，但仅靠静态差异还不能安全地说它属于哪条用户流程，所以这里不硬猜。

**谁会感觉到：** 代码内部行为；需要按源码证据继续确认入口

#### 以前 → 现在

```text
[以前]
旧代码路径
   │
   ▼
按 BASE 版本运行

                 变成
                  │
                  ▼

[现在]
同一代码路径
   │
   ▼
按 TARGET 版本运行
   │
   ▼
具体入口请看下面的源码证据
```

#### 这块主要改了哪些文件

- `src/cli/channel.rs`

#### 源码证据

- **修改后:** [`src/cli/channel.rs:L19-L19`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/src/cli/channel.rs#L19-L19)
- **修改前:** [`src/cli/channel.rs:L19-L19`](https://github.com/burncloud/burncloud/blob/018855c3c571e7eb42520db46e70bc8fc5ba726e/src/cli/channel.rs#L19-L19)
- **修改后:** [`src/cli/channel.rs:L34-L34`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/src/cli/channel.rs#L34-L34)
- **修改前:** [`src/cli/channel.rs:L33-L33`](https://github.com/burncloud/burncloud/blob/018855c3c571e7eb42520db46e70bc8fc5ba726e/src/cli/channel.rs#L33-L33)


---

## 10 个修改维度

这 10 个维度是**检查清单**，不是阅读顺序。上面的“变化块”才是人真正应该先看的。

| 维度 | 结果 | 大白话 |
|---|---|---|
| 1. 修改范围 | 🟡 有变化 | 代码文件和符号有变化。 |
| 2. 用户流程 | 🟡 有变化 | 有用户或管理员能直接感觉到的流程变化。 |
| 3. 运行过程 | 🟡 有变化 | 程序实际运行时的步骤有变化。 |
| 4. 控制流程 | 🟡 有变化 | 有 if / match / loop / return 等控制逻辑变化。 |
| 5. 状态变化 | ⚪ 没发现变化 | 没发现明确的状态写入变化。 |
| 6. API 契约 | 🟡 有变化 | 服务器公开接口的入口、状态码或返回契约有变化。 |
| 7. 数据库 / 持久化 | ⚪ 没发现变化 | 没发现数据库 / 持久化变化。 |
| 8. 外部依赖 | 🟡 有变化 | BurnCloud 调外部 Provider 的方式有变化。 |
| 9. 影响范围 | 🟡 有变化 | 下面会把直接修改和可能受影响的地方分开列出来。 |
| 10. 测试与证据 | ⚠️ 本提交没改测试 | 这个提交没有同步修改测试文件；不等于没有旧测试，只表示本次 diff 没加/改测试。 |

---

## 1. 修改范围

**改了什么：** 2 个文件，静态解析到 3 个 Rust 符号，其中 3 个函数。

**先看这些变化块：** 上游失败后换下一个渠道、模型请求选渠道、调用上游模型服务、其他代码整理。

### 主要文件（最多 20 个）

- 🟡 修改 `crates/router/src/lib.rs`
- 🟡 修改 `src/cli/channel.rs`

### 主要代码入口（最多 12 个）

- `get_channel_type_name` — `src/cli/channel.rs:L80`
- `get_default_models` — `src/cli/channel.rs:L41`
- `parse_channel_type` — `src/cli/channel.rs:L20`

---

## 2. 用户流程

- **上游失败后换下一个渠道：** 上游失败后的重试 / Failover
- **模型请求选渠道：** 模型请求选择 Channel
- **调用上游模型服务：** BurnCloud 调用上游 Provider

> 只有能从这次 diff 安全对应到用户 / 管理员操作的变化才写在这里。数据库迁移、CI、文档不会被硬说成“用户流程”。

---

## 3. 运行过程

- **上游失败后换下一个渠道：** 上游出错以后，BurnCloud 决定『直接报错』还是『换下一个渠道再试』的规则变了。 这次代码里还明确碰到了 429 限流处理。
- **模型请求选渠道：** 模型请求挑选 Channel 的规则变了。要关心的是：哪些渠道能进候选、怎么排序、最后先试谁。
- **调用上游模型服务：** BurnCloud 调上游模型服务的代码变了。可能涉及请求地址、请求头、协议转换或返回结果处理。
- **其他代码整理：** 这部分代码有变化，但仅靠静态差异还不能安全地说它属于哪条用户流程，所以这里不硬猜。

**真正的 BEFORE → AFTER 已经放在每个“变化块”的 plaintext 图里。不同功能不会被强行接成一条假 E2E。**

---

## 4. 控制流程

这里**不把不同函数里的 if 强行串成一条假 ICFG**。只列真正改过的控制语句：

**修改前删掉 / 改掉的控制点：**
- `if status == StatusCode::TOO_MANY_REQUESTS ｛`

**修改后新增 / 改过的控制点：**
- `if status == StatusCode::TOO_MANY_REQUESTS`

---

## 5. 状态变化

- 没发现能安全概括的关键状态变化。

---

## 6. API 契约

**🟡 检测到服务器 API 边界变化。**

重点文件：
- `crates/router/src/lib.rs`

---

## 7. 数据库 / 持久化

**⚪ 没发现数据库 / 持久化变化。**

---

## 8. 外部依赖

**🟡 外部调用相关行为有变化。**

- 上游出错以后，BurnCloud 决定『直接报错』还是『换下一个渠道再试』的规则变了。 这次代码里还明确碰到了 429 限流处理。
- 模型请求挑选 Channel 的规则变了。要关心的是：哪些渠道能进候选、怎么排序、最后先试谁。
- BurnCloud 调上游模型服务的代码变了。可能涉及请求地址、请求头、协议转换或返回结果处理。

---

## 9. 影响范围

```text
这次 Commit
 ├─ 上游失败后换下一个渠道
 │     └─ 上游失败后的重试 / Failover
 ├─ 模型请求选渠道
 │     └─ 模型请求选择 Channel
 ├─ 调用上游模型服务
 │     └─ BurnCloud 调用上游 Provider
 └─ 其他代码整理
       └─ 代码内部行为；需要按源码证据继续确认入口

继续往代码里看（这里只是词法引用候选，不装成真实调用链）：
 - 直接修改：get_channel_type_name → src/cli/channel.rs:L80
 - 直接修改：get_default_models → src/cli/channel.rs:L41
 - 直接修改：parse_channel_type → src/cli/channel.rs:L20
```

> “可能受影响（词法引用）”只说明代码里找到了同名引用，**不等于已经证明是一条真实调用链**。

---

## 10. 测试与证据

**⚠️ 本提交没有同步修改测试文件。** 这不等于项目没有旧测试，只表示这次提交本身没有加 / 改测试。

### 本次提交的历史源码证据

- **修改后:** [`crates/router/src/lib.rs:L2295-L2300`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/crates/router/src/lib.rs#L2295-L2300)
- **修改前:** [`crates/router/src/lib.rs:L2295-L2296`](https://github.com/burncloud/burncloud/blob/018855c3c571e7eb42520db46e70bc8fc5ba726e/crates/router/src/lib.rs#L2295-L2296)
- **修改后:** [`crates/router/src/lib.rs:L2302-L2304`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/crates/router/src/lib.rs#L2302-L2304)
- **修改前:** [`crates/router/src/lib.rs:L2298-L2299`](https://github.com/burncloud/burncloud/blob/018855c3c571e7eb42520db46e70bc8fc5ba726e/crates/router/src/lib.rs#L2298-L2299)
- **修改后:** [`crates/router/src/lib.rs:L2306-L2306`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/crates/router/src/lib.rs#L2306-L2306)
- **修改后:** [`crates/router/src/lib.rs:L2310-L2310`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/crates/router/src/lib.rs#L2310-L2310)
- **修改前:** [`crates/router/src/lib.rs:L2304-L2304`](https://github.com/burncloud/burncloud/blob/018855c3c571e7eb42520db46e70bc8fc5ba726e/crates/router/src/lib.rs#L2304-L2304)
- **修改后:** [`src/cli/channel.rs:L19-L19`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/src/cli/channel.rs#L19-L19)
- **修改前:** [`src/cli/channel.rs:L19-L19`](https://github.com/burncloud/burncloud/blob/018855c3c571e7eb42520db46e70bc8fc5ba726e/src/cli/channel.rs#L19-L19)
- **修改后:** [`src/cli/channel.rs:L32-L32`](https://github.com/burncloud/burncloud/blob/f06a156afd7f4c49ccbe08fe6e8b4a00f4168787/src/cli/channel.rs#L32-L32)

---

## 风险判断

**总分：20 → 高风险**

- 公开 API 契约：`+5`
- 路由 / Channel 选择：`+4`
- 上游 Provider 调用：`+4`
- 重试 / Failover：`+4`
- 改了生产代码但这个提交没有同步改测试：`+3`

这个分数来自固定规则，不是 AI 凭感觉打分。

## 程序员 Review 清单

- [ ] 我能用一句话说清这次改了哪几件事
- [ ] 每个变化块的“以前 → 现在”符合真实源码
- [ ] 没把两个不相关的功能画成一条假流程
- [ ] 登录 / 权限变化已经单独确认
- [ ] 数据库迁移变化已经单独确认
- [ ] API 契约和“客户端请求行为”没有混为一谈
- [ ] 外部 Provider 调用变化已经确认
- [ ] 影响范围里的“可能”没有被当成“确定”
- [ ] 测试是否覆盖关键变化已经人工确认
- [ ] 所有重要结论都能点回 BASE / TARGET 历史源码

## 直接看源码

- [完整 BASE → TARGET Diff](https://github.com/burncloud/burncloud/compare/018855c3c571e7eb42520db46e70bc8fc5ba726e...f06a156afd7f4c49ccbe08fe6e8b4a00f4168787)
- [原 Pull Request #329](https://github.com/burncloud/burncloud/pull/329)
