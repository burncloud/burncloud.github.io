---
title: "PR #343 · feat: Enhance EmptyResponseCounter with monitoring and admin capabilities"
slug: /commits/2026-06-08-pr-343-fe1b8765/
sidebar_position: 12
doc_type: commit-change-atlas-v2
truth: source-diff-derived
language: zh-CN
diagram_style: plaintext
repository: burncloud/burncloud
pr_number: 343
base_commit: 1114ebcaf38441116246c28a3aadedfe297b40c0
target_commit: fe1b87655b7123cc07da06f4d78f8deef5eb9ab4
risk: 高
merged_at: 2026-06-08T11:53:54Z
---

# PR #343：这次到底改了什么？

## 一句话先看懂

这次主要改了一件事：**模型请求选渠道**。

**风险：高（10 分）** · 改了 **1** 个文件 · 先看 **1** 个真正的变化块

> **作者写的目的（AUTHOR-STATED INTENT）：** feat: Enhance EmptyResponseCounter with monitoring and admin capabilities

> **作者补充说明（原文）：** ＃＃ Summary - Add ′get＿count（）′ method to query current counter value for a specific channel - Add ′force＿reset（）′ method for admin override to reset counters - Add ′get＿all＿counts（）′ method to list all channels with non-zero counters - Add early warning mechanism when counter reaches ′threshold - 1′ - Improve logging w……（原 PR 里还有更多说明）

**完整源码差异：** [BASE `1114ebcaf384` → TARGET `fe1b87655b71`](https://github.com/burncloud/burncloud/compare/1114ebcaf38441116246c28a3aadedfe297b40c0...fe1b87655b7123cc07da06f4d78f8deef5eb9ab4)

---

## 这次主要改了什么

不要先看 5 个符号。先把这次提交当成 **1 件独立的事情** 看：**模型请求选渠道**。

### 变化 1：模型请求选渠道

**大白话：** 模型请求挑选 Channel 的规则变了。要关心的是：哪些渠道能进候选、怎么排序、最后先试谁。

**谁会感觉到：** 模型请求选择 Channel

#### 以前 → 现在

```text
[以前]
模型请求进来
   │
   ▼
按旧规则找候选 Channel
   │
   ▼
选出要先尝试的 Channel

                 变成
                  │
                  ▼

[现在]
模型请求进来
   │
   ▼
按新规则过滤 / 排序候选 Channel
   │
   ▼
选出新的尝试顺序
```

#### 这块主要改了哪些文件

- `crates/router/src/lib.rs`

#### 源码证据

- **修改后:** [`crates/router/src/lib.rs:L56-L65`](https://github.com/burncloud/burncloud/blob/fe1b87655b7123cc07da06f4d78f8deef5eb9ab4/crates/router/src/lib.rs#L56-L65)
- **修改前:** [`crates/router/src/lib.rs:L56-L56`](https://github.com/burncloud/burncloud/blob/1114ebcaf38441116246c28a3aadedfe297b40c0/crates/router/src/lib.rs#L56-L56)
- **修改后:** [`crates/router/src/lib.rs:L97-L127`](https://github.com/burncloud/burncloud/blob/fe1b87655b7123cc07da06f4d78f8deef5eb9ab4/crates/router/src/lib.rs#L97-L127)


---

## 10 个修改维度

这 10 个维度是**检查清单**，不是阅读顺序。上面的“变化块”才是人真正应该先看的。

| 维度 | 结果 | 大白话 |
|---|---|---|
| 1. 修改范围 | 🟡 有变化 | 代码文件和符号有变化。 |
| 2. 用户流程 | 🟡 有变化 | 有用户或管理员能直接感觉到的流程变化。 |
| 3. 运行过程 | 🟡 有变化 | 程序实际运行时的步骤有变化。 |
| 4. 控制流程 | 🟡 有变化 | 有 if / match / loop / return 等控制逻辑变化。 |
| 5. 状态变化 | 🟡 有变化 | 有数据库、页面状态、计数器或健康状态等写入变化。 |
| 6. API 契约 | ⚪ 没发现变化 | 没发现服务器公开 API 契约变化；客户端自己加请求头不自动算契约变化。 |
| 7. 数据库 / 持久化 | ⚪ 没发现变化 | 没发现数据库 / 持久化变化。 |
| 8. 外部依赖 | ⚪ 没发现变化 | 没发现明确的外部 Provider 调用变化。 |
| 9. 影响范围 | 🟡 有变化 | 下面会把直接修改和可能受影响的地方分开列出来。 |
| 10. 测试与证据 | ⚠️ 本提交没改测试 | 这个提交没有同步修改测试文件；不等于没有旧测试，只表示本次 diff 没加/改测试。 |

---

## 1. 修改范围

**改了什么：** 1 个文件，静态解析到 5 个 Rust 符号，其中 5 个函数。

**先看这些变化块：** 模型请求选渠道。

### 主要文件（最多 20 个）

- 🟡 修改 `crates/router/src/lib.rs`

### 主要代码入口（最多 12 个）

- `force_reset` — `crates/router/src/lib.rs:L106`
- `get_all_counts` — `crates/router/src/lib.rs:L120`
- `get_count` — `crates/router/src/lib.rs:L99`
- `record_empty` — `crates/router/src/lib.rs:L51`
- `reset` — `crates/router/src/lib.rs:L84`

---

## 2. 用户流程

- **模型请求选渠道：** 模型请求选择 Channel

> 只有能从这次 diff 安全对应到用户 / 管理员操作的变化才写在这里。数据库迁移、CI、文档不会被硬说成“用户流程”。

---

## 3. 运行过程

- **模型请求选渠道：** 模型请求挑选 Channel 的规则变了。要关心的是：哪些渠道能进候选、怎么排序、最后先试谁。

**真正的 BEFORE → AFTER 已经放在每个“变化块”的 plaintext 图里。不同功能不会被强行接成一条假 E2E。**

---

## 4. 控制流程

这里**不把不同函数里的 if 强行串成一条假 ICFG**。只列真正改过的控制语句：

**修改前删掉 / 改掉的控制点：**
- `if exceeded ｛`

**修改后新增 / 改过的控制点：**
- `if *count == self.threshold - 1 ｛`
- `if previous_count › 0 ｛`

---

## 5. 状态变化

- 没发现能安全概括的关键状态变化。

---

## 6. API 契约

**⚪ 没发现服务器公开 API 契约变化。**

---

## 7. 数据库 / 持久化

**⚪ 没发现数据库 / 持久化变化。**

---

## 8. 外部依赖

**⚪ 没发现 BurnCloud 调外部 Provider 的方式发生明确变化。**

---

## 9. 影响范围

```text
这次 Commit
 └─ 模型请求选渠道
       └─ 模型请求选择 Channel

继续往代码里看（这里只是词法引用候选，不装成真实调用链）：
 - 直接修改：force_reset → crates/router/src/lib.rs:L106
 - 直接修改：get_all_counts → crates/router/src/lib.rs:L120
 - 直接修改：get_count → crates/router/src/lib.rs:L99
 - 直接修改：record_empty → crates/router/src/lib.rs:L51
 - 直接修改：reset → crates/router/src/lib.rs:L84
```

> “可能受影响（词法引用）”只说明代码里找到了同名引用，**不等于已经证明是一条真实调用链**。

---

## 10. 测试与证据

**⚠️ 本提交没有同步修改测试文件。** 这不等于项目没有旧测试，只表示这次提交本身没有加 / 改测试。

**名字匹配到的可能相关旧测试（只是候选，不冒充已覆盖）：**
- `reset` → `fe1b87655b7123cc07da06f4d78f8deef5eb9ab4:crates/tests/tests/e2e/auth_flow.rs:29:async fn test_reset_password_page_loads() {`

### 本次提交的历史源码证据

- **修改后:** [`crates/router/src/lib.rs:L56-L65`](https://github.com/burncloud/burncloud/blob/fe1b87655b7123cc07da06f4d78f8deef5eb9ab4/crates/router/src/lib.rs#L56-L65)
- **修改前:** [`crates/router/src/lib.rs:L56-L56`](https://github.com/burncloud/burncloud/blob/1114ebcaf38441116246c28a3aadedfe297b40c0/crates/router/src/lib.rs#L56-L56)
- **修改后:** [`crates/router/src/lib.rs:L97-L127`](https://github.com/burncloud/burncloud/blob/fe1b87655b7123cc07da06f4d78f8deef5eb9ab4/crates/router/src/lib.rs#L97-L127)

---

## 风险判断

**总分：10 → 高风险**

- 路由 / Channel 选择：`+4`
- 关键状态写入：`+3`
- 改了生产代码但这个提交没有同步改测试：`+3`

这个分数来自固定规则，不是 AI 凭感觉打分。

## 程序员 Review 清单

- [ ] 我能用一句话说清这次改了哪几件事
- [ ] 每个变化块的“以前 → 现在”符合真实源码
- [ ] 没把两个不相关的功能画成一条假流程
- [ ] 登录 / 权限变化已经单独确认
- [ ] 数据库迁移变化已经单独确认
- [ ] API 契约和“客户端请求行为”没有混为一谈
- [ ] 外部 Provider 调用变化已经确认
- [ ] 影响范围里的“可能”没有被当成“确定”
- [ ] 测试是否覆盖关键变化已经人工确认
- [ ] 所有重要结论都能点回 BASE / TARGET 历史源码

## 直接看源码

- [完整 BASE → TARGET Diff](https://github.com/burncloud/burncloud/compare/1114ebcaf38441116246c28a3aadedfe297b40c0...fe1b87655b7123cc07da06f4d78f8deef5eb9ab4)
- [原 Pull Request #343](https://github.com/burncloud/burncloud/pull/343)
