---
title: "PR #336 · fix（router）: 为流式响应添加 empty response 检测"
slug: /commits/2026-06-07-pr-336-cdb848f6/
sidebar_position: 18
doc_type: commit-change-atlas-v2
truth: source-diff-derived
language: zh-CN
diagram_style: plaintext
repository: burncloud/burncloud
pr_number: 336
base_commit: d2733ccac33fca5895c2a8da3a007642e82d1fae
target_commit: cdb848f6b0c9199bdd2df8879b4a5e0dd0734357
risk: 高
merged_at: 2026-06-07T10:11:15Z
---

# PR #336：这次到底改了什么？

## 一句话先看懂

这次主要改了这些事：**流式返回**、**数据库升级和保存数据**、**登录和鉴权**、**模型请求选渠道**、**上游失败后换下一个渠道**。

**风险：高（30 分）** · 改了 **7** 个文件 · 先看 **5** 个真正的变化块

> **作者写的目的（AUTHOR-STATED INTENT）：** fix（router）: 为流式响应添加 empty response 检测

> **作者补充说明（原文）：** ＃＃ 问题背景 在分析日志时发现 channel 5 持续被调用，尽管它返回的是空响应（zero tokens）。原因是流式响应（streaming）完成后没有检测 empty response，导致： 1. ′record＿upstream＿success′ 在流开始时就记录了成功 2. 流结束后没有检测是否真的收到了 tokens 3. affinity 缓存没有被驱逐，下一个请求继续路由到同一个有问题的 channel ＃＃ 解决方案 ＃＃＃ commit 1: feat（router）: 改进 Circuit Breaker 和失败检测机制 （Issue ＃333） - ＊＊AuthFailed/PaymentRequire……（原 PR 里还有更多说明）

**完整源码差异：** [BASE `d2733ccac33f` → TARGET `cdb848f6b0c9`](https://github.com/burncloud/burncloud/compare/d2733ccac33fca5895c2a8da3a007642e82d1fae...cdb848f6b0c9199bdd2df8879b4a5e0dd0734357)

---

## 这次主要改了什么

不要先看 36 个符号。先把这次提交当成 **5 件独立的事情** 看：**流式返回、数据库升级和保存数据、登录和鉴权、模型请求选渠道、上游失败后换下一个渠道**。

### 变化 1：流式返回

**大白话：** 这次改了流式响应的处理顺序。重点要看数据什么时候开始交给客户端，以及出错后还能不能重试。

**谁会感觉到：** 用户发起流式模型请求

#### 以前 → 现在

```text
[以前]
流式请求
   │
   ▼
按旧的流处理顺序返回

                 变成
                  │
                  ▼

[现在]
流式请求
   │
   ▼
按新的流处理顺序检查并返回
```

#### 这块主要改了哪些文件

- `crates/router/src/lib.rs`

#### 源码证据

- **修改后:** [`crates/router/src/lib.rs:L104-L151`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L104-L151)
- **修改后:** [`crates/router/src/lib.rs:L2472-L2497`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L2472-L2497)
- **修改前:** [`crates/router/src/lib.rs:L2147-L2153`](https://github.com/burncloud/burncloud/blob/d2733ccac33fca5895c2a8da3a007642e82d1fae/crates/router/src/lib.rs#L2147-L2153)
- **修改后:** [`crates/router/src/lib.rs:L2499-L2535`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L2499-L2535)
- **修改前:** [`crates/router/src/lib.rs:L2155-L2160`](https://github.com/burncloud/burncloud/blob/d2733ccac33fca5895c2a8da3a007642e82d1fae/crates/router/src/lib.rs#L2155-L2160)
- **修改后:** [`crates/router/src/lib.rs:L3035-L3107`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L3035-L3107)

### 变化 2：数据库升级和保存数据

**大白话：** 这次改了数据库结构或升级步骤。程序启动或升级数据库时，会按新的迁移规则处理。

**谁会感觉到：** 程序启动 / 数据库升级 / 保存数据

#### 以前 → 现在

```text
[以前]
读取旧数据库
   │
   ▼
按旧结构 / 旧迁移继续运行

                 变成
                  │
                  ▼

[现在]
读取旧数据库
   │
   ▼
执行新的数据库迁移
   │
   ▼
按新结构继续运行
```

#### 这块主要改了哪些文件

- `crates/database/crates/router/src/lib.rs`
- `crates/database/crates/router/src/log.rs`
- `crates/database/migrations/postgres/0017_router_request_logs.sql`
- `crates/database/migrations/sqlite/0017_router_request_logs.sql`

#### 源码证据

- **修改后:** [`crates/database/crates/router/src/lib.rs:L20-L22`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/database/crates/router/src/lib.rs#L20-L22)
- **修改前:** [`crates/database/crates/router/src/lib.rs:L20-L21`](https://github.com/burncloud/burncloud/blob/d2733ccac33fca5895c2a8da3a007642e82d1fae/crates/database/crates/router/src/lib.rs#L20-L21)
- **修改后:** [`crates/database/crates/router/src/lib.rs:L254-L270`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/database/crates/router/src/lib.rs#L254-L270)
- **修改后:** [`crates/database/crates/router/src/log.rs:L727-L908`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/database/crates/router/src/log.rs#L727-L908)
- **修改后:** [`crates/database/migrations/postgres/0017_router_request_logs.sql:L1-L51`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/database/migrations/postgres/0017_router_request_logs.sql#L1-L51)
- **修改后:** [`crates/database/migrations/sqlite/0017_router_request_logs.sql:L1-L45`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/database/migrations/sqlite/0017_router_request_logs.sql#L1-L45)

### 变化 3：登录和鉴权

**大白话：** 这次改了登录、Token 或权限检查。重点是让『拿到身份』和『后面检查身份』的代码更一致。

**谁会感觉到：** 用户登录 / 访问受保护的 Console 接口

#### 以前 → 现在

```text
[以前]
用户登录或发请求
   │
   ▼
走旧的 Token / 权限处理

                 变成
                  │
                  ▼

[现在]
用户登录或发请求
   │
   ▼
走这次修改后的 Token / 权限处理
   │
   ▼
再进入受保护功能
```

#### 这块主要改了哪些文件

- `crates/router/src/lib.rs`

#### 源码证据

- **修改后:** [`crates/router/src/lib.rs:L324-L466`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L324-L466)
- **修改后:** [`crates/router/src/lib.rs:L1880-L1897`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L1880-L1897)

### 变化 4：模型请求选渠道

**大白话：** 模型请求挑选 Channel 的规则变了。要关心的是：哪些渠道能进候选、怎么排序、最后先试谁。

**谁会感觉到：** 模型请求选择 Channel

#### 以前 → 现在

```text
[以前]
模型请求进来
   │
   ▼
按旧规则找候选 Channel
   │
   ▼
选出要先尝试的 Channel

                 变成
                  │
                  ▼

[现在]
模型请求进来
   │
   ▼
按新规则过滤 / 排序候选 Channel
   │
   ▼
选出新的尝试顺序
```

#### 这块主要改了哪些文件

- `crates/router/src/lib.rs`
- `crates/router/src/state.rs`

#### 源码证据

- **修改后:** [`crates/router/src/lib.rs:L175-L177`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L175-L177)
- **修改后:** [`crates/router/src/lib.rs:L683-L698`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L683-L698)
- **修改后:** [`crates/router/src/lib.rs:L761-L774`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L761-L774)
- **修改后:** [`crates/router/src/lib.rs:L782-L782`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L782-L782)
- **修改后:** [`crates/router/src/lib.rs:L799-L799`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L799-L799)
- **修改后:** [`crates/router/src/lib.rs:L1722-L1722`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L1722-L1722)

### 变化 5：上游失败后换下一个渠道

**大白话：** 上游出错以后，BurnCloud 决定『直接报错』还是『换下一个渠道再试』的规则变了。

**谁会感觉到：** 上游失败后的重试 / Failover

#### 以前 → 现在

```text
[以前]
请求发给当前 Channel
   │
   ▼
上游失败
   │
   ▼
按旧规则决定是否结束

                 变成
                  │
                  ▼

[现在]
请求发给当前 Channel
   │
   ▼
上游失败
   │
   ▼
记录失败状态
   │
   ▼
符合条件 → 换下一个候选 Channel
   │
   ▼
不符合条件 → 返回错误
```

#### 这块主要改了哪些文件

- `crates/router/src/lib.rs`

#### 源码证据

- **修改后:** [`crates/router/src/lib.rs:L39-L40`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L39-L40)
- **修改前:** [`crates/router/src/lib.rs:L39-L39`](https://github.com/burncloud/burncloud/blob/d2733ccac33fca5895c2a8da3a007642e82d1fae/crates/router/src/lib.rs#L39-L39)
- **修改后:** [`crates/router/src/lib.rs:L1766-L1801`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L1766-L1801)
- **修改后:** [`crates/router/src/lib.rs:L2262-L2269`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L2262-L2269)
- **修改后:** [`crates/router/src/lib.rs:L2286-L2293`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L2286-L2293)


---

## 10 个修改维度

这 10 个维度是**检查清单**，不是阅读顺序。上面的“变化块”才是人真正应该先看的。

| 维度 | 结果 | 大白话 |
|---|---|---|
| 1. 修改范围 | 🟡 有变化 | 代码文件和符号有变化。 |
| 2. 用户流程 | 🟡 有变化 | 有用户或管理员能直接感觉到的流程变化。 |
| 3. 运行过程 | 🟡 有变化 | 程序实际运行时的步骤有变化。 |
| 4. 控制流程 | 🟡 有变化 | 有 if / match / loop / return 等控制逻辑变化。 |
| 5. 状态变化 | 🟡 有变化 | 有数据库、页面状态、计数器或健康状态等写入变化。 |
| 6. API 契约 | 🟡 有变化 | 服务器公开接口的入口、状态码或返回契约有变化。 |
| 7. 数据库 / 持久化 | 🟡 有变化 | 数据库迁移、表结构或持久化做法有变化。 |
| 8. 外部依赖 | 🟡 有变化 | BurnCloud 调外部 Provider 的方式有变化。 |
| 9. 影响范围 | 🟡 有变化 | 下面会把直接修改和可能受影响的地方分开列出来。 |
| 10. 测试与证据 | 🟡 有变化 | 这个提交同时改了 1 个测试文件。 |

---

## 1. 修改范围

**改了什么：** 7 个文件，静态解析到 36 个 Rust 符号，其中 21 个函数。

**先看这些变化块：** 流式返回、数据库升级和保存数据、登录和鉴权、模型请求选渠道、上游失败后换下一个渠道。

### 主要文件（最多 20 个）

- 🟡 修改 `crates/database/crates/router/src/lib.rs`
- 🟡 修改 `crates/database/crates/router/src/log.rs`
- 🟢 新增 `crates/database/migrations/postgres/0017_router_request_logs.sql`
- 🟢 新增 `crates/database/migrations/sqlite/0017_router_request_logs.sql`
- 🟡 修改 `crates/router/src/lib.rs`
- 🟡 修改 `crates/router/src/state.rs`
- 🟡 修改 `crates/router/tests/common.rs`

### 主要代码入口（最多 12 个）

- `delete_old_request_logs` — `crates/database/crates/router/src/lib.rs:L267`
- `get_request_log_by_id` — `crates/database/crates/router/src/lib.rs:L260`
- `get_usage_by_user` — `crates/database/crates/router/src/lib.rs:L250`
- `insert_request_log` — `crates/database/crates/router/src/lib.rs:L256`
- `token` — `crates/database/crates/router/src/lib.rs:L15`
- `StoragePolicy` — `crates/database/crates/router/src/log.rs:L729`
- `as_str` — `crates/database/crates/router/src/log.rs:L740`
- `delete_old_logs` — `crates/database/crates/router/src/log.rs:L890`
- `from_str` — `crates/database/crates/router/src/log.rs:L748`
- `get_by_request_id` — `crates/database/crates/router/src/log.rs:L872`
- `insert` — `crates/database/crates/router/src/log.rs:L831`
- `CandidateInfo` — `crates/database/crates/router/src/log.rs:L820`

---

## 2. 用户流程

- **流式返回：** 用户发起流式模型请求
- **登录和鉴权：** 用户登录 / 访问受保护的 Console 接口
- **模型请求选渠道：** 模型请求选择 Channel
- **上游失败后换下一个渠道：** 上游失败后的重试 / Failover

> 只有能从这次 diff 安全对应到用户 / 管理员操作的变化才写在这里。数据库迁移、CI、文档不会被硬说成“用户流程”。

---

## 3. 运行过程

- **流式返回：** 这次改了流式响应的处理顺序。重点要看数据什么时候开始交给客户端，以及出错后还能不能重试。
- **数据库升级和保存数据：** 这次改了数据库结构或升级步骤。程序启动或升级数据库时，会按新的迁移规则处理。
- **登录和鉴权：** 这次改了登录、Token 或权限检查。重点是让『拿到身份』和『后面检查身份』的代码更一致。
- **模型请求选渠道：** 模型请求挑选 Channel 的规则变了。要关心的是：哪些渠道能进候选、怎么排序、最后先试谁。
- **上游失败后换下一个渠道：** 上游出错以后，BurnCloud 决定『直接报错』还是『换下一个渠道再试』的规则变了。

**真正的 BEFORE → AFTER 已经放在每个“变化块”的 plaintext 图里。不同功能不会被强行接成一条假 E2E。**

---

## 4. 控制流程

这里**不把不同函数里的 if 强行串成一条假 ICFG**。只列真正改过的控制语句：

**修改前删掉 / 改掉的控制点：**
- `if let Some（u） = parse_chunk_or_default（`
- `if let Some（u） =`
- `match parser.provider_name（） ｛`
- `if let Some（converted） =`
- `if let Some（headers_mut） = response_builder.headers_mut（） ｛`
- `if let Some（u） = parse_chunk_or_default（parser.as_ref（）, &text, 'stream'） ｛`

**修改后新增 / 改过的控制点：**
- `match self ｛`
- `match s ｛`
- `if let Some（data） = log_data ｛`
- `if body_bytes.is_empty（） ｛`
- `return （None, false）;`
- `if let Ok（mut json） = serde_json::from_str::‹serde_json::Value›（&body_str） ｛`

---

## 5. 状态变化

- 数据库里的表、迁移或保存数据步骤发生变化。
- 登录 / Token 相关状态的处理发生变化。

---

## 6. API 契约

**🟡 检测到服务器 API 边界变化。**

重点文件：
- `crates/router/src/lib.rs`
- `crates/router/src/state.rs`
- `crates/router/tests/common.rs`

---

## 7. 数据库 / 持久化

**🟡 有数据库变化。**

这次改了数据库结构或升级步骤。程序启动或升级数据库时，会按新的迁移规则处理。

涉及文件：
- `crates/database/crates/router/src/lib.rs`
- `crates/database/crates/router/src/log.rs`
- `crates/database/migrations/postgres/0017_router_request_logs.sql`
- `crates/database/migrations/sqlite/0017_router_request_logs.sql`

---

## 8. 外部依赖

**🟡 外部调用相关行为有变化。**

- 这次改了流式响应的处理顺序。重点要看数据什么时候开始交给客户端，以及出错后还能不能重试。
- 模型请求挑选 Channel 的规则变了。要关心的是：哪些渠道能进候选、怎么排序、最后先试谁。
- 上游出错以后，BurnCloud 决定『直接报错』还是『换下一个渠道再试』的规则变了。

---

## 9. 影响范围

```text
这次 Commit
 ├─ 流式返回
 │     └─ 用户发起流式模型请求
 ├─ 数据库升级和保存数据
 │     └─ 程序启动 / 数据库升级 / 保存数据
 ├─ 登录和鉴权
 │     └─ 用户登录 / 访问受保护的 Console 接口
 ├─ 模型请求选渠道
 │     └─ 模型请求选择 Channel
 └─ 上游失败后换下一个渠道
       └─ 上游失败后的重试 / Failover

继续往代码里看（这里只是词法引用候选，不装成真实调用链）：
 - 直接修改：delete_old_request_logs → crates/database/crates/router/src/lib.rs:L267
 - 直接修改：get_request_log_by_id → crates/database/crates/router/src/lib.rs:L260
 - 直接修改：get_usage_by_user → crates/database/crates/router/src/lib.rs:L250
 - 直接修改：insert_request_log → crates/database/crates/router/src/lib.rs:L256
 - 直接修改：token → crates/database/crates/router/src/lib.rs:L15
 - 直接修改：StoragePolicy → crates/database/crates/router/src/log.rs:L729
```

> “可能受影响（词法引用）”只说明代码里找到了同名引用，**不等于已经证明是一条真实调用链**。

---

## 10. 测试与证据

**本提交同步修改了测试：**
- `crates/router/tests/common.rs`

**名字匹配到的可能相关旧测试（只是候选，不冒充已覆盖）：**
- `token` → `cdb848f6b0c9199bdd2df8879b4a5e0dd0734357:crates/database/crates/router/tests/cost_roundtrip.rs:3:/// Integration tests for RouterLog per-type cost and token round-trip.`
- `as_str` → `cdb848f6b0c9199bdd2df8879b4a5e0dd0734357:crates/database/crates/sys/tests/setting_database_tests.rs:139:    let names: Vec<&str> = all.iter().map(|s| s.name.as_str()).collect();`
- `from_str` → `cdb848f6b0c9199bdd2df8879b4a5e0dd0734357:crates/database/tests/migration_rename_tests.rs:679:    let options = AnyConnectOptions::from_str(&url)`
- `insert` → `cdb848f6b0c9199bdd2df8879b4a5e0dd0734357:crates/database/crates/router/tests/cost_roundtrip.rs:32:/// `router_tokens` which `RouterLogModel::insert` updates for quota deduction.`

### 本次提交的历史源码证据

- **修改后:** [`crates/database/crates/router/src/lib.rs:L20-L22`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/database/crates/router/src/lib.rs#L20-L22)
- **修改前:** [`crates/database/crates/router/src/lib.rs:L20-L21`](https://github.com/burncloud/burncloud/blob/d2733ccac33fca5895c2a8da3a007642e82d1fae/crates/database/crates/router/src/lib.rs#L20-L21)
- **修改后:** [`crates/database/crates/router/src/lib.rs:L254-L270`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/database/crates/router/src/lib.rs#L254-L270)
- **修改后:** [`crates/database/crates/router/src/log.rs:L727-L908`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/database/crates/router/src/log.rs#L727-L908)
- **修改后:** [`crates/database/migrations/postgres/0017_router_request_logs.sql:L1-L51`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/database/migrations/postgres/0017_router_request_logs.sql#L1-L51)
- **修改后:** [`crates/database/migrations/sqlite/0017_router_request_logs.sql:L1-L45`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/database/migrations/sqlite/0017_router_request_logs.sql#L1-L45)
- **修改后:** [`crates/router/src/lib.rs:L39-L40`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L39-L40)
- **修改前:** [`crates/router/src/lib.rs:L39-L39`](https://github.com/burncloud/burncloud/blob/d2733ccac33fca5895c2a8da3a007642e82d1fae/crates/router/src/lib.rs#L39-L39)
- **修改后:** [`crates/router/src/lib.rs:L104-L151`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L104-L151)
- **修改后:** [`crates/router/src/lib.rs:L175-L177`](https://github.com/burncloud/burncloud/blob/cdb848f6b0c9199bdd2df8879b4a5e0dd0734357/crates/router/src/lib.rs#L175-L177)

---

## 风险判断

**总分：30 → 高风险**

- 登录 / 鉴权逻辑：`+5`
- 数据库迁移或持久化：`+5`
- 公开 API 契约：`+5`
- 路由 / Channel 选择：`+4`
- 流式响应：`+4`
- 重试 / Failover：`+4`
- 关键状态写入：`+3`

这个分数来自固定规则，不是 AI 凭感觉打分。

## 程序员 Review 清单

- [ ] 我能用一句话说清这次改了哪几件事
- [ ] 每个变化块的“以前 → 现在”符合真实源码
- [ ] 没把两个不相关的功能画成一条假流程
- [ ] 登录 / 权限变化已经单独确认
- [ ] 数据库迁移变化已经单独确认
- [ ] API 契约和“客户端请求行为”没有混为一谈
- [ ] 外部 Provider 调用变化已经确认
- [ ] 影响范围里的“可能”没有被当成“确定”
- [ ] 测试是否覆盖关键变化已经人工确认
- [ ] 所有重要结论都能点回 BASE / TARGET 历史源码

## 直接看源码

- [完整 BASE → TARGET Diff](https://github.com/burncloud/burncloud/compare/d2733ccac33fca5895c2a8da3a007642e82d1fae...cdb848f6b0c9199bdd2df8879b4a5e0dd0734357)
- [原 Pull Request #336](https://github.com/burncloud/burncloud/pull/336)
