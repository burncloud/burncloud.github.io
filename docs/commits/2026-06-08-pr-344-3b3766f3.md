---
title: "PR #344 · Fix: Add empty response check for non-streaming passthrough path"
slug: /commits/2026-06-08-pr-344-3b3766f3/
sidebar_position: 11
doc_type: commit-change-atlas-v2
truth: source-diff-derived
language: zh-CN
diagram_style: plaintext
repository: burncloud/burncloud
pr_number: 344
base_commit: f700a8ab7c0ff8ee964aa85a58e25b205e40fe77
target_commit: 3b3766f3c867b39ea35173a7458b9dcaf38612d2
risk: 高
merged_at: 2026-06-08T19:47:36Z
---

# PR #344：这次到底改了什么？

## 一句话先看懂

这次主要改了两件事：**流式返回**，还有 **调用上游模型服务**。

**风险：高（14 分）** · 改了 **1** 个文件 · 先看 **2** 个真正的变化块

> **作者写的目的（AUTHOR-STATED INTENT）：** Fix: Add empty response check for non-streaming passthrough path

> **作者补充说明（原文）：** ＃＃ Problem Channel 7 was returning empty responses （prompt＿tokens=0, completion＿tokens=0） with HTTP 200 status, but the empty response detection logic was not triggered. This caused requests to keep using channel 7 via affinity cache even though it was returning empty responses, resulting in users on 47.93.162.155 bein……（原 PR 里还有更多说明）

**完整源码差异：** [BASE `f700a8ab7c0f` → TARGET `3b3766f3c867`](https://github.com/burncloud/burncloud/compare/f700a8ab7c0ff8ee964aa85a58e25b205e40fe77...3b3766f3c867b39ea35173a7458b9dcaf38612d2)

---

## 这次主要改了什么

不要先看 1 个符号。先把这次提交当成 **2 件独立的事情** 看：**流式返回、调用上游模型服务**。

### 变化 1：流式返回

**大白话：** 这次改了流式响应的处理顺序。重点要看数据什么时候开始交给客户端，以及出错后还能不能重试。

**谁会感觉到：** 用户发起流式模型请求

#### 以前 → 现在

```text
[以前]
流式请求
   │
   ▼
按旧的流处理顺序返回

                 变成
                  │
                  ▼

[现在]
流式请求
   │
   ▼
按新的流处理顺序检查并返回
```

#### 这块主要改了哪些文件

- `crates/router/src/lib.rs`

#### 源码证据

- **修改后:** [`crates/router/src/lib.rs:L383-L396`](https://github.com/burncloud/burncloud/blob/3b3766f3c867b39ea35173a7458b9dcaf38612d2/crates/router/src/lib.rs#L383-L396)
- **修改前:** [`crates/router/src/lib.rs:L382-L390`](https://github.com/burncloud/burncloud/blob/f700a8ab7c0ff8ee964aa85a58e25b205e40fe77/crates/router/src/lib.rs#L382-L390)
- **修改后:** [`crates/router/src/lib.rs:L2824-L2828`](https://github.com/burncloud/burncloud/blob/3b3766f3c867b39ea35173a7458b9dcaf38612d2/crates/router/src/lib.rs#L2824-L2828)

### 变化 2：调用上游模型服务

**大白话：** BurnCloud 调上游模型服务的代码变了。可能涉及请求地址、请求头、协议转换或返回结果处理。

**谁会感觉到：** BurnCloud 调用上游 Provider

#### 以前 → 现在

```text
[以前]
BurnCloud 准备上游请求
   │
   ▼
按旧方式调用 Provider
   │
   ▼
读取 Provider 返回

                 变成
                  │
                  ▼

[现在]
BurnCloud 准备上游请求
   │
   ▼
按这次修改后的方式调用 Provider
   │
   ▼
读取并处理 Provider 返回
```

#### 这块主要改了哪些文件

- `crates/router/src/lib.rs`

#### 源码证据

- **修改后:** [`crates/router/src/lib.rs:L374-L374`](https://github.com/burncloud/burncloud/blob/3b3766f3c867b39ea35173a7458b9dcaf38612d2/crates/router/src/lib.rs#L374-L374)
- **修改后:** [`crates/router/src/lib.rs:L403-L404`](https://github.com/burncloud/burncloud/blob/3b3766f3c867b39ea35173a7458b9dcaf38612d2/crates/router/src/lib.rs#L403-L404)


---

## 10 个修改维度

这 10 个维度是**检查清单**，不是阅读顺序。上面的“变化块”才是人真正应该先看的。

| 维度 | 结果 | 大白话 |
|---|---|---|
| 1. 修改范围 | 🟡 有变化 | 代码文件和符号有变化。 |
| 2. 用户流程 | 🟡 有变化 | 有用户或管理员能直接感觉到的流程变化。 |
| 3. 运行过程 | 🟡 有变化 | 程序实际运行时的步骤有变化。 |
| 4. 控制流程 | 🟡 有变化 | 有 if / match / loop / return 等控制逻辑变化。 |
| 5. 状态变化 | 🟡 有变化 | 有数据库、页面状态、计数器或健康状态等写入变化。 |
| 6. API 契约 | ⚪ 没发现变化 | 没发现服务器公开 API 契约变化；客户端自己加请求头不自动算契约变化。 |
| 7. 数据库 / 持久化 | ⚪ 没发现变化 | 没发现数据库 / 持久化变化。 |
| 8. 外部依赖 | 🟡 有变化 | BurnCloud 调外部 Provider 的方式有变化。 |
| 9. 影响范围 | 🟡 有变化 | 下面会把直接修改和可能受影响的地方分开列出来。 |
| 10. 测试与证据 | ⚠️ 本提交没改测试 | 这个提交没有同步修改测试文件；不等于没有旧测试，只表示本次 diff 没加/改测试。 |

---

## 1. 修改范围

**改了什么：** 1 个文件，静态解析到 1 个 Rust 符号，其中 1 个函数。

**先看这些变化块：** 流式返回、调用上游模型服务。

### 主要文件（最多 20 个）

- 🟡 修改 `crates/router/src/lib.rs`

### 主要代码入口（最多 12 个）

- `check_empty_response` — `crates/router/src/lib.rs:L367`

---

## 2. 用户流程

- **流式返回：** 用户发起流式模型请求
- **调用上游模型服务：** BurnCloud 调用上游 Provider

> 只有能从这次 diff 安全对应到用户 / 管理员操作的变化才写在这里。数据库迁移、CI、文档不会被硬说成“用户流程”。

---

## 3. 运行过程

- **流式返回：** 这次改了流式响应的处理顺序。重点要看数据什么时候开始交给客户端，以及出错后还能不能重试。
- **调用上游模型服务：** BurnCloud 调上游模型服务的代码变了。可能涉及请求地址、请求头、协议转换或返回结果处理。

**真正的 BEFORE → AFTER 已经放在每个“变化块”的 plaintext 图里。不同功能不会被强行接成一条假 E2E。**

---

## 4. 控制流程

这里**不把不同函数里的 if 强行串成一条假 ICFG**。只列真正改过的控制语句：


**修改后新增 / 改过的控制点：**
- `if should_penalize ｛`
- `if check_empty_response（state, upstream, model_name, &session_id, &token_counter） ｛`
- `continue; // Try next candidate`

---

## 5. 状态变化

- 没发现能安全概括的关键状态变化。

---

## 6. API 契约

**⚪ 没发现服务器公开 API 契约变化。**

---

## 7. 数据库 / 持久化

**⚪ 没发现数据库 / 持久化变化。**

---

## 8. 外部依赖

**🟡 外部调用相关行为有变化。**

- 这次改了流式响应的处理顺序。重点要看数据什么时候开始交给客户端，以及出错后还能不能重试。
- BurnCloud 调上游模型服务的代码变了。可能涉及请求地址、请求头、协议转换或返回结果处理。

---

## 9. 影响范围

```text
这次 Commit
 ├─ 流式返回
 │     └─ 用户发起流式模型请求
 └─ 调用上游模型服务
       └─ BurnCloud 调用上游 Provider

继续往代码里看（这里只是词法引用候选，不装成真实调用链）：
 - 直接修改：check_empty_response → crates/router/src/lib.rs:L367
```

> “可能受影响（词法引用）”只说明代码里找到了同名引用，**不等于已经证明是一条真实调用链**。

---

## 10. 测试与证据

**⚠️ 本提交没有同步修改测试文件。** 这不等于项目没有旧测试，只表示这次提交本身没有加 / 改测试。

### 本次提交的历史源码证据

- **修改后:** [`crates/router/src/lib.rs:L374-L374`](https://github.com/burncloud/burncloud/blob/3b3766f3c867b39ea35173a7458b9dcaf38612d2/crates/router/src/lib.rs#L374-L374)
- **修改后:** [`crates/router/src/lib.rs:L383-L396`](https://github.com/burncloud/burncloud/blob/3b3766f3c867b39ea35173a7458b9dcaf38612d2/crates/router/src/lib.rs#L383-L396)
- **修改前:** [`crates/router/src/lib.rs:L382-L390`](https://github.com/burncloud/burncloud/blob/f700a8ab7c0ff8ee964aa85a58e25b205e40fe77/crates/router/src/lib.rs#L382-L390)
- **修改后:** [`crates/router/src/lib.rs:L403-L404`](https://github.com/burncloud/burncloud/blob/3b3766f3c867b39ea35173a7458b9dcaf38612d2/crates/router/src/lib.rs#L403-L404)
- **修改后:** [`crates/router/src/lib.rs:L2824-L2828`](https://github.com/burncloud/burncloud/blob/3b3766f3c867b39ea35173a7458b9dcaf38612d2/crates/router/src/lib.rs#L2824-L2828)

---

## 风险判断

**总分：14 → 高风险**

- 上游 Provider 调用：`+4`
- 流式响应：`+4`
- 关键状态写入：`+3`
- 改了生产代码但这个提交没有同步改测试：`+3`

这个分数来自固定规则，不是 AI 凭感觉打分。

## 程序员 Review 清单

- [ ] 我能用一句话说清这次改了哪几件事
- [ ] 每个变化块的“以前 → 现在”符合真实源码
- [ ] 没把两个不相关的功能画成一条假流程
- [ ] 登录 / 权限变化已经单独确认
- [ ] 数据库迁移变化已经单独确认
- [ ] API 契约和“客户端请求行为”没有混为一谈
- [ ] 外部 Provider 调用变化已经确认
- [ ] 影响范围里的“可能”没有被当成“确定”
- [ ] 测试是否覆盖关键变化已经人工确认
- [ ] 所有重要结论都能点回 BASE / TARGET 历史源码

## 直接看源码

- [完整 BASE → TARGET Diff](https://github.com/burncloud/burncloud/compare/f700a8ab7c0ff8ee964aa85a58e25b205e40fe77...3b3766f3c867b39ea35173a7458b9dcaf38612d2)
- [原 Pull Request #344](https://github.com/burncloud/burncloud/pull/344)
