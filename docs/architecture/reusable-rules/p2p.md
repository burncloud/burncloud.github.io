---
title: "P2P 规则"
slug: /architecture/reusable-rules/p2p/
---

# P2P 规则

- 节点身份、连接状态和业务资源是不同概念。
- 收到的节点信息和消息默认不可信，验证后再使用。
- 消息协议必须有版本和明确类型。
- 重复、乱序、断线和重连由 P2P 自己的规则处理。
- 一个节点失败不能拖垮其他连接。
- 测试聚焦发现、连接、消息、网络分区和恢复。

正确：消息带身份、版本和唯一标识。  
错误：收到任意节点消息后直接修改本地业务数据。
