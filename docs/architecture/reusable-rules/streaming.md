---
title: "Streaming 规则"
slug: /architecture/reusable-rules/streaming/
---

# Streaming 规则

- 明确流的开始、数据块、结束、取消和错误。
- 不等待完整内容后再假装成流式返回。
- 下游变慢时不能无限堆积内存。
- 中途失败必须保留真实错误，不能伪造正常结束。
- Usage 等结尾信息缺失时，按所属领域规则处理，不能统一默认为零。

正确：逐块转发并响应取消。  
错误：先把完整响应读入内存，再一次性返回。
