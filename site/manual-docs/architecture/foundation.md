---
title: "基础规则"
slug: /architecture/foundation/
sidebar_label: "基础规则"
description: "所有 BurnCloud 代码共同遵守的最小规则。"
---

# 基础规则

基础规则只保存所有目录共同需要的稳定要求，不能放 UI、Database、Download 等具体细节。

## 规则继承与优先级

规则按目录逐级继承：基础规则 → 可复用规则 → 一级领域规则 → crate 规则 → 有独立职责的子目录规则。

执行时的权威顺序是：业务 Owner 的服务端事实 → 基础规则 → 可复用规则 → 目录规则 → Issue 修改范围 → 代码。下层不能以实现方便为理由推翻上层。

- 下级只补充差异，不复制上级全文。
- 下级可以增加限制，不能悄悄取消上级规则。
- 可复用规则与业务规则冲突时，以更具体的业务规则为准，但必须明确写出原因。
- 当前代码、临时实现和 AI 的方便不能推翻已经确定的规则。
- 同一规则只能有一个正文位置，其他页面只能引用。

## 1. 目录与职责

每段代码必须有明确 Owner 和归属。目录同时写清楚“谁负责”“负责什么”和“不负责什么”。不知道归属时先停止，不得扔进 `common`、`shared` 或 `utils`。

## 2. 领域边界

一个业务概念、业务状态和数据只有一个主要 Owner。其他领域可以通过公开契约使用它，不能重新计算、重新保存或修改同一份业务真相。

## 3. 模块依赖

依赖必须明确、单向、无环。跨领域不能进入对方内部目录、Repository 或 Adapter。

## 4. 公开契约

领域之间通过公开 Command、Query、Event 或稳定接口协作。每个 crate 必须写清楚它对外提供什么、返回什么、失败是什么样以及如何保持兼容。公开契约只暴露完成当前事情需要的最小内容，不能泄露 Repository、数据库模型和内部 Adapter。

## 5. 基础代码规范

代码默认简单、私有、可读。没有真实需要时不提前增加层级、Trait、Factory 或通用框架。

## 6. 基础错误处理

错误必须明确返回并保留有用原因。不能吞掉错误、伪造默认结果或者用崩溃代替正常错误处理。

## 7. 基础日志规范

日志只记录重要入口、出口、状态变化和失败。日志必须能定位事情，但不能记录密码、Token、密钥或不必要的敏感内容。

## 8. 基础测试原则

修改什么行为，就验证什么行为。Bug 修复必须留下回归测试。具体测试方法由对应 crate 和可复用规则决定。

## 9. 安全底线

默认不允许，明确授权后才允许。外部输入先验证再使用；每个调用方只获得完成工作需要的最小权限。

## 10. 变更原则

一个 Issue 只完成一件事，并有一个主要 Owner。跨领域需求拆成多个 Issue；普通重构不得顺便改变公开接口、数据和业务规则。

每个 Issue 必须明确：

- **Allowed Paths**：可以修改的目录；
- **Conditional Paths**：说明理由并获得对应 Owner 同意后才能修改的目录；
- **Forbidden Paths**：当前 Issue 禁止修改的目录；
- **Protected Zone**：路由核心、认证核心、公开契约、账务核心、迁移历史等需要独立变更的区域。

出现以下情况必须停止并拆分或升级 Issue：需要跨领域修改内部代码、需要改变公开契约、需要修改其他 Owner 的业务真相，或者需要触碰未授权的 Protected Zone。

## 每个 crate 必须回答的问题

1. Owner 是谁？这个 crate 负责什么？
2. 不负责什么？拥有哪些业务真相和数据？
3. 对外提供哪些公开契约？
4. 可以依赖谁？谁可以依赖它？
5. 禁止哪些依赖、访问和越界行为？
6. 目录和文件怎样组织？
7. 代码应该怎样写？有哪些特殊错误、日志和安全要求？
8. 继承哪些上级规则和可复用规则？
9. 有哪些独有测试、状态、失败和迁移要求？
10. 完整正确示例和完整错误示例是什么？

已经由上级回答的内容，直接引用，不再复制。
