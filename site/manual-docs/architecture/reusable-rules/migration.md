---
title: "Migration 规则"
slug: /architecture/reusable-rules/migration/
---

# Migration 规则

- 先增加新结构，再迁移数据，最后删除旧结构。
- 不能在旧代码仍使用字段时直接删除或改名。
- 大数据迁移分批执行，并能知道已经处理到哪里。
- 迁移前后验证数量、关键值和关系完整性。
- 一个 Migration 只修改明确的数据 Owner 范围。

正确：增加新字段、双向兼容、回填、切换、确认后删除旧字段。  
错误：一次上线直接删除旧字段并假定所有程序已经更新。
